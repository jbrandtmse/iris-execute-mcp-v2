# QA Test Automation Summary — Story 16.3 (`iris_backup_manage`)

Generated by the `bmad-qa-generate-e2e-tests` skill (QA stage). Supersedes the
prior per-story summary that occupied this path (earlier content remains in git
history and in each story's own record).

**Story:** 16.3 — `iris_backup_manage` (ops tool: `run`/`freeze`/`thaw` writes default-disabled + `listHistory` read; `restore` DEFERRED, server-defended; backed by `Backup.General` StartTask/ExternalFreeze/ExternalThaw + `^SYS("BUHISTORY")`)
**Type:** MCP tool over custom REST (`POST /api/executemcp/v2/monitor/backup/manage`); no UI
**Test framework:** Vitest (project default); REST layer mocked (no live IRIS — and **NO real freeze/thaw ever issued**; every IRIS round-trip is a stubbed Atelier envelope). Lead's smoke covers live read/rejection paths.
**Date:** 2026-06-16

## Test framework & discoverability (Rule 8)
- Framework: **Vitest** (existing). Tests live in `packages/iris-ops-mcp/src/__tests__/` as `*.test.ts` (NOT `*.integration.test.ts`), so they ARE in the DEFAULT suite (`pnpm test` → turbo `test` → vitest). Confirmed against `vitest.config.ts` (`include: src/__tests__/**/*.test.ts`, `exclude: *.integration.test.ts`) and by the run below.
- The dev stage created `backup.test.ts` (20, real handler + MOCK ctx) + `backup-governance.test.ts` (4, real gate + SPIED handler). The QA stage ADDED one new file `backup-e2e.test.ts` (8) closing the seam neither sibling exercises; no existing tests changed.

## New test file (strictly additive)

### End-to-end — `backup-e2e.test.ts` (8 new)
Drives the FULL path the two existing suites each only half-cover:
SDK-registered callback → `McpServerBase.handleToolCall` (Zod + governance gate) → the **REAL** `backupManageTool.handler` → the **REAL** `IrisHttpClient` — with only `globalThis.fetch` mocked (URL/method-routed, order-independent, accounting for the CSRF-preflight HEAD before mutating POSTs).
- [x] `listHistory` renders entries end-to-end + returns full-shape `structuredContent` (timestamp/type/status/logFile/description) — item (c)
- [x] `listHistory` renders the empty-history message end-to-end
- [x] `run` (governance-enabled) forwards `taskName` + optional `device`/`jobbackup`/`backupType` through the real handler's body assembly (device NOT covered by the unit suite) — items (a),(b)
- [x] `run` DENIED end-to-end under empty governance — gate short-circuits; backup POST never issued — item (e)
- [x] `freeze` (governance-enabled) surfaces frozen status + POSTs `{action:freeze,logFile,description}` (stubbed — no real quiesce) — item (b)
- [x] `thaw` (governance-enabled) surfaces thawed status + forwards `username`/`password` — item (b)
- [x] IRIS application error on `run` surfaces as `isError` end-to-end (real `IrisApiError` from the real client), not a thrown crash — item (f)
- [x] `action=restore` rejected by Zod at the gate (absent from the enum) — no crash, no REST call — item (d)

## Coverage of requested focus areas
| Behavior | Covered | Where |
|---|---|---|
| (a) `run` requires `taskName` (rejects without it); body incl. optional jobbackup/device/backupType | ✅ | `backup.test.ts` (missing/empty taskName, no server call; backupType+jobbackup); `backup-e2e.test.ts` (optional **device** passthrough — gap filled) |
| (b) freeze/thaw request shape + status output | ✅ | `backup.test.ts` (logFile/description; username/password); `backup-e2e.test.ts` (end-to-end frozen/thawed status via real handler) |
| (c) `listHistory` result shape (timestamp/type/file/description) | ✅ | `backup.test.ts` (text + sc length); `backup-e2e.test.ts` (field-by-field `structuredContent` shape, empty-history) |
| (d) restore-not-supported, no crash | ✅ | `backup.test.ts` (server restore-refusal surfaced via direct handler); `backup-e2e.test.ts` (out-of-enum Zod rejection through the registered gate — no REST call) |
| (e) governance: run/freeze/thaw denied by default, listHistory enabled, per-action re-enable via real `handleToolCall` gate | ✅ | `backup-governance.test.ts` (all-3 deny + listHistory allow + run-flip + freeze-flip); `backup-e2e.test.ts` (run denied end-to-end → no POST; run/freeze/thaw enabled-and-executed end-to-end) |
| (f) server-error path surfaced as `isError` | ✅ | `backup.test.ts` (IrisApiError + non-IrisApiError rethrow); `backup-e2e.test.ts` (real client throws on error envelope → isError) |

## Run result (default suite)
`npx vitest run` (iris-ops-mcp): **15 test files passed, 244 tests passed** (backup 20, backup-governance 4, backup-e2e 8).
`eslint` + `tsc --noEmit` on the new file: clean. `ops.integration.test.ts` correctly excluded from the default run.

## Invariants verified (STRICTLY ADDITIVE)
- Frozen `governance-baseline.ts` (`1e62c5ad5bf7`, 141 keys): untouched (not in QA edit set; remains git-clean).
- No `BOOTSTRAP_VERSION` bump; `bootstrap-classes.ts` not touched by QA.
- No existing tool or test changed; ops count unchanged at 20.
- `.vscode/settings.json` untouched. No live IRIS driven; **no real freeze/thaw issued**.
- Only the one new test file (+ this summary) was added this stage. Not committed/staged (lead stages after smoke gate).

## Overall: PASS — focus areas (a)–(f) fully covered by the default vitest suite, with a new true end-to-end layer through the registered server callback + real handler + real HTTP client.

## Next Steps
- Lead per-story live smoke gate (Rule #26: drive deployed endpoint read path `listHistory`; assert `run` without a defined task and the deferred `restore` reject cleanly; **do NOT live-run a freeze** — instance-wide quiesce), then commit.

# Test Automation Summary — Story 21.1 (Episode Compression, Dedup & Docs)

Generated by the `bmad-qa-generate-e2e-tests` skill (QA stage). Supersedes the Story 21.0
summary that occupied this path (its record remains in git history and in the 21.0 story
file's QA section).

QA stage date: 2026-07-03. Frameworks (existing, per Step 0 detection): **vitest** for
TypeScript (plain `*.test.ts` under `src/__tests__/` — the DEFAULT suite include, NOT the
excluded `*.integration.test.ts` suffix; Rule 8) and **%UnitTest** for ObjectScript
(`ExecuteMCPv2.Tests` package, run via `iris_execute_tests` in HSCUSTOM after
`iris_doc_load path="c:/git/iris-execute-mcp-v2/src/**/*.cls"` — the `src/**` glob form).

## Scope / gap analysis (verify first, then fill)

Dev already delivered 20 new/extended ObjectScript tests (`DiagramEpisodeTest` 11,
`DiagramDedupTest` 6, `DiagramCorrelatorTest` +2 I3, `DiagramGenerateTest` +1 nested
facade + the ValidateMermaid nesting extension) and +3 TS dedup tests. Each stage-candidate
gap was VERIFIED against that suite before writing:

- **(a) Adversarial episode-tier cases** — §6.4 items 3/4a/4b/5/6/7, I1-in-signature,
  nested rendering, byte-for-byte pass-through, and the line-count threshold are covered
  by `DiagramEpisodeTest`. **Not duplicated.** Three REAL gaps found and filled (below):
  interleaved A-B-A-B never collapses; an errored iteration SPLITS a run and compression
  RESUMES into a second loop; the `tCur=0` leading-trace branch of
  `Compressor:EpisodeCore` had zero coverage.
- **(a4) Deep nesting grammar-validated** — covered: `TestRunEpisodeNestedViaFacade`
  (live DB fixtures) grammar-validates an episode(2)×pairloop(3) diagram via the extended
  `ValidateMermaid`; the maximum producible nesting IS 2 levels (an episodeloop body can
  contain only arrows/pairloops — no episode-in-episode). **No test needed.**
- **(b) TS dedup matrix** — `dedup=false` wire value and the `dedupOf` summary were
  covered; the EXPLICIT `dedup: true` wire value was asserted nowhere, and the
  different-flows/no-`dedupOf` summary case only indirectly. **One test closes both.**
- **(c) Live repetitive-trace facade** — NOT a gap: dev's live coverage is not
  in-memory-only. `TestRunPairLoopViaFacade` proves `loop 3 times` from live DB rows and
  `TestRunEpisodeNestedViaFacade` proves a live nested episode loop end-to-end
  (grammar-validated); scale (200×3-hop, <40 lines) is data-driven, covered in-memory by
  `TestLineCountThreshold`. **Not duplicated.**

## Generated Tests (additive — nothing of the dev's tests touched)

### ObjectScript — adversarial episode-tier edges (in-memory, clean-room `Demo.*`)
- [x] `src/ExecuteMCPv2/Tests/DiagramEpisodeEdgeTest.cls` — **+3 tests** (OS suite
  147 → 150). New sibling class (story-anticipated split name; keeps `DiagramEpisodeTest`
  under the ~500-line guidance). Pure in-memory — no SQL, no fixtures to leak.
  - `TestInterleavedEpisodesDoNotCollapse` — A B A B alternating different-signature
    episodes (4-arrow multi-hop vs 2-arrow ping): ZERO `episodeloop` events, all 12
    arrows pass through in original order, and the rendered mermaid is **byte-for-byte
    identical** to the pair-tier render (§6.3 contiguity pinned — only CONTIGUOUS
    identical runs merge; no loop block anywhere).
  - `TestErroredIterationSplitsAndResumes` — clean×3 + errored + clean×3 identical
    episodes: I1 splits the collapse AND compression resumes — stream is exactly
    [episodeloop(3), arrow×4, episodeloop(3)]; both `loop 3 times` blocks render with
    the errored episode's unindented arrows (`[ERROR]`,
    `%% Error on message 3132: boom`) between them. Dev's I1 test proved only the split
    with a trailing SINGLE clean episode — never the resume into a second loop.
  - `TestLeadingTraceEventDoesNotPoisonCollapse` — a leading `HS.Util.Trace.*` event
    before any business episode exercises the previously-uncovered `tCur=0` trace branch:
    it forms its own all-trace episode (signature `""` — never merged), the following 3
    identical episodes still collapse (count 3, representative body 4 — trace excluded),
    and the trace arrow renders unindented before the loop with its unpaired warning
    (§6.4 item 5 interplay with compression).

### TypeScript — dedup-matrix corner
- [x] `packages/iris-interop-mcp/src/__tests__/diagram.test.ts` — **+1 test** (interop
  suite 268 → 269; file 24 → 25). Explicit `dedup: true` sends `dedup=true` on the wire
  (Rule #10 asserted for BOTH the omitted default and the explicit value), and when the
  endpoint returns two genuinely different flows with no `dedupOf`, no "duplicate of"
  note appears in any summary line while `structuredContent` stays the endpoint object
  verbatim (decision G3).

## Coverage (QA additions, by AC)

| AC | Added coverage |
| --- | --- |
| 21.1.1 | Contiguity requirement of the collapse rule pinned adversarially (interleaved A B A B → zero loops, byte-for-byte pass-through). |
| 21.1.2 (+I1) | Split-and-RESUME across an errored iteration (two loops, errored episode plain between them); leading-trace `tCur=0` branch (item 5 interplay — trace never poisons nor joins the collapse). |
| 21.1.3 | Explicit `dedup=true` wire value (Rule #10 completeness) + no `duplicate of` summary text when flows differ, `structuredContent` verbatim. |
| 21.1.4 | Byte-for-byte pass-through re-proven on a NEW stream shape (interleaved episodes), beyond dev's simple non-repetitive fixture. |

## Validation (all run 2026-07-03)

- Deploy: `iris_doc_load` src/**/*.cls (35 uploaded, 0 failed) — compiled ONLY
  `ExecuteMCPv2.Tests.DiagramEpisodeEdgeTest`; all 34 other classes "up-to-date"
  (proof of no product-source modification).
- ObjectScript `iris_execute_tests` (HSCUSTOM): new class **3/3** on first run; all 15
  `ExecuteMCPv2.Tests` classes verified green individually — **150/150, 0 failures**
  (Command 16, DiagramCompressor 10, DiagramCorrelator 18, DiagramDedup 6,
  DiagramEpisodeEdge 3, DiagramEpisode 11, DiagramGenerate 11, DiagramLoader 10,
  DiagramWriter 13, Global 12, MappingRoundTrip 1, Mapping 9, Setup 5, UnitTest 6,
  Utils 19).
- Fixture-leak sweep: `SELECT COUNT(*) FROM Ens.MessageHeader WHERE SessionId BETWEEN
  990000001 AND 990000699` → **0 rows** (the new class is in-memory-only; the live
  suites' teardowns held).
- `pnpm --filter @iris-mcp/interop test` → **269/269** (17 files); lint → clean.
- Shared: untouched (no shared file changed this stage; dev's 551/551 stands).

## Notes

- No product source touched; no `BOOTSTRAP_VERSION` / `governance-baseline.ts` impact
  (test classes stay OUT of the gen-bootstrap array).
- The `iris_execute_tests` package-level partial-snapshot caveat reproduced twice
  (identical 44-of-150 early snapshots, 0 failures both times) — resolved via per-class
  runs, exactly as the story's Dev Notes prescribe.
- Live-fixture SessionId range 990000601+ reserved for this stage was NOT needed — all
  three new tests are pure in-memory (the live gaps were verified already covered).

## Next Steps

- Lead per-story smoke (AC 21.1.8, Rule #22/#26): live looping demo-production session →
  `loop N times` blocks in a real Mermaid renderer; dedup across two identical sessions;
  non-repetitive session unchanged vs Story 21.0.
- If a future story adds episode-in-episode nesting (not producible today), extend
  `DiagramEpisodeEdgeTest` with a depth-3 grammar case via `ValidateMermaid`.

## Code-review stage addendum (2026-07-03)

The Story 21.1 code review (bmad-code-review) added **+1 ObjectScript regression test**
for its MED auto-fix CR 21.1-A (`DiagramDedupTest:TestDedupJunkOptionKeepsDefaultOn` —
junk-string / JSON-null `dedup` option keeps the documented default ON; string "false"
still disables). Post-fix gates: ObjectScript **151/151** (DiagramDedup 6 → 7; all 15
classes green per-class; partial-snapshot caveat reproduced again, resolved per-class),
fixture-leak sweep still **0 rows**, interop **269/269**, shared **551/551**, lint +
`pnpm -r build` clean. `Generate.cls`/`RenderEvent.cls` review fixes re-embedded:
**BOOTSTRAP_VERSION `195ed1dbfe56` → `1040c6dcfce3`** (second regen byte-identical);
governance baseline `1e62c5ad5bf7` intact (`gen:governance-baseline:check` exit 0).

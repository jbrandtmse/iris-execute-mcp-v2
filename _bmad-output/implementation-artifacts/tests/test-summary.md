# Test Automation Summary — Story 27.4 (Env Docs + Capstone Smokes)

Generated by the `bmad-qa-generate-e2e-tests` skill (QA stage). Supersedes the Story 21.1
summary that occupied this path (its record remains in git history and in the 21.1 story
file's QA section).

QA stage date: 2026-07-10. Framework (existing, per Step 0 detection): **vitest**, plain
`*.test.ts` under `packages/*/src/__tests__/` — the DEFAULT suite include, NOT the excluded
`*.integration.test.ts` suffix (Rule 8). Story 27.4 is TS/content/docs-only (no ObjectScript,
no `%UnitTest`, no bootstrap change) — the live capstone (AC 27.4.2) and rejection smokes
(AC 27.4.3) are LEAD-executed per the story's explicit instruction and were NOT run here.

## Scope / gap analysis (verify first, then fill)

Dev already delivered strong coverage for the new `promote-environment-change` prompt in
`packages/iris-mcp-all/src/__tests__/prompt-safety-invariants.test.ts` (4 tests: diff→plan→
execute→re-diff ordering naming both real tools; no-deletions + `REDACTED` + out-of-scope-
entirely secrets text; `DEFAULT-DISABLED`/`GOVERNANCE_DISABLED`/`IRIS_GOVERNANCE`/enable-
snippet; both-required-args-interpolate + spec-omitted-renders-placeholder), plus the
mechanical counting/registration guards (`validate-prompts.test.ts` 10→11 + inverted gated-
prompt test, `docs-prompt-sync.test.ts` dev 3→4 + "104 tools"/"11 prompts" prose,
`skills-generated-frontmatter.test.ts` 11 skills, `prompts.test.ts` OWN_PROMPT_NAMES 4).
Each QA checkpoint from the brief was verified against that suite BEFORE writing:

- **Workflow order (AC 27.4.4)** — dev's existing order test only anchors
  `action:"plan"` → `action:"execute"` → last `iris_env_diff` occurrence (the verify step).
  It does **not** check where the "review WITH the user" step or the "EXPLICIT list of step
  indices" (step allowlist) instruction fall, nor that `confirm: true` gates the execute
  call. **Real gap — filled** with one test asserting the full 7-point order: initial
  `iris_env_diff` call → review-with-user → plan → explicit step allowlist → execute →
  `confirm: true` → re-diff verify.
- **Safety statements** — `DEFAULT-DISABLED`, no-deletions, `REDACTED`, out-of-scope-entirely
  were already pinned by the dev. The user-review + explicit-allowlist gating language was
  not directly asserted anywhere — closed by the same new order test above.
- **Argument handling (`build({})`, `build({source,target})`, `build({source,target,spec})`)**
  — dev covered the first two shapes only. The `spec`-**provided** branch of `specGuidance`
  (`isArgProvided(args.spec)` true branch) was exercised by **zero** existing tests anywhere
  in the suite. **Real gap — filled** with a test that provides `spec` and asserts the
  spec-aware sentence renders (value interpolated) instead of the generic omitted-spec
  example wording.
- **Rule #47 factual check** — read `packages/iris-dev-mcp/src/tools/env-diff.ts:96-104`
  directly: `DEFAULT_DIFF_DOMAINS` is exactly `mappings`/`defaultSettings`/`webapps`/`config`;
  `documents` is opt-in only. The prompt's claim matches the shipped tool. **Not a test gap**
  — `DEFAULT_DIFF_DOMAINS` isn't exported from `env-diff.ts`, so a cross-file live-cross-check
  test would require either an unrelated source export change (out of this docs/prompt
  story's scope) or a hardcoded duplicate list (no stronger than the prompt text itself) —
  not added; verified by direct source read instead.
- **Docs-rot guards genuineness (Rule #30/#31)** — `EXPECTED_COUNTS["iris-dev-mcp"] = 4` in
  `docs-prompt-sync.test.ts` IS live-cross-checked (compared against `own.length` computed
  from the built-dist prompt catalog, not a pass-through literal). The "11 prompts" assertions
  (root README heading regex, `tool_support.md` per-server tallies) are **genuinely live** —
  both construct their expected string from `prompts.length` at runtime, not a hardcoded
  number. The **"104 tools"** assertion, however, IS a bare hardcoded literal with no live
  tool-count cross-check anywhere in the suite (noted below, not fixed — see Notes).
  `validate-prompts.test.ts`'s `toHaveLength(11)` and the inverted gated-prompt assertion are
  both genuinely live (built-dist prompt catalog). **Not duplicated — confirmed only.**
- **Skill generation parity** — `skills-generated-frontmatter.test.ts`'s
  `EXPECTED_SKILL_NAMES` already includes `promote-environment-change` (11 entries) and its
  `it.each` DO-NOT-EDIT-banner + frontmatter check covers it. Independently re-ran
  `pnpm gen:skills:check` (OK — 12 files match source, no drift) and `pnpm validate:prompts`
  (OK — 22 sources checked, 105 known tool names). **Not duplicated — confirmed only.**

## Generated Tests (additive — nothing of the dev's tests touched)

- [x] `packages/iris-mcp-all/src/__tests__/prompt-safety-invariants.test.ts` — **+2 tests**
  (`@iris-mcp/all` suite 47 → 49) in the existing `promote-environment-change` describe block:
  - `"encodes the user-review step and the explicit step allowlist, with confirm:true gating
    the execute call, in full order (AC 27.4.4)"` — asserts all 7 workflow anchors are present
    and strictly increasing in index order: initial `iris_env_diff` call, "Review the diff
    report WITH the user", `action: "plan"`, "EXPLICIT list of step indices",
    `action: "execute"`, `confirm: true`, and the re-diff verify (last `iris_env_diff`
    occurrence).
  - `"the optional spec arg, when PROVIDED, renders the spec-aware guidance branch, not the
    generic omitted-spec example (Rule #47)"` — calls `build({source, target, spec:
    "MyApp.*.cls,*.mac"})`, asserts the interpolated `spec: "MyApp.*.cls,*.mac"` sentence
    renders and the omitted-spec branch's generic closing sentence ("No `spec` is needed for
    the four domains listed above.") does NOT appear — proves the correct `isArgProvided`
    branch fired.

## Coverage (QA additions, by AC)

| AC | Added coverage |
| --- | --- |
| 27.4.4 | Full ordered workflow pinned: diff → review-with-user → plan → explicit step allowlist → execute (confirm:true) → re-diff verify — closes the gap between the dev's plan/execute/verify-only order check and the spec's full review-before-write sequence. |
| 27.4.4 (Rule #47) | The `spec`-provided argument branch (previously untested) now asserted to render the spec-aware sentence, not the omitted-spec generic example — proves both `isArgProvided` branches of `specGuidance` are live-exercised. |

## Validation (all run 2026-07-10)

- `pnpm turbo run build` — 6/6 packages, all cache-hit, green (baseline, before test edits).
- `pnpm --filter @iris-mcp/all test` (baseline, before QA edits) — **47/47**, 6 files.
- Added the 2 tests above; re-ran — **49/49**, 6 files green.
- **Mutation check** (not vacuous): temporarily changed `confirm: true` → `confirmed: yes` in
  `promoteEnvironmentChange.ts`, rebuilt `@iris-mcp/dev`, re-ran the suite → new order test
  failed exactly as expected (`"confirm:true" not found in rendered body`), 18/19 passed.
  Reverted the source line, rebuilt `@iris-mcp/dev` again, re-ran → back to 49/49. Confirmed
  via `git status`/direct re-read that the prompt file is byte-identical to the dev's
  original (still untracked, no diff).
- `pnpm turbo run test` (full monorepo) — **13/13 tasks green**: `@iris-mcp/dev` 503/503
  unchanged, `@iris-mcp/admin` / `@iris-mcp/ops` / `@iris-mcp/interop` / `@iris-mcp/shared`
  all cache-hit/unchanged, `@iris-mcp/all` 49/49 (was 47, +2 new).
- `pnpm turbo run type-check` — 12/12 green (cache hit). `pnpm turbo run lint` — 6/6 green
  (cache hit; `@iris-mcp/all` has no lint/type-check script, consistent with the dev's DoD
  notes — my edit is in that package, so it rides on vitest's transpile-time check only, not
  a separate `tsc --noEmit`; the code uses only simple `Array<[string, number]>`/`indexOf`/
  `lastIndexOf` constructs already used elsewhere in the same file).
- `pnpm gen:skills:check` — OK, 12 generated files match source, no drift.
- `pnpm validate:prompts` — OK, 22 sources checked (11 prompts + 11 generated skills), 105
  known tool names.

## Notes

- **Bare-literal count flagged, not fixed** (per the QA brief's explicit instruction to note
  rather than fix): `docs-prompt-sync.test.ts`'s `"the root README still advertises 104
  tools"` test asserts a hardcoded string with no live tool-count cross-check (unlike the
  "11 prompts" assertions in the same file, which compute their expected value from
  `prompts.length` at runtime). This is a carried-over Epic 25 test-design convention — Rule
  #31's guard is "prompts must never inflate the documented tool count," not "the tool count
  is mechanically re-derived and verified" — and is not a defect introduced by Story 27.4's
  dev pass (the dev correctly updated the literal `102`→`104` per that pre-existing
  convention). No `loadAllToolNames`-based live total exists anywhere in the suite to compare
  against; out of this story's scope to add one.
- The `"gen-skills --check: STRAY/STALE generated file ... skills/audit-security-posture/
  NOTES.md"` console line seen during the `@iris-mcp/all` test run is emitted BY
  `gen-skills-stray-file.test.ts` itself (it creates a temp stray file to prove `--check`
  detects it, then removes it) — confirmed via `git status --porcelain -- skills/` (no such
  file persists; only `skills/README.md` modified + `skills/promote-environment-change/` new,
  matching the dev's File List exactly) and a clean `gen:skills:check` run afterward. Not a
  real drift; out of scope regardless (audit-security-posture is an unrelated Epic 25 prompt).
- No product/prompt/generator source touched (aside from the revert-verified mutation-test
  round-trip on `promoteEnvironmentChange.ts`, confirmed byte-identical afterward). No
  `BOOTSTRAP_VERSION` / `governance-baseline.ts` impact — this story has none by design.

## Next Steps

- Lead capstone + rejection smokes (AC 27.4.2/27.4.3, Rule #26/#34) per the dev's documented
  runnable plan in the story's Dev Agent Record: seed a disposable package-mapping drift on
  HSCUSTOM → `iris_env_diff` detects it → `iris_env_promote plan` orders it → `execute` with
  a single-step allowlist + `confirm:true` promotes it to SADEMO → re-diff shows clean →
  cleanup both namespaces; plus the 6 rejection smokes (no-confirm, no/empty steps, stale
  plan, target-governance-disabled, wide-spec-without-`allowWide`, reversed-direction second
  namespace pair).
- No further QA-stage action needed on this story; ready for code-review stage.

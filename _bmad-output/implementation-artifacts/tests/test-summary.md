# QA Test Automation Summary — Story 16.2 (`iris_database_action`)

Generated by the `bmad-qa-generate-e2e-tests` skill (QA stage). Supersedes the
prior per-story summary that occupied this path (earlier content remains in git
history and in each story's own record).

**Story:** 16.2 — `iris_database_action` (ops tool: mount/dismount/compact/defragment/truncate/expandVolume, all governed default-disabled, backed by `SYS.Database`)
**Type:** MCP tool over custom REST (`POST /api/executemcp/v2/monitor/database/action`); no UI
**Test framework:** Vitest (project default); REST layer mocked (no live IRIS — lead's smoke covers live)
**Date:** 2026-06-16

## Test framework & discoverability (Rule 8)
- Framework: **Vitest** (existing). Tests live in `packages/iris-ops-mcp/src/__tests__/` as `*.test.ts` (NOT `*.integration.test.ts`), so they ARE in the DEFAULT suite (`pnpm test` → turbo `test` → vitest). Confirmed in the run below.
- The dev stage created `database.test.ts` (17) + `database-governance.test.ts` (2). The QA stage ADDED targeted gap-filling cases to both; no new files created, no existing tests changed.

## Augmented tests (strictly additive)

### Unit — `database.test.ts` (17 → 23, +6)
- [x] mount surfaces `Read-only: yes` in text output
- [x] mount reports `Read-only: no` when mounted read-write
- [x] mount omits `readonly` from the POST body when not supplied
- [x] defragment surfaces server result via `structuredContent`
- [x] expandVolume omits `initialSize` from the body when not supplied
- [x] omitted `directory` (undefined, not just empty `""`) → clean error, server not called

### Governance — real `McpServerBase.handleToolCall` gate — `database-governance.test.ts` (2 → 3, +1)
- [x] explicit enable of the DESTRUCTIVE `dismount` flips just that action; `truncate` (other destructive verb) stays denied — per-action granularity proven for destructive writes (dev's existing test proved it only for the non-destructive `compact`)

## Coverage of requested focus areas
| Behavior | Covered | Notes |
|---|---|---|
| (a) each action POST body + param passthrough (readonly/percentFull/targetSize/newVolDir/initialSize) | ✅ | all 6 actions + omit-when-unset cases |
| (b) required-field errors (directory required for all incl. undefined; newVolDir for expandVolume) | ✅ | empty + undefined directory; missing newVolDir |
| (c) output fields surfaced (compact mbProcessed/mbCompressed; truncate returnSize; mount readonly; defragment structuredContent) | ✅ | |
| (d) governance: all 6 denied by default + per-action re-enable via real gate | ✅ | all-6 deny loop (dev); enable-flip for non-destructive `compact` (dev) AND destructive `dismount` (QA) |
| (e) destructive annotation present | ✅ | `destructiveHint: true` asserted |
| (f) server-error path (invalid/in-use dir) → isError not crash | ✅ | dismount invalid-dir, truncate DB-in-use, non-IrisApiError rethrow |

## Run result (default suite)
`pnpm --filter @iris-mcp/ops test`:
- **12 test files passed, 212 tests passed** (database 23, database-governance 3).
`pnpm --filter @iris-mcp/ops lint` + `type-check`: clean.

## Invariants verified
- Frozen `governance-baseline.ts` (`1e62c5ad5bf7`, 141 keys): untouched (not in QA edit set; remains git-clean).
- No `BOOTSTRAP_VERSION` bump; `bootstrap-classes.ts` not touched by QA.
- No existing tool changed; `.vscode/settings.json` untouched.
- Only the two test files were modified this stage. Not committed/staged (lead stages after smoke gate).

## Overall: PASS — focus areas (a)–(f) fully covered by the default vitest suite.

## Next Steps
- Lead per-story live smoke gate (Rule #26: destructive path rejected on a disposable/invalid target against the deployed endpoint), then commit.

# Test Automation Summary — Story 15.5 (`iris_resource_manage` SQL-privilege extension)

Generated by the `bmad-qa-generate-e2e-tests` skill (QA stage). Supersedes the
prior per-story summary that occupied this path (earlier content remains in git
history and in each story's own record).

QA fills the COVERAGE GAPS left by the dev-stage tests (`resource.test.ts`,
`resource-governance.test.ts`), all through REAL surfaces — the governance
ENGINE over the actual tool definition, the tool's `inputSchema` (`safeParse`),
and the tool `.handler` with mocked HTTP. No live IRIS, no generated-file or
ObjectScript edits. One file (Rule 8 default-suite `.test.ts` naming).

## Generated Tests

### `packages/iris-admin-mcp/src/__tests__/resource-sqlpriv-coverage.test.ts` (18 tests)

- **A. Engine back-compat (9 tests)** — `buildMutatesLookup([resourceManageTool])`
  over the REAL tool def classifies exactly the 3 NEW keys (`grant`=write,
  `revoke`=write, `listPrivileges`=read), lookup size 3, NO classification for
  create/delete/modify; the 3 existing keys ARE in the frozen `GOVERNANCE_BASELINE`
  and the read tool `iris_resource_list` too; the 3 new keys are NOT; baseline
  invariants intact (**141 keys / hash `1e62c5ad5bf7`**); `defaultSeed` →
  grant/revoke DISABLED, listPrivileges + create/delete/modify ENABLED
  (baseline-grandfathered).
- **B. Schema — `name`-optional widening (5 tests)** — drives
  `inputSchema.safeParse` the way the MCP SDK validates args (the dev's surface
  tests call `.handler` directly, bypassing the schema): grant/revoke/listPrivileges
  validate WITHOUT `name`; create/delete/modify still validate WITH `name`; an
  unknown action enum value is still rejected (the widening did not open the enum).
- **C. Surface gaps (3 tests)** — listPrivileges forwards `namespace` (and grantee)
  in the GET querystring; the listPrivileges (GET) error branch returns `isError` +
  the real error text (dev only covered the grant POST error path); column-level
  REVOKE forwards the `schema.table(col1,col2)` target unchanged.
- **C. Negative leakage proof — AC 15.5.6, release-critical (2 tests)** — a `create`
  (and a `modify`) call carrying STRAY `target`/`privilege`/`grantee`/`namespace`
  routes to `/security/resource` with a body that leaks NONE of them. Exact-equality
  (not `objectContaining`) is the load-bearing assertion: it FAILS if any future
  change forwards a SQL-priv field into the resource wire body.

## Coverage

- New SQL-privilege actions (grant / revoke / listPrivileges) exercised at the
  engine (classification + seed), schema (validation), and handler (wire body /
  error) layers — schema, table, and column levels.
- ACs filled: 15.5.2 (governance default via the pure engine), 15.5.5/15.5.7
  (namespace forwarding + listPrivileges error path), and the release-critical
  15.5.6 back-compat gate (negative leakage + baseline membership) plus the
  `name`-optional widening (Task 2).

## Results

- New tests: **18**, all passing.
- `@iris-mcp/admin` suite: 421 → **439** tests green (29 files).
- `tsc --noEmit` exit 0; `eslint` on the new file exit 0.

## Invariants held (not regressed)

- Governance baseline hash `1e62c5ad5bf7` / 141 keys — unchanged (asserted in the
  new tests).
- `BOOTSTRAP_VERSION 038102d88885` — bootstrapped `.cls`/`bootstrap-classes.ts`
  NOT touched (TS-only QA change). NOTE: code review later applied a HIGH fix to
  `Security.cls:SqlPrivilegeManage` (single-response/no-partial-grant), bumping
  `BOOTSTRAP_VERSION` to `e5f4f6d88c56`.
- No fixture lacks `mutates`: the only tool exercised is the real
  `resourceManageTool` (which declares its partial `mutates`); no synthetic
  non-baseline tool fixtures were introduced.
- No live IRIS; no `.vscode/settings.json` change; no git commit/push.

## Next Steps

- Run in CI alongside the rest of the default vitest suite.

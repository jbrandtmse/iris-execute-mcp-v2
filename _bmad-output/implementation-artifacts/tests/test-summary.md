# Test Automation Summary — Story 14.4 (Call-Time Governance Enforcement & Structured Denial Error)

Generated by the `bmad-qa-generate-e2e-tests` skill (QA stage). Supersedes the
prior Story 14.2 summary that occupied this path (that content remains in git
history and in Story 14.2's own record).

Tests are integration-level vitest coverage of the **governance enforcement gate**
wired into the central `McpServerBase.handleToolCall` dispatch (architecture
decision D5 — the one chokepoint that fronts every tool on all five servers).
Harness uses **mocked `fetch` + a mocked `bootstrap` spy** — no live IRIS. The
gate's policy decisions, denial envelope, ordering vs. validation/resolution, and
gate-before-establishment guarantees are all provable against the shared dispatch
with mocked transport. (The cross-server live end-to-end isolation + governance
test is owned by Story 14.5 / AC 14.5.6.)

## Generated Tests (complementary — additive to the dev suite)

The dev's `packages/shared/src/__tests__/governance-enforcement.test.ts` (14
tests) covers AC 14.4.1–14.4.5 head-on: seed-disabled-write denial shape +
handler/connection untouched (default and non-default profile), profile override
re-enable (over seed, and over a global `false`), back-compat per-server read +
multi-action all-actions under empty config, unknown-`server`-before-gate, Zod-
before-gate, multi-action `tool:action` key alignment with the baseline, single-op
bare-name key, call-time-still-advertised, two D7 fail-fast cases, and constructed-
but-not-started safe default. The new file below is strictly COMPLEMENTARY — it
exercises interactions that suite leaves open.

### `packages/shared/src/__tests__/governance-enforcement-coverage.test.ts` (14 tests)

**(1) governance × `server` × `namespace` interaction (2 tests):**
- [x] Same grandfathered read disabled globally + re-enabled for `prod`: DENIED on `default`, ALLOWED on `prod`, AND a per-call `namespace` override still wins WITHIN prod (`ns=OVERRIDENS`, `host=prod.example.com`) — the gate does not disturb AC 14.2.5 namespace precedence.
- [x] Allowed prod call with NO override falls back to the prod profile's own namespace (`ns=PRODNS`), not the default's — selection vs. namespace-default separation on the allowed path.

**(2) full three-layer cascade THROUGH the real gate (2 tests):**
- [x] Seed-disabled NEW write → `global:true` (enables) → `profiles.prod:false` (re-disables): ALLOWED on `default` (global lift), DENIED on `prod` (profile override wins), handler count proves only the allowed call ran, and the denied prod profile created no client. (Dev only proved seed→profile and global→profile two-layer paths; this is the full seed→global→profile precedence.)
- [x] `global:true` on an already-grandfathered read is an observable no-op (still allowed) — precedence sanity.

**(3) per-action enforcement driven by the PROFILE layer (1 test):**
- [x] On `prod`, `iris_database_manage:delete` is DENIED (key carries the action discriminator) while `:modify` is ALLOWED on the SAME tool/SAME profile; on `default` `:delete` is ALLOWED (deny was prod-scoped). Exactly the two allowed calls reach the handler. (Dev disabled `delete` GLOBALLY; this drives the divergence via the per-profile layer.)

**(4) strict denial-envelope invariant on a profile-scoped `tool:action` denial (1 test):**
- [x] `isError === true`; `content` is a single text block with a non-empty string naming both action + profile (exact text asserted); `structuredContent` has EXACTLY the keys `action`/`code`/`server` and nothing more (`Object.keys(...).sort()` pinned); handler not invoked.

**(5) case/whitespace in `args.action` → validation, not the gate (4 tests, parametrized):**
- [x] `" delete "`, `"DELETE"`, `"Delete"`, `"delete\n"` — each a near-miss of a real enum value — are rejected by Zod ("Invalid arguments"), with `:delete` disabled globally so a leak past validation would surface as EITHER an allow or a governance denial. Correct outcome each time: validation error, no `structuredContent` code, handler untouched. Proves the gate key only ever matches exact enum values (validation precedes the gate).

**(6) concurrency — interleaved denied + allowed stay independent (1 test):**
- [x] `Promise.all` of `default:delete` (globally disabled → must DENY) and `prod:delete` (profile-enabled → must ALLOW) fired simultaneously: each resolves on its own profile's policy; only the allowed prod call reaches the handler. The gate holds no shared mutable state across concurrent calls.

**(7) a denial establishes NOTHING (1 test):**
- [x] Denying a never-touched `prod` profile creates no client (`clients.has("prod") === false`), registers NO in-flight establishment entry (`establishing.has("prod") === false` — no leaked Promise), attempts no bootstrap, issues zero fetches to the prod host, and adds zero net-new fetches of any kind — gate strictly before `getOrCreateClient`.

**(8) back-compat strengthening — the seed's read branch through the gate (2 tests):**
- [x] A NEW (non-baseline) `read` tool (`mutates:"read"`) is ALLOWED and its handler runs under empty `IRIS_GOVERNANCE` (baseline-membership guarded false first, so this proves the new-read seed branch, not grandfathering).
- [x] Contrast: the matching NEW `write` tool is seed-DISABLED under the SAME empty config — proving the seed's read/write split (not the absence of config) is what differentiates them.

## Coverage

- Surface under test: `server-base.ts` — `computeGovernanceKey` (D4 key shape), the enforcement gate's `effective(...)` call + structured denial (D5), ordering vs. Zod validation and `resolveProfile`, gate-before-`getOrCreateClient`; via `governance.ts` `effective` cascade (seed/global/profile via `??`) and `governance-baseline.ts` membership; with namespace precedence from `buildToolContext` and client/in-flight state from `ProfileClientRegistry` / the private `establishing` map.
- Targeted ACs (complementary to the dev's AC 14.4.1–14.4.5 suite): deepens 14.4.1/14.4.2 (full cascade + strict envelope + per-action divergence + concurrency), 14.4.3 (namespace precedence undisturbed on the allowed path), 14.4.4 (new-read seed-enabled vs. new-write seed-disabled under empty config), 14.4.5 (near-miss action → validation not gate; denial leaves no client/no in-flight entry).
- Not in scope (by epic design): the cross-server live end-to-end test (Story 14.5 / AC 14.5.6) and the advisory `iris-governance://{profile}` resource (Story 14.5).

## Results

- `pnpm --filter @iris-mcp/shared test`: **425 passed** (19 files), up from **411** (18 files) — **+14**.
  - Dev's `governance-enforcement.test.ts` (14) and the new `governance-enforcement-coverage.test.ts` (14) both present in the default-run output.
- `pnpm turbo run lint`: 6/6 successful (`@iris-mcp/shared:lint` re-ran on the new file — clean, exit 0, no errors/warnings).
- Discoverability (Rule 8): the new file matches the default `vitest run` include glob (`src/__tests__/**/*.test.ts`), is NOT `*.integration.test.ts` (the excluded live-IRIS pattern), is not ignored, and is not tagged out — confirmed present in default-run output.
- No `BOOTSTRAP_VERSION` bump (TypeScript-only, all in `@iris-mcp/shared`); no generated-file edits (`governance-baseline.ts` / `bootstrap-classes.ts` untouched).

## Next Steps

- Lead runs the smoke gate, then commits (changes left uncommitted per the epic-cycle QA instruction).
- Story 14.5 owns the cross-server end-to-end integration test (per-profile session isolation + governance enforcement against a live target) and the advisory governance resource.

# QA Test Automation Summary — Story 17.2 (`iris_production_item` add/remove + arbitrary settings)

Generated by the `bmad-qa-generate-e2e-tests` skill (QA stage). Supersedes the prior
per-story summary that occupied this path (Story 16.4's record remains in git history).

**Story:** 17.2 — extend the EXISTING `iris_production_item` tool with `add`/`remove`
(mutates:write, default-disabled) + arbitrary host/adapter settings routed to `Ens.Config.Setting`.
**Package:** `@iris-mcp/interop`
**Type:** Enhancement of an existing leaf tool. Heart of the story = Rule #19 mechanical back-compat gate.
**Test framework:** Vitest (project default). Suite = DEFAULT (`*.test.ts`) — mocked HTTP + real governance gate, no live IRIS.
**Date:** 2026-06-16

## Coverage analysis (vs. the dev's existing tests — NO duplication)

The dev's `item.test.ts` + `item-governance.test.ts` already cover non-duplicatively:
- add/remove/arbitrary-setting `set` request-body forwarding
- back-compat `toEqual` REQUEST-body snapshots for enable/disable/get/set + the 5-property-key `set`
- enum membership + `mutates` shape
- real-gate governance proof (add/remove denied by default, existing 4 allowed, opt-in flip)

These were NOT re-created. The tests added below close only the genuine remaining gaps.

## Generated Tests (appended to `packages/iris-interop-mcp/src/__tests__/item.test.ts`, +11)

### QA gap #1 — output-shape pass-through, FULL object (AC 17.2.4)
AC 17.2.4 asks the back-compat snapshots assert the FULL output object. The dev's
get/enable/disable tests assert individual fields via `.toBe()` (a subset only); the
back-compat block pins REQUEST bodies, not OUTPUT. The tool forwards the REST envelope
`result` verbatim as `structuredContent`, so a full `toEqual` is a non-vacuous, fail-if-drift
pin of the OUTPUT contract:
- [x] enable / disable / get / set output `structuredContent` `toEqual` REST result (full object)
- [x] add / remove output `structuredContent` `toEqual` REST result (full object)
- [x] get full-object snapshot encodes the comment/category-omitted-when-empty shape (AC 17.2.4b)

### QA gap #2 — structuredContent is an OBJECT, not an array
Project invariant (structuredContent must be an object, never an array). No prior assertion guarded it:
- [x] get / add / remove return a non-null, non-array object as `structuredContent`

### QA gap #3 — add request-body defaulting (additive)
- [x] `add` OMITS `className` from the body when the caller omits it (server enforces Required)
- [x] `add` forwards an arbitrary-setting key (`Charset@Host`) verbatim (same routing as `set`)

## Gaps intentionally NOT covered at the unit layer (structurally server-side)

These are ObjectScript-handler behaviors the mocked-HTTP tool layer cannot exercise without
duplicating the handler — they belong to the lead's live-HTTP smoke (Rule #26) and the
handler/governance suites already in place:
- **property-key-wins / collision** (the 5 keys map to `Ens.Config.Item` properties; the tool is
  a pure pass-through, so routing is decided in `ItemManage`/`ApplyArbitrarySetting`)
- a previously-silently-ignored key now routing to an `Ens.Config.Setting` (server-side, additive)
- `@Host`/`@Adapter` suffix parsing + default-Adapter target (server-side `ApplyArbitrarySetting`)
- `add` without `className` REJECTED; `remove` of a non-existent item; `production` defaulting to
  the active production; the `LoadFromClass` add→remove round-trip restore (live IRIS / handler)

The unit suite covers the forwarding contract for these; the live-HTTP smoke covers the routing.

## Result
- `item.test.ts`: 32 → 43 tests
- `@iris-mcp/interop` suite: **219 passed** (12 files, was 208), lint clean.
- Frozen governance baseline (`1e62c5ad5bf7`) and `BOOTSTRAP_VERSION` untouched by QA (no ObjectScript/generated-artifact edits this stage).

## Overall: PASS — output-shape + array-invariant + add-defaulting gaps closed without duplicating dev coverage; DEFAULT suite green.

## Next Steps
- Lead per-story gate: live-HTTP smoke (Rule #26) — add a disposable item to a disposable
  production, set an arbitrary `@Host`/`@Adapter` setting, remove it, and confirm get/set-5-keys
  output is unchanged on the live server. Then commit.

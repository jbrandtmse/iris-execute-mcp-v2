# Test Automation Summary — Story 15.4 (`iris_audit_manage` — Audit Configuration & Log)

Generated by the `bmad-qa-generate-e2e-tests` skill (QA stage). Supersedes the
prior per-story summary that occupied this path (earlier content remains in git
history and in each story's own record).

QA fills the COVERAGE GAPS left by the dev-stage tests (`audit.test.ts`,
`audit-governance.test.ts`), all through REAL surfaces with mocked transport — no
live IRIS, no generated-file or ObjectScript edits. Two files, mirroring the
Story 15.2/15.3 two-file split (Rule 8 default-suite `.test.ts` naming).

## Generated Tests

### Handler surface — `packages/iris-admin-mcp/src/__tests__/audit-coverage.test.ts` (20 tests)

Through the REAL `auditManageTool.handler` with mocked HTTP:

- **purge safety hardened (AC 15.4.5, destructive).** An explicit `confirm:false`
  (not just omission) is rejected; `confirm:true` + ZERO bounds NEVER reaches the
  wire (the unbounded-wipe guard, re-asserted at `mockHttp.post`); `confirm:true`
  + EACH individual bound (begin/end/user/event/source/type) is sufficient and the
  POST body carries `confirm:true` + that bound (a future change dropping any one
  accepted bound would fail here); the returned deleted COUNT surfaces.
- **export path-traversal guard exhaustive (AC 15.4.5, security).** Each of
  `/ \ .. :` — plus absolute POSIX, drive-letter `C:\`, bare `..`, `x/..`, nested
  dirs — is rejected at the tool layer with NO POST; a BARE name is accepted and
  POSTed, with the resolved server-side `location` + `exported` count surfaced; a
  dotted name (`audit.2026.xml`) is NOT mistaken for traversal.
- **view filtering precision + default cap (AC 15.4.4):** each present filter maps
  to the query string; empty-string/undefined filters omitted; `maxRows` forwarded
  verbatim (the server applies the default-100/cap-1000 — the tool does not clamp,
  proven with a value past the cap); non-array `events` coerced to empty.
- **REAL pagination (`view`):** drives the actual slice-and-cursor algorithm
  (`encodeCursor`/`decodeCursor`) — `nextCursor` on a full first page, cursor
  advances to page 2, final page omits `nextCursor`.
- **Error propagation (AC 15.4.6):** real `%Status` text preserved with the action
  name on the `view` GET path and the `purge`/`export` POST paths; non-IrisApiError
  re-thrown.
- **content text ↔ structuredContent consistency** for a read (status) and a write
  (purge).

### Governance + engine back-compat — `packages/iris-admin-mcp/src/__tests__/audit-governance-coverage.test.ts` (8 tests)

GATE (real `McpServerBase.handleToolCall`, default profile startup stubbed,
handler spied):

- mirror-image opt-in: a GLOBAL opt-in of `export` flips `export` while every OTHER
  write (enable/disable/configureEvent/purge) stays denied — per-action granularity
  proved from the other side vs. the dev's `purge` opt-in, across all four siblings.
- reads (`status`/`view`) stay allowed under a PARTIAL `IRIS_GOVERNANCE` that only
  toggles a write (the dev proved reads under EMPTY config only).
- an enabled `purge` receives the FULL args (`confirm` + bound) intact — the gate
  is a pass-through, not a filter, so the handler-side confirm/bound guard remains
  reachable behind the gate.

ENGINE (pure `defaultSeed` / `buildMutatesLookup` vs. the REAL frozen
`GOVERNANCE_BASELINE`):

- frozen baseline unchanged: **141 keys, hash `1e62c5ad5bf7`**.
- none of the 7 `iris_audit_manage:*` keys are baselined (no `iris_audit_manage`
  substring anywhere in the baseline).
- `buildMutatesLookup([auditManageTool])` yields EXACTLY the 7 keys
  (status/view=read; enable/disable/configureEvent/purge/export=write; no bare
  `iris_audit_manage`, no extras).
- `defaultSeed`: the 2 reads default-ENABLED, the 5 writes default-DISABLED.
- additive-only (AC 15.4.8): no audit-manage key collides with `iris_audit_events`.

## Coverage

- `iris_audit_manage` actions: 7/7 (status, view, enable, disable, configureEvent,
  purge, export) exercised through the real handler AND the real governance gate.
- AC 15.4.2 (governance), 15.4.4 (I/O / view filtering), 15.4.5 (purge + export
  safety), 15.4.6 (errors), 15.4.8 (engine back-compat / additive) — gaps filled.

## Results

- New tests: **28** (20 + 8), all passing.
- `@iris-mcp/admin` suite: 372 → **400** tests green (27 files).
- Full monorepo: turbo `test`+`lint`+`type-check` — **24/24 tasks green** (tsc
  strict + ESLint clean).

## Invariants held (not regressed)

- Governance baseline hash `1e62c5ad5bf7` / 141 keys — unchanged.
- `BOOTSTRAP_VERSION` `e353c54c5547` — bootstrapped `.cls`/`bootstrap-classes.ts`
  NOT touched.
- New synthetic non-baseline fixtures: none introduced beyond the real
  `auditManageTool` (which declares its `mutates`); no tool fixture lacks
  `mutates`.
- No live IRIS; no `.vscode/settings.json` change; no git commit/push.

## Next Steps

- Run in CI alongside the rest of the default vitest suite.

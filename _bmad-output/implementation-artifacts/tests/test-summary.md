# Test Automation Summary — Story 21.0 (Message Diagram Core)

Generated by the `bmad-qa-generate-e2e-tests` skill (QA stage). Supersedes the prior
per-story summary that occupied this path (Story 19.0's record remains in git history).

QA stage date: 2026-07-02. Frameworks (existing, per Step 0 detection): **vitest** for
TypeScript (plain `*.test.ts` under `src/__tests__/` — the DEFAULT suite include, NOT the
excluded `*.integration.test.ts` suffix; Rule 8) and **%UnitTest** for ObjectScript
(`ExecuteMCPv2.Tests` package, run via `iris_execute_tests` in HSCUSTOM after
`iris_doc_load path="c:/git/iris-execute-mcp-v2/src/**/*.cls"` — the `src/**` glob form
per dev Completion Note #8).

## Scope / gap analysis (verify first, then fill)

Dev already delivered 46 ObjectScript tests (4 pipeline-stage classes) + 23 TS tests
(wrapper + governance + AC 21.0.9 back-compat). Verified BEFORE writing anything:

- §6.4 items 1/2/10 — covered by `DiagramLoaderTest` (NULL-body retained, ID tiebreak +
  direct fallback-mode `RunQuery`, truncation). Item 11 — covered by
  `DiagramCompressorTest` (`ErroredPairNotMerged`). Item 8 — warning retention/dedup
  covered. **Not duplicated.**
- TS Zod rejections (empty/>20/non-positive/non-integer sessionIds, bad labelMode,
  out-of-range maxRows), wiring, G3 output, governance, back-compat — covered. **Not
  duplicated.**
- **Real gaps filled:** (a) NO facade-level test existed — `Generate.Run`/`GenerateOne`/
  `ApplyFailure` had zero direct coverage; (b) NO structural validation that emitted
  Mermaid stays inside the decision-G3 subset grammar; (c) TS matrix holes: non-integer
  `maxRows`, wrong-typed inputs, boundary ACCEPTS (20 ids, maxRows 1/10000), and the
  handler's zero-/missing-`diagrams` content fallback branches.

## Generated Tests (additive — nothing of the dev's 69 touched)

### ObjectScript E2E — facade over live Ens.MessageHeader fixtures
- [x] `src/ExecuteMCPv2/Tests/DiagramGenerateTest.cls` — **+10 tests** (suite 46 → 56).
  Reserved SessionId range 990000201+ (disjoint from DiagramLoaderTest's 990000001+),
  synthetic `Demo.*` fixtures only (G2 clean-room), teardown sweep verified (0 leaked
  rows via `SELECT COUNT(*) ... SessionId >= 990000000`).
  - `TestRunMultiSessionEnvelope` — one populated + one unknown-empty + one non-positive
    session id in a single `Run`: envelope `{diagrams, count}`, request order, per-entry
    fields/types, empty-diagram path (`%% No data available`) end-to-end.
  - `TestRunPipelineContent` — exact 6-line diagram for a sync round trip through the
    full Load → Correlate → Compress → Write pipeline (I2 header with real timestamps).
  - `TestRunLabelModeShort` / `TestRunOptionsHandling` — options through the facade:
    short labels; omitted options → full; junk `labelMode` + non-positive `maxRows`
    fall back to defaults; over-limit `maxRows` clamped and succeeds.
  - `TestRunTruncationViaFacade` — §6.4 item 10 through the ENVELOPE (`truncated: true`,
    capped `messageCount`), not just the loader flag.
  - `TestRunPairLoopViaFacade` — 3 identical DB round trips → `loop 3 times` block.
  - `TestRunErrorFlagViaFacade` — I1 end-to-end from a stored caret-global
    `ErrorStatus`: ` [ERROR]` suffix, sanitized `%%` comment, mirrored structured
    warning, NO `^` anywhere (Rule #33).
  - `TestRunUnpairedWarningAggregated` — structured `warnings[]` aggregation + matching
    `%%` comment.
  - `TestApplyFailureShape` — the per-session hard-failure (comment-only) diagram shape,
    grammar-valid, sanitized.
  - `TestGrammarRichDiagram` — composite trace (pair loop + async queue pair via CorrId
    + errored pair + sanitized/colliding participant names + unpaired request):
    **every emitted line validated against the G3 subset grammar** by the new
    `ValidateMermaid`/`ValidateLine` helpers (header, I2 comment, `%%` comments,
    `participant <id> [as <label>]` with `[A-Za-z0-9_.]` ids, `->>`/`-->>` arrow shape,
    balanced `loop N times`/`end` with 2-space body indent, no nesting, no blank lines,
    no CR). Grammar checks also applied in the empty-session, loop, error, round-trip,
    and failure-shape tests.

### TypeScript — validation-matrix completion + handler fallback branches
- [x] `packages/iris-interop-mcp/src/__tests__/diagram-qa.test.ts` — **+7 tests**
  (interop suite 257 → 264).
  - Boundary ACCEPTS: exactly 20 sessionIds; maxRows 1 and 10000.
  - Rejections completing the matrix: non-integer maxRows (2.5); wrong-typed inputs
    (string sessionIds element, non-array sessionIds, string maxRows, numeric
    labelMode); sessionIds missing entirely.
  - Handler fallbacks: zero-`diagrams` result and missing-`diagrams` key both render
    "No diagrams generated." while returning the endpoint result OBJECT verbatim as
    `structuredContent` (decision G3).

## Coverage (QA additions, by AC)

| AC | Added coverage |
| --- | --- |
| 21.0.1 | `Generate.Run` proven callable independently of REST, end-to-end over live rows (was untested as a unit). |
| 21.0.2 | Non-positive id → empty-diagram path THROUGH the facade; maxRows clamp/fallback via options. |
| 21.0.3 | Full-pipeline arrow/participant/label emission (exact lines); G3 grammar conformance validator. |
| 21.0.4 | Pair-loop compression end-to-end (DB rows → `loop 3 times`). |
| 21.0.5 | I1 `[ERROR]` + sanitized comment + structured warning end-to-end (no carets, Rule #33); I2 header with real load timestamps; empty session via facade; `ApplyFailure` best-effort shape. |
| 21.0.7 | Zod boundary accepts + wrong-type rejections; handler zero/missing-diagrams fallbacks with G3 object-verbatim `structuredContent`. |
| 21.0.12 | §6.4 item 10 `truncated` flag through the envelope (items 1/2/11 verified already covered — not duplicated). |

## Validation (all run 2026-07-02)

- ObjectScript `iris_execute_tests` (HSCUSTOM): `DiagramGenerateTest` **10/10**;
  `DiagramLoaderTest` 10/10; `DiagramWriterTest` 10/10; package run over
  `ExecuteMCPv2.Tests` passed everything reported with **0 failures** (incl.
  Compressor 10/10, Correlator 16/16, CommandTest 16/16 alongside the new class —
  no cross-class interference; fixture-leak query returned 0 rows).
- `pnpm --filter @iris-mcp/interop test` → **264/264** (17 files; `diagram-qa.test.ts`
  discovered by the default run).
- `pnpm --filter @iris-mcp/shared test` → **551/551** (unchanged — no shared impact).
- `pnpm --filter @iris-mcp/interop lint` → clean; `tsc --noEmit` → clean.
- Deploy/compile: `iris_doc_load` (32 uploaded, 0 failed) — only the NEW test class
  compiled; every product class reported "up-to-date" (proof of no source modification).

## Notes

- No product source touched; no `BOOTSTRAP_VERSION` / `governance-baseline.ts` impact
  (test classes stay OUT of the gen-bootstrap array per AC 21.0.11).
- REST `%request`-level validation (CSV >20, junk labelMode, maxRows clamp on the wire)
  is intentionally NOT unit-tested: it needs CSP context, was dev-probed live, and is
  the lead's AC 21.0.13 live-HTTP smoke per Rule #26; client-side the Zod matrix covers
  the same contract.
- `iris_execute_tests` occasionally returns an early snapshot of an async run right
  after a fresh compile (first class run reported 1 test; immediate rerun reported the
  full 10/10). Rerun if a count looks short.

## Next Steps

- Lead per-story smoke (Rule #22/#26): live diagram for a real demo-production session,
  trace-filter/[ERROR]/empty-session checks over HTTP (AC 21.0.13).
- Story 21.1 can reuse `ValidateMermaid` for the episode tier (extend for nested-loop
  depth ×2 indentation when nesting arrives).

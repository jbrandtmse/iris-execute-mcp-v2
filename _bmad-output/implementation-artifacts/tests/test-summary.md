# Test Automation Summary — Story 18.0 (Epic 17 Deferred Cleanup)

Generated by the `bmad-qa-generate-e2e-tests` skill (QA stage). Supersedes the prior
per-story summary that occupied this path (Story 17.4's record remains in git history).

QA stage date: 2026-06-17. Framework: **vitest** (existing). All generated tests use the
default `*.test.ts` naming convention (NOT `*.integration.test.ts`, which the per-package
vitest config `exclude` opts out of the default run — Rule 8 / Rule #21).

## Scope

Story 18.0 is a deferred-work hardening/cleanup story. The discoverable-by-default-suite
surface is the **TS layer**:
- ops %SYS-scoped tools (`process`/`database`/`backup`) `namespace` param — accepted-but-ignored.
- `iris_production_item` tool-description note (extent/XData add-then-get split).

The **ObjectScript guards** in `Interop.cls` (CR 17.2-1/-2/-3/-5) live behind the new opt-in,
default-disabled `iris_production_item` add/arbitrary-settings surface and are exercised over
**live HTTP only** (Rule #26 — mocked-HTTP TS tests structurally cannot drive server-side
guards). Those rejection assertions are scoped to the lead's per-story live smoke and are
intentionally NOT generated here.

## Added Tests (additive — complement the dev's suite)

### Accepted-but-ignored `namespace` — empty-string edge (the gap the dev's tests left)
The dev asserted a NON-empty namespace is accepted and forwarded. The added tests close the
orthogonal **empty-string** edge of the accepted-but-ignored contract: an empty namespace is
accepted without error AND is dropped from the wire (query path for `process get`; POST body
for the control/database/backup paths).

- [x] `packages/iris-ops-mcp/src/__tests__/process.test.ts` — +2:
  - empty-string namespace dropped from the GET query path
  - empty-string namespace dropped from the control POST body
- [x] `packages/iris-ops-mcp/src/__tests__/database.test.ts` — +1: empty-string namespace dropped from the POST body
- [x] `packages/iris-ops-mcp/src/__tests__/backup.test.ts` — +1: empty-string namespace dropped from the POST body

### Already covered by dev tests (verified present + passing, NOT duplicated)
- namespace description states "NO EFFECT" + "%SYS-scoped" — all three ops tools.
- namespace accepted-and-forwarded (non-empty) back-compat — all three ops tools.
- `iris_production_item` description documents the add-then-get extent/XData visibility split
  (asserts "NOT visible to an immediate 'get'/'set'" + "LoadFromClass").
- 17.2 back-compat mechanical gate (`toEqual` request-body + output-shape snapshots for
  enable/disable/get/set; add/remove additive) — present and green, NOT weakened.

## Coverage

- ops-mcp: 254 tests pass (15 files) — was 250, +4 added.
- interop-mcp: 220 tests pass (12 files) — unchanged; existing 17.2 back-compat snapshots green.
- Lint (`@iris-mcp/ops`): clean. Type-check (`@iris-mcp/ops`): clean.

## Out of scope for the default suite (deferred to lead live smoke, Rule #26)
- CR 17.2-2 `@Host`/`@Adapter` malformed-suffix rejection (`@Foo`, leading `@`).
- CR 17.2-3 `add` with non-existent className / duplicate item name → REJECTED.
- CR 17.2-1/-5 `%Status` propagation on failed setting application / failed `LoadFromClass`.
- CR 17.1-1 `||` IdKey-delimiter guard rejection.

## Next Steps
- Lead per-story live-HTTP smoke for the ObjectScript guard rejections (Rule #26).

# Test Automation Summary — Story 14.1 (Multi-Server Profiles: Config Model & Connection Resolution)

Generated by the `bmad-qa-generate-e2e-tests` skill (QA stage). Tests are
integration-level vitest coverage of the new shared modules with **mocked
`fetch`** — no live IRIS dependence (Story 14.1 is internal shared
infrastructure; the user-visible `server` parameter is wired in Story 14.2, so
there is no live end-to-end MCP surface to drive yet).

## Generated Tests (complementary — additive to the dev suite)

### `packages/shared/src/__tests__/profiles-resolution.test.ts` (26 tests)
Pure-unit coverage of `profiles.ts` edges the dev's `profiles.test.ts` leaves open:
- [x] Multiple profiles each overriding **disjoint** field subsets (host-only / namespace-only / creds-only / port-only); every non-overridden field inherits the synthesized `default`; per-profile object independence.
- [x] Inheritance of `https=true` into an override-host profile → re-derived **https** baseUrl on the new host; explicit per-profile `timeout` override vs inherited timeout; profile `timeout` flows into the `IrisHttpClient` default timeout.
- [x] `resolveProfile` whitespace-only name is **not** trimmed (→ `ProfileResolutionError`); requested value reported verbatim; case-sensitivity (`Prod` ≠ `prod`).
- [x] `ProfileResolutionError` lists **every** registry key (asserted against the live key set, not a hard-coded list); real `Error` subclass with stable `name`.
- [x] Malformed `IRIS_PROFILES` **message text** names the offending var across JSON-syntax / null-root / number-root / empty-profile-name / bad-field-type shapes (dev only asserted that it throws).
- [x] `default`-override warning fires **exactly once** (count pinned); no warning when no profile is named `default`.
- [x] `ProfileClientRegistry` `has()` lifecycle (false → true per profile, no implicit sibling creation); `destroyAll()` clears the cache so a later `getOrCreate` yields a **new** instance; `getOrCreate` throws for an unregistered profile.
- [x] Back-compat gate — `IRIS_PROFILES=""` (empty string) treated as absent → registry is byte-for-byte `loadConfig` (complements the dev's "var unset" case with the "var set but empty" case), via both `buildProfileRegistry` and `loadProfileRegistry`.

### `packages/shared/src/__tests__/profiles-bootstrap.test.ts` (4 tests)
D8 lazy per-profile bootstrap contract at the `server-base.ts` seam, isolating
the assertion by mocking the `bootstrap` orchestration (`vi.mock("../bootstrap.js")`,
an established project pattern) so the call count is decoupled from bootstrap
internals / the embedded hash:
- [x] A non-default profile is bootstrapped **exactly once** across repeated custom-REST `getOrCreateClient` calls; cached client reused throughout.
- [x] Bootstrap targets the profile's **own** config (its host/name), not the default profile.
- [x] An Atelier-only call (`needsBootstrap=false`) never bootstraps; a later custom-REST call on the same profile then bootstraps once (first custom-REST use), and not again.
- [x] Bootstrap **failure** on a non-default profile is graceful (logged warning, `getOrCreateClient` resolves rather than rejects) and marked attempted (no retry).

## Coverage

- New shared modules under test: `profiles.ts` (registry build, inheritance, resolution, `ProfileClientRegistry`) and `server-base.ts` D1/D8 per-profile client establishment.
- Targeted ACs (complementary to dev's AC 14.1.6 suite): 14.1.1, 14.1.3, 14.1.4, 14.1.5, 14.1.7, plus the 14.1.2 back-compat gate.
- Not in scope (by epic design): the per-call `server` parameter / `handleToolCall` selection (Story 14.2) and the cross-server end-to-end integration test (Story 14.5 / AC 14.5.6).

## Results

- `pnpm --filter @iris-mcp/shared test`: **260 passed** (13 files), up from **230** (11 files) — **+30** (26 + 4).
- `pnpm turbo run test`: 12/12 tasks successful (no cross-package regression).
- `pnpm turbo run lint`: 6/6 successful. `tsc --noEmit` (shared): clean.
- Discoverability (Rule 8): both new files match the default `vitest run` include glob `src/__tests__/**/*.test.ts` and are NOT `*.integration.test.ts` (the excluded, live-IRIS pattern) — confirmed present in default-run output.

## Next Steps

- Story 14.2 will wire the `server` parameter and `handleToolCall` selection; extend coverage there for per-call profile selection.
- Story 14.5 owns the cross-server end-to-end integration test (per-profile session isolation + governance enforcement).

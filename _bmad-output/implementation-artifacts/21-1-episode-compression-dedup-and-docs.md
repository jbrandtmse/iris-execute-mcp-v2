# Story 21.1: Episode-Level Compression, Cross-Session Dedup & Docs Rollup

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As an **integration engineer tracing high-volume flows**,
I want **repetitive multi-hop passes and duplicate session flows compressed**,
so that **large traces produce short, readable diagrams instead of hundreds of repeated lines**.

## Acceptance Criteria

(From [epics.md](../planning-artifacts/epics.md) Epic 21 / Story 21.1, including AC 21.1.9 added 2026-07-02 by stakeholder decision. [sprint-change-proposal-2026-07-02.md](../planning-artifacts/sprint-change-proposal-2026-07-02.md) **§6.3/§6.4 are the binding spec** for the episode tier — read §6 in full.)

- **AC 21.1.1** — Episode-level compression per proposal §6.3: build multi-hop "episodes" via a depth stack over synchronous (Inproc) calls (sync request pushes, its response pops); episode signatures built from business events only (trace-utility events excluded from signatures but retained in rendered bodies; pair-loops contribute fragments including `LOOPCOUNT=N`; error state included — I1); contiguous identical-signature episodes collapse to an outer `loop N times <representative label>` block; nested rendering with 2-space indentation per level (pair-loops render inside episode loops).
- **AC 21.1.2** — Episode edge cases (proposal §6.4) each covered by a fixture test: abandoned/unanswered sync request unwinds the depth stack (no giant swallowed episode — unwind when a new request's source ≠ top frame's destination, and when a response answers a deeper frame); episode iteration counts are part of the signature (different loop counts never merge — §6.4 item 3); trace events between episodes attach to the previous episode (contiguity preserved — item 5); queue-rooted episodes (async request with known paired response) compress (item 6); envelope-wrapper flows start episode detection at depth 2 (exact reference heuristic or a documented general simplification — dev's call, recorded — item 7); warnings survive both compression tiers (item 8); dedup of an empty key must not error (item 9).
- **AC 21.1.3** — Cross-session dedup (default ON; `dedup?: boolean` on tool + endpoint to disable): identical diagrams across the requested sessions collapse to one, comparing with the session metadata header normalized (I2); `structuredContent` reports the mapping via `dedupOf`; dedup of an empty/blank input must not error. Warnings survive compression at both levels.
- **AC 21.1.4** — Back-compat within the epic: Story 21.0's fixture outputs for non-repetitive traces are byte-for-byte unchanged (episode compression only activates on repetition); `iris_production_messages` snapshot still green.
- **AC 21.1.5** — Readability made mechanical: a large synthetic trace (e.g. 200 repeated pairs across 3-hop episodes) produces a compact diagram — assert an output line-count threshold.
- **AC 21.1.6** — BOOTSTRAP_VERSION: regenerate + record from→to hash (Rule #24 — this story edits `Diagram/` classes); `bootstrap.test.ts` green; `gen:bootstrap` idempotent at epic close.
- **AC 21.1.7** — Docs rollup (Rule #30): `packages/iris-interop-mcp/README.md` (new tool section + example fenced mermaid output + options incl. `dedup`), root `README.md` + `packages/iris-mcp-all/README.md` (counts + tool mention), `tool_support.md` (new row: 🟥 ExecuteMCPv2 `/interop/production/messages/diagram`; interop 20→21; Mix + rollup totals +1), `docs/migration-v1-v2.md` (interop count refresh), `docs/tool-annotation-audit.md` if it enumerates tools, `CHANGELOG.md` entry (incl. one-line clean-room provenance note + I3 note). **Default-state callout: `iris_message_diagram` is a read, enabled by default.** Counts cross-verified vs `index.test.ts`.
- **AC 21.1.8** — Lead smoke (Rule #22/#26): live trace of a looping demo-production session → verify `loop N times` blocks render correctly in a Mermaid renderer; dedup verified across two identical sessions; a non-repetitive session's diagram unchanged vs Story 21.0. *(Lead executes; dev delivers the working surface.)*
- **AC 21.1.9** *(stakeholder decision 2026-07-02 — resolves CR 21.0-1)* — **Improvement I3:** the Queue-pairing `ReturnQueueName` fallback fires only when BOTH values are non-empty and equal (empty==empty no longer pairs). Fixture tests: one-way async request + later CorrId-less reverse response-typed row stays UNPAIRED (both warned, not merged into any loop); a legitimate non-empty equal-ReturnQueue fallback pair still pairs. Update the CR 21.0-1 entry in `deferred-work.md` to RESOLVED (implemented as I3); the proposal §6.2 [I3] note is already recorded; mention I3 in the CHANGELOG entry.

## Tasks / Subtasks

- [x] **Task 1 — Read the binding spec** (all ACs)
  - [x] Read proposal §6.3 (episode tier + dedup) and §6.4 (items 3–9 are THIS story's edge ledger) in full. Also re-read §6.2 [I3] (the amended Queue-fallback rule).
  - [x] Read Story 21.0's Dev Agent Record + Review Findings in [21-0-message-diagram-core.md](./21-0-message-diagram-core.md) — the class shapes, escape choke points, and test conventions you MUST build on (summarized in Dev Notes below).

- [x] **Task 2 — I3: Queue-fallback non-empty guard** (AC: 21.1.9) — do this FIRST; it's small and de-risks the correlator seam before the episode work.
  - [x] `src/ExecuteMCPv2/Diagram/Correlator.cls` (~line 76): the fallback condition additionally requires `tReq.ReturnQueue '= ""` (both sides non-empty; equality already checked). Keep the CorrId-primary path untouched.
  - [x] Fixture tests: one-way async false-pair scenario now yields TWO warnings (unpaired request + unpaired response, both emitted); non-empty equal-queue fallback still pairs. Update any existing test that relied on empty==empty pairing (check `DiagramCorrelatorTest` — if a fallback fixture used empty ReturnQueues, give it non-empty values so it keeps testing the fallback). *(Existing `TestQueueFallbackPairing` already used non-empty "QDemo" — kept; +2 new tests.)*
  - [x] Update `deferred-work.md` CR 21.0-1 → **RESOLVED (Story 21.1, improvement I3)** with a one-line note.

- [x] **Task 3 — Episode-level compression** (AC: 21.1.1, 21.1.2, 21.1.4, 21.1.5)
  - [x] Extend `src/ExecuteMCPv2/Diagram/Compressor.cls` (or add a sibling `Episode`/second-pass method — dev finalizes; keep pair-tier output byte-identical when no repetition exists): input = the pair-compressed render-event stream from 21.0; output = episode-compressed stream for the Writer. *(Extended `Compressor.cls`: `CompressEpisodes` entry + `EpisodeCore`/`EpisodeSignature`/`IsTraceEvent`/`RepresentativeLabel` internals; new `episodeloop` RenderEvent kind with `Body` list.)*
  - [x] Depth stack over SYNC (Inproc) calls: sync request pushes a frame (src→dst), its response pops. Unwind rules (§6.4 item 4): a new request whose source ≠ top frame's destination unwinds; a response answering a DEEPER frame unwinds to it. One unanswered sync request must NOT swallow all later episodes.
  - [x] Episode signature: built from **business events only** — trace-utility events (classifier: rendered label/body class starting `HS.Util.Trace.` — the loader already excludes the exact `HS.Util.Trace.Request` class, so this classifier catches sibling trace classes; dev may generalize, record the choice) are excluded from signatures but RETAINED in rendered bodies; between-episode trace events attach to the PREVIOUS episode (item 5). Pair-loop events contribute their fragments + `LOOPCOUNT=N` (item 3 — different counts never merge). Error state joins the signature (I1). Invocation values are EXCLUDED from signatures (arrow style substitutes — §6.3).
  - [x] Queue-rooted episodes (item 6): an async request with a known paired response opens a compressible episode that closes when that response is consumed.
  - [x] Envelope-wrapper flows (item 7): outer sync wrapper request whose response arrives at the very end → start episode detection at depth 2. The reference used a domain heuristic (first-request class / `.SERVICES`→`.PROCESS` naming + wrapper response in the last few events); **a simplified general rule is acceptable if fixtures prove equivalent — record the choice in Dev Notes/Completion Notes.** *(Simplified general rule implemented — see Completion Notes #3.)*
  - [x] Contiguous identical-signature episodes (count > 1) → one outer `loop N times <representative label>` block wrapping the episode body; representative label = dev's call per §6.3 (record it). *(Label = first business event's label; see Completion Notes #4.)*
  - [x] `src/ExecuteMCPv2/Diagram/Writer.cls`: nested rendering — 2-space indentation per nesting level; pair-loops render INSIDE episode loops; `end` lines close blocks at the right depth. Warnings from compressed episodes deduped + retained (item 8).
  - [x] Byte-for-byte guard (AC 21.1.4): episode compression activates ONLY on repetition — a non-repetitive stream passes through unchanged. Add a fixture asserting a 21.0-style simple trace's full mermaid text is IDENTICAL pre/post episode tier. *(`TestNonRepetitivePassThroughByteForByte`.)*
  - [x] Line-count threshold (AC 21.1.5): synthetic 200×(3-hop episode) trace → assert emitted line count under a stated threshold (pick and record, e.g. < 40 lines). *(Threshold recorded: < 40 lines; actual output is 14 lines for 1200 arrows.)*

- [x] **Task 4 — Cross-session dedup** (AC: 21.1.3)
  - [x] `src/ExecuteMCPv2/Diagram/Generate.cls` facade: after per-session generation, compare diagrams with the FULL `%% Session …` metadata header line normalized (I2 — normalize the whole line, not just the id); identical → later session's entry keeps `sessionId`/`messageCount`/`warnings`/`truncated` but sets `dedupOf: <first sessionId>` and its `mermaid` MAY be the shared text (decision: keep mermaid populated — simplest for clients; record the choice). Default ON; `dedup` option (boolean, default true) disables. Empty/blank input (empty session diagrams) must not error (item 9) — two empty sessions DO dedup (both normalize identical). *(Mermaid kept populated with the entry's OWN text; see Completion Notes #5.)*
  - [x] REST `MessageDiagram()` handler (`src/ExecuteMCPv2/REST/Interop.cls`): read `&dedup=` (accept `0`/`1`/`true`/`false`; default true; wire-explicit from the TS layer anyway), pass into options. Update the `///` banner. *(Invalid values rejected with a clear sanitized error — live-verified.)*
  - [x] TS tool `packages/iris-interop-mcp/src/tools/diagram.ts`: add `dedup: z.boolean().optional().describe(...)` (default true documented); **send explicitly on the wire** (`params.set("dedup", String(dedup ?? true))` — Rule #10); surface `dedupOf` in the per-session summary line when present ("duplicate of session N"). `structuredContent` stays the verbatim endpoint object.
  - [x] This story's TS changes are limited to the thin wrapper — NO shared-foundation change.

- [x] **Task 5 — Deploy + bootstrap** (AC: 21.1.6)
  - [x] Deploy: `iris_doc_load path="c:/git/iris-execute-mcp-v2/src/**/*.cls" compile=true namespace=HSCUSTOM` (NOTE: `src/**/*.cls` — the `src/ExecuteMCPv2/**` form mis-maps class names; Story 21.0 verified this). *(34 uploaded, 10 compiled, clean.)*
  - [x] `pnpm run gen:bootstrap` → record BOOTSTRAP_VERSION from `5bd5579c25c1` → to `195ed1dbfe56`; `bootstrap.test.ts` green; verified a SECOND `gen:bootstrap` run produces no diff (idempotence — this is the epic's closing story). `bootstrap-classes.ts` never hand-edited (Rule #18). No gen-bootstrap.mjs array change needed (no new product class files added).
  - [x] `pnpm run gen:governance-baseline:check` → exit 0; `governance-baseline.ts` git-clean (`1e62c5ad5bf7`). Bare generator NOT run (Rule #25).

- [x] **Task 6 — Tests** (AC: 21.1.2, 21.1.4, 21.1.5, 21.1.9, 21.1.3)
  - [x] ObjectScript: new `src/ExecuteMCPv2/Tests/DiagramEpisodeTest.cls` (≤~500 lines; split into `DiagramEpisodeEdgeTest` if needed) — §6.4 items 3/4/5/6/7/8 fixtures (in-memory event lists per 21.0 convention), byte-for-byte non-repetitive guard, line-count threshold, nested-indent rendering. Extend `DiagramCorrelatorTest` for I3 (Task 2). New `DiagramDedupTest` for dedup: identical sessions collapse with `dedupOf`, dedup=false disables, empty-input no-error (item 9), warnings survive (item 8 at facade level). Live-fixture SessionId range: **990000401+** (Loader uses 990000001+, Generate 990000201+ — stay disjoint; teardown deletes + verifies 0 remaining — verified 0 leaked rows across all reserved ranges). `%OnNew(initvalue)` + `##super(initvalue)`; triple-`$$$` macros; no `$$$AssertFalse`.
  - [x] The QA G3 grammar validator (`DiagramGenerateTest.ValidateMermaid`) must accept the nested-indent output — extend its indent rule for nesting levels (QA left it reusable for exactly this). *(Extended: arbitrary nesting depth 2×level, `end` at its loop's depth, optional loop label after "times"; +`TestRunEpisodeNestedViaFacade` exercising it end-to-end over live DB fixtures.)*
  - [x] TS `diagram.test.ts` / `diagram-qa.test.ts`: `dedup` wire-explicit default true + explicit false; `dedupOf` summary rendering; existing 21.0 assertions updated ONLY where the wire now includes `dedup` (the one expected-URL string; `diagram-qa.test.ts` had no URL assertions — untouched).
  - [x] Back-compat: `iris_production_messages` snapshot (from 21.0) still green untouched; `index.test.ts` counts UNCHANGED this story (21 tools already).
  - [x] Full gates: `pnpm --filter @iris-mcp/interop test` (268/268), `pnpm --filter @iris-mcp/shared test` (551/551), lint clean, build clean (all packages); OS suite via `iris_execute_tests` — every `ExecuteMCPv2.Tests` class green, 147 total, 0 failures (partial-snapshot caveat hit repeatedly; per-class runs confirm full coverage).

- [x] **Task 7 — Docs rollup** (AC: 21.1.7)
  - [x] `packages/iris-interop-mcp/README.md`: new `iris_message_diagram` section — description, options table incl. `dedup`, a SHORT example fenced ```` ```mermaid ```` output block, **"read tool, enabled by default under IRIS_GOVERNANCE"** callout (Rule #30), pointer to `iris_production_messages` for raw rows; bumped "All 20 interoperability tools" → 21; + a `<details>` input/output example.
  - [x] `tool_support.md`: new row `iris_message_diagram` → 🟥 ExecuteMCPv2 `/interop/production/messages/diagram` in the interop section; interop header (20)→(21) + Mix `0 Atelier · 20 ExecuteMCPv2` → `21`; **Suite-wide rollup**: ExecuteMCPv2 column 74→75, interop row 20/21→21/22, package Total 98→99, advertised Total 103→104 (+1 nowhere else — normal package tool, Rule #31 split does NOT apply). Epic 21 governance-defaults note states the default-state (read, enabled). Also refreshed the three prose 74-of-98 dependency figures → 75-of-99.
  - [x] Root `README.md` + `packages/iris-mcp-all/README.md`: interop count 20→21, suite totals 98→99, ASCII tree `(20)`→`(21)` (interop slot), tool mention in both server tables, + a read/default-enabled row in the root README governance-defaults table.
  - [x] `docs/migration-v1-v2.md`: interop count refresh (98→99 ×2, interop 20→21 + tool mention).
  - [x] `docs/tool-annotation-audit.md`: checked — it is a frozen, dated (2026-04-07) point-in-time audit of 85 tools in pre-Epic-9 dotted naming; no Epic 15–20 tool was ever added to it, so per precedent NO row added (recorded decision).
  - [x] `CHANGELOG.md`: new dated entry — Epic 21 / `iris_message_diagram` (Story 21.0 core + Story 21.1 episode compression, dedup, I1/I2/I3 improvements), clean-room provenance note (reference consulted for spec only; no code/sample data), default-state callout, BOOTSTRAP_VERSION notes (both story bumps).
  - [x] Cross-verified every count against `packages/iris-interop-mcp/src/__tests__/index.test.ts` (`toHaveLength(21)`, advertised 22) before writing (Rule #30).

## Dev Notes

### Previous-story intelligence (Story 21.0 — build on, do NOT rediscover)

- **Class shapes:** `Event` (loaded row) + `RenderEvent` (render stream, `Kind` discriminator — arrows/comments/pair-loops). The Correlator applies label mode BEFORE signatures (signatures see RENDERED labels). `Writer.SingleLine` is the single escape choke point (`;`→`#59;`, CR/LF/TAB collapse) — route ANY new label/comment text through it. Reserved-word participant ids (`end`/`loop`/…) get `_2`-style suffixes; empty-after-strip labels get a placeholder. Rule #8 `ERROR #N:` prefix strip already applied in Loader/Generate error summaries.
- **Deploy glob (Rule #17 correction from 21.0):** `iris_doc_load path="c:/git/iris-execute-mcp-v2/src/**/*.cls"` — NOT `src/ExecuteMCPv2/**/*.cls` (mis-maps document names; 21.0 verified no junk classes created).
- **Enum normalization** uses literal values with macro-citing comments (deliberate — `Include EnsConstants` rides ENSLIB mappings absent in non-interop namespaces).
- **BOOTSTRAP_VERSION now:** `5bd5579c25c1` (after CR fixes). This story moves it again (record from→to).
- **Test baselines:** OS `ExecuteMCPv2.Tests` diagram suite 59 (Correlator 16 + Compressor 10 + Writer 13 + Loader 10 + Generate 10); interop TS 265; shared 551. Live-fixture SessionId ranges used: 990000001+ (Loader), 990000201+ (Generate) — use **990000401+** and verify 0 leaked rows in teardown (`%SQLCODE` checked).
- **`iris_execute_tests` caveat:** immediately after a fresh compile it can return an early partial snapshot — rerun if the reported test count looks short (QA observed once; rerun showed full results).
- **Known deferred (NOT this story unless opportunistic):** CR 21.0-2 (O(n²) correlator scan — LOW; index-map optimization routed to the future cleanup epic; do not restructure pairing for it here).

### Episode algorithm risk framing

The depth-stack/episode tier is the epic's risk center (proposal §3). Implement AGAINST the §6.4 fixtures FIRST (write failing fixtures, then the algorithm). The §6.4 items are pre-paid reference-tool bugs — each maps to one fixture test in Task 6. Keep the pair tier's output byte-identical when no repetition exists (AC 21.1.4 is a mechanical Rule #19-style gate INSIDE the epic).

### Trace-utility classifier nuance

The 21.0 Loader already excludes the exact class `HS.Util.Trace.Request` at SQL level — so most traces contain no trace rows at all. The §6.3 "trace-utility excluded from signatures" rule exists for OTHER trace-like classes that ARE loaded (e.g. hypothetical `HS.Util.Trace.Response`/sibling utility classes). Classifier: prefix match on `HS.Util.Trace.` against the ORIGINAL body class (Event.BodyClass, not the rendered label, so `short` label mode can't break it). Record the classifier choice in Completion Notes.

### Dedup normalization (I2 interplay)

Normalize the ENTIRE `%% Session <id>: …` header line (replace it with a fixed token) before comparing full diagram text — session id, message count AND time-span all differ between otherwise-identical sessions. Two empty sessions (both `%% No data available`) normalize identical → dedup (that IS §6.4 item 9's no-error case). `dedupOf` = the FIRST session id carrying that flow, in request order.

### Docs rollup exact targets (verified 2026-07-02)

`tool_support.md`: interop Mix line 151 (`0 Atelier · 20 ExecuteMCPv2`), Suite-wide rollup line 288 (`| **Total** | **19** | **74** | **5** | **98** | **103** |` → 75/99/104). Interop package `index.test.ts` already asserts 21 (updated in 21.0) — docs catch up to it this story. The Epic 15/20 README precedent for the Rule #30 callout style: state "read — enabled by default" where the tool is documented, not only in the governance section.

### Project Structure Notes

- MODIFIED (ObjectScript): `Diagram/Correlator.cls` (I3), `Diagram/Compressor.cls` (+episode tier or sibling class — if a NEW .cls is added, ALSO add it to `scripts/gen-bootstrap.mjs` in the Diagram block), `Diagram/Writer.cls` (nesting), `Diagram/Generate.cls` (dedup), `REST/Interop.cls` (dedup param).
- MODIFIED (TS): `packages/iris-interop-mcp/src/tools/diagram.ts` only (thin wrapper). NO shared/ change; NO index.ts change; NO new tool.
- MODIFIED (tests): `Tests/DiagramCorrelatorTest.cls`, `Tests/DiagramGenerateTest.cls` (+ValidateMermaid indent extension), NEW `Tests/DiagramEpisodeTest.cls` (+`DiagramDedupTest` if split), `__tests__/diagram.test.ts`, `diagram-qa.test.ts`.
- MODIFIED (docs): the AC 21.1.7 list. REGENERATED: `bootstrap-classes.ts`.
- NOT touched: `iris_production_messages`, governance baseline, `tools/index.ts`, shared foundation.
- 🚫 Do NOT `git commit`/`git push` — the lead commits after the smoke gate.

### References

- [Proposal §6.3/§6.4 + §6.2 [I3]](../planning-artifacts/sprint-change-proposal-2026-07-02.md) — binding spec; [epics.md Story 21.1](../planning-artifacts/epics.md) (AC 21.1.9 added 2026-07-02)
- [Story 21.0 file](./21-0-message-diagram-core.md) — Dev Agent Record + Review Findings (class shapes, CR 21.0-A…H fixes)
- [deferred-work.md CR 21.0-1/-2](./deferred-work.md) — 21.0-1 resolves HERE as I3; 21.0-2 stays deferred
- Project rules: #7/#8/#33 (handler + sanitize discipline), #10 (wire-explicit `dedup`), #15, #17 (corrected glob), #18, #19 (in-epic byte-for-byte gate), #23/#24/#25, #30 (docs default-state callout), #31 (does NOT apply — normal package tool)
- Conventions: [.claude/rules/iris-objectscript-basics.md](../../.claude/rules/iris-objectscript-basics.md), [.claude/rules/object-script-testing.md](../../.claude/rules/object-script-testing.md)

## Dev Agent Record

### Agent Model Used

Claude Fable 5 (claude-fable-5) — 2026-07-02

### Debug Log References

- No `^ClineDebug` sessions needed — all fixtures passed on first deployed run.
- `iris_execute_tests` partial-snapshot caveat (flagged in Dev Notes) reproduced repeatedly on the full-package run (61-, 44-, and 64-of-147 early snapshots, each 0 failures); resolved by per-class runs — every one of the 14 `ExecuteMCPv2.Tests` classes verified green individually (147 tests total, 0 failures).
- Dev-side live-HTTP probes of the deployed endpoint (the formal smoke is the lead's AC 21.1.8): `sessionIds=80569,80569&dedup=true` → second entry `dedupOf: 80569`, first entry none; `dedup=false` → no `dedupOf`; `dedup=banana` → single clean sanitized envelope `ERROR #5001: Parameter 'dedup' must be one of: 0, 1, true, false` with `result:{}` (no work done).

### Completion Notes List

1. **I3 (AC 21.1.9)** — `Correlator.cls` Queue fallback now additionally requires `tReq.ReturnQueue '= ""` (equality with the response side already checked, so both sides must be non-empty). CorrId-primary path untouched (fixture-proven: empty-ReturnQueue CorrId pairs still pair). One-way async false-pair scenario now yields BOTH warnings (unpaired request + unpaired response), PairId 0 on both — never merged into any loop. `deferred-work.md` CR 21.0-1 marked RESOLVED.
2. **Episode tier (AC 21.1.1/21.1.2)** — implemented in `Compressor.cls` as a second pass (`CompressEpisodes`) over the pair-compressed stream; new `episodeloop` RenderEvent kind carries `Count`, representative `Body` (first iteration's events), `Label`, and deduped warning EXTRAS from iterations 2..N (texts not already on the representative body — §6.4 item 8; the Writer renders body-inline warnings plus the extras before `end`, and `Generate.CollectWarnings` recurses the same way, so nothing is lost or double-counted). Depth-stack rules: sync requests always push (paired or not); paired-async requests push queue-rooted frames (item 6 — known-paired queue frames are never unwound by rule A); rule A pops abandoned SYNC frames whose destination ≠ the new request's source; rule B pops to the (possibly deeper) frame a response's PairId answers; responses and trace events never open an episode boundary (a depth-0 response attaches to the previous episode, which also keeps late async responses from breaking contiguity).
3. **Envelope-wrapper simplification (item 7, recorded per AC)** — instead of the reference's domain naming heuristic, a general structural rule: when the FIRST event of the pair-compressed stream is a paired sync request arrow AND the LAST event is its paired response, the wrapper pair is peeled and episode detection runs on the events between them (equivalent to starting detection at depth 2). Applied once, no recursive peeling. Fixture `TestEnvelopeWrapperDepth2` proves the inner episodes compress and the wrapper arrows render outside the loop.
4. **Representative episode-loop label (recorded per AC)** — the label of the episode's first business (non-trace) event: an arrow's label, or a pair-loop's request-leg label; fallback `"episode"`. Emitted as `loop N times <label>` (label routed through `Writer.SingleLine` — semicolon/CR/LF discipline preserved). Pair loops keep their label-less `loop N times` form, byte-identical to 21.0.
5. **Trace-utility classifier (recorded per Dev Notes)** — prefix match `HS.Util.Trace.` against the ORIGINAL `Event.BodyClass` (not the rendered label, so `short` label mode cannot break it), applied by the Correlator into a new `RenderEvent.IsTrace` flag; a pair-loop counts as trace only when BOTH legs are trace. Prerequisite Correlator change: `Role` is now set from the effective type for EVERY arrow (unpaired arrows carry `"req"`/`"resp"` too; PairId 0 still marks unpairedness) — the pair tier gates on `PairId > 0` so this is invisible to it (21.0 suites all green).
6. **Cross-session dedup (AC 21.1.3)** — in `Generate.Run` after per-session generation: exact full-text comparison with ONLY the line-2 `%% Session …` header replaced by a fixed token (I2 interplay — id, count, and time span all normalize away; the failure shape's `%% Session <id>: generation failed` line normalizes identically). Linear scan (≤20 sessions), request order; first occurrence keeps the flow, later matches get `dedupOf` (number). **Recorded decision:** the duplicate keeps its OWN `mermaid` populated (simplest for clients; nothing is removed — dedup only ADDS the mapping). Two empty sessions dedup; empty id list is a no-op (§6.4 item 9). Note: sessions whose diagrams carry message-id-bearing warnings/error comments genuinely differ textually and therefore do not dedup — honest behavior (their diagrams are not identical); warnings are never dropped by dedup.
7. **REST + TS wiring** — `MessageDiagram()` reads `&dedup=` (case-insensitive `0`/`1`/`true`/`false`; default true; other values rejected with a clear sanitized error — mirrors the labelMode validation discipline); passes `dedup` into the facade options as a boolean; `///` banner updated. `diagram.ts` adds `dedup: z.boolean().optional()`, sends `String(dedup ?? true)` wire-explicitly (Rule #10, appended after `maxRows` so only ONE expected-URL test string changed), surfaces `duplicate of session N` in the summary extras, and types `dedupOf?: number` on the entry. No shared-foundation change; `tools/index.ts` untouched.
8. **Byte-for-byte + thresholds (AC 21.1.4/21.1.5)** — `TestNonRepetitivePassThroughByteForByte` asserts the full mermaid of a mixed non-repetitive trace is IDENTICAL rendered from the pair-tier stream vs the episode-tier stream; all 21.0 exact-content facade tests (`TestRunPipelineContent`, `TestRunPairLoopViaFacade`, `TestGrammarRichDiagram`, Writer suite 13/13) pass unchanged through the new pipeline. Line-count threshold recorded as < 40 lines: the 200×(3-hop, 6-arrow) 1200-message trace renders 14 lines (`loop 200 times …`).
9. **Grammar validator extension** — `ValidateMermaid`/`ValidateLine` now validate arbitrary nesting (indent = 2 spaces × depth, `end` at its loop's opening depth, optional non-empty loop label after "times", participants only at depth 0); new `TestRunEpisodeNestedViaFacade` (live DB fixtures, SESSIONBASE+10) proves a real nested episode(2)×pairloop(3) diagram end-to-end and grammar-validates it. `DiagramGenerateTest` is ~510 lines after the extension (within the "≤~500" guidance; the dedup suite went to a NEW `DiagramDedupTest` to keep it there).
10. **Tests summary** — ObjectScript diagram suite 59 → **79** (Correlator 16→18 [+2 I3], NEW Episode 11 [§6.4 items 3/4-ruleA/4-ruleB/5/6/7 + I1-in-signature + nested-indent + byte-for-byte + threshold + collapse], NEW Dedup 6 [SessionIds 990000401+, teardown sweep %SQLCODE-checked], Generate 10→11 [+nested facade]); full `ExecuteMCPv2.Tests` = 147/147, 0 failures, 0 leaked fixture rows (verified by SQL count over 990000001–990000499). TS: interop 265 → **268** (+3: dedup=false wire, dedupOf summary + verbatim structuredContent, dedup schema accept/reject; 1 expected-URL string updated for the wire-explicit `dedup=true`), shared 551/551 (bootstrap drift tests red-then-green around the regen, as designed), full workspace suite green, interop lint clean, `pnpm -r build` clean.
11. **Bootstrap (AC 21.1.6)** — BOOTSTRAP_VERSION **`5bd5579c25c1` → `195ed1dbfe56`** (regenerated only, Rule #18/#24; RenderEvent/Correlator/Compressor/Writer/Generate/REST-Interop re-embedded). Second `gen:bootstrap` run verified byte-identical (epic-close idempotence). `gen:governance-baseline:check` exit 0; `governance-baseline.ts` git-clean (`1e62c5ad5bf7`); bare generator not run.
12. **Docs rollup (AC 21.1.7)** — all targets updated with the Rule #30 default-state callout ("read, enabled by default") where the tool is documented; counts cross-verified against `index.test.ts` (21/22) BEFORE writing; `docs/tool-annotation-audit.md` deliberately left untouched (frozen 2026-04-07 point-in-time audit in pre-Epic-9 dotted naming; no Epic 15–20 tool was ever appended — recorded decision). CHANGELOG entry covers both stories, provenance, I3, and both BOOTSTRAP_VERSION moves.
13. **NOT done here (per story scope)** — CR 21.0-2 (O(n²) correlator scan) remains deferred (no pairing restructure); no commits made (lead commits after the AC 21.1.8 smoke gate).

### File List

**New:**
- src/ExecuteMCPv2/Tests/DiagramEpisodeTest.cls
- src/ExecuteMCPv2/Tests/DiagramDedupTest.cls

**Modified (product):**
- src/ExecuteMCPv2/Diagram/Correlator.cls (I3 guard; Role on all arrows; IsTrace classifier)
- src/ExecuteMCPv2/Diagram/RenderEvent.cls (+IsTrace, +Body, episodeloop kind docs)
- src/ExecuteMCPv2/Diagram/Compressor.cls (+episode tier: CompressEpisodes/EpisodeCore/EpisodeSignature/IsTraceEvent/RepresentativeLabel)
- src/ExecuteMCPv2/Diagram/Writer.cls (recursive depth-aware RenderBody + RegisterFromEvents; flat output byte-identical)
- src/ExecuteMCPv2/Diagram/Generate.cls (episode tier in pipeline; dedup + NormalizeForDedup; recursive CollectWarnings)
- src/ExecuteMCPv2/REST/Interop.cls (MessageDiagram dedup param + banner)
- packages/iris-interop-mcp/src/tools/diagram.ts (dedup schema/wire; dedupOf summary + typing)
- packages/shared/src/bootstrap-classes.ts (REGENERATED ONLY — `5bd5579c25c1` → `195ed1dbfe56`)

**Modified (tests):**
- src/ExecuteMCPv2/Tests/DiagramCorrelatorTest.cls (+2 I3 tests; fallback test doc updated)
- src/ExecuteMCPv2/Tests/DiagramGenerateTest.cls (ValidateMermaid/ValidateLine nesting extension; +TestRunEpisodeNestedViaFacade)
- packages/iris-interop-mcp/src/__tests__/diagram.test.ts (+3 dedup tests; 1 expected-URL updated; header comment)

**Modified (docs):**
- packages/iris-interop-mcp/README.md (tool row + iris_message_diagram section + example + governance callout + count bump)
- tool_support.md (row + header/Mix 21 + Epic 21 note + rollup 75/99/104 + prose figures)
- README.md (counts 99/21 + tree + governance-defaults table row + tool mention)
- packages/iris-mcp-all/README.md (counts 99/21 + tool mention)
- docs/migration-v1-v2.md (counts 99/21 + tool mention)
- CHANGELOG.md (Epic 21 entry)

**Modified (process artifacts):**
- _bmad-output/implementation-artifacts/deferred-work.md (CR 21.0-1 → RESOLVED)
- _bmad-output/implementation-artifacts/sprint-status.yaml (story status)
- _bmad-output/implementation-artifacts/21-1-episode-compression-dedup-and-docs.md (this file)

**Not touched (verified):** packages/iris-interop-mcp/src/tools/monitor.ts + index.ts (`iris_production_messages` untouched, tool array unchanged at 21), packages/iris-interop-mcp/src/__tests__/diagram-qa.test.ts + index.test.ts (no URL/count assertions affected), packages/shared source (no foundation change), packages/shared/src/governance-baseline.ts (git-clean, `1e62c5ad5bf7`), scripts/gen-bootstrap.mjs (no new class files), docs/tool-annotation-audit.md (frozen audit — recorded decision).

## QA Test Additions (qa-generate-e2e-tests stage, 2026-07-03)

Gap-driven additions only — every stage-candidate gap VERIFIED against the dev suite before writing; details in [tests/test-summary.md](./tests/test-summary.md). Verified already covered and NOT duplicated: §6.4 items 3/4a/4b/5/6/7 + I1 + nesting + byte-for-byte + line-count (`DiagramEpisodeTest`), the six-case dedup matrix (`DiagramDedupTest`), deep-nesting grammar validation live via `TestRunEpisodeNestedViaFacade` (max producible nesting IS 2 levels — an episodeloop body can contain arrows/pairloops only), and candidate (c) live repetitive-trace facade coverage — dev's live coverage is NOT in-memory-only (`TestRunPairLoopViaFacade` = live `loop 3 times` from DB rows; `TestRunEpisodeNestedViaFacade` = live nested episode loop, grammar-validated), so no duplicate was added.

**New: `src/ExecuteMCPv2/Tests/DiagramEpisodeEdgeTest.cls` (+3, ObjectScript suite 147 → 150)** — adversarial episode-tier cases verified ABSENT from the dev suite (sibling class per the story's ~500-line guidance; pure in-memory `Demo.*` clean-room fixtures):
- `TestInterleavedEpisodesDoNotCollapse` — A B A B alternating different-signature episodes emit ZERO `episodeloop` events, preserve original event order, and render **byte-for-byte identical** to the pair-tier stream (pins the §6.3 contiguity requirement — no false merge of non-adjacent identical episodes; dev's `TestLoopCountInSignature` had only adjacent X,Y).
- `TestErroredIterationSplitsAndResumes` — clean×3 + errored + clean×3 identical episodes: I1 splits the collapse AND compression **resumes** — TWO `loop 3 times` blocks with the errored episode's plain arrows (unindented, `[ERROR]`, `%% Error on message 3132: boom` retained) between them. Dev's `TestErrorStateInEpisodeSignature` proved only the split with a trailing SINGLE clean episode, never a second loop.
- `TestLeadingTraceEventDoesNotPoisonCollapse` — a LEADING trace-utility event (the previously-uncovered `tCur=0` trace branch of `Compressor:EpisodeCore`) forms its own never-merged all-trace episode (signature ""); the following 3 identical episodes still collapse (count 3, representative body 4 — the leading trace stays OUT of it); the trace arrow renders unindented before the loop with its unpaired warning (§6.4 item 5 interplay with compression).

**Extended: `packages/iris-interop-mcp/src/__tests__/diagram.test.ts` (+1, interop suite 268 → 269)** — dedup-matrix corner: an EXPLICIT `dedup: true` sends `dedup=true` on the wire (only the omitted-default path was asserted — Rule #10 both ways), and when the endpoint returns two DIFFERENT flows with no `dedupOf`, no "duplicate of" note appears in any summary line and `structuredContent` stays the endpoint object verbatim.

**Gates re-run (2026-07-03):** `iris_doc_load` (src/**/*.cls form, 35 uploaded) compiled ONLY the new test class — all 34 other classes reported "up-to-date" (proof: no product source touched). ObjectScript: all 15 `ExecuteMCPv2.Tests` classes green individually — **150/150, 0 failures**; 0 leaked fixture rows (SQL count over SessionId 990000001–990000699; the new class uses no live fixtures). Interop TS **269/269** (17 files, `diagram.test.ts` 24→25), lint clean. Shared untouched (no shared file changed). The `iris_execute_tests` package-level partial-snapshot caveat reproduced twice (identical 44-of-150 early snapshots, 0 failures both times) — resolved per-class as documented in Dev Notes.

## Review Findings (bmad-code-review, 2026-07-03)

Three-layer adversarial review (Blind Hunter 13 / Edge Case Hunter 5 / Acceptance Auditor 2 findings) + reviewer deep-verification of the review priorities. **Acceptance Auditor verdict: conformant — ACs 21.1.1–21.1.7 + 21.1.9 met with evidence; 21.1.8 is the lead's smoke gate.** Reviewer independently traced every §6.3/§6.4 fixture through the algorithm (each fails if its rule breaks: rule A/B unwind, queue-rooted frames, wrapper peel, LOOPCOUNT, I1, trace exclusion/attachment), confirmed the AC 21.1.4 byte-for-byte gate is non-vacuous and the flat Writer path renders byte-identical literals to 21.0, and re-verified I3 (guard at `Correlator.cls:78` + equality at :83 = both non-empty; CorrId-primary path untouched; `deferred-work.md` CR 21.0-1 accurately RESOLVED). Mechanical gates all PASS: **G2 provenance re-check** (repo-wide `MALIB` grep — planning/story artifacts only, zero code hits; all fixtures synthetic `Demo.*`/`MyApp.*`; only spec-mandated `HS.Util.Trace.*` literals; `../DiagramTool/` not consulted — the only "DiagramTool" strings in code are our own `messageDiagramTool` export), Rule #15 grep clean, Rule #33 (no caret-globals in `$$$ERROR` texts), Rule #10 (dedup wire-explicit both ways), Rule #17/#18/#24 (`gen:bootstrap` idempotent), Rule #23/#25 (`governance-baseline.ts` git-clean `1e62c5ad5bf7`, `:check` exit 0, bare generator NOT run), Rule #28 (scalar `mutates:"read"` unchanged), Rule #19 (`iris_production_messages` snapshot untouched), Rule #30 (docs default-state callout verified at all sites; counts 21/22 + 75/99/104 cross-verified vs `index.test.ts`; stale-count sweep clean).

- [x] [Review][Patch/MED] **CR 21.1-A** — Facade `dedup` option: junk strings and JSON null silently DISABLED the documented default-ON dedup via numeric coercion (`''"yes"` = 0, `''""` = 0) [src/ExecuteMCPv2/Diagram/Generate.cls:49] — **FIXED**: option parse now type-aware (`%GetTypeOf` boolean/number honored, `"true"/"false"/"0"/"1"` strings honored case-insensitively, anything else keeps the default ON); REST/TS paths were already validated and are unchanged. +1 regression test `DiagramDedupTest:TestDedupJunkOptionKeepsDefaultOn` (junk → ON, null → ON, string-"false" still OFF). *(Sources: Blind #9 + Edge #4.)*
- [x] [Review][Patch/LOW] **CR 21.1-B** — `RenderEvent.Role` doc comment still described `""` (unpaired) though the Correlator now sets req/resp on every arrow [src/ExecuteMCPv2/Diagram/RenderEvent.cls:51] — **FIXED**: doc now states Role = effective type on every correlator arrow, unpairedness signaled by `PairId=0`. *(Sources: Blind #7 + Auditor #1 + reviewer.)*
- [x] [Review][Patch/LOW] **CR 21.1-C** — interop README example diagram: header said `14 messages` but the body shows 13 arrows, and the trailing errored unpaired request lacked the `%% Unpaired request` warning the engine always emits alongside the error comment [packages/iris-interop-mcp/README.md:167] — **FIXED**: 13 messages + warning line added (engine-faithful example). *(Sources: Blind #8 + reviewer.)*
- [x] [Review][Patch/LOW] **CR 21.1-D** — CHANGELOG attributed the whole "Two-tier loop compression" to Story 21.1; the pair tier shipped in 21.0 [CHANGELOG.md:14] — **FIXED**: rephrased "pair tier Story 21.0, episode tier + nesting Story 21.1". *(Source: Blind #12.)*
- [x] [Review][Defer/LOW] **CR 21.1-1** — Rule-A unwind is gated on `Kind="arrow"`: a `pairloop` immediately following an abandoned sync frame attaches to the abandoned episode instead of unwinding it (grouping asymmetry vs the same traffic un-collapsed) [src/ExecuteMCPv2/Diagram/Compressor.cls:242] — deferred; never renders untruthfully (pass-through preserves order; merges only on identical composite signatures) → deferred-work.md.
- [x] [Review][Defer/LOW] **CR 21.1-2** — Repeated unanswered sync SELF-calls (Src=Dst) never trigger rule A, grow the depth stack linearly, and defeat episode compression for that pathological shape [src/ExecuteMCPv2/Diagram/Compressor.cls:243] — deferred; degradation-not-corruption → deferred-work.md.

**Dismissed (10, with rationale):** bootstrap/deferred-work "missing from diff" (review-surface packaging only — regen verified idempotent, auditor ran `bootstrap.test.ts` 41/41); async-queue wrapper not peeled (§6.4 item 7 is explicitly the SYNC-wrapper case; a session-spanning queue-rooted episode is item-6 by-design); representative-body trace rendering ×N (inherent, spec-intended lossy loop representation — §6.3 excludes traces from signatures precisely so trace variance can't break compression; warnings survive); dedup keeps full mermaid (recorded stakeholder-sanctioned story decision, Task 4 + Completion Note 6); <40-lines claim "only warning-free" (AC 21.1.5 is a stated synthetic-fixture threshold; per-iteration warnings surviving is the competing item-8 REQUIREMENT); signature delimiter collision + magic prefix length (21.0-accepted `PairSignature` precedent, class-name domain, worst case cosmetic over-merge); duplicated test scaffolding (recorded QA choice per the ~500-line split guidance); I3 whitespace-padded queue equality (queue names are IRIS identifiers; guard matches §6.2 [I3] wording exactly); episode signature lacks pairedness (signature fields are §6.3-fixed; both iterations render textually identical so the collapse is faithful, unpaired warnings survive); truncated sessions can dedup (AC 21.1.3 compares DIAGRAMS by contract; `truncated` flag survives on both entries).

**Post-fix gates:** redeployed via `iris_doc_load` (`src/**/*.cls`, 35 up, only `Generate`+`DiagramDedupTest` recompiled); ObjectScript **151/151** (150 + 1 new regression test; all 15 classes green individually — the package-level partial-snapshot caveat reproduced twice, resolved per-class as documented); 0 leaked fixture rows (SQL sweep 990000001–990000699); **BOOTSTRAP_VERSION `195ed1dbfe56` → `1040c6dcfce3`** (regenerated only, Rule #18/#24; second run byte-identical); shared **551/551** (bootstrap drift green on the new hash); interop **269/269**, lint clean, `pnpm -r build` clean; `gen:governance-baseline:check` exit 0, `governance-baseline.ts` git-clean (`1e62c5ad5bf7`).

## Change Log

- 2026-07-02 — Story 21.1 implemented (dev agent): I3 Queue-fallback non-empty guard (resolves CR 21.0-1); episode-level compression tier (depth stack, §6.4 items 3–8 fixtures, envelope peel, nested rendering); cross-session dedup (`dedup` option + `dedupOf`, I2 header normalization); docs rollup across README/tool_support/migration/CHANGELOG with Rule #30 default-state callout. ObjectScript suite 147/147 (diagram 79), interop 268/268, shared 551/551, workspace green, lint/build clean. BOOTSTRAP_VERSION `5bd5579c25c1` → `195ed1dbfe56` (idempotent second run); governance baseline untouched. Status → review (lead smoke gate AC 21.1.8 next).
- 2026-07-03 — QA stage (qa-generate-e2e-tests): +3 adversarial episode-tier ObjectScript tests (`DiagramEpisodeEdgeTest.cls` — interleaved-ABAB no-collapse with byte-for-byte pass-through, errored-iteration split-and-RESUME into two loops, leading-trace `tCur=0` branch); +1 TS dedup-matrix test in `diagram.test.ts` (explicit `dedup=true` wire + no duplicate-of note when flows differ). ObjectScript 150/150 (0 leaked fixture rows), interop 269/269, lint clean; no product source touched (deploy compiled only the new test class); candidate gaps (a4 deep-nesting grammar, c live repetitive facade) verified already covered — not duplicated. Summary superseded at `tests/test-summary.md`.
- 2026-07-03 — Code review (bmad-code-review, 3-layer): Auditor conformant, no HIGH. 4 findings auto-fixed inline (CR 21.1-A MED facade `dedup` junk/null coercion → type-aware parse + regression test; CR 21.1-B RenderEvent.Role doc; CR 21.1-C README example; CR 21.1-D CHANGELOG attribution), 2 deferred (CR 21.1-1/-2 → deferred-work.md), 10 dismissed with rationale. G2 provenance re-check clean. Post-fix: OS **151/151**, 0 leaked rows, interop 269/269, shared 551/551, lint/build clean; **BOOTSTRAP_VERSION `195ed1dbfe56` → `1040c6dcfce3`** (review fixes re-embedded, idempotent); baseline `1e62c5ad5bf7` intact. Status → done (lead smoke gate AC 21.1.8 + commit next). See Review Findings section.

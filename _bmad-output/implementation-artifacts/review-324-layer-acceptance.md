# Acceptance Auditor — Story 32.4 (Late Review-Layer Findings Cleanup)

Reviewed `/tmp/324-review.diff` (36 files: tracked diff since 78b32ba + 2 untracked test files) against `_bmad-output/implementation-artifacts/32-4-late-findings-cleanup.md` (ACs 32.4.1–32.4.4), the ledger's Story 32.4 disposition section, `epics.md`, and `architecture.md`. All tallies re-derived mechanically; code claims spot-verified against the diff and the working tree; one probe and three full suite runs executed by this layer.

## Findings

### LOW — Story record not reconciled after QA (Rule #51; fourth consecutive story with this gap)
**Violates:** Rule #51 (the surviving record must match the tree) / AC 32.4.3's tally-consistency intent.
**Evidence:** The story's File List omits QA's additions now in the diff: `packages/iris-mcp-all/src/__tests__/host-guard-process-gate.test.ts` (new, 4 cases), precedence-gate Cases E/F, and governance-gate Case J. The AC 32.4.4 Completion Note claims "iris-mcp-all 16/105" — dev-stage; the current tree is **17 files / 112** (cycle-log `qa_complete`: `tests_added=7`, `all112`). The extension (20/327) and shared (65/1294) story counts are current and verified exact by this layer (below).

### LOW (observation) — One transient, unidentified shared-suite test failure in this layer's first recount; green on two immediate identical re-runs
**Violates:** Nothing — AC 32.4.4's "all green" claim is reproducibly true — reported for the record (a flake without a name).
**Evidence:** First `npx vitest run` in `packages/shared`: **1 failed / 1293 passed (1294)**; the output capture kept only the tail, so the failing test's identity was lost. Two immediate full re-runs with zero code changes: **65/65 files, 1294/1294, exit 0** both times. If the failure recurs it needs a name and an owner; a one-off in a 1294-test suite with live-IRIS-adjacent tests is most likely timing, but that is a guess, not evidence.

## No HIGH or MEDIUM findings. Verified per mission item:

1. **AC 32.4.1 (mechanical recount)** — Extracted the ledger's Story 32.4 section and recounted the disposition column directly: **23 rows · 20 RESOLVED · 2 CLOSED-WITH-EVIDENCE (32-3-R15, 32-1-R2) · 2 rows carrying CLOSED-BY-DECISION (32-3-R4 standalone; 32-3-R13 as a split row whose boundary half is RESOLVED) · 0 unmarked · 0 duplicate IDs**. ID set = 32-3-R1…R15 + 32-1-R1…R7 + 32-2-R1 exactly. The ledger's own derivation block and the story's Completion Notes tally (20/2/1 + 1 split) agree with the mechanical counts. `32-1-R2`/`32-3-R15` verified as already-fixed-on-current-tree closures (the resolveFilePath empty-flag guard and the vscodeMock getSession rejection shape are both in situ) — closed-with-evidence is legitimate, not re-litigation.
2. **AC 32.4.1 amendment** — `epics.md` AC 32.4.1 amended in place: original "22" preserved with the dated "amended 2026-07-27 … originally read 22 — a hand-count error; the three ranges enumerate 23" parenthetical (skill Rule 5 satisfied; matches the story's own AC text).
3. **AC 32.4.2 (8 MEDIUMs)** — every one carries named evidence, none waved through: **32-3-R1** decision option (a) recorded in code + ledger; `nameStates.set(drop.name, "invalid")` correctly inside the first-sighting guard (a lower valid entry is skipped, a later drop can't clobber a valid claim — verified in the diff); check-3 message now states BOTH halves (terminal-invalid NOT reconsidered; 31-0-2 username-only skip yields); cross-file fixtures both directions; mutation-verified. **32-3-R2** — this layer re-ran the probe: `path.win32.isAbsolute("/x")` is **true**, so the ledger's "premise partially disproven" claim is accurate; the explicit posix disjunct + host-OS-independent pin is honest defense-in-depth. **32-3-R3** once-per-session dedupe + `getServerManagerApiFailureReason` suppression in both consumers; mutation-verified. **32-3-R4** closed-by-decision with verify-first reasoning (eager activation predates 32.3 — `extensionDependencies` + AC 31.5.4; recorded in the `extension.ts` header; no GUI-observable claims). **32-3-R5** recorded product decision (effective count in the `[]` state when known non-zero; AC 31.5.3's pinned tests untouched; tooltip signal preserved; CHANGELOG entry). **32-3-R6** guard moved to the FINAL host (override or inherited `IRIS_HOST`), extended to `\`/`:` with the inline-port rationale, origin named, value never echoed; `loadConfig` fails fast on hostile `IRIS_HOST` naming the var. **32-1-R1** Case G spawns now use the case-insensitively-scrubbed `childEnv`. **32-1-R3** `serializeConfigPreservingUnknown` round-trips unknown top-level keys at original position + layer-shaped typo warning; Case J proves it at the wire byte-level.
4. **Closed-by-decision entries** — both recorded WITH reasoning (R4: predates 32.3 + 31-5-2 dependency; R13-TOCTOU: OS-atomic write, message-only mislabel, no cross-keychain check-and-set primitive — recorded at `cmdSet`).
5. **Rule 3 (real-runtime gates GENUINE)** — host-guard gate: dist-only startup-failure assertions (A/B/C fail fast, no value echo) + live handshake control (D, no over-trigger). Precedence Case E: real MCP handshake asserting the parser-dropped name is NOT imported from a lower valid file, the shadowed host never reaches the roster, and both drop warnings fire. Case F: required-mode spawn asserting check-3 ("NONE could be imported" + "NOT reconsidered") and NOT the misleading check-1. Governance Case J: `spawnSync` of the BUILT bin asserting unknown-key preservation byte-for-byte and the typo warning on BOTH set and unset. Each would genuinely fail if its guard broke (mutation logs corroborate).
6. **Rule 6 (ADR)** — Reviewed D1–D8 + J1 in `architecture.md`. No Accepted ADR constrains these surfaces in a way this diff violates: D7's fail-fast-naming-the-var is honored by the R6 `IRIS_HOST` guard and the CLI's error texts; J1 gains no new discovery channel; D5's chokepoint is untouched. **No HIGH.**
7. **Integration AC declaration TRUTHFUL** — no new exported service; the one new export, `parseGovernanceFileText`, has a named consumer (the CLI's read-once pre-image in `cmdSet`/`cmdUnset` — the 32-1-R7 fix itself) and a dedicated fs-spy pin (`governance-cli-preimage.test.ts`, exactly-one-read proof).
8. **AC 32.4.4 consistency** — the diff touches no `src/tools/**` file and registers no tool/action (Rule #31 untriggered; the baseline `:check` cannot move — confirmed consistent with the 141/201/60 state verified by this layer earlier this session); no `bootstrap-classes.ts`/`BOOTSTRAP_VERSION`/`src/ExecuteMCPv2/**` change; NUL scan of 13 new/changed source+test files clean (Rule #55). Suite recounts by this layer: extension **20 files / 327 tests all green** (matches the claim exactly); shared **65 files / 1294 green** (see the flake observation above).

## Considered and rejected as findings

- **R5 vs AC 31.5.3's fresh-install signal** — the zero-state text change applies only when the effective count is known and non-zero (a state AC 31.5.3 did not contemplate); the pinned AC 31.5.3 tests are byte-untouched and the fresh-install guidance survives in the tooltip; the product decision is recorded in the ledger AND the CHANGELOG (Rule #43). Not a violation.
- **R3 once-per-session vs R7 rising-edge asymmetry** — the shape-mismatch warning dedupes once-per-session while the four other static warnings got rising-edge semantics; deliberate per the ledger (a version mismatch is not a fix-then-rebreak class; a recovered API is cached, so no re-evaluation occurs). Not a contradiction.
- **32-1-R7 read-failure double-path** — on a raw read failure the CLI re-runs the real loader purely to obtain the server's exact error text (the EISDIR contract); the unreachable-success race prints a plain "could not read" — cosmetic, single-process CLI.

## Severity tally

- HIGH: 0
- MEDIUM: 0
- LOW: 2 (story-record reconciliation; transient unidentified suite flake, non-reproducing)

# Story 37.1: MEDIUM - Default-Profile Outage Must Not Take Down the Server — Degrade and Retry Instead of `process.exit(1)`

Status: done

<!-- Created 2026-09-16 by the correct-course session (Mary) with the lead pre-story probe below, per the practice the Epic 35/36 retros credited. The epic-cycle's create-story step should ADOPT this file rather than create a second one. Validation is optional: run validate-create-story before dev-story if desired. -->

## Story

As an **operator (or the AI client acting for them) who has registered several IRIS instances as server profiles**,
I want **every server in the suite to start and serve calls against the reachable profiles even when the reserved `default` profile's instance is down, and calls against the down profile to fail per call with the standard connection error and recover on their own once it is back**,
so that **one instance's outage never takes the whole MCP server with it, and I never have to reconfigure `IRIS_*` or restart the client to route around it**.

## Context — why this story exists

Second beta-feedback defect (verbal, 2026-09-16, relayed by the Project Lead): "the MCP server fails to start if the default server is down but the other registered servers are up." Full record with evidence, the four fix options and the Lead decisions: `docs/bugs-2026-09-16.md` and `sprint-change-proposal-2026-09-16-default-profile-outage.md`.

`McpServerBase.start()` (`packages/shared/src/server-base.ts:2026-2034`) calls `process.exit(1)` when the eagerly-created `default` client fails `checkHealth`, BEFORE the transport connects (`:2090`). Non-default profiles are established lazily and already have the desired semantics (retryable first touch, structured per-call error). The asymmetry is written down in the JSDoc — "only the default profile's startup failure is fatal" (`:2141`) — and is a carried-over single-server behavior from Epic 14 decision D1 / Story 14.1 (`213756b`), never re-examined once profiles existed. Architecture record M1 (added with this epic) fixes the principle: invalid config fails fast; an unreachable peer never does.

**This is the ONLY story of Epic 37** (Project Lead decision L-5, 2026-09-16): there is no 37.0 gate and no burn-down in this epic; the Rule #37 forward obligation transfers to the Epic 38 gate as a recorded exception (epic preamble in `epics.md`; `sprint-status.yaml`; the ledger's 2026-09-16 section). The gate duties that matter to this story are folded into AC 37.1.12. The change proposal's PRD (FR145 + Reliability bullet) and `architecture.md` M1 edits are ALREADY applied — do not re-apply.

**This story is pure TypeScript** in `@iris-mcp/shared` (`server-base.ts`, `profiles.ts` comments, tests, docs). No ObjectScript change, so NO bootstrap regen; `BOOTSTRAP_VERSION` must NOT move (`2159ec9e6f69` per the Epic 36 retro §6 — verify at pickup). Prove that.

**Epic constraints in force:** F-1 (no tool, no action key, no schema field, no env var; frozen baseline `1e62c5ad5bf7`/141, live/post-foundation 203/62; `gen:governance-baseline:check` ONLY); F-2 (success path byte-for-byte — the 38 `stageDefaultStartup` files and the eager pins pass unmodified); F-3 (config fail-fast paths untouched). Lead decisions: **L-2** non-fatal everywhere, single-server included; **L-3** no strictness knob (ledger candidate); **L-4** no discovery reachability field (one doc line instead); **L-6** changeset `@iris-mcp/shared` patch.

**The shared IRIS instance is in use by other projects.** An outage is ALWAYS simulated with a closed local port (`IRIS_HOST=127.0.0.1 IRIS_PORT=9`), never by stopping the instance; traffic to the real instance is read-only (`HEAD`/`GET`, `iris_server_info`).

## Acceptance Criteria

Sourced verbatim from `_bmad-output/planning-artifacts/epics.md`, "### Story 37.1" (added via correct-course 2026-09-16).

1. **AC 37.1.1** - Root cause confirmed at `packages/shared/src/server-base.ts:2023` (eager `getOrCreate(DEFAULT_PROFILE_NAME)`), `:2026-2034` (`checkHealth` → `process.exit(1)`), `:2037-2043` (negotiation), `:2049-2072` (bootstrap), `:2082-2085` (`profileMeta.set(DEFAULT_PROFILE_NAME, …)`), `:2090` (transport connect — reached only after the above). Evidence recorded: the closed-port run (exit 1; no connect line; second profile never probed).
2. **AC 37.1.2** - Probe-first (#16/#47), BEFORE coding, recorded in Dev Notes: (a) confirm `ProfileClientRegistry.drop` (`profiles.ts:708`) is safe for `default` (no special-casing) and that `getOrCreate` re-creates it from the registry's synthesized default (`profiles.ts:424`); (b) confirm `establishProfile` (`:2220`) handles `profile.name === "default"` without change — its `attemptProfileBootstrap(profile, …)` passes the `IrisProfile` (which spreads the full `IrisConnectionConfig`) where `start()` passed `this.config`, so the bootstrap inputs are equivalent; (c) enumerate every reader of the private `atelierVersion` field (`:381`) — only the startup log line and the eager bootstrap — and decide, recording why: sync it in `establishProfile` when `profile.name === DEFAULT_PROFILE_NAME`, or leave it log-only with `profileMeta` as the sole source of truth for calls; (d) read the MCP `instructions` string in `server-base.ts` and the `Fatal:` catch in each server's `index.ts` for wording that assumes the default is reachable.
3. **AC 37.1.3** - **Fix (option A; L-2).** In `start()`, on `checkHealth(defaultClient)` rejection: log at ERROR the existing message PLUS one line stating that startup continues, that tools targeting the `default` profile will return a connection error until it is reachable, and that it recovers automatically; `this.clients.drop(DEFAULT_PROFILE_NAME)`; do NOT set `profileMeta` for `default`; skip negotiation and bootstrap; proceed to connect the transport. Remove the `process.exit(1)` for this case ONLY (F-3). On the success path nothing changes (F-2). The same semantics apply with only `default` registered.
4. **AC 37.1.4** - **Startup survives (Rule #21 capstone, unit half).** With the health `HEAD` rejected and ≥1 other profile registered: `start("stdio")` resolves, `process.exit` is NOT called, the transport is connected; the ERROR log names host:port, the reason and the continuation semantics. Same assertion with only `default` registered (L-2).
5. **AC 37.1.5** - **Others serve; default degrades then recovers without restart.** After the failed eager attempt: a call with `server:"other"` (fetch mocks: `HEAD` 200 + version) succeeds; a call on `default` while its `HEAD` still rejects returns the EXISTING envelope `Could not connect to server profile "default": …` with `isError: true` (no throw out of the SDK handler, no exit); the client was dropped (`registry.has("default")` false after the failure; a fresh instance on the next attempt); once the `HEAD` resolves, the next call establishes through `establishProfile` (health → negotiation → one-time bootstrap when `needsCustomRest`) and succeeds; bootstrap is attempted exactly once across the recovery; two concurrent first-touch calls on `default` share ONE establishment (`establishing` coalescing, AC 14.2.7 symmetry). Every fake returns a shape the real `fetch`/Atelier API can return (#54).
6. **AC 37.1.6** - **Success path unchanged (F-2, Rule #19 mechanical proof).** `server-base.test.ts:558` is the ONLY existing test that changes (it becomes "continues startup, logs, does not exit"); `git diff --stat -- 'packages/*/src/__tests__'` lists that file plus the new test file(s) and nothing else; the eager pins named in F-2 pass unmodified; a `toEqual` pin of the startup fetch-call sequence under a reachable default fails on any drift.
7. **AC 37.1.7** - **Gate proven RED on the path it guards (Rules #48/#59).** The AC 37.1.4/37.1.5 tests live in the DEFAULT suite (never an excluded suffix); with the fix reverted (reinstate `process.exit(1)`) they go RED; restore → GREEN. The Rule #59 statement names the path: `McpServerBase.start()` under a mocked-rejecting `fetch`, the same call the real entry points make.
8. **AC 37.1.8** - **Live proof from the BUILT dist in a fresh Node process (#22/#26/#34), no shared-instance changes.** `default` → `127.0.0.1:9`; `other` → the real instance (read-only). Over stdio JSON-RPC: `initialize` succeeds; `tools/call iris_server_info {server:"other"}` → result; `tools/call iris_server_info {}` → the `isError` envelope naming `default`; stderr carries the ERROR + continuation lines and `Connected via stdio transport`. Repeat on a SECOND server package (e.g. `iris-ops-mcp`, `needsCustomRest` differs from dev) to prove the framework path, and once with `IRIS_SERVER_MANAGER=auto` if a Server-Manager profile is available locally (source `server-manager` profiles take the same lazy path — record the verdict). Mutation leg: the pre-fix dist reproduces exit code 1. A permanent armed live gate in the default suite (read `epic35-defect-gate.test.ts` for the arming convention first — #47) pins the closed-port-default + reachable-other round trip.
9. **AC 37.1.9** - **Docs (#43 self-documenting; #56 enumerate exhaustively, verdict per surface).** `README.md`: Backward Compatibility (`:528-540`) gains a bullet stating the failure-mode change ("startup no longer exits when the `default` instance is unreachable; calls to it return a connection error and recover on their own; invalid configuration still fails fast"), and the Profiles section (`:344-356`) gains one paragraph on per-profile independence at startup; `docs/client-config/README.md` startup-failure guidance near `:30` and the `claude mcp list` note near `:289` ("a running server no longer implies the default IRIS is reachable — call a cheap read, e.g. `iris_server_info`, to check") — the L-4 doc line; `docs/client-config/claude-code.md`, `claude-desktop.md`, `cursor.md`, the five per-server READMEs, `packages/iris-mcp-all/README.md`, the `iris-mcp-launcher` README: checked for startup/health-check claims with a recorded verdict each; `tool_support.md`: no key moves — verdict recorded; `CHANGELOG.md` "Pre-release — <date>" entry under Fixed (since-when anchored: Story 14.1, `213756b`); JSDoc/comments at `server-base.ts:1506-1510`, `:1924-1935`, `:2025-2026`, `:2137-2141`, `:2220-2230` and `profiles.ts:655-665`; the Story 14.1 file is NOT edited (M1 supersedes its clause). Run every documented example.
10. **AC 37.1.10** - **Ledger candidates recorded (not stories):** option D strictness knob with its named trigger (L-3); `iris_server_profiles` per-profile reachability (L-4); periodic re-ping (`health.ts` `ping()` is unused by `server-base` today). Each as a LOW row with rationale, Rule #37 count 0.
11. **AC 37.1.11** - Gates: `pnpm turbo run build test lint type-check` green (per-package standalone runs acceptable per `35-1-DEV-1`, substitution recorded); `gen:governance-baseline:check` (`:check` ONLY) exit 0 at frozen `1e62c5ad5bf7`/141, live/post-foundation 203/62 (F-1); tool counts unmoved, no new key (#31); `bootstrap-classes.ts` absent from the diff, `BOOTSTRAP_VERSION` stays `2159ec9e6f69`; changeset added (`@iris-mcp/shared` patch — L-6); the OS suite untouched (`total` vs `Test*` count, #35, unchanged).
12. **AC 37.1.12** - **Gate duties folded in (no 37.0 in this epic — L-5).** Confirm `epic37` exists off `main` (`2ed83ff` or later) and is the current branch before any code lands (L-1; HALT and escalate otherwise); Rule #57 machinery armed for this story's review (bounded-close, frozen-diff snapshot, delivery receipts — Epic 36 closed non-degraded, hold that bar); suite baselines re-measured at pickup with per-package standalone runs (prior figures in `deferred-work.md` → "Epic 36 retro-review gate" → "Suite baselines", per `36-0-CR-Z`; the `35-1-DEV-1` and `36-4-CR-4` parallel flakes are environmental); the ledger is NOT triaged here — the Rule #37 exception in the epic preamble stands and the Epic 38 gate owns the transferred obligation.

## Lead pre-story probe — what the correct-course session verified (2026-09-16)

Everything below is READ-ONLY evidence from source reading on `main` @ `2ed83ff` and one run of the built dist against a closed local port. **The shared instance was not touched.** Re-run cheaply if you extend anything (Rule #16). Line numbers are as of `2ed83ff`.

**P1 — The fatal path, end to end (`packages/shared/src/server-base.ts`).** `start()` (`:1937`) → `loadConfig()` (`:1941-1942`, THROWS on missing `IRIS_USERNAME`/`IRIS_PASSWORD` — `config.ts:135-141` — the rejection reaches the entry point's `Fatal:` catch, `packages/iris-dev-mcp/src/index.ts:31-34`, which exits 1: that is the CONFIG fail-fast route and stays) → profile registry (`:1943-1952`) → governance/preset/file/audit parses (`:1954-2019`; explicit `process.exit(1)` at `:1995` audit config and `:2011` unwritable audit dir — F-3, untouched) → **`:2023` eager `getOrCreate(DEFAULT_PROFILE_NAME)`** → **`:2026-2034` `await checkHealth(defaultClient)`; catch → `logger.error("IRIS health check failed: …")` → `process.exit(1)` → `return`** → `:2037-2043` negotiation (failure = warn + v1) → `:2045-2047` "starting with Atelier API v…" log → `:2049-2072` bootstrap when `needsCustomRest` (failure = warn) → `:2082-2085` `profileMeta.set(DEFAULT_PROFILE_NAME, { atelierVersion, bootstrapAttempted: needsCustomRest === true })` → **`:2088-2091` stdio transport connect**. The exit at `:2033` precedes the connect at `:2090`; nothing after it runs.

**P2 — The lazy path already has the target semantics.** `getOrCreateClient` (`:2166`): fast path when `profileMeta` has the profile (`:2181-2186`); otherwise coalesced via `establishing` (`:2192-2211`). `establishProfile` (`:2220`): `registry.getOrCreate(profile.name)` → if no meta: `checkHealth(client)`; on throw `registry.drop(profile.name)` then rethrow (AC 14.2.8); negotiation (failure = warn + v1); `meta = { atelierVersion, bootstrapAttempted: false }`; then `attemptProfileBootstrap(profile, client, version)` once when `needsBootstrap`. `handleToolCall` (`:1506-1531`) wraps `getOrCreateClient` and returns `{ content: [{ text: 'Could not connect to server profile "<name>": <message>' }], isError: true }` on throw (text at `:1528`). NOTHING in `establishProfile` special-cases `default`; it is simply never reached for `default` today because `start()` seeds `profileMeta` first (or exits). `ProfileClientRegistry` (`profiles.ts:667-723`): `getOrCreate` resolves via `resolveProfile` (works for `default`, `:637`), `drop` (`:708`) destroys + deletes any cached client, no name checks. The `default` profile is synthesized at `profiles.ts:424` as `{ name: "default", ...defaultConfig }` — it carries EVERY `IrisConnectionConfig` field, so `bootstrap(client, profile, version)` on the lazy path receives the same inputs as `bootstrap(client, this.config, version)` on the eager path (`bootstrap` signature: `bootstrap.ts:446-450`, second arg `IrisConnectionConfig`).

**P3 — Empirical reproduction (no IRIS involved).** Built dist of 2026-09-11 06:07. Command and result:

```
IRIS_HOST=127.0.0.1 IRIS_PORT=9 IRIS_USERNAME=probe IRIS_PASSWORD=probe \
IRIS_PROFILES='{"other":{"host":"127.0.0.1","port":52773}}' \
node packages/iris-dev-mcp/dist/index.js </dev/null
# exit code 1
# stderr:
# [INFO] Tool visibility: preset="full" visible=30 hidden=0
# [ERROR] IRIS health check failed: Failed to connect to IRIS at 127.0.0.1:9. Verify the host and port are correct, and that IRIS is running.
```

No `Connected via stdio transport` line; the `other` profile was never probed (nothing listens on 52773 on this host either — a probe would have logged a second connection error). macOS has no `timeout` binary: wrap timed runs as `perl -e 'alarm 30; exec @ARGV' -- node …` (exit code preserved).

**P4 — Test blast radius (mechanical).** Exactly ONE existing test pins the exit: `packages/shared/src/__tests__/server-base.test.ts:558-569` ("should call process.exit when health check fails" — `fetchMock.mockRejectedValue(new TypeError("Connection refused"))`, then `expect(exitMock).toHaveBeenCalledWith(1)`; `process.exit` is spied at `:546-550`). The eager-establishment pins that MUST stay green unmodified: `server-base.test.ts:728` ("establishes the default profile eagerly in start() and reuses that client" — asserts no extra fetches after start and `atelierVersion === 8`), `server-param.test.ts:340`, `server-param-integration.test.ts:357`, `governance-enforcement.test.ts:285`, `governance-preset-cross-surface.test.ts:329`. **38 test files** define a LOCAL `stageDefaultStartup(fetchMock)` helper (one `HEAD` 200 + one version response — e.g. `governance-file.test.ts:764`, `governance-enforcement-coverage.test.ts:253`, `resolve-profile-client.test.ts:170`; count via `grep -rl stageDefaultStartup packages/*/src/__tests__/*.ts | wc -l`); every one stages the SUCCESS path, which option A leaves byte-for-byte. `server-param.test.ts:529-599` is the existing AC 14.2.8 non-default retry suite — the natural template for the `default` recovery test (AC 37.1.5). Tests that read `ctx.atelierVersion` expect the fixture's `8` (`server-base.test.ts:386`, `resolve-profile-client.test.ts:223`, `tool-types.test.ts:166`) — unaffected.

**P5 — The private `atelierVersion` field.** `private atelierVersion = 1` (`:381`, JSDoc "unchanged back-compat field" — it is PRIVATE; no external reader). Writers/readers: `:2039`/`:2042` (negotiation), `:2046` (startup log), `:2055` (eager bootstrap arg), `:2083` (meta seed). After a failed eager attempt it stays `1` and the startup log would print "Atelier API v1" — misleading; on the failed path log "default profile not established" instead. Calls never read the field; they read `profileMeta` (`:2186`, `:2257`, `:2261`). Decision per AC 37.1.2 (c).

**P6 — Other consumers and surfaces.** `checkHealth` (`health.ts:29`) = `HEAD /api/atelier/` with a 5 s timeout (`HEALTH_CHECK_TIMEOUT`), throws `IrisConnectionError` (`errors.ts:15-27`; message already ends with the recovery suggestion). `ping()` (`health.ts:58`) is exported but has NO caller in `server-base.ts` (grep) — the "monitoring loop (Story 1.4)" its comment mentions does not exist; ledger candidate (AC 37.1.10). `iris_server_profiles` never connects (README `:472`) — unaffected, and it remains the right first call after startup. The `iris-mcp-credentials test --connect` CLI reuses `checkHealth` and throws — correct for a diagnostic, unchanged. The `iris-mcp-launcher` extension only spawns the server (`extensions/iris-mcp-launcher/src/extension.ts`); no health handling of its own — verdict for AC 37.1.9.

**P7 — Docs surfaces found by grep (verdict each, AC 37.1.9).** `README.md`: `:78` (`IRIS_PROFILES` row), `:97` (single-server "behaves exactly as it always has" — still true for the SUCCESS path; consider one clause), `:109` (removing `IRIS_PASSWORD` "breaks startup for all of them" — CONFIG, stays true), `:344-356` (Profiles), `:472` (discovery works when unreachable), `:528-540` (Backward Compatibility — add the bullet). `docs/client-config/README.md`: `:30` ("Omitting `IRIS_USERNAME` fails at startup" — config, stays true), `:289` (`claude mcp list` health check — add the L-4 line). `claude-code.md:164/:235/:313`, `claude-desktop.md:23/:280`, `cursor.md:233/:311` mention startup only for config failures — verdicts expected "unchanged". `tool_support.md`: no key moves.

**P8 — Branch, gate and version facts.** `main` = `2ed83ff`; wave-5 tip `63bf908` has an IDENTICAL tree (`git diff --stat 63bf908 2ed83ff` empty). `epic37` was cut off `main` (`2ed83ff`) on 2026-09-16 and the planning-artifact commit is its first commit (L-1). No 37.0 gate (L-5) — AC 37.1.12 carries the gate duties. `BOOTSTRAP_VERSION` at Epic 36 close: `2159ec9e6f69` (retro §6); confirm at pickup with the read-only `SELECT ExecuteMCPv2.Setup_GetBootstrapVersion()` via `iris_sql_execute` and by reading `packages/shared/src/bootstrap-classes.ts`. Changesets: `.changeset/config.json` has `fixed: [["@iris-mcp/*"]]` — one `@iris-mcp/shared: patch` entry bumps the suite together.

**Not verified — yours (AC 37.1.2):** (a) `drop` + `getOrCreate` round trip for `default` in a unit test; (b) `establishProfile` with `profile.name === "default"` end to end incl. bootstrap; (c) the `atelierVersion` decision; (d) the `instructions` string and each `index.ts` wording. Also: whether `epic35-defect-gate.test.ts`'s arming convention fits a closed-port-default gate (read it first); whether a Server-Manager profile is available locally for the `IRIS_SERVER_MANAGER=auto` leg; the `iris-ops-mcp` dist leg.

## Tasks / Subtasks

- [x] **Task 1 — Probe-first (AC 37.1.1, 37.1.2).** Re-confirm the anchors on the epic branch; run probes (a)–(d); record verbatim in Dev Notes; decide the `atelierVersion` handling with the reason.
- [x] **Task 2 — The fix (AC 37.1.3).** Rewrite the `:2026-2034` catch: ERROR log (existing message + continuation line) → `this.clients.drop(DEFAULT_PROFILE_NAME)` → skip `:2037-2085` (negotiation, "starting with" log, bootstrap, meta seed) via a single `defaultEstablished` flag → fall through to the transport connect. Keep the success path byte-identical. Update the startup log for the failed path. Update JSDoc/comments at `:1506-1510`, `:1924-1935`, `:2025-2026`, `:2137-2141`, `:2220-2230`, `profiles.ts:655-665`.
- [x] **Task 3 — Unit tests (AC 37.1.4–37.1.7).** Flip `server-base.test.ts:558`; add a new default-suite file (e.g. `server-base-default-outage.test.ts`) using `server-param.test.ts:529-599` as the template: startup survives (multi-profile AND single-profile), other serves, default degrades → recovers, drop/re-create, bootstrap-once across recovery, coalescing; a `toEqual` pin of the success-path fetch sequence. Mutation-prove RED with `process.exit(1)` reinstated; record the Rule #59 path statement.
- [x] **Task 4 — Live proof + armed gate (AC 37.1.8).** Rebuild; run the built `iris-dev-mcp` and `iris-ops-mcp` dists with `default` → `127.0.0.1:9` and `other` → the real instance; drive `initialize` + two `tools/call`s over stdio; keep the JSON-RPC driver script disposable (delete before staging). Pre-fix dist reproduces exit 1. Add the permanent armed gate (arming convention from `epic35-defect-gate.test.ts`). Server-Manager leg with a recorded verdict.
- [x] **Task 5 — Docs, CHANGELOG, changeset (AC 37.1.9, 37.1.11).** Every surface in P7 with a verdict; CHANGELOG "Pre-release — <date>" Fixed entry (since Story 14.1, `213756b`); `.changeset/*.md` with `"@iris-mcp/shared": patch`.
- [x] **Task 6 — Ledger candidates (AC 37.1.10).** Three LOW rows in a new `deferred-work.md` section for this story (strictness knob + trigger; discovery reachability; `ping()` unused). No count claims beyond "+3 LOW opened" with the arithmetic shown (#51).
- [x] **Task 7 — Gates and gate duties (AC 37.1.11, 37.1.12).** Branch check; Rule #57 arming; baselines re-measured; `pnpm turbo run build test lint type-check` (standalone substitution recorded if needed); `gen:governance-baseline:check`; `bootstrap-classes.ts` absent from the diff; `BOOTSTRAP_VERSION` unchanged; OS suite untouched.

## Dev Notes

### Fix shape (option A) — target code, refine in review

```ts
// start(), replacing the :2026-2034 catch. Success path unchanged.
let defaultEstablished = true;
try {
  await checkHealth(defaultClient);
} catch (error: unknown) {
  const message = error instanceof Error ? error.message : String(error);
  logger.error(`IRIS health check failed: ${message}`);
  logger.error(
    `Continuing startup without the "${DEFAULT_PROFILE_NAME}" profile: tools targeting it will ` +
      `return a connection error until it is reachable, then recover automatically (no restart needed).`,
  );
  // AC 14.2.8 symmetry: drop the un-established client so the first call
  // re-creates it and re-attempts establishment via establishProfile().
  this.clients.drop(DEFAULT_PROFILE_NAME);
  defaultEstablished = false;
}

if (defaultEstablished) {
  // :2037-2085 exactly as today — negotiation, "starting with" log, bootstrap, profileMeta seed.
} else {
  logger.info(`${this.options.name} v${this.options.version} starting; "${DEFAULT_PROFILE_NAME}" profile not yet established`);
}
// :2088-2091 transport connect, unchanged.
```

With `profileMeta` unset for `default`, the first `getOrCreateClient("default", needsCustomRest)` misses the fast path (`:2181`) and runs `establishProfile` (`:2220`), which is EXACTLY the non-default first-touch flow: health → negotiation → meta → one-time bootstrap. No new establishment logic (Rule #47). The `establishing` map coalesces concurrent first touches (AC 14.2.7).

### Traps

- **Do not go fully lazy (option B).** Keeping the eager attempt preserves the startup-time bootstrap report and keeps the 38 `stageDefaultStartup` files untouched. Deleting steps 2–4.5 would move a possibly long bootstrap inside the first tool call (the Epic 36 client-ceiling problem) and re-sequence dozens of tests for the same user-visible outcome.
- **`profileMeta.set(DEFAULT_PROFILE_NAME, …)` must stay INSIDE the success branch** with `bootstrapAttempted: this.options.needsCustomRest === true` — the comment at `:2074-2081` explains why hard-coding `true` would skip the first-use bootstrap on a `needsCustomRest: false` server.
- **Only the health-check exit moves.** `:1995` (audit config) and `:2011` (audit dir) stay `process.exit(1)`; `loadConfig`/governance/preset/file parse errors stay THROWN (they surface through each entry point's `Fatal:` catch). F-3.
- **Reuse the existing per-call envelope** at `:1506-1531`; do not add a second "could not connect" shape.
- **Startup log on the failed path** must not print "Atelier API v1" — print that the default profile is not yet established.
- **Test seams:** existing tests spy `process.exit` (`server-base.test.ts:546-550`) and call `server.start("stdio")` under `globalThis.fetch` mocks — the new tests must assert `start()` RESOLVES and `exitMock` was NOT called, then drive `handleToolCall` via the existing `callTool` helpers the neighbouring suites use. For the recovery leg, make the mock path-aware (route `HEAD` vs version `GET` by URL) so the first rejected `HEAD` and the later successful `HEAD` do not shift a shared response queue (the AC 36.1.7 lesson).
- **Every fake must be a real shape (#54):** a rejected `fetch` (`TypeError`) IS what Node's `fetch` produces on ECONNREFUSED; a `HEAD` 200 + the version JSON are the real success shapes (see `versionResponse()` in `server-base.test.ts:690-700`).
- **Rule #55:** never generate file content through a shell heredoc; `git diff --stat` after every write.
- **The shared instance is never stopped.** Closed local port only. Real-instance traffic read-only.

### Testing standards

- vitest; new tests in the DEFAULT suite (no excluded suffix); per-package standalone runs acceptable per `35-1-DEV-1` with the substitution recorded.
- Rule #59: state the guarded path for each pin and show it RED with the fix reverted.
- Rule #19 mechanical proof: `git diff --stat -- 'packages/*/src/__tests__'` lists only the intended files; the F-2 pins pass unmodified.
- Rule #22/#26: the live proof runs the BUILT dist in a fresh Node process over real stdio JSON-RPC, on two server packages.

### Doc surfaces — enumerate exhaustively (#56), verdict each

`README.md` (`:78`, `:97`, `:109`, `:344-356`, `:472`, `:528-540`); `docs/client-config/README.md` (`:30`, `:289`); `docs/client-config/claude-code.md`, `claude-desktop.md`, `cursor.md`; `packages/iris-dev-mcp/README.md`, `packages/iris-admin-mcp/README.md`, `packages/iris-interop-mcp/README.md`, `packages/iris-ops-mcp/README.md`, `packages/iris-data-mcp/README.md`, `packages/iris-mcp-all/README.md`; `extensions/iris-mcp-launcher/README.md`; `tool_support.md`; `CHANGELOG.md`; JSDoc in `server-base.ts` and `profiles.ts`; the MCP `instructions` string; each server's `index.ts` `Fatal:` wording.

### Project Structure Notes

- `packages/shared/src/server-base.ts` — the fix (`start()`), JSDoc.
- `packages/shared/src/profiles.ts` — header comment only (`:655-665`); no code change expected.
- `packages/shared/src/__tests__/server-base.test.ts` — one test flips.
- `packages/shared/src/__tests__/server-base-default-outage.test.ts` (new) — AC 37.1.4–37.1.7.
- `packages/shared/src/__tests__/<armed live gate>.test.ts` (new) — AC 37.1.8; arming convention from `epic35-defect-gate.test.ts`.
- `.changeset/<name>.md` — `"@iris-mcp/shared": patch`.
- Docs listed above; `_bmad-output/implementation-artifacts/deferred-work.md` — this story's ledger-candidate section.

### References

- `docs/bugs-2026-09-16.md` — the bug record: evidence, four fix options, workaround.
- `_bmad-output/planning-artifacts/sprint-change-proposal-2026-09-16-default-profile-outage.md` — Lead decisions L-1 … L-6, Rule #37 exception.
- `_bmad-output/planning-artifacts/architecture.md` — D1 (`:396`), M1 (Epic 37 record).
- `_bmad-output/implementation-artifacts/14-1-multi-server-profiles-config-and-resolution.md:134` — the original "default stays fatal" clause (historical; not edited).
- `.claude/rules/project-rules.md` — #16/#47 (probe-first), #19 (mechanical back-compat), #21 (capstone in the default suite), #22/#26 (live smoke on the built dist), #48/#59 (RED proof on the guarded path), #54 (real fake shapes), #55 (no heredocs), #56 (enumeration completeness), #57 (review layers), #31 (no tool count moves), #23/#25 (baseline `:check` only).
- `packages/shared/src/__tests__/server-param.test.ts:529-599` — the AC 14.2.8 retry suite (template for the recovery tests).

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5), via the `bmad-dev-story` skill.

### Debug Log References

**Task 1 probes (AC 37.1.2), re-confirmed on `epic37` before coding:**

- **(a) `ProfileClientRegistry.drop`/`getOrCreate` round trip for `default`.** Read `profiles.ts:667-723`. `drop(profileName)` has no name special-casing (`Used when a profile's first-touch establishment fails`, generic); `getOrCreate` resolves via `resolveProfile(this.registry, profileName)` (works identically for `"default"` — `registry.get("default")` always exists, synthesized at `buildProfileRegistry:424`). Confirmed empirically by the new unit tests (`registry.has("default")` false immediately after the failed eager attempt, true again after the recovered call) and by the live gate.
- **(b) `establishProfile` with `profile.name === "default"`.** Read `server-base.ts:2220-2262` (then `:2272-2293` for `attemptProfileBootstrap`). No branch anywhere tests `profile.name`/`DEFAULT_PROFILE_NAME`. `attemptProfileBootstrap(profile, client, atelierVersion)` calls `bootstrap(client, profile, atelierVersion)` where `profile: IrisProfile` spreads the full `IrisConnectionConfig` (`profiles.ts:424-428`) — structurally identical input shape to the eager path's `bootstrap(defaultClient, this.config, this.atelierVersion)`. Confirmed end-to-end (incl. bootstrap) by `server-base-default-outage.test.ts`'s recovery test (`bootstrapSpy` called with a profile object named `"default"`) and by the live gate (`[INFO] Bootstrap: REST service is current...` in stderr for the `default`-targeted call after recovery).
- **(c) `atelierVersion` private-field decision.** Grepped `this\.atelierVersion\b` in `server-base.ts`: only 5 hits, all inside `start()` itself (negotiation write, warn-fallback write, the "starting with Atelier API vN" log, the eager-bootstrap arg, the `profileMeta` seed). Grepped `\.atelierVersion\b` across every package's `src/` (excluding `__tests__`): every hit is `ctx.atelierVersion` (the per-call `ToolContext` field, populated from `getOrCreateClient`'s RETURN VALUE via `profileMeta`/`establishProfile`'s own local variable) — never the private field. **Decision: leave it log-only.** On the failed path it is never read for calls (they read `profileMeta`/`establishProfile`'s local `meta.atelierVersion` instead), so there is nothing to "sync"; printing the stale default `1` as "Atelier API v1" on the failed path would be actively misleading (P5), so the failed-path log states the profile is "not yet established" instead and omits a version number entirely.
- **(d) `instructions` string + each `index.ts` `Fatal:` wording.** `SERVER_DISCOVERY_INSTRUCTIONS` (`server-discovery.ts:54-67`) tells the client to call `iris_server_profiles` first and covers production-recovery guidance; it makes no reachability claim about `default` — no edit needed. All 5 servers' `index.ts` catch is the generic `console.error("Fatal:", err); process.exit(1);` on a REJECTED `server.start(transport)` promise — after this fix, `start()` only rejects on a genuine unexpected error (e.g. the HTTP-transport-not-implemented throw) or lets a thrown CONFIG error (loadConfig/governance parse) propagate; the wording is generic and still accurate — no edit needed. Both verdicts folded into the P7 doc-surface sweep below.

**Task 3 RED proof (Rule #48/#59), the exact commands run:** `git stash` (reverts `server-base.ts`/`profiles.ts`/the `server-base.test.ts` flip; the new test files are untracked and unaffected) → `cd packages/shared && npx vitest run src/__tests__/server-base-default-outage.test.ts` → **3 of 5 tests RED** (both "AC 37.1.4" tests, and AC 37.1.5's "a call on `other` succeeds... default degrades then recovers" test) — see the corrected, precise claim in the test file's own header comment (the coalescing test and the AC 37.1.6 toEqual pin do NOT go red, and are not claimed as RED-proof coverage; reasoning recorded inline in the file). `git stash pop` restored the fix; re-ran the same file → 5/5 GREEN. Separately ran `npx vitest run src/__tests__/server-base.test.ts` pre-flip against the reverted code → the OLD `"should call process.exit when health check fails"` pin passed (RED for the NEW flipped assertion, i.e. before the flip existed the new semantics were unproven); post-flip + fix restored → 56/56 GREEN.

**Task 4 live proof + mutation leg (AC 37.1.8), the exact commands run:** Built `iris-dev-mcp`/`iris-ops-mcp`/`shared` dists (`pnpm turbo run build`). A disposable driver script (`packages/scratchpad`, deleted before staging — never committed) used the real `@modelcontextprotocol/sdk` `Client` + `StdioClientTransport` (resolved from `node_modules/.pnpm`, same convention as `host-guard-process-gate.test.ts`) to spawn each dist with `IRIS_HOST=127.0.0.1 IRIS_PORT=9` (closed port) for `default` and `IRIS_PROFILES={"other":{...real instance...}}`, then drove `initialize` (implicit in `client.connect`) + `tools/call iris_server_info {server:"other"}` (dev) / `iris_license_info {server:"other"}` (ops) + the same call with `{}` (default). Result: `other` succeeded on both servers; `default` returned `isError:true` with text `Could not connect to server profile "default": Failed to connect to IRIS at 127.0.0.1:9...`; stderr on both carried the ERROR health-check line, the continuation line, and `Connected via stdio transport`. **Mutation leg:** `git stash` → rebuilt `shared`+`dev` with `--force` → `node packages/iris-dev-mcp/dist/index.js` against the closed port → **exit code 1** (confirmed via `grep process.exit(1) packages/shared/dist/server-base.js` showing the pre-fix 3-exit shape first) → `git stash pop` → rebuilt with the fix restored → re-ran the same closed-port command → **exit code 0** (stdin EOF triggers a clean shutdown; no `process.exit(1)`) → re-ran the full JSON-RPC driver script → GREEN again, confirming the fix survives a real rebuild, not just the in-memory unit mocks.

**Server-Manager leg (recorded verdict, AC 37.1.8).** A Server-Manager profile WAS available locally (`~/Library/Application Support/Code/User/settings.json`, entry `ocupilot-slot-b`, plus this repo's own `.vscode/settings.json` entry `local`). Ran the dev dist with `IRIS_SERVER_MANAGER=auto` and the closed-port `default`: `iris_server_profiles` returned all three profiles (`default(env)`, `local(server-manager)`, `ocupilot-slot-b(server-manager)`); `iris_server_info {server:"local"}` succeeded (health → negotiation → bootstrap, the identical lazy path); the `default` continuation-line/non-fatal behavior was unaffected by `IRIS_SERVER_MANAGER` being active. **Verdict: CONFIRMED empirically** (not just by code inspection) — a `source:"server-manager"` profile establishes through the exact same `establishProfile` path as `env`/`IRIS_PROFILES` profiles, because `establishProfile` never reads a profile's `source` field.

**Disposable scripts:** both driver scripts were written under the session scratchpad directory (never under the repo) and deleted (`rm`) after use, before this write-up — confirmed absent via `git status` (never appeared, since scratchpad is outside the repo).

**Process hygiene:** the live-proof and Server-Manager-leg child processes spawned by this session's own tool calls (PIDs under this session's own top-level PPID) were identified via `ps -o pid,ppid` ancestry tracing and terminated (`kill -TERM`) after use; lingering `iris-dev-mcp`/`iris-ops-mcp` dist processes belonging to OTHER, concurrently-running Claude Code sessions on this machine were left untouched (confirmed via PPID liveness — their parent `claude` processes are still running).

**Full-suite gates (AC 37.1.11), the exact commands + results:** `pnpm turbo run build test lint type-check --continue` → **28/29 tasks green**; the ONE failure is `@iris-mcp/client-config#test`, a single pre-existing test (`certification-process.test.ts`, "drift at PROJECT scope") failing on a macOS `/tmp` → `/private/tmp`/`/private/var` realpath mismatch — reproduced identically via `git stash` on the unmodified base tree (same failure, same diff shape), confirming it is a pre-existing environmental flake wholly unrelated to this story's package (`@iris-mcp/shared`) or files touched. Recorded as the `35-1-DEV-1`-style substitution: standalone per-package run of every OTHER package is unconditionally green (`shared` 66/66 files, 1339/1339 tests; `dev` 44/44, 807/807; `admin` 34/34, 486/486; `interop` 22/22, 337/337; `data` 8/8, 161/161; `ops` 20/20, 348/348; `all` 21/21, 132/132 — the +1 file/+2 tests over the pre-story baseline is this story's own `epic37-default-outage-gate.test.ts`). **[Code review correction] The `shared` figure above is the DEV-STAGE measurement and was already stale when written up**: the QA stage's `server-base-default-outage-coverage.test.ts` (+1 file, +5 tests) lands after it, so the as-delivered figure was `shared` 67/67 files, 1344/1344 tests — independently re-measured by both the lead and this review. After this review's own +3 tests in that same file, `shared` stands at **67/67 files, 1347/1347 tests**; `all` is unchanged at 21/21, 132/132. `pnpm run gen:governance-baseline:check` → frozen 141 / live 203 / post-foundation 62, exit 0 (matches the story's stated frozen baseline exactly — F-1 confirmed). `bootstrap-classes.ts` absent from `git status`/`git diff` throughout. `BOOTSTRAP_VERSION` = `2159ec9e6f69` unchanged (confirmed both before and after all edits). `git diff --stat -- 'packages/*/src/__tests__/*'` (the pathspec needed a trailing `/*` to actually enumerate files under git 2.50.1 — the story's literal command as written matches zero paths since a bare mid-path `*` does not cross `/` and has no directory-recursion fallback without a further segment; noted here as a Dev Notes-command correction, not a scope change) lists exactly `server-base.test.ts` (modified) plus the two new files (untracked) — nothing else. `grep -rl stageDefaultStartup packages/*/src/__tests__/*.ts | wc -l` → 38, unchanged. **[Code review correction] "the two new files" is the dev-stage count and is stale as delivered — there are THREE new test files** (`server-base-default-outage.test.ts`, the QA stage's `server-base-default-outage-coverage.test.ts`, and `epic37-default-outage-gate.test.ts`). The claim is also imprecise as phrased: `git diff --stat` never lists untracked files at all, so the modified leg comes from `git diff` and the new leg from `git status`/`git ls-files --others`. Re-derived at review time: `git diff HEAD --stat -- 'packages/*/src/__tests__/*'` → `server-base.test.ts` only; `git ls-files --others --exclude-standard -- 'packages/*/src/__tests__/*'` → the three new files. The 38-file `stageDefaultStartup` count and the five named F-2 eager pins were re-verified UNCHANGED (`git diff HEAD --stat` on the four other pin files is empty).

### Completion Notes List

- Implemented the fix exactly as Dev Notes' target shape prescribes (`defaultEstablished` flag around the existing `:2026-2034` catch), with the health-check `try` unchanged on the success path and only the catch body diverging: log → continuation-log → `this.clients.drop(DEFAULT_PROFILE_NAME)` → `defaultEstablished = false`; negotiation/bootstrap/`profileMeta` seed wrapped in `if (defaultEstablished) { ... } else { <deferred-establishment log> }`; transport connect unchanged and unconditional.
- Updated every JSDoc/comment anchor named in Task 2 (`:1506-1510`, the `start()` docblock covering `:1924-1935`, the inline `:2025-2026`-area comment rewritten in place as part of the fix itself, the `getOrCreateClient` docblock + its `@throws`, the `establishProfile` docblock, and `profiles.ts`'s `ProfileClientRegistry` creation-policy docblock) to state the M1 semantics without contradicting any surviving claim.
- `server-base.test.ts:558`'s pin flipped from asserting `process.exit(1)` to asserting `start()` resolves, `process.exit` is never called, and both the health-check-failed and continuation ERROR lines are logged (added a `logger` import for the spy).
- New file `server-base-default-outage.test.ts` (5 tests) covers AC 37.1.4 (multi- and single-profile startup survival with ERROR-log content assertions), AC 37.1.5 (other-serves / default-isError-envelope / drop-then-recover / bootstrap-attempted-exactly-once-for-default / coalescing), and AC 37.1.6 (`toEqual` pin of the exact 2-call startup fetch sequence on a reachable default). Uses a URL/method-ROUTED fetch fake (never a shared `mockResolvedValueOnce` queue) per the AC 36.1.7 lesson the Dev Notes flag.
- New permanent default-suite gate `packages/iris-mcp-all/src/__tests__/epic37-default-outage-gate.test.ts` (2 tests: `iris-dev-mcp` + `iris-ops-mcp` legs) spawns the BUILT dists over real stdio JSON-RPC via the real SDK `Client`, mirroring the sibling `host-guard-process-gate.test.ts`/`server-manager-process-gate.test.ts` shape (Rule #45 placement) with the `epic35-defect-gate.test.ts` `IRIS_REQUIRE_LIVE` skip→hard-fail arming convention layered on top (documented rationale for the hybrid in the file's own header comment). Never fails on a pristine/offline checkout; both legs passed live in this session.
- Docs: README.md gained a Backward-Compatibility bullet + a Profiles-section paragraph; `docs/client-config/README.md` gained the L-4 `claude mcp list` caveat near the existing health-check note. Every other P7-enumerated surface (README `:97`/`:109`/`:472`, `docs/client-config/README.md:30`, the three per-client guides, all 5 per-server READMEs + `iris-mcp-all` README + the launcher README, `tool_support.md`) was checked and recorded UNCHANGED (config-failure or connection-agnostic claims, all still true) — no edit needed. CHANGELOG.md gained a new top entry ("Pre-release — 2026-09-16", since Story 14.1 `213756b`). `.changeset/default-profile-outage-non-fatal.md` added (`"@iris-mcp/shared": patch`, L-6).
- `deferred-work.md` gained one new section, "Story 37.1 ledger candidates (2026-09-16)", with exactly 3 fresh LOW rows (`37-1-DEV-1` strictness knob/L-3, `37-1-DEV-2` discovery reachability/L-4, `37-1-DEV-3` `ping()` unused) each at Rule #37 count 0, plus the mechanical Rule #51 recount (62→65 LOW, 223→226 distinct, 87→90 open, 136 terminal unchanged, arithmetic shown).
- No ObjectScript touched; `BOOTSTRAP_VERSION` unmoved; no new tool/action/schema-field/env-var (F-1 intact, governance baseline check green); the OS `%UnitTest` suite was not run/changed by this story (pure TypeScript change) — "unchanged" per AC 37.1.11, nothing to re-measure.
- Two disposable Node scripts used for the live proof and Server-Manager-leg probes were written to the session scratchpad (never the repo) and deleted before this write-up; confirmed never present in `git status` at any point.

### File List

- `packages/shared/src/server-base.ts` — modified (the fix; JSDoc updates)
- `packages/shared/src/profiles.ts` — modified (JSDoc only, `ProfileClientRegistry` creation-policy comment)
- `packages/shared/src/__tests__/server-base.test.ts` — modified (flipped the `:558` exit pin)
- `packages/shared/src/__tests__/server-base-default-outage.test.ts` — new (AC 37.1.4–37.1.7 unit coverage)
- `packages/shared/src/__tests__/server-base-default-outage-coverage.test.ts` — new (**QA stage**, 5 tests: `start("http")` + failed default, visibility-preset composition, `bootstrapAttempted` on the recovery path for `needsCustomRest:false`, re-degradation after recovery, coalesced rejection for `default`; **+3 added by the code review**: Gap 6's 401 and AbortError fixtures, Gap 7's degraded-path startup line). *Added to this list by the code review — the dev-stage File List omitted it.*
- `packages/iris-mcp-all/src/__tests__/epic37-default-outage-gate.test.ts` — new (AC 37.1.8 permanent armed live gate)
- `packages/iris-mcp-all/scripts/prepublish-gate.mjs` — modified (**code review**: added the Epic 37 gate to `gateTestFiles` and `iris-ops-mcp` to `DRIVEN_PACKAGES` — without this the gate's `IRIS_REQUIRE_LIVE` arming was set by nothing, Rule #59)
- `README.md` — modified (Backward Compatibility bullet; Profiles-section paragraph; **code review**: scoped the byte-for-byte and "behaves exactly as it always has" claims, corrected the `above`→`below` cross-reference, and made the non-fatal set precise)
- `docs/client-config/README.md` — modified (L-4 `claude mcp list` caveat; **code review**: missing-vs-unreachable clause in the startup-failure guidance near `:30`)
- `CHANGELOG.md` — modified (new "Pre-release — 2026-09-16" entry)
- `.changeset/default-profile-outage-non-fatal.md` — new (`"@iris-mcp/shared": patch`)
- `_bmad-output/implementation-artifacts/deferred-work.md` — modified (dev's "Story 37.1 ledger candidates" section, 3 LOW rows — renumbered `37-1-CR-*` → `37-1-DEV-*` by the code review to match the origin-prefix convention; plus the code review's own 12-row DEFER section)
- `_bmad-output/implementation-artifacts/37-1-default-profile-outage-degrade-and-retry.md` — modified (this story file: Status, Tasks, Dev Agent Record, QA Results, Review Findings, Change Log)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — modified (story status synced by the skill; **code review**: restored the Story 36.0 iteration-2 record the dev-stage rotation dropped)
- `_bmad-output/implementation-artifacts/cycle-log-epic-37.md` — new (epic-cycle stage log, written by the lead). *Added to this list by the code review.*

## Change Log

- 2026-09-16 — Story 37.1 implemented: `McpServerBase.start()`'s default-profile health-check failure no longer calls `process.exit(1)` — it logs, drops the un-established client, and lets the transport connect; the first call against `default` establishes it through the same lazy path every non-default profile already uses (architecture record M1). Unit tests (5, new file) + a permanent live default-suite gate (2, new file, two server packages) + the flipped `server-base.test.ts:558` pin. Docs (README, `docs/client-config/README.md`, CHANGELOG), changeset, and 3 new ledger candidates (LOW, Rule #37 count 0) added. `pnpm turbo run build test lint type-check` 28/29 green (the one failure is a pre-existing, unrelated `@iris-mcp/client-config` macOS-tmp-symlink flake, reproduced identically on the unmodified base tree); governance baseline, `BOOTSTRAP_VERSION`, and tool counts all unmoved.
- 2026-09-16 — Story 37.1 code review (`bmad-code-review`, close kind **CLEAN**, `review_degraded=false`, all 3 layers delivered inside the 20-minute bound). 44 raw findings → 12 patched / 12 deferred / 0 decision-needed / 8 dismissed. **One HIGH:** the Epic 37 live gate carried the `IRIS_REQUIRE_LIVE` contract but was never added to `prepublish-gate.mjs`'s `gateTestFiles`, so the arming variable was set by nothing on the one path that arms it (Rule #59) — fixed and proven live. Also patched: host:port + non-recoverable-cause wording on the continuation line; Rule #58 error-class fixture diversity (a real 401 and a real `AbortError`); the gate header's false read-only claim; the gate's weak/asymmetric assertions; the AC 37.1.4 single-profile leg and the flipped `:558` pin (transport-connected + host:port); a pin for the degraded-path startup line; `profileMeta.delete` on the degraded path; an `atelierVersion` log-only note; the story's File List / empty QA Results / two stale evidence figures; the dropped Story 36.0 iteration-2 sprint-status record; the contradictory README backward-compat bullets and the `:97` verdict; and the `-CR-` → `-DEV-` ledger renumbering. Re-verified UNCHANGED by the review: F-1 (141/203/62, exit 0), F-2 (`git diff -w` shows ZERO non-whitespace changes inside the re-indented success block; 38 `stageDefaultStartup` files and all four other eager-pin files byte-identical), F-3 (exactly two `process.exit(1)` remain, both config), `BOOTSTRAP_VERSION` `2159ec9e6f69`. Suites after patches: `shared` 67/1347, `all` 21/132, lint 0 errors, type-check clean. Ledger +12 code-review rows → 1 HIGH / 29 MEDIUM / 72 LOW = 102 open / 238 distinct (136 terminal).

## QA Results

*This section was empty as delivered — reconstructed by the code review from `cycle-log-epic-37.md`'s `qa_complete` record and from reading the QA artifact itself, so the QA stage is visible in the story rather than only in the cycle log.*

- **Stage record (cycle log, 2026-09-16T11:53:20Z):** `qa_complete`, model `claude-sonnet-5`, cycle iteration 1, `tests_added=5`, `first_run_failures=1`, `clarifications=0`, `rule54_refusal=1_unreachable_string_error_branch`, `red_proof=4_of_5`.
- **Artifact:** `packages/shared/src/__tests__/server-base-default-outage-coverage.test.ts` (new, 5 tests as delivered) — gaps the dev-stage suite left open: (1) `start("http")` under a FAILED default health check (the pre-existing http test only pairs http with a HEALTHY default); (2) a failed default composed with an Epic 30 tool-visibility preset; (3) `bootstrapAttempted` on the RECOVERY path for a `needsCustomRest: false` server (the dev recovery test covers only `true`); (4) re-degradation AFTER a successful recovery (a full down → up → down cycle, proving the established fast path never re-checks health and never exits); (5) the `establishing` coalescing map when the coalesced first touch REJECTS for `default` specifically (`server-param-integration.test.ts:511` proves that shape only for a non-default profile).
- **QA's Rule #54 refusal — VERIFIED CORRECT by this review.** QA declined to write a non-`Error` health-check-rejection test on the grounds that `start()`'s `error instanceof Error ? error.message : String(error)` false branch is unreachable. Confirmed by reading `health.ts:29-45`: `checkHealth` has exactly two exits — re-throw of an `IrisConnectionError`, or a freshly constructed one — and both are `Error` subclasses (`errors.ts:15`), while `start()`'s `try` contains nothing but `await checkHealth(defaultClient)`. The `String(error)` arm is therefore genuinely dead, the refusal was right (Rule #54: a fake that manufactures an impossible state is worse than a missing test), and no coverage gap exists. The line is pre-existing diff CONTEXT, not added by this story, so it was left as-is rather than simplified.
- **QA's RED proof (4 of 5) re-measured by the code review:** with the fix mutated back to `process.exit(1)`, **7 of 8** tests in this file now go RED (the +3 review-added tests are all RED-proof too); Gap 4 correctly stays green, since it starts from a HEALTHY default and never exercises the changed catch. Restored → 8/8 GREEN.
- **Code-review additions to this file:** Gap 6 (two tests — a real HTTP 401 response and a real `AbortError`, closing the Rule #58 error-class fixture-diversity gap) and Gap 7 (one test — the degraded-path startup INFO line, which nothing previously asserted).

## Code Review

### Review Findings

**Rule #57 record.** `layers_mode=parallel_async`, all three layers launched 2026-09-16T11:54:07Z against the frozen snapshot `review-diff-snapshot-37-1-default-profile-outage-degrade-and-retry-20260916-115347.diff` (1743 lines), each against a real armed 20-minute timer (a backgrounded `sleep 1200`, cancelled once all three returned). **Blind Hunter ~2.2 min (18 findings, DELIVERED) · Edge Case Hunter ~3.5 min (13, DELIVERED) · Acceptance Auditor ~3.7 min (13 + a per-AC table, DELIVERED).** No layer timed out; no layer returned without a payload. **`failed_layers` = (none), `review_degraded=false`, close kind CLEAN.** 44 raw findings → 12 patched, 12 deferred, 0 decision-needed, 8 dismissed.

**HIGH (patched)**

- [x] [Review][Patch] **The "permanent armed live gate" was armed by nothing — `IRIS_REQUIRE_LIVE` is set only by `prepublish-gate.mjs`, which ran a roster this file was not on** [`packages/iris-mcp-all/scripts/prepublish-gate.mjs:100`] — found independently by all three layers and by the reviewer. `gateTestFiles` listed only `epic35-defect-gate.test.ts`, so on the ONE path that arms the variable the Epic 37 gate never executed; on every path that does run it (`pnpm test`, `turbo run test`) it silently `ctx.skip()`s whenever IRIS is unreachable or a dist is unbuilt. The file's own header asserted the opposite ("set by `scripts/prepublish-gate.mjs` on the packaging path… this gate must not be silently absent from a packaging run"), and the roster's own comment carries the standing rule the story broke: "arming one means adding the REQUIRE_LIVE contract to the file AND listing it here." Rule #59's named disqualifier, verbatim. **Fixed:** added `src/__tests__/epic37-default-outage-gate.test.ts` to `gateTestFiles`, and `["iris-ops-mcp", "@iris-mcp/ops"]` to `DRIVEN_PACKAGES` (the gate spawns the ops dist, which had no freshness check). Verified: the roster now yields `vitest run epic35-defect-gate.test.ts epic37-default-outage-gate.test.ts`; `IRIS_REQUIRE_LIVE=1 vitest run epic37-default-outage-gate.test.ts` → 2/2 PASS (both legs genuinely ran, not skipped) against freshly rebuilt dists; `checkDistFresh` passes for all six driven packages.

**MEDIUM (patched)**

- [x] [Review][Patch] **The startup log's only diagnostic omitted host:port on the refusal path, and promised recovery that cannot happen** [`packages/shared/src/server-base.ts` — the health-check catch] — `checkHealth` carries host:port only on its `NETWORK_ERROR`/`TIMEOUT` variants; when IRIS ANSWERS and refuses (HTTP 401/403 for a wrong-but-present password, 404 for a disabled `/api/atelier`) it re-wraps an `IrisApiError` as `HEALTH_CHECK_FAILED`, whose text names neither host nor port — while the continuation line told the operator it would "recover automatically". After this story that log is the ONLY signal, because the process no longer exits. **Fixed:** the continuation line now names `host:port` unconditionally (making AC 37.1.4's "the ERROR log names host:port" true on every path, and the sprint-status/gate claims about it accurate) and states that a configuration cause "will NOT clear on its own: fix the setting and restart." The deeper question — whether a refusal should fail fast at all — is `37-1-CR-1`.
- [x] [Review][Patch] **No fixture anywhere drove a non-`TypeError` health-check rejection through the changed catch** [`packages/shared/src/__tests__/server-base-default-outage-coverage.test.ts`] — all 12 new/flipped tests and both live-gate legs produced exactly one error class (`TypeError` → `NETWORK_ERROR`); an implementation mishandling every 401/403/404/timeout would have stayed green (Rule #58 fixture diversity, applied to error classes). **Fixed:** added Gap 6 — a real non-ok HEAD `Response` (401) and a real `AbortError` — both proven RED under mutation.
- [x] [Review][Patch] **The live gate's header claimed "read-only traffic only" against the shared instance, which the code contradicts** [`packages/iris-mcp-all/src/__tests__/epic37-default-outage-gate.test.ts`] — the `other` leg's tool call is the profile's FIRST TOUCH, and `handleToolCall` passes `needsCustomRest` (true on all five servers) as `needsBootstrap`, so it runs `bootstrap()` — a no-op only when the probe returns `current`, otherwise a deploy/compile/configure (architecture D8's mutate-on-first-use). The sibling process-gates it says it mirrors call only the never-connecting `iris_server_profiles`. **Fixed:** the header now states the mutation honestly and explains why AC 37.1.8 makes an establishing call unavoidable. The policy question — may a DEFAULT-suite test carry a deploy path? — is `37-1-CR-4`, which also records the interaction with the open HIGH `36-4-CR-1`.
- [x] [Review][Patch] **The story's own back-compat evidence could not be reproduced against the delivered tree** [story File List / Debug Log] — the File List omitted `server-base-default-outage-coverage.test.ts` (493 lines, 5 tests) and `cycle-log-epic-37.md`; the `## QA Results` section was empty although the cycle log records `qa_complete`; the Debug Log claimed "the two new files" (there are three) and `shared` 66/1339 (as delivered: 67/1344, independently re-measured by the lead and this review). **Fixed:** File List completed, QA Results reconstructed, both stale claims corrected in place with the re-derived commands.
- [x] [Review][Patch] **`sprint-status.yaml`'s history rotation dropped the most recent Story 36.0 record and preserved the older one under an invented key** [`_bmad-output/implementation-artifacts/sprint-status.yaml:119`] — the removed `last_updated` was Story 36.0 CODE REVIEW **iteration 2**, close kind **CLEAN**, which moved that story to `done`; it was overwritten without being rotated into the established `# prior last_updated:` comment chain, while the older **iteration 1 / DEGRADED** record was kept under a new `prior_previous_update:` key. The file itself documents this exact mistake happening once before. Anyone reconstructing Rule #57 close-kind history would conclude 36.0's last review closed DEGRADED. **Fixed:** the iteration-2 CLEAN record restored into the comment chain and the invented key removed.
- [x] [Review][Patch] **The new Backward-Compatibility bullet contradicted the bullet three lines above it** [`README.md`] — "With **neither** `IRIS_PROFILES` nor `IRIS_GOVERNANCE` set, behavior is byte-for-byte identical to before" sits in the same list as the new "Startup no longer exits when the `default` instance is unreachable." AC 37.1.9 enumerated `:528-540` as a unit; the edit was append-only. The same class applies at `:97` ("the suite behaves exactly as it always has"), which the dev recorded UNCHANGED although the lead's own P7 probe had flagged it ("consider one clause"). **Fixed:** both claims scoped to the success path with a pointer to the new bullet; the non-fatal set made precise (outage, timeout, AND refusal); and the new Profiles paragraph's "See Backward Compatibility **above**" corrected to "below" (Profiles is at `:344`, Backward Compatibility at `:531`).

**LOW (patched)**

- [x] [Review][Patch] AC 37.1.4's single-profile leg did not carry "the same assertion" as the multi-profile leg — no transport-connected and no host:port pin, on precisely the single-server shape the beta reporter hit [`server-base-default-outage.test.ts`]. Both added.
- [x] [Review][Patch] The flipped pin's comment claimed "the transport still connects" but asserted only resolve + no-exit + two log strings — a half-fix that returns early before the connect step would have left it green [`server-base.test.ts:558`]. `Connected via stdio transport` assertion added to the test that owns the flip.
- [x] [Review][Patch] The degraded-path startup INFO line (`"default" profile not yet established`) was asserted by no test and no gate leg, so a refactor restoring the misleading `starting with Atelier API v1` the fix deliberately removed (story P5) would ship green. Gap 7 added, pinning both the new line's presence and the old line's absence.
- [x] [Review][Patch] The live gate's `other` leg asserted only `isError !== true`, which also passes on a malformed or empty envelope; and the `iris-ops-mcp` leg was a strict SUBSET of the `iris-dev-mcp` leg despite the header marketing it as "the same round trip". Both legs now assert non-empty content plus the identical five stderr properties.
- [x] [Review][Patch] `start()` could hand out a never-health-checked client on a SECOND `start()` after a successful first one: `clients` is rebuilt per `start()` but `profileMeta` is `readonly` and never cleared, so a stale entry would send the next call down `getOrCreateClient`'s fast path. Pre-fix this was unreachable (the process had exited); the change makes `start()` complete with `default` unestablished. `this.profileMeta.delete(DEFAULT_PROFILE_NAME)` added on the degraded path — a no-op on a first start, and it makes the `getOrCreateClient` JSDoc's "no `profileMeta` entry exists for it" true unconditionally.
- [x] [Review][Patch] The private `atelierVersion` field keeps its `1` default forever on the degraded path and is never corrected on recovery; nothing in the code marked it log-only, so a future contributor reading it into a context builder would silently talk v1 to a v8 instance. The dev's decision (probe (c)) was correct but lived only in the story. Field JSDoc now records the constraint and points readers at `profileMeta`.
- [x] [Review][Patch] Dev-opened ledger rows used the `-CR-` prefix (`37-1-CR-1..3`) although `-CR-` is reserved for code-review-opened items (`-DEV-`/`-QA-` are the dev/QA prefixes, per `36-3-DEV-1`, `35-4-DEV-1`, `35-1-DEV-1`); this would have mis-attributed them in a Rule #37 burn-down and collided with this review's own IDs. Renamed to `37-1-DEV-1..3` across the ledger, story and sprint status; no severity or tally changed.
- [x] [Review][Patch] Missing/incomplete AC 37.1.9 verdicts: `README.md:78` was enumerated in P7 but appears in no verdict (checked — correctly UNCHANGED), and the AC's own candidate list omitted `packages/shared/README.md` (the README of the package this story changes) and `packages/client-config/README.md` — the Rule #56 "what is MISSING from this list?" question. Both swept: every startup/health claim in them is `IRIS_GOVERNANCE_FILE`/credential CONFIGURATION failure, still true. Verdicts recorded below.

**DEFERRED (12 — recorded in `deferred-work.md` → "Deferred from: code review of 37-1-default-profile-outage-degrade-and-retry (2026-09-16)")**

- [x] [Review][Defer] `37-1-CR-1` (MEDIUM) — the health-check catch does not discriminate a CONFIGURATION refusal (401/403/404) from an unreachable peer, though `IrisConnectionError.code` is available and unused; M1 clause (1) says `start()` "distinguishes" them. Needs a Lead decision (reverses L-2 for an error class, or folds into `37-1-DEV-1`'s strictness knob) and probably an M1 amendment — the user-visible half was patched instead.
- [x] [Review][Defer] `37-1-CR-2` (MEDIUM) — after a degraded start the first `default` call runs the full custom-REST bootstrap INSIDE the request, unbounded (only `checkHealth` is bounded, at 5 s) and coalesced, so a retry re-awaits the same stuck promise. All five servers set `needsCustomRest: true`. The story's Traps section rejected option B to avoid exactly this; the degraded path reintroduces it.
- [x] [Review][Defer] `37-1-CR-3` (MEDIUM) — no negative cache or backoff: against a BLACKHOLED host every call pays the full 5 s health-check timeout, forever.
- [x] [Review][Defer] `37-1-CR-4` (MEDIUM) — the gate's `other` leg carries a deploy path into the DEFAULT suite (D8 mutate-on-first-use); policy question, plus a recorded interaction with the open HIGH `36-4-CR-1` (running it from an older branch could reinstall a pre-36.4 `Global.cls`).
- [x] [Review][Defer] `37-1-CR-5` (MEDIUM) — the "recovers automatically" promise has no LIVE proof (bootstrap mocked in every unit test; the gate never brings `default` up). **Blocked by the non-negotiable read-only shared-instance constraint** — a real recovery would run a real `bootstrap()` write. Residual risk recorded.
- [x] [Review][Defer] `37-1-CR-6` (LOW) — a MID-SESSION outage returns `Tool error: …`, not the `Could not connect to server profile "default"` envelope the new docs tell agents to expect (the fast path never re-checks health).
- [x] [Review][Defer] `37-1-CR-7` (LOW) — health passes but negotiation fails ⇒ `default` pinned to Atelier v1 for the process lifetime with no recovery path (pre-existing; unchanged by this story).
- [x] [Review][Defer] `37-1-CR-8` (LOW) — `bootstrapAttempted: true` is recorded when `bootstrap()` threw, under a comment asserting it reflects whether bootstrap "ACTUALLY ran" (pre-existing).
- [x] [Review][Defer] `37-1-CR-9` (LOW) — double `IRIS health check failed:` prefix on the refusal path (pre-existing, `health.ts`).
- [x] [Review][Defer] `37-1-CR-10` (LOW) — the gate checks dist EXISTENCE, not freshness; now covered on the turbo and packaging paths, residual on the bare-vitest path.
- [x] [Review][Defer] `37-1-CR-11` (LOW) — a gate child process can outlive a vitest-timed-out test.
- [x] [Review][Defer] `37-1-CR-12` (LOW) — M1 clause (2)'s "ONE path" is not literally met: only the FAILURE path converges on `getOrCreateClient`/`establishProfile`; the success path still runs the inline eager sequence (required by AC 37.1.3 + F-2). The cycle log's `m1_clauses=4_of_4_conformant` overstates clause (2).

**DISMISSED (8)**

- `drop()` does not destroy the client (Blind) — FALSE: `profiles.ts:714` does `client.destroy()` then `delete`. The "destroyed and dropped" JSDoc is accurate.
- An unreachable `default` stalls startup "for up to a minute" (Blind) — the health check passes `HEALTH_CHECK_TIMEOUT = 5_000` and `http-client.ts:178` honors the per-request override, so the warm-up is bounded at 5 s, and it was bounded identically pre-fix. The residual (a 5 s delay is still a delay) is folded into the README wording patch and `37-1-CR-3`.
- The gate's `transport.stderr?.on(...)` is registered before `connect()` and may silently no-op (Blind) — demonstrably works (every stderr assertion passed on two live runs), and it is the sibling process-gates' established shape; changing it here alone would diverge.
- The bootstrap-once oracle keys on `call[1].name`, which the eager path cannot set (Blind) — true, but the adjacent `expect(bootstrapSpy).toHaveBeenCalledTimes(2)` bounds total calls, so a spurious eager bootstrap still fails the test.
- The new tests connect real stdio transports with no teardown (Blind) — the 38 pre-existing `stageDefaultStartup` files already do exactly this on the success path; both suites run clean with no open-handle warnings.
- Two `logger.error` lines for a supported state will look like a broken server (Blind) — AC 37.1.3 explicitly mandates "log at ERROR the existing message PLUS one line".
- `String(error)` in the changed catch is unreachable (Edge) — CONFIRMED unreachable, which is why QA's Rule #54 refusal to test it was correct; recorded under QA Results rather than patched, since the line is pre-existing diff context and `checkHealth`'s `@throws` contract makes the defensive arm harmless.
- AC 37.1.9's "Run every documented example" has no recorded execution for the `claude mcp list` line (Auditor) — the actionable half (a server that answers the handshake while `default` is unreachable, and `iris_server_info` revealing it) is exactly what the live gate drives end-to-end; running `claude mcp list` would exercise the user's own client registration, not this change.

### AC 37.1.9 verdict completions (Rule #56)

Added by the review to close the enumeration. `README.md:78` (`IRIS_PROFILES` env-var row) — **UNCHANGED**, describes the profile map only, no reachability or startup claim. `packages/shared/README.md` — **UNCHANGED**, its only startup/health text is `IRIS_GOVERNANCE_FILE` configuration failure, still fail-fast. `packages/client-config/README.md` — **UNCHANGED**, same class. All five per-server READMEs, `packages/iris-mcp-all/README.md`, the `iris-mcp-launcher` README and `tool_support.md` re-checked independently: every "fails to start"/"fails startup" hit is a CONFIGURATION failure (missing governance file, invalid preset value, missing credentials) and every "connection refused" hit is a per-call troubleshooting row — all still true, **UNCHANGED** verdicts confirmed accurate. The three per-client guides likewise. The MCP `instructions` string (`server-discovery.ts:54-67`) and all five `index.ts` `Fatal:` catches re-read — generic, no reachability assumption, **UNCHANGED** confirmed.

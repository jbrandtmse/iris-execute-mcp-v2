# Deferred Work

All deferred items have been formally triaged and closed as of Story 8.0 (Epic 7 Deferred Cleanup).
This is the final feature epic; no further feature work is planned beyond Epic 8 (Documentation).

> **Epic 1-4 items formally closed in Story 7.0** (26 items). See git history for provenance.

---

## Closed Items --- Epic 5 (Interoperability MCP Tools)

### From Story 5-0 (Epic 4 Deferred Cleanup)
- ~~`pastEnd` flag added to `PaginateResult`~~ --- **Closed (Story 8.0)**: Removed as dead code. No consumer existed after 7 epics. The `pastEnd` field has been removed from `PaginateResult`, `paginate()` in server-base.ts, and related tests.

### From Story 5-2 (Production Lifecycle)
- ~~`ProductionControl` tTimeout=0 silently overridden to 120~~ --- **Closed (won't-fix, Story 8.0)**: Edge case with no reported incidents. The server-side default is acceptable for all known use cases.
- ~~`ProductionSummary` inner catch block swallows errors silently~~ --- **Closed (Story 8.0)**: Added console log for skipped namespaces with error reason in Interop.cls.
- ~~`ProductionManage` uses `%Dictionary.ClassDefinition.%ExistsId`~~ --- **Closed (won't-fix, Story 8.0)**: IRIS API limitation. Non-production classes with the same name is an unlikely edge case with no practical impact.
- ~~`productionControlTool` Zod schema marks `name` as optional for all actions~~ --- **Closed (Story 8.0)**: Added `.refine()` validation requiring `name` for `start` and `restart` actions.

### From Story 5-3 (Production Item and Auto-Start)
- ~~`ItemManage` "set" silently ignores unknown settings keys~~ --- **Closed (won't-fix, Story 8.0)**: Acceptable behavior. Unknown keys are ignored per standard IRIS configuration patterns.
- ~~`ItemManage` "set" save/update consistency~~ --- **Closed (won't-fix, Story 8.0)**: Pre-existing IRIS pattern limitation with no simple rollback mechanism. No incidents reported.

### From Story 5-5 (Credential and Lookup Table)
- ~~`LookupTransfer` import is additive/merge~~ --- **Closed (won't-fix, Story 8.0)**: Merge semantics are safer than replace. Tool description is adequate.
- ~~`CredentialManage` empty string username/password handling~~ --- **Closed (won't-fix, Story 8.0)**: Edge case. Users can delete and recreate credentials instead of clearing fields.

### From Story 5-7 (Interop Integration Tests)
- ~~`probeCustomRest` duck-typing instead of instanceof~~ --- **Closed (won't-fix, Story 8.0)**: Cosmetic improvement. Works reliably in practice.
- ~~No integration tests for interopRest/ruleGet/transformTest~~ --- **Closed (won't-fix, Story 8.0)**: Requires IRIS test production setup. Unit tests provide adequate coverage.

---

## Closed Items --- Epic 6 (Operations & Monitoring MCP Tools)

### From Story 6-0 (Epic 5 Deferred Cleanup)
- ~~No npm script for gen-bootstrap.mjs~~ --- **Closed (Story 8.0)**: Added `gen:bootstrap` script to root package.json.
- ~~No error handling in gen-bootstrap.mjs for missing .cls files~~ --- **Closed (won't-fix, Story 8.0)**: Node.js already provides clear error messages for missing files. Low value add.

### From Story 6-3 (Jobs and Locks Tools)
- ~~AC1 "start time" field~~ --- **Closed (won't-fix, Story 8.0)**: IRIS %SYS.ProcessQuery does not expose start time. Cannot be implemented without IRIS-side changes.
- ~~No ObjectScript unit test for dual-format Owner parsing~~ --- **Closed (won't-fix, Story 8.0)**: Verified by lead during review. TypeScript-side tests cover the parsing.

### From Story 6-6 (Task Scheduling Tools)
- ~~AC6 "duration" not computed~~ --- **Closed (won't-fix, Story 8.0)**: lastStart and completed timestamps are available for client-side derivation.
- ~~No schedule properties in iris.task.manage create action~~ --- **Closed (won't-fix, Story 8.0)**: Complex scheduling subsystem. No planned future enhancement epics.

### From Story 6-7 (System Configuration Tools)
- ~~Dynamic annotations via `_meta.readOnly`~~ --- **Closed (won't-fix, Story 8.0)**: Known MCP protocol limitation. Static annotations are the correct approach.
- ~~GetConfig reads only 11 hardcoded properties~~ --- **Closed (won't-fix, Story 8.0)**: Covers most common configuration needs. Full property iteration is a future enhancement.
- ~~No whitelist/validation on SetConfig property names~~ --- **Closed (won't-fix, Story 8.0)**: Config.config.Modify() validates server-side. Defense-in-depth only.
- ~~ExportConfig only includes config section~~ --- **Closed (won't-fix, Story 8.0)**: Intentional per original design. No incidents or requests for expansion.

### From Story 6-8 (Ops Integration Tests)
- ~~Duplicate getConfig()/declare global blocks~~ --- **Closed (Story 8.0)**: Extracted shared `getIntegrationConfig()` helper to `@iris-mcp/shared/test-helpers/integration-config`.
- ~~Task creation unchecked `as` cast~~ --- **Closed (won't-fix, Story 8.0)**: Low risk. Cleanup runs via afterAll regardless.

---

## Closed Items --- Epic 7 (Data & Analytics MCP Tools)

### From Story 7-3 (Analytics Tools)
- ~~`%BuildCube` synchronous with no timeout~~ --- **Closed (won't-fix, Story 8.0)**: Story spec explicitly calls for synchronous operation. Timeout/async is a future enhancement.

### From Story 7-4 (REST API Management)
- ~~`encodeURIComponent()` percent-encoding slashes~~ --- **Closed (won't-fix, Story 8.0)**: Follows spec-prescribed pattern. No reported issues.

### From Story 7-5 (Data Integration Tests)
- ~~`insertedDocId` regex only matches numeric IDs~~ --- **Closed (won't-fix, Story 8.0)**: Fallback only; structuredContent extraction is primary. Low risk.
- ~~Find-with-filter test does not assert document count~~ --- **Closed (won't-fix, Story 8.0)**: Other lifecycle tests verify content correctness. Low priority.

---

## Deferred from: code review of 8-0-epic-7-deferred-cleanup (2026-04-07)

- Duplicate `getIntegrationConfig` helper: `packages/shared/src/__tests__/integration-helpers.ts` (pre-existing, accepts overrides) and `packages/shared/src/test-helpers/integration-config.ts` (new, includes `declare global` augmentation). Both produce the same config from env vars. Functionally harmless but could be consolidated into the new cross-package helper if `integration-helpers.ts` is refactored to delegate to it.

## Deferred from: code review of 8-1-suite-level-readme-and-architecture-overview (2026-04-07)

- Per-package README links in root README.md point to files that do not yet exist (e.g., `packages/iris-dev-mcp/README.md`). These will be created in Story 8.2. Until then, links are dead. Pre-existing by design (story spec says to link ahead).

## Deferred from: code review of 8-4-mcp-client-configuration-examples (2026-04-07)

- DRY: Env var table and shell export examples (bash/PowerShell) are duplicated identically across all three client-config docs (`claude-desktop.md`, `claude-code.md`, `cursor.md`). This is a deliberate design choice for self-contained docs but creates a maintenance burden if variables change. Could be consolidated into a shared include or a single env-var reference page linked from each doc.

## Deferred from: code review of 8-6-meta-package-iris-mcp-all (2026-04-07)

- Missing `license` field in `packages/iris-mcp-all/package.json`. All packages in the monorepo may need license, repository, author, and keywords fields added before npm publish. Pre-existing publish-readiness concern applicable to all packages.

---

## Triaged via Story 9.0 (2026-04-09)

Story 9.0 formally triaged all Epic 8 retrospective action items and unclosed `deferred-work.md` entries against Epic 9 (Tool Name Flattening) scope. Epic 9 is a narrowly scoped pre-publish compatibility fix, so most deferred items fall outside its boundary and are either resolved, retained as cosmetic follow-ups, or redirected to the publishing checklist. See `9-0-epic-8-deferred-cleanup.md` for the authoritative triage tables.

### Resolved (no longer an open concern)

- **Per-package README dead links (from 8.1 code review)** — Resolved by Story 8.2, which created the per-package READMEs that the root README was linking to. The dead-link condition no longer exists. No further action required.

### Retained as cosmetic follow-ups (not tracked by any Epic 9 story)

These items remain open and visible in this file for future contributors. They are explicitly NOT in Epic 9 scope and will not be addressed by Stories 9.1, 9.2, or 9.3. They may be picked up opportunistically during a future cleanup pass or left as-is indefinitely.

- **Duplicate `getIntegrationConfig` helper (from 8.0 code review / Retro Post-Project Action #4)** — `packages/shared/src/__tests__/integration-helpers.ts` and `packages/shared/src/test-helpers/integration-config.ts` both produce the same config from env vars. Cosmetic consolidation only; both helpers are functionally correct.
- **DRY env var docs across client-config guides (from 8.4 code review / Retro Post-Project Action #5)** — Env var table and shell export examples are duplicated across `claude-desktop.md`, `claude-code.md`, and `cursor.md`. Deliberate self-contained-doc design; could be consolidated into a shared include if maintenance burden grows.

### Deferred to the publishing checklist (not Epic 9)

These items are genuine pre-publish readiness requirements but belong to the npm publishing session that happens *after* Epic 9 unblocks Claude Desktop compatibility. They are tracked in the authoritative publishing checklist, not in Epic 9 stories.

- **Missing `license`, `repository`, `author`, `keywords`, `engines`, `publishConfig` in `package.json` files (from 8.6 code review)** — Tracked in [`../planning-artifacts/research/publishing-checklist-npm-ipm.md`](../planning-artifacts/research/publishing-checklist-npm-ipm.md) item A2. To be addressed during the pre-publish session, not during Epic 9.

### Dropped (post-Epic 9 publishing work, unrelated to rename)

These retrospective action items are the publishing activity itself and live beyond Epic 9's boundary. They are not retained in `deferred-work.md` — they belong to the publishing checklist and post-project planning.

- Retro Post-Project Action #1: npm publishing session (account, org, package.json fields, publish)
- Retro Post-Project Action #2: Bootstrap drift check before every publish — already embedded as an AC in Story 9.2
- Retro Post-Project Action #3: IPM publishing via Open Exchange
- Retro Follow-Through: test production + integration tests for interopRest/ruleGet/transformTest — already formally deferred by Epic 8 retro as "too complex for cleanup"
- Retro Follow-Through: MCP test harness — already formally deferred by Epic 8 retro as "post-project"

---

## Deferred from: code review of 10-1-iris-package-list (2026-04-20)

- **`generated` query parameter ignored on the `/modified/{ts}` Atelier branch** — Both `iris_package_list` and the sibling `iris_doc_list` pass `generated` to `/docnames/{cat}/{type}` but not to `/modified/{ts}`. Atelier's `/modified/` endpoint accepts a `generated` query param, so a caller with `modifiedSince: X, generated: true` expecting generated docs gets only non-generated ones. Pre-existing inconsistency inherited from `doc.ts`; out of scope for 10.1. Fix would apply to both tools together.
- **Digit-prefixed "package" rows (e.g., a `"2"` row with 13 docs observed on USER)** — Documents with class-name stems that start with digits (typically compiled/generated artifacts like `2.SomeName.cls`) roll up under a numeric top-level package name. The rollup is technically correct — these really are dotted names — but the output looks odd. Callers can filter via `category: "CLS"` if undesired. Not worth synthetic bucketing (too speculative about intent). Leaving as observed behavior; documented in the CSP-bucketing comment on `rollupPackage`.

## Deferred from: code review of 10-2-iris-doc-export (2026-04-20)

- **`.manifest.json.tmp` cleanup on rename failure** (export.ts:742-746) — If `fsp.rename` fails (e.g., AV scanner holding the final file open on Windows), the `.manifest.json.tmp` is left behind on disk. Not a security or correctness issue — the next successful run overwrites it, and the temp name makes its purpose obvious. LOW severity. A `finally { unlink(tmp).catch(() => {}) }` could clean up, but adds complexity for a rare failure mode.
- **Weird doc-name edge cases in `docNameToFilePath`** (load.ts:148-169) — Inputs like `".cls"` (only an extension), `"Foo..cls"` (empty segment in middle), or `".LeadingDot.cls"` produce slightly unusual paths with double slashes that `path.resolve` normalizes into the right tree. Atelier is not observed to emit such names. Defensive normalization could be added if the export tool ever ingests non-Atelier sources. LOW severity.

## Deferred from: code review of 11-3-db-metrics-config-accuracy-and-bootstrap-bump (2026-04-21)

- **`%ResultSet.Close()` not called on exception path in `DatabaseList()` loop** (`src/ExecuteMCPv2/REST/Config.cls:179-213`) — The `tRS.Close()` call on line 213 only runs when the `While tRS.Next()` loop completes normally. If an exception is thrown mid-loop (e.g., from `%Set("resource", ...)` or a `Config.Databases.Get()` that returns a non-$$$OK status that the current code doesn't pre-check), the catch block restores namespace and renders an error response but leaves `tRS` unclosed. Pre-existing; not introduced by Story 11.3. Same latent pattern exists throughout the file. LOW severity — `%ResultSet` cleanup is refcount-driven in modern IRIS, so the leak is theoretical. A `finally`-equivalent would be an outer-level `If $IsObject(tRS) { Do tRS.Close() }` in the catch. Defer to a future Config-handler hardening pass.
- **`%ResultSet.Close()` not called on exception path in `SystemConfig.GetConfig` locale branch** (`src/ExecuteMCPv2/REST/SystemConfig.cls:192-201`) — Same pattern as above: `Do tRS.Close()` is conditional on the `$$$ISOK(tSC2)` branch completing, but if an exception fires inside the `While tRS.Next()` loop (or inside `%Set("current", ...)`), `tRS` is left open. Pre-existing; not introduced by Story 11.3. LOW severity. Same fix applies.

## Deferred from: code review of 11-1-objectscript-error-envelope-and-sanitization (2026-04-21)

- **Missing ObjectScript unit test for `SanitizeError` prefix-strip (Bug #11)** (`src/ExecuteMCPv2/Tests/UtilsTest.cls`) — Story 11.1 adds the `"ERROR #N: "` / `"خطأ #N: "` strip to `Utils.SanitizeError` but no unit test exercises the strip itself. `TestSanitizeErrorStripsDetails` still passes (it only checks caret-reference removal), so a future regression of the prefix-strip would not be caught by the existing test suite. AC 11.1.5 explicitly scoped tests to +2 TypeScript tests, and AC 11.1.6 defers end-to-end validation to Story 11.3, so adding a `TestSanitizeErrorStripsLeadingErrorPrefix` test is out of Story 11.1's scope. LOW–MEDIUM severity. Recommend adding as part of Story 11.3 or a follow-up test-hardening story: assert that `GetErrorText(SanitizeError($$$ERROR($$$GeneralError, "ERROR #5001: Foo")))` contains exactly one `#5001` prefix.
- **Prefix-strip only handles English `ERROR` and Arabic `خطأ`; other locales still double-wrap** (`src/ExecuteMCPv2/Utils.cls:141`) — IRIS ships `ERREUR` (French), `FEHLER` (German), and other localized prefixes from `$System.Status.GetErrorText`. The current two-entry `For tPrefix = "ERROR #", "خطأ #"` list will miss those locales and produce `ERROR #5001: ERREUR #5001: ...` chains on locale-mixed environments. AC 11.1.2 and the sprint-change-proposal explicitly name only these two variants, so this is in-scope as-specified but fragile for multi-locale installs. LOW severity. If more locales matter, generalize to a regex-style scan over a configured prefix list or use `$System.Status.DecomposeStatus` to get the code + message fields directly instead of string-parsing `GetErrorText` output.
- **`Use tInitIO` without mnemonic clause leaves mnemonic routine bound after restore** (`src/ExecuteMCPv2/REST/Command.cls:75,96`) — Line 55 binds the redirect mnemonic via `Use tInitIO::("^"_$ZNAME)`, and the restore uses bare `Use tInitIO` (no mnemonic clause). In IRIS, `Use device` without a third colon-clause does NOT unbind the previously-set mnemonic routine — it stays bound on the device even after `ReDirectIO(0)`. Since the redirect flag is disabled, writes go to the default output, so there is no observable bug today. Latent concern only: if any code further up the stack later re-enables redirect on this device without first rebinding a mnemonic, the stale `^ExecuteMCPv2.REST.Command.1` would be used. LOW severity. A belt-and-braces fix would be `Use tInitIO::("")` or `Use tInitIO:::$Select(tOldMnemonic="":"", 1:"^"_tOldMnemonic)` to explicitly clear/restore the mnemonic. Deferred because the live verification in AC 11.1.4 confirms the current restore is sufficient for the Atelier request lifecycle.

---

## Triaged via Story 12.0 (2026-04-22)

Story 12.0 formally triaged all Epic 11 retrospective action items and all unclosed `deferred-work.md` entries against Epic 12 scope. See `12-0-epic-11-deferred-cleanup.md` for the authoritative triage tables. Summary of decisions:

### Resolved by Story 12.0

- **Missing ObjectScript unit test for `SanitizeError` prefix-strip** (`src/ExecuteMCPv2/Tests/UtilsTest.cls`) — **RESOLVED in Story 12.0**. Two new test methods added: `TestSanitizeErrorStripsLeadingErrorPrefix` and `TestSanitizeErrorStripsArabicPrefix`. Both verify that calling `SanitizeError` is idempotent — repeated calls do not accumulate additional `#N:` prefix nesting. This closes the coverage gap flagged by the Epic 11 code review (CR 11.1) and codified in Rule #8. 19/19 tests pass.

### Dropped (already resolved, no further action needed)

- **Retro action item: Build publishing checklist (npm account, package.json fields)** — Already captured in `publishing-checklist-npm-ipm.md` and the `user_npm_publish_experience` memory. No Story 12.0 action needed.
- **Retro action item: Grep for stale `protocols:` references in mocks/tests** — Completed during Epic 11 Story 11.4 code review. One instance found and fixed at that time. No further action.

### Retained as open deferred items (not addressed in Story 12.0 or Epic 12)

The following items remain open. They are NOT in Epic 12 scope and will be re-triaged after Epic 12's retrospective:

**Epic 11 retrospective action items deferred:**
- Retro action #1: Run pre-publish smoke test (Story 9.3 rerun) — deferred to pre-publish session.
- Retro action #3: Update `/bmad-retrospective` skill to enforce Rules codification step — low urgency; Rule #1 is self-enforcing without formal skill update.
- Retro action #6: Generalize locale prefix-strip for non-English/Arabic (FR, DE, etc.) — low priority; scoped out pending future hardening pass.

**Code-review deferred items retained from prior epics:**
- CR 10.1: `generated` query param ignored on `/modified/{ts}` Atelier branch — pre-existing inconsistency; not Epic 12 scope.
- CR 10.1: Digit-prefixed "package" rows (e.g., `"2"`) — documented behavior with workaround; not worth synthetic bucketing.
- CR 10.2: `.manifest.json.tmp` cleanup on rename failure — LOW severity; next run overwrites.
- CR 10.2: Weird doc-name edge cases in `docNameToFilePath` — LOW severity; Atelier not observed to emit such names.
- CR 11.3: `%ResultSet.Close()` not called on exception path — `Config.cls DatabaseList()` — LOW; same pattern throughout file; defer to future Config-handler hardening pass.
- CR 11.3: `%ResultSet.Close()` not called on exception path — `SystemConfig.cls locale branch` — LOW; same shape as above.
- CR 11.1: Prefix-strip only handles English + Arabic; other locales double-wrap — LOW for this project (HSCUSTOM is `enuw` + mixed message tables).
- CR 11.1: `Use tInitIO` without mnemonic clause — stale mnemonic binding — latent concern only; live Epic 11 verification confirmed current restore is sufficient.
- Epic 8.x CR/retro legacy: Duplicate `getIntegrationConfig` helpers, DRY env-var docs, missing package.json fields — cosmetic/publishing-checklist items unchanged from Story 9.0 closure.

## Deferred from: code review of 12-1-password-change-fix-and-policy-surface (2026-04-22)

- **No test for `changePasswordOnNextLogin: false → 0` path** (`packages/iris-admin-mcp/src/__tests__/user.test.ts`) — The `false → 0` conversion is the symmetric counterpart to the tested `true → 1` path (handler: `changePasswordOnNextLogin ? 1 : 0`). LOW severity. The logic is trivial and the handler code is correct; adding a `changePasswordOnNextLogin: false` test case would fully close the branch. Could be added in a future test-hardening pass or alongside Story 12.4's live-verification tests.

## Deferred from: code review of 12-2-production-control-dynamicobject-audit (2026-04-22)

- **`tTimeout=0` silently overrides caller-supplied zero timeout** (`src/ExecuteMCPv2/REST/Interop.cls:148`) — `If tTimeout = 0 Set tTimeout = 120` treats caller-supplied `timeout: 0` (intentional immediate stop) the same as omitted timeout. Pre-existing behavior carried forward from the old `$Get(tBody.%Get("timeout"), 120)` default; not introduced by the BUG-3 fix. LOW severity. If zero-timeout is ever needed, add `If tBody.%IsDefined("timeout") { Set tTimeout = +tBody.%Get("timeout") } Else { Set tTimeout = 120 }`.
- **CHANGELOG ordering — BUG-3 entry above Story 12.1's BUG-1 in `### Fixed` block** (`CHANGELOG.md:9`) — AC 12.2.6 specified "append below any Story 12.1 entries." BUG-3 was prepended instead (appears on line 9, BUG-1 on line 10). Cosmetic; same block; no functional impact. Could be reordered in a docs-cleanup pass.
- **Test count delta +2 vs AC-stated +3-4** (`packages/iris-interop-mcp/src/__tests__/production.test.ts`) — AC 12.2.4 + AC 12.2.8 targeted +3-4 new interop tests; dev added +2. Coverage is complete because the other 2 specified cases ("stop forwards optional timeout + force", "start requires name parameter") were covered by pre-existing tests. INFO — discrepancy is in stated count only, not in actual test coverage.

## Deferred from: code review of 12-3-production-create (2026-04-22)

- **`ProductionSummary` fallback hardcodes stateCode 2 / "Stopped" for never-started productions** (`src/ExecuteMCPv2/REST/Interop.cls:924`) — The `^Ens.Config.ProductionD` fallback path assigns `state: "Stopped"` (stateCode 2) for all enumerated productions when `GetProductionStatus` returns empty name. "Stopped" is the closest valid sentinel and matches live-verified behavior; "None" (no stateCode equivalent) could be more precise but is non-standard. LOW severity. Acceptable as-is.
- **New create unit test partially duplicates pre-existing test** (`packages/iris-interop-mcp/src/__tests__/production.test.ts:123`) — `"create action returns created envelope with name"` and `"should send POST with create action and name in body"` (line 23) test the same path; only difference is the namespace value. Harmless redundancy. LOW severity.
- **Delete running-check guards only state=1 (Running), not states 4 (Troubled) or 5 (NetworkStopped)** (`src/ExecuteMCPv2/REST/Interop.cls:106`) — Both Troubled and NetworkStopped are active states that may have locks or running jobs. Pre-existing; not introduced by this story. LOW severity. A future hardening pass could extend the guard to `(tState = 1) || (tState = 4) || (tState = 5)`.
- **Orphaned `Ens.Config.Production` record without class definition not handled** (`src/ExecuteMCPv2/REST/Interop.cls:96`) — If a class was deleted externally but its `^Ens.Config.ProductionD` record remains, the exists-check (`%Dictionary.ClassDefinition.%ExistsId`) returns false and the delete returns "does not exist" without cleaning up the orphaned record. LOW severity; edge case requiring external class deletion. If this becomes an issue, add a secondary check on `Ens.Config.Production.%ExistsId` and delete the record if the class is gone.

## Deferred from: live verification of 12-4-database-modify-docdb-and-bootstrap-bump (2026-04-22)

- **`iris_docdb_find` filter translation works, but queries against typed properties return empty when values aren't auto-extracted from `%Doc`** — The filter translation fix (MongoDB-style → DocDB `{restriction: [field, value, operator]}`) IS correct: empty filter `{}` returns all docs, non-existent-field filter (e.g., `{"name": "Alice"}`) returns IRIS error `ERROR #25541: DocDB Property 'name' does not exist`, and declared-property filters reach the server. BUT: when docs are inserted with `iris_docdb_document insert {document: {"age": 30}}`, the typed `age` column in the underlying SQL table remains `0` (or NULL) — the JSON value isn't auto-extracted into the typed property. SQL probe confirms: `SELECT %DocumentId, age FROM ISC_DM.TESTMCPDocs4` returns `[1,0]` and `[2,0]` for docs that had `"age":30` and `"age":25` in their JSON. Therefore `{"age":{"$gt":26}}` returns empty — the filter hits the typed column which is all zeros. This is a DocDB property-extraction/indexing issue independent of the filter translation. LOW–MEDIUM severity — BUG-5 (type) IS fixed; BUG-6 (find) is PARTIALLY fixed (filter translation works; property value population doesn't). Needs an Epic 13 follow-up to investigate whether (a) IRIS requires an explicit `index` step after property definition before values populate, (b) the `iris_docdb_property index` action must be invoked and has its own bug (the `index` action currently errors with `ERROR #5805: ID key not unique for extent '%Dictionary.PropertyDefinition'`), or (c) a new ExecuteMCPv2 handler is needed to force re-indexing. For now, `iris_docdb_find` is usable with empty filter (`{}`) to list all documents, and the structured filter translation is in place for when the upstream property-population issue is resolved.

## Deferred from: code review of 12-4-database-modify-docdb-and-bootstrap-bump (2026-04-22)

- **`buildDocDbRestriction` JSDoc says "skipped with a console warning" for unknown operators but no `console.warn` call exists** (`packages/iris-data-mcp/src/tools/docdb.ts:~102`) — The JSDoc comment at the top of the function states "Unsupported operators are skipped with a console warning" but the implementation only has an inline comment ("Unknown operators are silently skipped — the caller will get all docs") with no actual `console.warn` call. Behavior is correct; only the JSDoc is inaccurate. LOW severity. Fix: either add `console.warn(\`buildDocDbRestriction: unsupported operator "${mongoOp}" on field "${field}" — skipped\`)` inside the branch, or update the JSDoc to say "silently skipped". Defer to a future docs-cleanup pass.

- **Config.cls create branch: no rollback if `Config.Databases.Create()` fails after `SYS.Database.CreateDatabase()` succeeds** (`src/ExecuteMCPv2/REST/Config.cls:~376`) — `CreateDatabase()` creates the physical `IRIS.DAT` file on disk. If the subsequent `Config.Databases.Create()` call fails (e.g., name collision, invalid config props), the physical file is left on disk without a CPF registration. A subsequent re-attempt to create the same DB name with the same directory would then fail at `CreateDatabase()` because the directory already has an IRIS.DAT. Pre-existing architectural pattern (the original single-step code had the same risk if Create failed internally). LOW severity — the scenario requires Config.Databases.Create to fail after CreateDatabase succeeds, which is unlikely when the directory is freshly created. A complete fix would add a `SYS.Database.DeleteDatabase(tDir)` call in the cleanup path when `Config.Databases.Create` fails. Defer to a future Config-handler hardening pass.

## Deferred from: code review of 12-5-typescript-tool-surface-cleanup (2026-04-22)

- **`iris_oauth_manage` Zod schema for `supportedScopes` accepts only `string`, but callers cannot pass an array directly** (`packages/iris-admin-mcp/src/tools/oauth.ts:81`) — The handler converts the string to an array via `/[\s,]+/` split before sending to IRIS. This is the correct caller contract (callers provide space- or comma-separated string; handler normalizes). However the schema description does not mention the splitting behavior, and an array input would be rejected by Zod. LOW informational — schema design is intentional per AC 12.5.1. Could improve by adding "split by whitespace or comma before sending to IRIS" to the description. Defer to a future docs-cleanup pass.

- **`iris_rest_manage` scope:"all" deduplication collapses apps with identical/empty name** (`packages/iris-data-mcp/src/tools/rest.ts:172`) — The deduplication `seen` set uses `String(item.name ?? "")` as the key. If multiple apps share an empty or identical name (pathological but not impossible), only the first is included and the rest are silently dropped. Pre-existing pattern introduced in the same PR as the union behavior. LOW severity — IRIS REST applications always have unique non-empty path names by construction. No fix needed unless IRIS changes its webapp naming semantics.

## Deferred from: Story 12.6 — iris_alerts_manage new tool (2026-04-22)

- **Per-alert `clear` by index — deferred to Epic 13 (if demand)**: IRIS exposes no API for removing individual lines from `alerts.log`. The `alerts.log` file is append-only text located at `$zu(12)`. A fix would require direct file I/O under `IRIS.Temp.Alerts` lock — risky, brittle, and out of Epic 12 scope. API surface research confirmed: `$SYSTEM.Monitor.GetAlerts(.tAlertData, .tMessages, .tLastAlert)` reads the file but there is no corresponding `ClearAlert(index)` method. Only `Clear()` (clear all) and `ClearAlerts()` (clear counter only) exist.

- **Alert `acknowledge` — deferred to Epic 13 (if demand)**: Not supported natively for system enterprise-monitor alerts. `%Monitor.Alert.Notified` is a `Transient` property that applies to alert *definitions*, not individual alert *instances*. Ensemble managed alerts DO have an `Acknowledge` timestamp, but that is a separate subsystem. A native-style `acknowledge` action for system alerts would require a custom `ExecuteMCPv2` tracking table (storing which alert indexes have been seen by which client, keyed by timestamp or content hash) and a new REST handler for insert/query/delete on that table. This is app-level custom state — out of Epic 12 scope.

## Deferred from: code review of 13-2-documentation-rollup (2026-04-23)

- **Pre-existing `@iris-mcp/ops` tool-count drift: section heading and Mix line say 17, but Suite-wide rollup and READMEs say 16** (`tool_support.md:145`, `tool_support.md:167`, `tool_support.md:253`, plus cross-refs at `README.md:16`, `packages/iris-mcp-all/README.md:26`, `docs/migration-v1-v2.md:30`) — The `@iris-mcp/ops` per-table section at `tool_support.md:145` reads `Operations & Monitoring (17)` and the Mix line at line 167 reads `17 ExecuteMCPv2`. Counting the ops table rows confirms 17 tools (including `iris_alerts_manage` added in Epic 12 Story 12.6, commit `a373316`). However the Suite-wide rollup at line 253 says `| \`@iris-mcp/ops\` | 0 | 16 | 0 | **16** |` and the suite/meta-package READMEs show `@iris-mcp/ops` with tool count 16. Likely root cause: Epic 12 Story 12.6 (which added `iris_alerts_manage`) updated the per-table section and Mix line but did not propagate the count change into the Suite-wide rollup or the suite READMEs. **Not in scope for Story 13.2** — dev followed the AC literally (87 → 88 suite total, based on +1 dev tool) and flagged this drift in the review context. Out-of-scope but worth fixing in a follow-up docs-rollup pass: increment ops by 1 to 17 across the rollup row (changing `@iris-mcp/ops` row from `0 | 16 | 0 | **16**` to `0 | 17 | 0 | **17**`), update suite README dev/ops row, meta-package README, migration guide. Suite total should then become 89 (not 88). LOW severity — a cosmetic count inconsistency, not a functional issue.

---

## Triaged via Story 14.0 (2026-06-15)

Story 14.0 (Epic 14 retro-review gate) formally re-triaged every open `deferred-work.md` item against Epic 14's scope. Epic 13's retrospective was skipped (commit `079ed17`), so this triage runs off `deferred-work.md`. See [`14-0-epic-13-deferred-cleanup.md`](14-0-epic-13-deferred-cleanup.md) for the authoritative per-item triage tables.

**Decision summary: 0 INCLUDE, all DEFER** (one item ROUTED). Epic 14 is a strictly-additive Platform Foundation epic (multi-server profiles, governance cascade, MCP resources); none of the open items intersect its scope, and all are LOW / INFO / cosmetic / "future hardening pass" items already triaged across Stories 7.0/8.0/9.0/12.0.

### Routed (assigned to an Epic 14 story)

- **`@iris-mcp/ops` tool-count drift (CR 13.2, from 13-2 code review)** — **ROUTED to Story 14.6 (Documentation Rollup) — ✅ CLOSED in Story 14.6 (code review 2026-06-15)**. The section heading/Mix line said 17 while the suite-wide rollup and READMEs said 16 (`iris_alerts_manage` from Story 12.6 not propagated everywhere). Story 14.6 reconciled `@iris-mcp/ops` to **17** and the suite total to **89** across `tool_support.md` (rollup row + Total + derived "66 of 89, 74%" prose + ExecuteMCPv2 column 65→66), `README.md` (Servers table + "89 tools" callout + ASCII architecture diagram), `packages/iris-mcp-all/README.md`, and `docs/migration-v1-v2.md`. Code review re-counted independently (ops source = 17 tools incl. `iris_alerts_manage`; per-server 24+22+19+17+7 = 89; rollup columns 18+66+5 = 89) — all reconcile. Closed. (NOTE: `architecture.md:167`/`:32` retain stale `(16 tools)`/`86 tools` — see the Story 14.6 deferral below; that planning artifact was outside this story's user-facing-docs scope.)

### Retained as open deferred items (NOT in Epic 14 scope)

All remaining open items are explicitly carried forward unchanged — re-triage after Epic 14's retrospective. They are NOT addressed by any Epic 14 story:

**Carried from Story 12.0 "retained open":** Epic 11 retro #1 (pre-publish smoke → publishing checklist), #3 (retrospective-skill automation), #6 (locale prefix-strip generalization); CR 10.1 (`generated` on `/modified/{ts}`; digit-prefixed package rows); CR 10.2 (`.manifest.json.tmp` cleanup; `docNameToFilePath` edge cases); CR 11.3 (`%ResultSet.Close()` exception path — `Config.cls` + `SystemConfig.cls`); CR 11.1 (EN/AR-only prefix-strip; `Use tInitIO` mnemonic); Epic 8.x legacy (duplicate `getIntegrationConfig`, DRY env-var docs, missing `package.json` fields).

**New Epic 12 code-review deferrals:** CR 12.1 (`changePasswordOnNextLogin:false→0` test); CR 12.2 (`tTimeout=0` override; CHANGELOG ordering; test-count delta); CR 12.3 (`ProductionSummary` stateCode-2 fallback; duplicate create test; delete running-check state coverage; orphaned `Ens.Config.Production` record); live-verify 12.4 (`iris_docdb_find` typed-property population — LOW–MEDIUM, strongest future-cleanup candidate); CR 12.4 (`buildDocDbRestriction` JSDoc; `Config.cls` create no-rollback); CR 12.5 (`iris_oauth_manage` `supportedScopes` schema; `iris_rest_manage` `scope:all` dedup); Story 12.6 (per-alert `clear` by index; alert `acknowledge` — both "if demand").

---

## Deferred from: code review of story-14.1 (2026-06-15)

Origin story: **14.1** (Multi-Server Profiles — Config Model & Connection Resolution). All items LOW/MED; none block Story 14.1 (the HIGH bootstrap-flag finding and the File-List MED were auto-resolved inline during review). Several are explicitly routed to **Story 14.2**, which wires `getOrCreateClient` into `handleToolCall` and thus first creates the per-call/concurrent code path these items concern.

- **[14.1 / MED] Concurrency race in `getOrCreateClient` first-touch establishment** (`packages/shared/src/server-base.ts:566-593`) — Source: code review (Blind Hunter #1). Issue: the method is `async` with `await checkHealth(client)` + `await negotiateVersion(client)` sitting between the `profileMeta.get(name)` existence check and the `profileMeta.set(name, meta)` write, and there is no in-flight promise cache. Two concurrent first-touch calls for the same non-default profile both observe `existingMeta === undefined`, both establish, and both call `attemptProfileBootstrap` on the same shared client — defeating the documented "bootstrap at most once per profile" contract. Deferral rationale: Story 14.1 has **no production caller** of `getOrCreateClient` (it is exercised only by unit tests; `handleToolCall` calls `this.clients.getOrCreate(DEFAULT_PROFILE_NAME)` synchronously). The concurrent-dispatch path is introduced by Story 14.2 when it wires `getOrCreateClient` into per-call selection. Adding promise-caching now would be speculative infrastructure for a caller that does not yet exist and risks pre-empting 14.2's concurrency design. Suggested resolution: in **Story 14.2**, cache the establishment `Promise` per profile name (e.g. `Map<string, Promise<{client, atelierVersion}>>`) so concurrent first-touch calls await one shared establishment; add a concurrent-dispatch test (two simultaneous `getOrCreateClient(sameProfile, true)` → bootstrap called once).

- **[14.1 / MED] Non-default first-touch health-check failure caches the client but records no meta** (`packages/shared/src/server-base.ts:565` vs `:580`/`:593`) — Source: code review (Edge Case Hunter #2). Issue: `this.clients.getOrCreate(name)` caches the client at line 565; if `await checkHealth(client)` throws at line 580 (server unreachable), the throw happens before `profileMeta.set` at line 593, so no meta is recorded and the cached (un-established) client is never `destroy()`-ed. Every subsequent `getOrCreateClient` for that profile re-enters the first-touch path and re-runs health/negotiation. Deferral rationale: low impact — no session is established on a failed health check (nothing to leak), and re-trying establishment on the next call is defensible behavior (you generally *want* a transient failure to be retryable rather than permanently cached). Best revisited alongside 14.2's real call path, where the retry/error UX is observable. Suggested resolution: in 14.2, decide the desired semantics (retry vs. cache-failure-with-backoff) and either `destroy()` + drop the cached client on health-check failure, or record a `failed`-state meta; add a test that drives a non-default first-touch health-check rejection.

- **[14.1 / LOW] `port` and `timeout` accept coerced strings/booleans/arrays via `Number()` (asymmetric with strict `https`)** (`packages/shared/src/profiles.ts:140-149, 195-206`) — Source: code review (Edge Case Hunter #3/#4, Blind Hunter #5). Issue: `mergeProfile` coerces `port`/`timeout` through `Number()`, so `"443"`→443, `true`→1, `[443]`→443 pass silently, whereas `https` strictly requires `typeof === "boolean"`. Inconsistent strictness; `timeout:true` yields a 1 ms timeout that aborts every request with no validation error. Deferral rationale: not a correctness bug — NaN, fractional, zero, negative, and out-of-range values are still rejected; the coercion is forgiving rather than wrong, and `IRIS_PROFILES` is operator-authored JSON. Hardening only, strictly additive. Suggested resolution: tighten `mergeProfile` to require `typeof === "number"` for `port`/`timeout` (or to reject non-numeric/boolean/array inputs explicitly), mirroring the `https` strictness; add tests for the coercion-rejection cases.

- **[14.1 / LOW] Whitespace-only profile name registered without error; unknown/typo'd profile keys silently ignored** (`packages/shared/src/profiles.ts:277` and `mergeProfile`) — Source: code review (Edge Case Hunter #5, Blind Hunter #6). Issue: the `name === ""` guard rejects only the empty string, so a `"   "` key registers an effectively-unreachable profile (resolvable only by the exact whitespace string, since `resolveProfile` does not trim); and `mergeProfile` reads only recognized keys, so a typo'd field (`hostname` for `host`, `ssl` for `https`) is silently dropped and the profile inherits the default's value — pointing at the wrong server with no error. Deferral rationale: operational footguns, not crashes or correctness bugs; `IRIS_PROFILES` is opt-in operator config. Suggested resolution: trim-and-reject blank profile names; optionally reject unrecognized keys in a profile entry (fail-fast naming the offending key + profile), consistent with the "fail-fast on malformed `IRIS_PROFILES`" promise.

- **[14.1 / LOW] No `destroyAll()`/shutdown drain on `McpServerBase`; default-vs-override profile construction divergence** (`packages/shared/src/server-base.ts`, `packages/shared/src/profiles.ts` `mergeProfile`) — Source: code review (Blind Hunter #2/#11). Issue (a): `ProfileClientRegistry.destroyAll()` exists but `McpServerBase` never calls it — there is no `stop()`/`dispose()` that drains live `IrisHttpClient` instances (cookie jars + in-flight `AbortController`s) on teardown or re-`start()`. This is a **pre-existing** lifecycle gap, not a 14.1 regression — the former single `this.http` was likewise never destroyed by the server; the registry just makes the gap marginally larger. Issue (b): the default profile is built via `{ name, ...defaultConfig }` spread (carries every `IrisConnectionConfig` field automatically) while non-default profiles enumerate fields explicitly in `mergeProfile`'s return, so a future *optional* `IrisConnectionConfig` field would silently drop from every non-default profile (required fields are type-guarded by the `IrisProfile` return type). Deferral rationale: (a) pre-existing and unbounded only across restarts (bounded by profile count in normal single-start operation); (b) speculative — no optional `IrisConnectionConfig` field exists today and the dangerous case (new *required* field) is compile-checked. Suggested resolution: add an `McpServerBase.stop()` that calls `clients.destroyAll()` and clears `profileMeta`; and/or build non-default profiles by spreading the merged base then overriding, so new config fields propagate uniformly.

## Deferred from: code review of story-14.2 (2026-06-15)

Origin story: **14.2** (`server` Parameter Across All Tool Schemas — D2). Code review found **no HIGH findings and no D2 mismatch** (Acceptance Auditor confirmed exact D2 conformance + genuine test coverage of all 8 ACs). Two MED hardening patches (F1 reserved-field collision guard, F5 missing-extended-schema fail-fast) were auto-resolved inline. The items below are deferred — two MED that are latent/scope-bounded, one LOW pre-existing, one INFO coverage nicety.

- **[14.2 / MED] Coalesced in-flight establishment Promise ignores `needsBootstrap` variance** (`packages/shared/src/server-base.ts:726` — `getOrCreateClient` in-flight branch) — Source: code review (Blind Hunter). Issue: the `this.establishing` cache is keyed by profile name only. If an establishment that started with `needsBootstrap=false` is in flight when a second caller arrives with `needsBootstrap=true`, the second caller coalesces onto the first promise and may receive a client whose `attemptProfileBootstrap` never ran (subsequent callers re-establish + bootstrap correctly because `bootstrapAttempted` stays `false`). Deferral rationale: **latent only** — the sole production caller (`handleToolCall`) always passes the constant `this.options.needsCustomRest ?? false`, so within one server instance `needsBootstrap` never varies per profile; the hole is reachable only by a future caller passing a per-call `true` while `needsCustomRest` is false. Fixing it now would re-touch 14.2's just-landed concurrency design for a caller that does not exist. Suggested resolution: either key the in-flight cache by `(profileName, needsBootstrap)`, or have the coalesced caller re-check `bootstrapAttempted` after awaiting and run a one-shot bootstrap if still false; OR document + assert the "needsBootstrap is constant per server instance" invariant on `getOrCreateClient`. Add a test driving mixed-`needsBootstrap` concurrent first-touch.

- **[14.2 / MED] Untrimmed / mis-cased `server` value yields a confusing (but safe) "unknown profile" error** (`packages/shared/src/profiles.ts:333` `resolveProfile`, reached from `server-base.ts:396`) — Source: code review (Edge Case Hunter). Issue: `z.string().optional()` accepts `"prod "`, `" prod"`, `"PROD"`, `"   "`; `resolveProfile` does an exact `registry.get(key)` with no trim/normalize, so a copy-pasted profile name with a trailing space or wrong case throws `ProfileResolutionError` with the stray whitespace hidden inside the quotes (`Unknown server profile "prod "`). Not a crash — it surfaces as a structured `isError`. Deferral rationale: **not patched deliberately** — the QA integration suite (`server-param-integration.test.ts:366`) explicitly asserts whitespace-only `"   "` is NOT treated as default and surfaces unknown-profile; trimming at resolution would change that QA-asserted semantics, and AC 14.2 does not mandate trimming. The related 14.1 CR item already deferred whitespace-name handling to a profile-parsing hardening pass. Conservative classification (prefer defer over a speculative behavior change to tested semantics). Suggested resolution: in a profile-parsing hardening pass, decide trimming/case policy holistically (registration-time trim-and-reject blank names per the 14.1 item, and/or resolution-time trim of surrounding whitespace while preserving the original value in the error for whitespace-only input), then update the QA assertion accordingly.

- **[14.2 / LOW] `addTools()` re-adding an already-registered name throws uncaught, now after `extendedSchemas` + `tools` are mutated** (`packages/shared/src/server-base.ts:480-486`) — Source: code review (Edge Case Hunter). Issue: the MCP SDK's `registerTool` throws `Tool <name> is already registered` on a duplicate; this throw propagates uncaught out of `addTools` (no try/catch). Story 14.2 adds one more internal map (`extendedSchemas`, written at registration before the SDK call) → marginally larger partial-state-on-throw. Deferral rationale: **pre-existing** duplicate-registration behavior, not introduced by 14.2; the legitimate remove→add cycle is handled correctly (`removeTools` deletes `extendedSchemas` + the SDK entry together). Calling `addTools` with a name that is already live is a caller error. Suggested resolution: in a future server-base hardening pass, guard `registerTool` against re-registering an existing name (skip-with-warning, or delete-then-re-add), making both internal maps and the SDK registry consistent on the duplicate path.

- **[14.2 / INFO] Secondary coalescing branch (already-established client + first concurrent bootstrap) is code-correct but lacks a dedicated test** (`packages/shared/src/server-base.ts:715,788`) — Source: code review (Acceptance Auditor). Issue: the established-but-not-yet-bootstrapped + concurrent-first-bootstrap path correctly flows through `this.establishing` coalescing before `establishProfile` bootstraps once, but the existing AC 14.2.7 test exercises only first-touch (no prior meta), so this secondary interleaving is asserted-by-reasoning rather than exercised. Deferral rationale: code is correct (verified by trace); this is an optional coverage nicety, not a defect. Suggested resolution: add a test that establishes a profile via an Atelier-only (`needsBootstrap=false`) call, then fires two concurrent `getOrCreateClient(sameProfile, true)` and asserts bootstrap runs exactly once.

## Deferred from: code review of story-14.3 (2026-06-15)

Origin story: **14.3** (Governance Policy Model, Action Classification & Cascade Resolution — D3/D4/D7). Code review found **no HIGH findings and no D3/D4/D7 mismatch**. The two release-critical items were empirically verified clean: the AC 14.3.7 back-compat gate is **non-vacuous** (proves all 141 real baseline keys enable under empty `IRIS_GOVERNANCE`; non-emptiness independently asserted at `governance-edge.test.ts` `> 100`; drift test re-derives from built dists — live probe confirmed 89 tools → 141 keys, zero anomalies, byte-identical regeneration, hash `1e62c5ad5bf7`), and the cascade uses `??` not `||` (explicit-`false` precedence verified). One MED hardening cluster (prototype-member key handling) was **auto-resolved inline** (see story Review Findings CR-14.3-1). The items below are deferred — all LOW / future-hardening, none reachable in the current tool surface, none block Story 14.3 or its consumers (14.4 enforcement, 14.5 resource).

- **[14.3 / LOW] `defaultSeed` fails OPEN for an unclassified non-baseline key (a new write tool that forgets `mutates` ships ENABLED)** (`packages/shared/src/governance.ts` `defaultSeed`) — Source: code review (Blind Hunter). Issue: `mutatesLookup.get(key) === "write" ? false : true` means any NEW action absent from both the baseline and the `mutates` lookup defaults to enabled (treated as a read). For a governance engine whose purpose is making new mutating capability opt-in, a future dev who adds a destructive tool but omits its `mutates` classification would ship it enabled-by-default. Deferral rationale: this is the **D3-specified seed semantics** (in-baseline ⇒ enabled; new read ⇒ enabled; only a *classified* write ⇒ disabled) and is explicitly blessed by the dev + QA tests; not a defect against Story 14.3. The risk only materializes when Epics 15–17 add real `mutates`-bearing tools. Suggested resolution: in the epic that first adds a governed write tool (or in 14.4 enforcement wiring), add a registration-time assertion that every non-baseline tool/action key carries a `mutates` classification (fail-fast on an unclassified new key), so "forgot to classify" is caught at startup rather than silently enabling a write.

- **[14.3 / LOW] `mutates` metadata values are compile-time-only — a typo (`"wite"`) flows through unvalidated and is treated as a read (enabled)** (`packages/shared/src/governance.ts` `buildMutatesLookup`; `packages/shared/src/tool-types.ts:89`) — Source: code review (Blind + Edge). Issue: `Record<string, "read" | "write">` is erased at runtime; `buildMutatesLookup` does `lookup.set(..., cls)` with no check that `cls ∈ {"read","write"}`, and `inputSchema` is cast `as any` throughout, so a misspelled class is silently accepted and `defaultSeed`'s `=== "write"` check then enables it. Deferral rationale: not reachable today (no real tool declares `mutates` until Epics 15–17 — the Epic-14 reality check forbids classifying existing tools); purely a forward-looking robustness gap. Suggested resolution: when real `mutates` declarations arrive, validate each value in `buildMutatesLookup` (throw on anything not exactly `"read"`/`"write"`), or add a Zod/`satisfies` guard at tool-registration time.

- **[14.3 / LOW] `buildMutatesLookup` silently loses a `__proto__` action key in a `mutates` record literal** (`packages/shared/src/governance.ts` `buildMutatesLookup`) — Source: code review (Edge Case Hunter). Issue: a `mutates` map literal `{ __proto__: "write" }` sets the object's prototype rather than an own property, so `Object.entries(m)` never yields it and the classification is lost. Deferral rationale: `mutates` is **author-controlled tool metadata** (not external operator input like `IRIS_GOVERNANCE`, which was hardened inline via CR-14.3-1), and no tool would name an action `__proto__`/`constructor`. Far lower stakes than the config-parse path; not reachable until real `mutates` maps exist. Suggested resolution: when real `mutates` maps land, screen action keys against the reserved set in `buildMutatesLookup` (mirror the `RESERVED_KEYS` guard already added to `validateLayer`), or build per-action maps with `Object.create(null)` at the tool-definition site.

- **[14.3 / LOW] Generator + drift test enumeration is not independent, and several malformed-tool shapes are silently downgraded to a bare key** (`scripts/gen-governance-baseline.mjs:60-75`; `packages/shared/src/__tests__/governance.test.ts` `deriveBaselineFromDists`) — Source: code review (Blind + Edge). Issue: the drift test replicates the generator's exact `inputSchema?.shape?.action?.options` derivation, so a tool shape the introspection mishandles is misclassified identically in both (hashes still match, drift test still green). Specific unguarded shapes: a tool missing `name` → key `"undefined"`; missing `inputSchema` → bare key; an `action` that is `z.optional(z.enum(...))` (wrapper `.options` is undefined) → action keys dropped to a bare key; an empty `z.enum([])` → bare key; non-string enum options → `tool:1`. Deferral rationale: **none of these shapes exist in the current surface** — a live probe across all 5 packages confirmed 89 tools, every one with a string `name` + `inputSchema`, every `action` a non-empty bare `ZodEnum` with all-string options, zero anomalies. The non-independence mirrors the established `gen:bootstrap` drift-test pattern (also self-referential). Suggested resolution: when a future tool uses `z.enum(...).optional()` for `action` or any non-bare-enum shape, unwrap the wrapper (`action?._def?.innerType ?? action`) in BOTH the generator and the drift test, and add generator guards that throw (not silently downgrade) on a tool missing `name`/`inputSchema`, an empty action enum, or a non-string option — turning a silent baseline poisoning into a build failure.

- **[14.3 / LOW] Cross-package tool-name collisions are silently merged with no detection** (`scripts/gen-governance-baseline.mjs:35-77`) — Source: code review (Blind + Edge). Issue: governance keys accumulate into one `Set` across all five packages; if two packages ever register the same tool name (or `tool:action`), the second silently dedupes into the first while `toolCount++` double-counts, so the only signal is a `Tools enumerated: N` vs `Baseline keys: M` discrepancy in the console summary. Deferral rationale: **not reachable today** — a live probe confirmed zero duplicate tool names across the five packages. Tool names are globally unique by the suite's naming convention. Suggested resolution: have the generator throw on a duplicate tool name across packages (`if (seen.has(name)) throw …`), making an accidental cross-server collision a hard build error rather than a silent surface merge.

- **[14.3 / LOW] `SERVER_PACKAGES` list is duplicated in three places kept in sync only by comment** (`scripts/gen-governance-baseline.mjs:18-24`; `packages/shared/src/__tests__/governance.test.ts:520-526`; `packages/shared/src/__tests__/governance-edge.test.ts` note) — Source: code review (Blind). Issue: the most correctness-critical list in the feature (which packages form the baseline) is hard-coded in the generator and re-hard-coded in the drift test with a "MUST stay in sync" comment. If a sixth server package is added and only one copy is updated, the drift test would validate a different surface than the generator produced. Deferral rationale: maintainability, not a current correctness bug (the two lists are identical today, verified); the in-test drift guard would surface most divergences as a baseline mismatch. Suggested resolution: export the package list from a single shared module and import it into both the generator and the drift test.

- **[14.3 / LOW] Generator always overwrites with no `--check` mode for CI drift enforcement** (`scripts/gen-governance-baseline.mjs:141-142`) — Source: code review (Blind). Issue: the script unconditionally `writeFileSync`s `governance-baseline.ts`; determinism rests entirely on `Set` insertion + `.sort()`, and drift detection is delegated wholly to the vitest drift test. A developer who runs the generator but not the tests can commit a file without an independent reproducibility gate, and there is no `gen:governance-baseline --check` for a CI pipeline to fail on drift without writing. Deferral rationale: this mirrors the existing `gen:bootstrap` discipline (also test-enforced, no `--check`, no CI workflow file in the repo); regeneration WAS verified byte-identical during this review. Suggested resolution: if/when a CI drift-check job is added for `gen:bootstrap`, give both generators a `--check` flag that re-derives and exits non-zero on any diff (without writing), and run it in CI alongside the build.

## Deferred from: code review of story-14.4 (2026-06-15)

Origin story: **14.4** (Call-Time Governance Enforcement & Structured Denial Error — D5/D4/D7). Code review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) found **no HIGH and no MED findings, and no D5/D4/D7 mismatch**. The security boundary was verified clean and un-bypassable: the gate sits as the SOLE chokepoint strictly AFTER `resolveProfile` and BEFORE `getOrCreateClient`+handler (no second gate, no bypass path; lines 520–555 are fully synchronous so nothing can interleave between resolution and the gate); it fails CLOSED on an engine throw (the `effective()` call is un-`try`-wrapped, so any throw propagates as a JSON-RPC error and still never reaches the handler); the denial envelope is well-formed (`isError:true` + non-empty text + object `structuredContent:{code,action,server}`); the MCP SDK's `validateToolOutput` early-returns on `result.isError` BEFORE touching any tool `outputSchema` (empirically confirmed in SDK `mcp.js` — a denial's `structuredContent` is never validated against an `outputSchema`); back-compat is non-vacuous (a representative existing read from each of the 5 servers + all three `iris_database_manage` actions resolve enabled under empty `IRIS_GOVERNANCE`, each asserted `GOVERNANCE_BASELINE.has(key)` first); D4 key alignment was proven through `handleToolCall` (`iris_database_manage:delete` gate key == baseline entry) and the gate predicate is byte-identical to the generator's; only `server-base.ts` + the two new test files changed (no server-package source, no `governance-baseline.ts` / `bootstrap-classes.ts` edit, no `BOOTSTRAP_VERSION` bump). Two LOW documentation nits in the story (test-count "+14" should be "+28"; File List omitted the QA coverage suite) were **auto-resolved inline** during review. The single code item below is deferred — LOW, latent, not reachable on the current tool surface, and a direct continuation of an already-deferred 14.3 item.

- **[14.4 / LOW] `computeGovernanceKey` degrades per-action governance to whole-tool governance (and can build a `tool:undefined` key) if a future `action` enum is wrapped in `.optional()` / `.default()` / `.nullable()`** (`packages/shared/src/server-base.ts:419-428`; mirror `scripts/gen-governance-baseline.mjs:62-64`) — Source: code review (Blind Hunter + Edge Case Hunter, merged). Issue: the multi-action predicate is `Array.isArray((tool.inputSchema as any)?.shape?.action?.options) && options.length > 0`. Empirically (Zod 4.3.6): a bare `z.enum([...])` and `z.enum([...]).describe(...)` expose `.options`, but `z.enum([...]).optional()` (→ `ZodOptional`), `.default("x")` (→ `ZodDefault`), and `.nullable()` all expose `.options === undefined`. So a future tool declaring `action: z.enum(["read","wipe"]).optional()` would make the gate compute the **bare tool key** instead of `tool:action` — and because the baseline generator uses the IDENTICAL access path, it would emit the bare key too, keeping gate and baseline *consistent with each other* but causing every per-action deny an operator writes against `tool:action` (e.g. `{"global":{"iris_x:wipe":false}}`) to silently NEVER match → the action the operator intended to disable ships ENABLED (a fail-open for targeted per-action policy). Separately, if any schema ever exposed a non-empty `.options` while permitting `action` to be absent at validation, `String(validatedArgs.action)` would yield the literal `"tool:undefined"` key, matching no policy/baseline entry and falling to the default seed. Deferral rationale: **not reachable on the current surface** — all 21 production multi-action tools across the five packages declare `action` as a required bare `z.enum(...).describe(...)` (every one verified), so the gate and baseline agree and no shipped tool triggers either path; nothing in this story regresses. It is a forward-looking robustness trap for Epics 15–17 (when real `mutates`-bearing governed tools land), and it directly extends the **already-deferred 14.3 CR item** that defers unwrapping `z.optional(z.enum(...))` action shapes in BOTH the generator and the drift test (see "[14.3 / LOW] Generator + drift test enumeration … several malformed-tool shapes are silently downgraded to a bare key" above). Patching only the gate now would be incomplete: a correct fix must (a) unwrap `ZodOptional`/`ZodDefault`/`ZodNullable` via `.unwrap()` / `.def.innerType` before reading `.options` in BOTH `computeGovernanceKey` and `gen-governance-baseline.mjs` so they stay in lock-step, AND (b) add a registration-time assertion (or lint) that a tool's `action` field, when present, is a non-optional bare `ZodEnum` with non-empty `.options` (and harden the gate's branch to also require `validatedArgs.action !== undefined`), AND (c) add a `governance-edge.test.ts` case for the `.optional()`/`.default()` action shapes (currently it covers `z.enum`/`z.string`/empty-enum only). Suggested resolution: fold this into the epic that first adds a governed write tool (the same epic that the 14.3 item routes the unwrap fix to), implementing (a)+(b)+(c) together so per-action governance cannot silently downgrade.

---

## Deferred from: code review of story-14.5 (2026-06-15)

Origin story: **14.5** (Governance Discovery Resource & `resources` Capability — D6/D1/D5). Code review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) found **no HIGH and no MED findings, and no D1/D5/D6 mismatch**. The Epic-14-done capstone (AC 14.5.6) was verified **GENUINE, not illusory** on every axis: per-profile session isolation (D1) asserts the actual `Cookie` header the production `IrisHttpClient` emits from genuinely separate per-instance cookie jars, plus four distinct cached client instances, under `Promise.all`; uniform enforcement (D5) asserts byte-identical `structuredContent` denials flowing through the single shared `handleToolCall` gate, in both deny and re-enable directions, across two servers; it runs in the DEFAULT vitest suite (`.test.ts`, not the excluded `.integration.test.ts`); advisory-only (AC 14.5.4) is structurally guaranteed (resource read callbacks are pure — `resolveProfile` lookup + pure `getEffectivePolicy`; no enforcement path reads resource state) and empirically confirmed cross-server; AC 14.5.3 cross-checks the FULL effective map incl. the global∩profile cascade (not spot-checks); D6 minimalism is honored (one static resource + one template with `list: undefined`, no generalized `ResourceDefinition` framework). One **MED** test-portability finding (the two new capstone `makeHostAwareFetch` mocks set `Set-Cookie` via the `Response` constructor but did not patch `Headers.getSetCookie()`, diverging from the package-wide convention and risking empty cookie jars / failing D1 assertions on the supported Node-18 floor) was **auto-resolved inline** during review (both mocks now patch `getSetCookie` to mirror the convention; an imprecise "tool's GET" comment was also tightened). Re-verified after the fix: shared 450 tests pass, `tsc` strict build clean, lint clean. The single code item below is deferred — LOW, narrow, no crash, and not required by any AC.

- **[14.5 / LOW] URL-encoded profile name is not decoded in the `iris-governance://{profile}` resource template read callback** (`packages/shared/src/server-base.ts:531-535`, via `buildGovernancePolicyResult` → `resolveProfile`) — Source: code review (Edge Case Hunter). Issue: the template read callback does `const profileName = Array.isArray(raw) ? String(raw[0]) : String(raw)` and passes the **raw, undecoded** authority segment to `resolveProfile`, which does a literal `registry.get(profileName)`. Verified against the MCP SDK `ResourceTemplate.uriTemplate.match`: `iris-governance://a%20b` yields `{ profile: "a%20b" }` (the SDK does not percent-decode the capture). So a profile whose `IRIS_PROFILES` name contains any character that percent-encodes in a URI authority (space, non-ASCII, etc. — e.g. a profile literally named `"a b"`) is **unreachable via its natural encoded URI** `iris-governance://a%20b`: `resolveProfile` throws `ProfileResolutionError` → mapped to a clean `McpError(InvalidParams)` "Unknown server profile \"a%20b\"" (**no crash**), and the error's own "Valid profiles: …, a b, …" list shows the confusing encoded-vs-decoded mismatch. Severity LOW. Deferral rationale: **realistic profile names never percent-encode** — every profile name in this suite's docs/tests and the natural operator vocabulary (`prod`, `stage`, `dr`, `qa`, `default`) is `[A-Za-z0-9_-]+`, which is identity-encoded; no shipped or documented configuration triggers it, and the failure mode is a clean structured error, not a crash or a security/enforcement gap (the resource is advisory only). A naive fix (`decodeURIComponent(profileName)`) introduces its OWN edge cases — a literal `%` in a profile name, or a malformed `%`-sequence, makes `decodeURIComponent` throw `URIError`, which would need its own catch to avoid converting a clean "unknown profile" error into an unhandled crash — so it is not a reflexive one-liner and is not warranted by any current requirement. Suggested resolution: if/when profile names with URI-reserved characters become a supported scenario, decode the captured `{profile}` with a `try/catch` around `decodeURIComponent` (falling back to the raw value on `URIError` so a malformed URI still yields the clean "unknown profile" `McpError` rather than a crash), and add a `governance-resource` test for an encoded profile name (`iris-governance://a%20b` → policy for `"a b"`) plus a malformed-sequence case (`iris-governance://%zz` → clean `McpError`, no throw). Until then, document in the resource's JSDoc that profile names are matched literally (un-decoded), so operators avoid URI-reserved characters in `IRIS_PROFILES` names.

## Deferred from: code review of story-14.6 (2026-06-15)

Origin story: **14.6** (Documentation Rollup — Multi-Server & Governance). **Docs-only story — no code changed.** Code review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) found **no HIGH and no MED findings**: every load-bearing documented fact was verified against the shipped code (`profiles.ts`/`governance.ts`/`server-base.ts`), the three client-config JSON-in-env examples were `node`-parsed clean (outer config + each escaped inner value re-parse to the intended objects), and the routed CR 13.2 count reconciliation was confirmed (ops = 17, suite = 89; rollup columns 18+66+5 = 89; per-server 24+22+19+17+7 = 89; derived "66 of 89, 74%" prose updated). One **LOW** patch (the six per-package READMEs implied the `server` param appears only "When `IRIS_PROFILES` is set", whereas the shipped code advertises it on every tool unconditionally — `server-base.ts:558`) was **auto-resolved inline** during review (reworded to "Every tool accepts an optional `server` parameter … omit it to use the `default` profile"). The two items below are deferred — both LOW, pre-existing, and outside this story's scope.

- **[14.6 / LOW] `docs/migration-v1-v2.md:28` documents v2 tool naming as dot-namespaced (`iris.execute.command`, `iris.global.get`)** — Source: code review (Blind + Edge + Acceptance Auditor, merged; originally surfaced by QA). Issue: the v1→v2 "Breaking Changes" comparison table row reads `| **Tool naming** | execute_command, get_global, etc. | Dot-namespaced: iris.execute.command, iris.global.get, etc. |`, but the shipped v2 suite uses **underscore** tool names (`iris_execute_command`, `iris_global_get`, `iris_global_list`, `iris_database_manage`, etc. — confirmed across the whole codebase and the memory note "Never use dots in tool names; Anthropic API rejects them"). The dotted scheme was an intermediate/aspirational naming that Epic 9 (Tool Name Flattening) replaced with underscores before first publish. Deferral rationale: **pre-existing** — the line was NOT touched by Story 14.6 (only the adjacent count cells `88→89`/`16→17` in the same file were changed), it predates Epic 14, and a tool-naming-convention correction in the migration guide is outside a count/governance docs-rollup story's scope. It is nonetheless a genuine user-facing inaccuracy in a release-facing doc. Suggested resolution: in a future docs-cleanup pass (or whenever `migration-v1-v2.md` is next edited substantively), change the v2 cell to `Underscore-namespaced: iris_execute_command, iris_global_get, etc.` and audit the rest of the file for any other dotted-name references.

- **[14.6 / LOW] `architecture.md` retains stale ops/suite tool counts (`(16 tools)` and "86 tools")** — Source: code review (Acceptance Auditor). Issue: `_bmad-output/planning-artifacts/architecture.md:167` reads `iris-ops-mcp/  # @iris-mcp/ops (16 tools)` and `architecture.md:32` reads "… 86 tools organized across 5 domain servers". The routed CR 13.2 reconciliation (ops 16→17, suite 88→89) was applied correctly across **all the user-facing docs** in this story's changed-file set (`tool_support.md`, `README.md` incl. its ASCII architecture diagram, `iris-mcp-all/README.md`, `migration-v1-v2.md`), but `architecture.md` is the design-authority **planning artifact** and was explicitly outside the routed scope (which targeted the published docs). The story's Completion-Note phrase "architecture diagram" referred to the README ASCII diagram (correctly updated to `ops (17)`), not `architecture.md`. The "86 tools" figure on line 32 is long-pre-existing staleness unrelated to this story (the suite has been >86 for several epics). Deferral rationale: pre-existing planning-doc staleness in a non-published artifact; out of scope for a user-facing docs rollup; zero user impact (architecture.md is internal). Suggested resolution: bump `architecture.md:167` to `(17 tools)` and refresh the `architecture.md:32` total to the current 89 the next time the architecture doc is revised (e.g. during the Epic 14 retrospective or the next architecture edit).

---

## Triaged via Story 15.0 (2026-06-16)

Story 15.0 (Epic 14 Deferred Cleanup — the Epic 15 retro-review gate) hardens the governance action-classification machinery **before** Epic 15's first governed write tool (Story 15.1 `iris_service_manage:create/delete`) and re-triages every open `deferred-work.md` item against Epic 15's scope. See [`15-0-epic-14-deferred-cleanup.md`](15-0-epic-14-deferred-cleanup.md) for the authoritative triage tables. Unlike Story 14.0, this is a **code-changing** cleanup story (TypeScript + generator + tests + docs only — no ObjectScript, no `BOOTSTRAP_VERSION` bump; the governance baseline regenerated **byte-identical**, hash unchanged at `1e62c5ad5bf7`, 141 keys / 89 tools).

**Decision summary: 7 INCLUDE (resolved here), 5 ROUTE (to a later Epic 15 story), 11 DEFER (no Epic 15 intersection, carried forward unchanged).**

### INCLUDE — resolved in Story 15.0

- **[CR 14.4 / LOW] `computeGovernanceKey` action-enum `.optional()`/`.default()`/`.nullable()` downgrade + `tool:undefined` key** — **RESOLVED (AC 15.0.1, 15.0.2).** Added a shared `unwrapActionOptions` helper (`governance.ts`) that peels `ZodOptional`/`ZodDefault`/`ZodNullable` to the inner ZodEnum before reading `.options`, applied identically in `computeGovernanceKey` AND `rebuildGovernedKeys` (`server-base.ts`) AND the baseline generator (`gen-governance-baseline.mjs`, mirrored with a "MUST mirror" comment) so gate and baseline stay in lock-step. The gate's multi-action branch now also requires `validatedArgs.action !== undefined`, so the literal `tool:undefined` key can never be built.
- **[CR 14.3 / LOW] Generator + drift-test silently downgrade malformed shapes** (missing name/inputSchema, empty enum, non-string option, wrapped enum) — **RESOLVED (AC 15.0.1, 15.0.5).** The wrapped-enum case is fixed by the lock-step unwrap above; the other malformed shapes now throw in the generator (see the AC 15.0.5 item below).
- **[CR 14.3 / LOW] `defaultSeed` fails OPEN for an unclassified non-baseline key** (a new write that forgets `mutates` ships ENABLED) — **RESOLVED (AC 15.0.3).** Added a registration-time assertion (`assertGovernanceClassification`, wired into `McpServerBase` construction + `addTools`/`removeTools`) that throws, naming the offending key, when any non-baseline tool/action key reaches registration without a `mutates` classification. Per the lead's strict-contract decision (Story 15.0 clarification, 2026-06-16): **every** new (non-baseline) tool MUST classify — read OR write — so a forgotten classification fails fast at startup rather than mis-seeding. Baseline/grandfathered keys (all 141 current) are exempt. `tool-types.ts` + `governance.ts` doc comments reconciled to the new contract; the read-default in `defaultSeed` is now defense-in-depth (unreachable unclassified at registration).
- **[CR 14.3 / LOW] `mutates` typo (`"wite"`) flows through unvalidated as a read** — **RESOLVED (AC 15.0.4).** `buildMutatesLookup` now throws (via `assertMutationClass`) on any `mutates` value not exactly `"read"`/`"write"`, for both scalar and per-action forms, naming the offending key.
- **[CR 14.3 / LOW] `buildMutatesLookup` loses a `__proto__` action key** — **RESOLVED (AC 15.0.4).** `buildMutatesLookup` now screens record-form action keys against the reserved set (`__proto__`/`constructor`/`prototype`), mirroring the `RESERVED_KEYS` guard in `validateLayer` (CR-14.3-1), and throws on a reserved action key.
- **[CR 14.3 / LOW] Cross-package tool-name collision silently merged** — **RESOLVED (AC 15.0.5).** The generator now tracks a `seen` map across all five `SERVER_PACKAGES` and throws (naming both producing packages/tools) on a duplicate `tool` or `tool:action` key — turning a silent Set merge into a hard build error. The generator also throws on a tool missing `name`, missing `inputSchema`, an empty `action` enum, or a non-string enum option.
- **[CR 14.6 / LOW] `migration-v1-v2.md:28` documents dotted v2 tool names (pre-Epic-9)** — **RESOLVED (AC 15.0.8).** Corrected the "Tool naming" row from `Dot-namespaced: iris.execute.command, …` to `Underscore-namespaced: iris_execute_command, …`, and (per the 14.6 deferral's "audit the rest of the file" note) converted every remaining dotted v2 tool name in the file — the full Tool Mapping table, the "Additional v2 tools" list, and the two FAQ prose references — to the shipped underscore scheme. Zero dotted `iris.x.y` references remain in the file.

### ROUTE — assigned to a later Epic 15 story

- **[Epic 14 retro AI#3] Verify the `mutates:'write'` seed actually disables the first write tool end-to-end** — **ROUTED to Story 15.1** (`iris_service_manage` — the first governed write tool). Add a back-compat/governance AC there exercising a real `mutates:'write'` action denied-by-default and re-enabled via `IRIS_GOVERNANCE`. (The machinery is now in place from Story 15.0; 15.1 is the real-tool exercise.)
- **[Epic 14 retro AI#4] Regenerate `governance-baseline.ts` whenever Epic 15 adds a tool; drift test enforces** — **ROUTED to Story 15.6** (BOOTSTRAP bump + verification) **and each new-tool story** (regenerate after the tool lands; the drift test in `governance.test.ts` enforces).
- **[CR 14.6 / LOW] `architecture.md:32` "86 tools" + `:167` "(16 tools)" stale counts** — **ROUTED to Story 15.6** (docs/count reconciliation — set to the FINAL post-Epic-15 suite total so 15.1–15.5's intermediate additions don't immediately stale it). NOT touched in Story 15.0 (AC 15.0.10 scope boundary).
- **[CR 14.3 / LOW] `SERVER_PACKAGES` list duplicated across generator + drift test (sync-by-comment)** — **ROUTED to Story 15.6** (or a future generator-refactor) — maintainability, not a current correctness bug; the cross-package duplicate-key guard added in AC 15.0.5 reduces the blast radius of a divergence.
- **[CR 14.3 / LOW] Generator has no `--check` mode for CI drift enforcement** — **ROUTED / deferred to a future CI story** (mirrors the existing `gen:bootstrap` discipline; out of Epic 15 scope). Carried in the DEFER set below.

### DEFER — no Epic 15 intersection; carried forward unchanged

All remaining open items are re-affirmed DEFER — none intersect Epic 15's scope (Epic 15 adds admin Security/LDAP/X.509/Audit/SQL-privilege **tools**, not new connection/profile-parsing callers). Carried forward unchanged for re-triage after Epic 15's retrospective:

- **[CR 14.1 / MED]** Concurrency race in `getOrCreateClient` first-touch (no in-flight promise cache for mixed callers) — connection-layer; Epic 15 adds admin tools, not new connection callers.
- **[CR 14.1 / MED]** Non-default first-touch health-check failure caches client, records no meta — same connection-layer; no admin-tool intersection.
- **[CR 14.1 / LOW]** `port`/`timeout` accept `Number()`-coerced strings/booleans/arrays (asymmetric with strict `https`) — profile-parsing hardening; operator-authored JSON; not reached by Epic 15.
- **[CR 14.1 / LOW]** Whitespace-only profile name registered; typo'd profile keys silently ignored — profile-parsing hardening; not reached by Epic 15.
- **[CR 14.1 / LOW]** No `destroyAll()`/shutdown drain on `McpServerBase`; default-vs-override profile construction divergence — lifecycle/pre-existing; not reached by Epic 15.
- **[CR 14.2 / MED]** In-flight establishment Promise ignores `needsBootstrap` variance — latent (sole caller passes a constant); connection-layer; not reached by Epic 15.
- **[CR 14.2 / MED]** Untrimmed/mis-cased `server` value → confusing-but-safe "unknown profile" error — tested semantics; profile-resolution hardening pass; not reached by Epic 15.
- **[CR 14.2 / LOW]** `addTools()` re-adding a live name throws after maps mutated (partial state) — pre-existing caller-error path; server-base hardening pass.
- **[CR 14.2 / INFO]** Secondary coalescing branch lacks a dedicated test — optional coverage nicety; code verified correct.
- **[CR 14.5 / LOW]** URL-encoded profile name not decoded in `iris-governance://{profile}` resource template — advisory resource; realistic names are identity-encoded; no crash; defer to a URI-policy pass.
- **[CR 14.3 / LOW]** Generator `--check` mode for CI drift (also ROUTE-tagged above) — mirrors `gen:bootstrap`; out of Epic 15 scope; deferred to a CI story.
- **[Story 14.0 retained]** All Epic 11/12 retained-open items (Epic 11 retro #1/#3/#6; CR 10.1/10.2; CR 11.3/11.1; Epic 8.x legacy; CR 12.1–12.5; Story 12.6 alert clear/ack; live-verify 12.4 docdb typed-property population) — re-affirmed DEFER; none intersect Epic 15. Carried forward unchanged.

### Story 15.0 code review (2026-06-16) — new findings

One HIGH was found and RESOLVED inline during the Story 15.0 code review (CR-15.0-1: `.nullable()` action enum `action: null` built the never-matching key `tool:null` → per-action governance fail-open; fixed in `computeGovernanceKey` by requiring the action to be an enum member, `options.includes(...)`; regression-tested in `governance-classification.test.ts`; baseline hash unchanged `1e62c5ad5bf7`). The following LOW items are DEFERRED — none reachable on a real Zod-schema tool today, and the gate/generator lock-step is preserved (both fail identically), so none is a current correctness bug:

- **[CR 15.0-2 / LOW]** Gate `unwrapActionOptions` does not type-validate option elements; gate uses `String(value)` with no non-string guard while the generator THROWS on a non-string option. Reachable only via a non-Zod field masquerading with numeric `.options`; the generator hard-stops such a tool at build time before any baseline ships. Fold into the Story 15.6 generator/drift-test hardening pass.
- **[CR 15.0-3 / LOW]** `unwrapActionOptions` peels only `.unwrap()`/`._def.innerType` (covers the story's declared `.optional()`/`.default()`/`.nullable()` set); `ZodEffects` (`.refine`/`.transform`, stored in `_def.schema`) / `ZodPipeline` would fall back to the bare key. Out of the declared wrapper set; `.refine()` is forbidden on MCP schemas by project rule; gate and generator fail identically (no lock-step disagreement). Defer to a future wrapper-support story.
- **[CR 15.0-4 / LOW]** `unwrapActionOptions` calls `field.unwrap()` unguarded — a throwing accessor would propagate out of the constructor. Not reachable for real Zod types. Defer.
- **[CR 15.0-5 / LOW]** Drift test `governance.test.ts:540` derives keys WITHOUT the shared `unwrapActionOptions` (pre-existing, not in Story 15.0's changed set). Harmless on today's all-bare surface, but a lock-step gap that must close before a wrapped action enum ships — same class as the already-ROUTED "SERVER_PACKAGES duplicated across generator + drift test." Fold into the **Story 15.6** generator/drift-test de-duplication.
- **[CR 15.0-6 / LOW]** `buildMutatesLookup` silently accepts a `mutates` map key that is not an enum member (stale/typo'd action key); the missing REAL action is still caught by the registration assertion, so only a dead entry goes unflagged. Cosmetic. Defer.

### Story 15.1 code review (2026-06-16) — new findings

Three findings auto-fixed inline during the Story 15.1 code review (not deferred): (1) `get` with an empty/missing `name` silently degraded to a list-all — now rejected up front in `service.ts` + 3 regression tests; (2) `set` with no/empty `settings` issued a no-op `Modify` reported as `success:true` — now rejected in both the TS tool AND the ObjectScript `ServiceManage` handler (defense in depth via `'$Data(tProps)`); (3) stale Dev-Notes line in the story contradicting the resolved AC 15.1.8 (Option-A bootstrap regen) — reconciled. Bootstrap regenerated (`BOOTSTRAP_VERSION` fae7cadc22fb → d0cf367c3cfc); governance hash unchanged `1e62c5ad5bf7`. The following items are DEFERRED:

- **[CR 15.1-1 / LOW]** `ServiceList` list-path does not `tRS.Close()` on the in-loop exception path — if `tRS.Get()`/`%Push()` throws inside the `While tRS.Next()` loop, the outer Catch restores the namespace and renders the error but never closes the result set, leaking the handle for that request. This is a PRE-EXISTING pattern shared by every list method in `Security.cls` (`UserList`, `RoleList`, `ResourceList`) and the broader REST handler tree — not introduced by this story. Suggested resolution: a codebase-wide result-set-Close-in-catch hardening pass (wrap list iteration so `Close` runs on all paths), tracked separately so it is fixed uniformly rather than only in `ServiceList`. `src/ExecuteMCPv2/REST/Security.cls:ServiceList`.
- **[CR 15.1-2 / LOW]** `ClientSystems` get→set round-trip format is unverified. `Security.Services.ClientSystems` is a `list Of %String`; `Get()` returns it semicolon-separated (per the IRIS class doc) and `Modify()` is fed a plain string from `settings.clientSystems`. If `Modify` expects a differently-delimited or `$ListBuild` value than `Get` emits, a get→set round-trip could corrupt the allowed-IP list. All current tests mock the REST layer, so this is not exercised against live IRIS. Suggested resolution: a live get→set→get round-trip on a safe, restorable service (or a non-`%Service_` test service) to confirm the `;`-delimited string is symmetric, during the Story 15.6 live-verification/docs rollup. `src/ExecuteMCPv2/REST/Security.cls:ServiceManage` (set branch) + `ServiceList` (get branch).

### Story 15.2 code review (2026-06-16) — new findings

`iris_ldap_manage` (LDAP configurations). **No HIGH or MEDIUM findings; nothing auto-fixed.** The security-critical AC (15.2.4 bind-password redaction) was independently verified at the redaction authority (the ObjectScript server): `LDAPSearchPassword`/`ldapSearchPassword` appears in `Security.cls` only as a comment and as the write-only `Set tProps("LDAPSearchPassword")` in `LdapManage` (line 2170) — it is NEVER read into any output buffer. `BuildLdapEntry` deliberately omits it; the `List` ROWSPEC (`Name, LDAP Enabled, Description, LDAPCACertFile`) has no password column; the create/modify/delete success envelopes carry only `{action,name,success}`; error paths route a `%Status` through `SanitizeError` and the IRIS `LDAPConfigs` %Status does not embed the password. QA's recursive deep-scan tests (`ldap-coverage.test.ts`) pin this contract and would fail if any future change passed a password through. CRUD signatures verified against `irissys/Security/LDAPConfigs.cls` (Rule #2): `Create(Name,ByRef Properties)`, `Modify(Name,ByRef Properties)`, `Delete(Name)`, `Get(Name,ByRef Properties)`, `List` ROWSPEC — all match. `LDAPDomainName` (`[Deprecated, Internal]`) correctly avoided (Rule #4). Namespace save/restore correct on every path incl. catch-first-line and the no-op guard (Rule #3); no `New $NAMESPACE`. `test` is an honest non-mutating config-validity check (no fabricated bind — Rule #16), documented in tool description + `note`/`checkType` response fields. Governance: 6 keys all classified, none in the frozen baseline (hash `1e62c5ad5bf7`, 141 keys — verified green), writes default-disabled / reads (incl. `test`) default-enabled through the real `McpServerBase.handleToolCall` gate with per-action granularity. Bootstrap regenerated (Rule #18 — re-running `gen:bootstrap` is idempotent and reproduces `BOOTSTRAP_VERSION` `d0cf367c3cfc` → `80487cda8d82`, embedded `Security.cls`/`Dispatch.cls` match on-disk). Full monorepo green: build 6/6, type-check 12/12, lint 0, tests (shared 500, admin 300, dev 293, interop 171, data 120, ops 159). The following items are DEFERRED:

- **[CR 15.2-1 / LOW]** `BuildLdapValidity` flags a blank `LDAPHostNames` as a validity issue ("LDAPHostNames is not set"), but `Security.LDAPConfigs.LDAPHostNames` is NOT a `[Required]` property — per the class doc, on a Windows client a blank host means "connect to the default LDAP server for the domain." So a legitimately host-blank Windows AD config would report `valid:false` from the scoped-down `test` action. Low impact: `test` is an explicitly-heuristic, non-authoritative config-validity check (the response `note` says so) and never blocks a real operation; the three genuinely `[Required]` user-facing fields it checks (`LDAPBaseDN`, `LDAPBaseDNForGroups`, `LDAPSearchUsername`) are correct. Suggested resolution: downgrade the blank-host check to an advisory/informational issue (or gate it on the absence of the ActiveDirectory `LDAPFlags` bit) rather than a hard validity failure, in a future LDAP-tool refinement. `src/ExecuteMCPv2/REST/Security.cls:BuildLdapValidity`.
- **[CR 15.2-2 / LOW]** `LdapList` does not `tRS.Close()` on the in-loop exception path (same pre-existing pattern as **CR 15.1-1** and every other list method in `Security.cls`). Not introduced by this story; explicitly folded into the same codebase-wide result-set-Close-in-catch hardening pass tracked under CR 15.1-1. `src/ExecuteMCPv2/REST/Security.cls:LdapList`.
- **[CR 15.2-3 / LOW]** `test`'s config-validity is computed via a separate `GET ?name=&test=1` round-trip that re-`Get`s the config server-side; a caller wanting BOTH the redacted config AND its validity makes two calls. Cosmetic/ergonomic only (each call is correct and cheap); no correctness impact. Defer to a future LDAP-tool refinement if a combined shape is ever requested. `packages/iris-admin-mcp/src/tools/ldap.ts` (get/test branch).

### Story 15.3 code review (2026-06-16) — new findings

`iris_x509_manage` (X.509 certificate credentials) — **the most security-sensitive story so far**. **No HIGH findings.** The security-critical AC (15.3.3 — NO private-key material in output) was independently verified at the exclusion authority: the ObjectScript handler `BuildX509Entry` reads ONLY safe public props (`Alias`, `SubjectDN`, `IssuerDN`, `SerialNumber`, hex `Thumbprint`/`SubjectKeyIdentifier`, `ValidityNotBefore/After`, `PeerNames`, `CAFile`) plus the `HasPrivateKey` presence boolean, and **deliberately never calls `%SYS.X509Credentials.GetProperties`** (confirmed at `irislib/%SYS/X509Credentials.cls:1042–1043` to populate `Properties("PrivateKey")`/`("PrivateKeyPassword")`) and never reads `pCred.PrivateKey` (whose `PrivateKeyGet` override at class line 68 reads from the security global). `PrivateKey`/`PrivateKeyPassword` are `XMLIO="IN"` write-only / `Transient`; `*Export` variants are `XMLIO="OUT", [Internal, Private]` and correctly never set or read. Import is object-create + `Save` (Rule #16 mechanism resolved correctly; native file-based `Import` is XML-export-file-based, not exposed). On `Save()` failure, every `%OnValidateObject`/`%OnBeforeSave` error `%Status` uses fixed message macros and the IRIS audit path redacts `PrivateKey*` (class line 376) — so `SanitizeError` cannot leak a key/password (AC 15.3.7 holds). QA's hostile-payload deep-scan (`x509-coverage.test.ts` lines 235, 326) is genuinely non-vacuous: it feeds a server payload carrying `privateKey`/`PrivateKeyPassword`/`*Export`/nested `privKey` + PEM armor and asserts the scanner **throws** — proving it would catch a real leak. Governance: 4 keys all classified, none in the frozen baseline (hash `1e62c5ad5bf7`, 141 keys — verified green), writes (`import`/`delete`) default-disabled / reads (`list`/`get`) default-enabled through the real `McpServerBase.handleToolCall` gate with per-action granularity (proven both directions). Namespace save/restore correct on every path incl. catch-first-line and no-op guard (Rule #3); no `New $NAMESPACE`. ROWSPEC/signatures verified against the IRIS class (Rule #2): `ListDetails` (`Alias, OwnerList, PeerNames, HasPrivateKey, CAFile`), `Exists(Name,.handle,.status)`, `Delete(alias)`, `BinaryToHexString`. Bootstrap regenerated after the fixes below (Rule #18 — idempotent; `BOOTSTRAP_VERSION` `80487cda8d82` → `3dfe34fbe183` (dev) → `dc6e10143476` (CR hardening); embedded `Security.cls`/`Dispatch.cls` match on-disk). Full monorepo green after fixes: build 6/6 (tsc strict 0), lint 0, tests (shared 500, admin 343, dev 293, interop 171, data 120, ops 159).

**Three MEDIUM/LOW findings auto-fixed inline in `Security.cls` (not deferred), then deployed + compiled clean on HSCUSTOM and live-verified via a disposable probe (`Exists` single-arg returns clean `0` for an absent alias; 3-arg returns `0` with `tCred` unset + `tStatus` error — confirming both guards route correctly), probe then removed:**
1. **[CR 15.3-1 / MEDIUM — data loss]** `import` silently OVERWROTE an existing credential. `Alias` is the `IdKey`, so `%New()`+`Save()` onto an in-use alias clobbered the existing certificate AND its stored private key with a `success:true` response. **Fixed:** added an `Exists(tAlias)` precheck on the import path that rejects an import onto an existing alias ("delete it first to replace it"), making create-vs-overwrite explicit.
2. **[CR 15.3-2 / MEDIUM — correctness]** Get-path `Exists` truthy-`%Status` misread. `%SYS.X509Credentials.Exists` returns a **truthy `%Status`** (not `0`) when the caller lacks `%Admin_Secure:USE`, leaving `tCred` unset — the `If 'tExists` guard misread that as "exists" and would `<INVALID OREF>` on the unset OREF. **Fixed:** guard now requires `'tExists || $$$ISERR(tStatus) || '$IsObject(tCred)` and falls back to a clear not-found status.
3. **[CR 15.3-3 / LOW — resource leak]** `X509List` leaked the `%ResultSet` cursor on a mid-loop exception (`tRS.Close()` ran only on the success path; the catch skipped it). **Fixed:** the catch now closes `tRS` when `$IsObject`. This also resolves the X509List instance of the codebase-wide pattern tracked under **CR 15.1-1 / CR 15.2-2** (the other list methods in `Security.cls` remain folded into that pass).

The following items are DEFERRED — none is a current correctness/security bug:

- **[CR 15.3-4 / LOW]** `import` does not validate that the decoded `certificate` is a real DER X.509 cert before `Save()`. Invalid base64, non-DER bytes, or a still-PEM-armored body decode to garbage; `CertificateSet` runs `X509GetField` on it (returns empty metadata rather than erroring), so a junk credential with empty `SubjectDN`/`Thumbprint`/etc. could persist (or `Save()` fails with an opaque encryption error). Low impact: the operator is the trust boundary for a security-admin tool, and the overwrite guard (CR 15.3-1 fix) prevents junk from clobbering a good credential. Suggested resolution: pre-validate via `$System.Encryption.X509GetField(cert,"SerialNumber")'=""` (or a thumbprint check) before `Save`, in a future X.509-tool refinement. `src/ExecuteMCPv2/REST/Security.cls:X509Manage` (import branch).
- **[CR 15.3-5 / LOW]** Raw IRIS timestamps in `notBefore`/`notAfter` are not ISO-8601-converted (Rule #11). `BuildX509Entry` emits `ValidityNotBefore/After` (`%TimeStamp` form) verbatim and the TS tool forwards the envelope unchanged — no `*Raw` companion / ISO conversion as Rule #11 prescribes for `$HOROLOG`. Lower urgency than the canonical Rule #11 case because `%TimeStamp` is already a readable `YYYY-MM-DD HH:MM:SS` string (not opaque horolog), but still not normalized ISO-8601. Fold into a future X.509/admin-tool timestamp-normalization pass. `src/ExecuteMCPv2/REST/Security.cls:BuildX509Entry` + `packages/iris-admin-mcp/src/tools/x509.ts`.
- **[CR 15.3-6 / LOW]** The `namespace` schema field is declared but never forwarded on the wire (not destructured in the handler; the ObjectScript hard-switches to `%SYS`). This is a **pre-existing suite-wide pattern**, not a Story 15.3 regression — sibling `%SYS`-scoped admin tools `ldap.ts` (line 131), `service.ts` (line 74), `mapping.ts`, `user.ts`, `webapp.ts` all declare a vestigial `namespace` field with the same non-forwarding. Fixing it here would diverge from the established sibling pattern; a cross-tool cleanup (wire it through OR drop it from all six schemas) belongs in a future admin-tool consistency pass, not this leaf story. `packages/iris-admin-mcp/src/tools/x509.ts` (+ 5 sibling admin tools).

### Story 15.4 code review (2026-06-16) — new findings

`iris_audit_manage` (audit configuration & audit log — the broadest Epic-15 tool, 7 actions incl. destructive `purge`). **One HIGH auto-fixed inline (not deferred); one MEDIUM/LOW hardening auto-fixed; the rest dismissed or deferred.** All 7 IRIS APIs verified against source (Rule #2): `Security.System.Get/Modify(AuditEnabled BooleanYN)` (`irissys/Security/System.cls:29,541,649`), `Security.Events.Modify` + `Security.Events:List` ROWSPEC `Source,Type,Name,Description,Enabled,Total,Written,Lost` (`irissys/Security/Events.cls:205,288`), `%SYS.Audit.Delete(.NumDeleted,Begin,End,EventSources,EventTypes,Events,Usernames,SystemIDs)` and `Export(FileName,.NumExported,Flags,Begin,End,...)` and `%SYS.Audit:List` Execute params (`irislib/%SYS/Audit.cls:524,742,1367`) — all match the handler positionally. The dev's probe correction (enable/disable via `Security.System.AuditEnabled`, NOT Internal `%SYS.Audit.Stop`; purge via bounded `Delete`, NOT unbounded `Erase`) is verified correct. Governance: 7 keys all classified (`status`/`view` read; `enable`/`disable`/`configureEvent`/`purge`/`export` write), none in the frozen baseline (hash `1e62c5ad5bf7`, 141 keys — verified green), writes default-disabled / reads default-enabled through the real `McpServerBase.handleToolCall` gate with per-action granularity (`audit-governance.test.ts`). No `server` field (D2). Namespace save/restore correct on every path incl. catch-first-line (Rule #3); no `New $NAMESPACE`. `SanitizeError` on all error paths (Rule #9). Back-compat: existing `iris_audit_events` (ops) untouched — ops 159 pass. Bootstrap regenerated after the fix (Rule #18 — `e353c54c5547` → `8b074e457c3c`). Full monorepo green after fix: admin 401, shared 500, ops 159; build / lint / tsc all exit 0.

**HIGH auto-fixed inline (not deferred), then deployed + compiled clean on HSCUSTOM, bootstrap regenerated:**
1. **[CR 15.4-1 / HIGH — full unbounded audit-log wipe (AC 15.4.5)]** The purge bounded-scope gate counted any NON-EMPTY filter as a bound, so the match-all wildcard `"*"` satisfied it. A direct REST caller (the handler explicitly defends against TS-layer bypass) — or the TS layer itself — passing `{action:"purge", confirm:true, source:"*"}` (with empty begin/end and the other filters defaulting to `"*"`) produced `%SYS.Audit.Delete(.n,"","","*","*","*","*")` = delete EVERY audit record. Confirmed against `irislib/%SYS/Audit.cls:524–606`: empty Begin/End default to earliest/latest record and `"*"` filters match all, so the `List` cursor walks the entire log. This is exactly the silent unbounded wipe AC 15.4.5 requires to be impossible. **Fixed in BOTH layers (defense in depth):** `Security.cls:AuditManage` purge gate and `audit.ts` `hasBound` now treat `"*"` as NOT a bound (non-empty begin/end still count as real date bounds). Regression test added (`audit-coverage.test.ts` — wildcard-only purge on each of user/event/source/type is rejected and never reaches HTTP).

**MEDIUM/LOW hardening auto-fixed inline (not deferred):**
2. **[CR 15.4-2 / MEDIUM→LOW — export path containment]** Export confinement rested solely on the character blacklist (`/ \ .. :`) plus `%File.NormalizeFilename`, which (per `irislib/%Library/File.cls`) does NOT clamp its result to the supplied directory — if the blacklist were ever weakened, an escape would be possible. **Fixed:** added a post-`NormalizeFilename` containment re-check in `Security.cls:AuditManage` export branch — the resolved `tFullPath` must still begin with the fixed `auditexport` directory or the request is rejected.

The following items are DEFERRED or DISMISSED — none is a current correctness/security bug:

- **[CR 15.4-3 / LOW — DISMISSED]** `export` has no bounded-scope requirement (unlike purge): `{action:"export", fileName:"x.xml"}` exports the entire audit log to a server-side file. This is intentional — AC 15.4.5 scopes export safety to PATH control (satisfied), not scope-limiting; a full compliance export is a legitimate operation, the write is non-destructive to the log, and the file lands only inside the fixed `auditexport` directory. No action.
- **[CR 15.4-4 / LOW]** `export` overwrites an existing same-named file in the `auditexport` dir with no uniqueness/overwrite guard (`%SYS.Audit.Export` → `%XML.Writer.OutputToFile` truncates). Confined to the controlled directory (no traversal); a caller reusing `fileName` clobbers its own prior export. Suggested resolution: optional timestamp-suffix or an explicit `overwrite` flag, in a future audit-tool refinement. `src/ExecuteMCPv2/REST/Security.cls:AuditManage` (export branch).
- **[CR 15.4-5 / LOW]** `export` fileName accepts Windows reserved device names (`CON`, `NUL`, `LPT1`, etc.) and trailing dot/space — they pass the separator/`..`/`:` blacklist and the new containment check (no separators → stays in-dir) but can resolve to an OS device or a normalized-away name, producing a confusing failure rather than a clean rejection. Proper path traversal IS blocked. Low impact (the operator is the trust boundary for a security-admin tool). Suggested resolution: a reserved-name + trailing-dot/space reject in a future X.509/audit fileName-hardening pass. `src/ExecuteMCPv2/REST/Security.cls:AuditManage` (export branch).
- **[CR 15.4-6 / LOW]** `view` paginates client-side via `ctx.paginate` over a server result already capped at `maxRows` (default 100, max 1000), and the response `count` is the page length, not the total matched — a caller cannot distinguish "truncated by the server cap" from "end of results", and the `cursor` never reaches IRIS so paging beyond `maxRows` total is impossible. This is the SAME pattern as the sibling admin list tools (not a 15.4 regression); AC 15.4.4 is met. Fold into a future pagination-semantics pass across the admin tools. `packages/iris-admin-mcp/src/tools/audit.ts` (view branch).
- **[CR 15.4-7 / LOW]** `enable`/`disable` echo the requested `auditEnabled` value derived from the action name rather than re-reading `Security.System.Get` after `Modify` — if IRIS ever coerced the value while returning `$$$OK`, the reported state could be inaccurate. Very low likelihood (BooleanYN 1/0 is not coerced). Suggested resolution: re-read AuditEnabled into the response in a future refinement. `src/ExecuteMCPv2/REST/Security.cls:AuditManage` (enable/disable branch).
- **[CR 15.4-8 / LOW]** `AuditStatus` (status branch) reuses the `tRS` variable for two sequential result sets; a mid-iteration exception closes only the most-recent `tRS` in the catch. Same pre-existing list-Close-in-catch pattern tracked under **CR 15.1-1 / 15.2-2 / 15.3-3**; folded into that codebase-wide hardening pass. `src/ExecuteMCPv2/REST/Security.cls:AuditStatus`.

### Story 15.6 code review (2026-06-16) — Epic 15 closer (docs-only rollup + bootstrap verification)

Docs-only story (mirror of Story 14.6). All load-bearing numbers re-derived independently against the code: per-package tool counts confirmed from each `index.test.ts` (`toHaveLength`) AND by counting the `tools[]` arrays — dev **24**, admin **26**, interop **19**, ops **17**, data **7** → suite **93**; `tool_support.md` rollup columns re-added (Atelier 18 · ExecuteMCPv2 70 · Other 5 = 93) and derived prose verified (70/93 = 75.3% → "75%"). The 4 new tools' action enums + read/write governance classifications checked against the actual Zod `action` enums and `mutates:` maps in `service.ts`/`ldap.ts`/`x509.ts`/`audit.ts`/`resource.ts` (all match CHANGELOG + README). Invariants confirmed: `BOOTSTRAP_VERSION e5f4f6d88c56` and `GOVERNANCE_BASELINE_HASH 1e62c5ad5bf7` / 141 keys unchanged (git diff on both generated files empty; the new Epic-15 keys correctly ABSENT from the frozen baseline, governed via `mutates`+`defaultSeed`); no code/ObjectScript/generated files changed; `gen:governance-baseline` NOT run. Full monorepo green (build/test/lint exit 0; admin 439 tests, FULL TURBO).

**Two HIGH/MEDIUM stale-count findings auto-fixed inline in the review (docs-only; counts re-verified consistent after fix):**
1. **[CR 15.6-1 / HIGH — wrong count ships to users]** `docs/migration-v1-v2.md:94` — the per-package comparison table still listed `@iris-mcp/admin … | 22`, contradicting the same file's "93 tools total" (L11, L29) and summing to 89, not 93. The story's File List claimed migration-v1-v2.md was rolled "89→93 (both occurrences)" but missed this third occurrence (the admin row in the secondary CaretDev-comparison table — outside the two changed hunks, so the diff-only layer could not see it). **Fixed:** admin → **26** + domain description aligned with the other surfaces (added SQL privileges / services / LDAP / X.509 / auditing). Table now sums 24+26+19+17+7 = 93.
2. **[CR 15.6-2 / MEDIUM — internal inconsistency in a file this story already touched]** `_bmad-output/planning-artifacts/architecture.md:319` — the pagination note "max is 22 tools" (the per-server maximum) was made stale by Epic 15 (admin is now the largest server at 26). Since the story explicitly opened architecture.md to reconcile its stale counts (AC 15.6.5 internal consistency), this same-class defect was fixed inline → "max is **26** tools".

The following items are DEFERRED or DISMISSED — none is a current correctness/security bug:

- **[CR 15.6-3 / LOW — DEFERRED, out of scope]** `docs/tool-annotation-audit.md` is a dated point-in-time snapshot ("Date: 2026-04-07", "Scope: All 85 tools", `iris-admin-mcp (22 tools)`, dotted v1 names `iris.doc.get`). It is NOT in Story 15.6's changed-files set and is a historical audit artifact, not a release rollup. Rolling its counts forward would falsify a dated record. Left as-is; if desired, stamp it as a point-in-time snapshot in a future docs pass. `docs/tool-annotation-audit.md`.
- **[CR 15.6-4 / LOW — DISMISSED]** `_bmad-output/planning-artifacts/architecture.md:35` FR-distribution prose ("iris-admin-mcp (FR40-FR62, 23 FRs)") was not updated. This is an **FR** count, not a tool count — the 109-FR planning framework is a fixed pre-Epic-15 baseline ("109/109 FRs covered"); Epic 15 added tools under the existing FR40-FR62 admin range without expanding the FR count, and FR↔tool is not 1:1. Out of scope for a tool-count rollup (AC 15.6.3 named only the architecture.md tool-count fixes). No action.
- **Planning artifacts (`prd.md`, `prd-validation-report.md`, `epics.md`, `product-brief*`, etc.)** still carry pre-Epic-15 suite/admin counts ("86 tools" / "admin 22"). These are planning artifacts, not release-facing published docs, and are correctly out of the docs-rollup scope. Noted; no action.

## Deferred from: code review of story 16.0 (2026-06-16)

Story 16.0 added the `--check` (no-write drift) / `--force` (gated write) modes + footgun guard to `scripts/gen-governance-baseline.mjs` (Epic 15 retro AI#3 / Rule #25). One HIGH-value robustness gap was auto-fixed inline during this review (see story Review Findings — `--check` now refuses a false "OK" if the committed `GOVERNANCE_BASELINE` import yields a non-iterable/empty export). The frozen baseline stayed git-clean (141 keys / `1e62c5ad5bf7`); the new test (4) and the authoritative `governance.test.ts` drift guard (41) both pass.

The following items are DEFERRED — none is a current correctness/security bug; all are tracked for a future generator/drift-test de-duplication or hardening pass:

- **[CR 16.0-1 / LOW — DEFERRED, known/out of scope (CR 15.0-5)]** Lock-step divergence between the generator's robust `unwrapActionOptions` (peels `ZodOptional`/`ZodDefault`/`ZodNullable`) on the `--check` path and `governance.test.ts:555`'s bare `tool.inputSchema?.shape?.action?.options` read. On today's all-bare action surface they AGREE (verified: `--check` and the drift test both green against the same surface). They would only diverge if a future tool wraps its `action` enum (`.optional()`/`.default()`), at which point `--check` would emit per-action keys while the test collapses to the bare tool key. This is the pre-existing CR 15.0-5 gap; Story 16.0 Dev Notes explicitly scoped closing it OUT and instructed not to regress the generator side (it did not). Suggested resolution: de-duplicate the two derivations into one shared helper (e.g. export the generator's `unwrapActionOptions` / `deriveLiveKeys` and have `governance.test.ts` import it) so the CLI `--check` and the in-suite drift guard can never disagree. `scripts/gen-governance-baseline.mjs:57` (unwrap) vs `packages/shared/src/__tests__/governance.test.ts:555` (bare read).
- **[CR 16.0-2 / LOW — DEFERRED, blocked by additive constraint]** The `--check` failure branch (a vanished frozen key → exit 1 + `VANISHED_HINT` regression guidance) has no direct CLI-level automated coverage. The new test (`governance-baseline-check.test.ts`) covers only the success + refusal paths and explicitly documents (lines 130-137) that reproducing the vanished-key scenario would require mutating the real built server dists OR stubbing the frozen `governance-baseline.ts` — both forbidden by Story 16.0's strictly-additive constraint (AC 16.0.7). The same one-directional logic IS exercised by the mirrored `governance.test.ts` drift guard, but against an in-process re-implementation (`deriveBaselineFromDists`), not the real CLI exit-code path. Suggested resolution: when the generator's derivation is extracted into an importable helper (CR 16.0-1), add a unit test that feeds it a synthetic live-key set missing a committed key and asserts the exit-1 / hint behavior — no dist/frozen-file perturbation needed. `packages/shared/src/__tests__/governance-baseline-check.test.ts:130-137`; `scripts/gen-governance-baseline.mjs:269-281`.
- **[CR 16.0-3 / LOW — DISMISSED]** `--check --force` passed together silently runs `--check` only (the safe verify path wins; `--force` write is suppressed) with no conflict diagnostic; and unknown/typo flags (`--chek`, case-sensitive `--Force`) fall through to the refusal path (benign, non-zero exit with guidance). The safe path always wins and the refusal is non-destructive, so this is correct-by-design for a build-time generator; adding flag-conflict / unknown-flag validation is gold-plating. No action. `scripts/gen-governance-baseline.mjs:56-58`.

## Deferred from: code review of story 16.1 (2026-06-16)

Story 16.1 added `iris_process_manage` (`get`/`terminate`/`suspend`/`resume`) on `@iris-mcp/ops` plus the `ProcessGet`/`ProcessManage` ObjectScript handlers on `ExecuteMCPv2.REST.Monitor`. Three MED handler-correctness findings were auto-fixed inline during this review (see story Review Findings: exact-match action validation replacing the substring `[`-contains test that let comma-injected actions fall through the dispatch `Else` to `Resume()`; fail-CLOSED guard when `%SYS.ProcessQuery.%OpenId` fails in the safety check; one-time numeric `pid` coercion so the string-keyed `%ExistsId`/`%OpenId` and the numeric `$JOB` self-guard can never disagree). All three were verified live over HTTP on HSCUSTOM (non-numeric/negative pid rejected; `"suspend,resume"` rejected as invalid action; protected daemon 3736 terminate REFUSED and stayed RUNW). Bootstrap regenerated `3a395abc1eba` → `d4e197ef5ffc`; frozen `governance-baseline.ts` stayed git-clean (`1e62c5ad5bf7`, 141 keys); `--check` exit 0; ops 186 + shared bootstrap 47 green.

The following items are DEFERRED — none is a current correctness/security bug:

- **[CR 16.1-1 / LOW — DEFERRED, documented intentional]** The TS tool declares an optional `namespace` param and forwards it on the wire (`&namespace=` for `get`, `body.namespace` for control), but `ProcessGet`/`ProcessManage` never read it — both hard-switch to `%SYS` unconditionally, so the value is silently discarded. This is a deliberate design choice (process inspection/control is %SYS-scoped against the system-wide process table; the tool description states the param is "usually omitted") and AC 16.1.3 lists `namespace` as an optional input without mandating a behavioral effect, so it is AC-compliant. The residual gap is only that an accepted-but-ignored param is mildly misleading. Suggested resolution: either drop `namespace` from the schema, or tighten the description to state explicitly that the value has no effect (rather than "usually omitted"). `packages/iris-ops-mcp/src/tools/process.ts:61-67,101-103,160`; `src/ExecuteMCPv2/REST/Monitor.cls` ProcessGet/ProcessManage.
- **[CR 16.1-2 / LOW — DEFERRED, inherent to the domain]** TOCTOU window: `ProcessManage` queries live process state three separate times (`%SYS.ProcessQuery.%ExistsId`, then `%SYS.ProcessQuery.%OpenId` for the guard flags, then `SYS.Process.%OpenId` for control); a target can exit or transition between steps. The post-fix code now fails CLOSED if the guard-flag open fails, and a vanished process at the final `SYS.Process.%OpenId` yields a clean "Unable to open process … for control" error — so the worst case is a benign refusal/error, never an unguarded control action. True atomicity is not available in the `SYS.Process` API. No action; accepted as the standard pattern for live process control. `src/ExecuteMCPv2/REST/Monitor.cls` ProcessManage.
- **[CR 16.1-3 / LOW — DEFERRED, perf only]** `ProcessGet` reads `MemoryUsed` (and could read other mailbox-requiring columns) via `%SYS.ProcessQuery.%OpenId`, which per the IRIS class doc sends a mailbox message to the target process and "may take several seconds" for some columns; the sibling `JobsList` avoids this by SELECTing only no-mailbox columns over SQL. This affects single-PID `get` latency only, not correctness. Suggested resolution: if `get` latency is ever reported as an issue, drop `memoryUsedKB` (or move the detail read to an SQL projection of the no-mailbox columns). No action now. `src/ExecuteMCPv2/REST/Monitor.cls` ProcessGet.
- **[CR 16.1-4 / LOW — DISMISSED]** Self-PID guard protects `$JOB` (the worker serving the request) rather than the external MCP-client process or sibling web-app pool workers. `$JOB` is the only PID the handler can meaningfully self-protect and matches AC 16.1.4's intent ("the calling process"); protecting an arbitrary unrelated OS process is out of the tool's scope. No action.

## Deferred from: code review of story 16.2 (2026-06-16)

Story 16.2 added `iris_database_action` (`mount`/`dismount`/`compact`/`defragment`/`truncate`/`expandVolume`) on `@iris-mcp/ops` plus the `DatabaseAction` ObjectScript handler on `ExecuteMCPv2.REST.Monitor` (+ `POST /monitor/database/action` route). Three-layer review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) found all 9 ACs PASS and NO HIGH/MED code defects. Verified: six `SYS.Database` signatures match `irissys/SYS/Database.cls` exactly (arg count/order, ByRef `.MbProcessed`/`.MbCompressed`/`.ReturnSize`); `%ExistsId` directory guard → clean SanitizeError envelope with Catch backstop (no opaque `<...>` crash); namespace save/restore with catch-first-line restore, no `New $NAMESPACE`; all 6 actions `mutates:"write"`; `iris_database_action:*` keys absent from frozen `governance-baseline.ts` (`1e62c5ad5bf7`, git-clean, `--check` exit 0); bootstrap `d4e197ef5ffc` → `f8b3a9e9704c` (`bootstrap.test.ts` green, not hand-edited); strictly additive (`iris_database_check` + admin `iris_database_manage` unchanged, ops 18→19); no Config/SYS duplication (Rule #3); governance test genuinely drives `McpServerBase.handleToolCall`. One MED documentation discrepancy (story-doc test counts 17/2 vs actual 23/3) was fixed inline during review. ops 212 + shared bootstrap 47 green.

The following items are DEFERRED — none is a current correctness/security bug:

- **[CR 16.2-1 / LOW — DEFERRED, documented intentional (mirrors CR 16.1-1)]** The TS tool declares an optional `namespace` param and forwards it in the body (`body.namespace`, with a passthrough test), but `DatabaseAction` never reads it — the handler hard-switches to `%SYS` unconditionally, so the value is silently discarded. Database operations are genuinely %SYS-scoped: `SYS.Database` classmethods key on the directory path and run in `%SYS` regardless of the caller's namespace, so honoring a caller namespace would be meaningless. AC 16.2.3 lists `namespace` as an optional input without mandating a behavioral effect, and the description says "usually omitted" → AC-compliant. Identical pattern/resolution to CR 16.1-1. Suggested resolution: fold into the same future pass — either drop `namespace` from the schema across the ops %SYS-scoped tools, or tighten the descriptions to state the value has no effect. `packages/iris-ops-mcp/src/tools/database.ts:108-113,206`; `src/ExecuteMCPv2/REST/Monitor.cls` DatabaseAction.
- **[CR 16.2-2 / LOW — DEFERRED, clean-envelope path covers it]** No range validation on the numeric params: Zod uses bare `z.number()` (no `.min()`/`.max()`) for `percentFull`/`targetSize`/`initialSize`, and the handler coerces with bare `+tBody.%Get(...)`. So `percentFull:-50`/`150`, negative `targetSize`, or `initialSize:0` flow straight to the `SYS.Database` call. NOT a crash/corruption risk: each API returns a `%Status` that is propagated via `SanitizeError`, with the `Catch` block as backstop for any `<...>` runtime error → the caller always gets a clean error envelope (the HIGH-if-violated guard invariant is satisfied). Edge Case Hunter rated these HIGH assuming an unguarded crash; downgraded after confirming the clean-envelope path. Suggested resolution: add `.min()/.max()` (e.g. percentFull 1-100) and a non-negative guard in a future input-hardening pass for friendlier pre-flight rejection. `packages/iris-ops-mcp/src/tools/database.ts:90-107`; `src/ExecuteMCPv2/REST/Monitor.cls` DatabaseAction.
- **[CR 16.2-3 / LOW — DEFERRED, cosmetic]** The compact/truncate ByRef outputs (`tMbProcessed`/`tMbCompressed`/`tReturnSize`) are pre-initialized to 0 before the call, so a failure that leaves them untouched is indistinguishable from a genuine zero-result. In practice this is invisible to the caller: when `tActionSC` is non-OK the handler renders the SanitizeError envelope and the `success` result object (carrying the zeroed counters) is never rendered — the success path does not run on failure. Cosmetic only. No action. `src/ExecuteMCPv2/REST/Monitor.cls` DatabaseAction.

## Deferred from: Story 16.3 (`iris_backup_manage`) — restore action (2026-06-16)

Story 16.3 added `iris_backup_manage` (`run`/`freeze`/`thaw`/`listHistory`) on `@iris-mcp/ops` plus the `BackupManage` ObjectScript handler on `ExecuteMCPv2.REST.Monitor` (+ `POST /monitor/backup/manage` route). The following is DEFERRED by design (Rule #16, AC 16.3.3) — it is NOT a bug:

- **[Story 16.3 / restore — DEFERRED, verify-first (Rule #16)]** A `restore` action is intentionally NOT implemented. Pre-dev research against `irissys/Backup/General.cls` (doc comments L47–L65) confirmed IRIS restore is **interactive** (`^DBREST` database-restore utility / `CLUMENU^JRNRESTO` journal-restore routine) and has **no scriptable `RestoreDatabase()`-style classmethod** on `Backup.General` — the class explicitly directs callers to "restore the database files using your external restore mechanism." Shipping a half-working restore would be worse than none. **Decision:** restore is absent from the tool's action enum, AND the `BackupManage` handler defends against an `action="restore"` request with a clear "Restore is not supported via this tool — use the IRIS restore utility / Management Portal restore" message (no crash). The tool description states the same. **Suggested follow-up:** if scriptable restore is ever required, a dedicated handler would need to wrap the interactive utilities (driving `^DBREST` / `CLUMENU^JRNRESTO` non-interactively is non-trivial and risky), or expose the operator-facing Management Portal restore as the supported path. `packages/iris-ops-mcp/src/tools/backup.ts`; `src/ExecuteMCPv2/REST/Monitor.cls` BackupManage.

- **[Story 16.3 / run task-name reality — DOCUMENTED, Rule #16 live-probe outcome]** AC 16.3.4 originally specified `backupType` (full/incremental/cumulative) as the `run` selector. Live-probe (2026-06-16) found there are NO predefined/shipped backup task names on a stock IRIS instance (`^SYS("BUTASKS")` undefined; `%SYS.Task` has no backup-type rows). `Backup.General.StartTask` runs a USER-DEFINED task BY NAME, and the type is a property of that task definition — it cannot be auto-mapped to a guessed shipped name. The story spec + AC 16.3.4 were amended in place (Rule #5): `run` takes a required `taskName`; `backupType` is kept only as an optional informational field. The handler/tool reflect the amendment. Not a deferred bug — recorded for retrospective traceability of the spec-vs-reality correction.

- **[CR 16.3 / LOW — DEFERRED, mirrors CR 16.1-1 / CR 16.2-1]** As with the sibling %SYS-scoped ops tools, `iris_backup_manage` declares an optional `namespace` param and forwards it (`body.namespace`), but `BackupManage` hard-switches to `%SYS` and never reads it (backup operations are instance-wide / %SYS-scoped). AC-compliant (optional input, no mandated behavioral effect; description says "usually omitted"). Fold into the same future pass that resolves CR 16.1-1 / CR 16.2-1. `packages/iris-ops-mcp/src/tools/backup.ts`; `src/ExecuteMCPv2/REST/Monitor.cls` BackupManage.

- **[CR 16.3 / HIGH — RESOLVED in code review, 2026-06-16]** `BackupManage` listHistory decoded every `^SYS("BUHISTORY")` index node's timestamp via `$ZDateTime((tKey\1000000)_","_(tKey#1000000), 3)` with no per-entry guard. The decode is correct for well-formed `hindex` values (verified `days*1000000+seconds` against `irissys/Backup/General.cls:366-367` + `irissys/DBACKB.int:38`), but a single corrupt / foreign-written / out-of-$HOROLOG-range subscript (e.g. seconds ≥ 86400, or a non-canonical-numeric key passing the old `+tKey>0` skip) would throw `<ILLEGAL VALUE>` INSIDE the `While` loop and propagate to the outer `Catch`, failing the ENTIRE read instead of degrading one row. **Fix applied:** (a) the index-node filter is now `(tKey = +tKey) && (tKey = (tKey\1)) && (+tKey > 0)` — accepts only canonical positive integers, skipping the `0` metadata node AND any non-numeric subscript; (b) the `$ZDateTime` render is wrapped in a per-entry `Try/Catch` that degrades a bad node to `timestamp:""` rather than aborting. Redeployed + recompiled clean on HSCUSTOM; `gen:bootstrap` re-run (`04984d638f8d` → `fe972c4cb317`); `bootstrap.test.ts` green; frozen baseline git-clean. `src/ExecuteMCPv2/REST/Monitor.cls` BackupManage (listHistory branch).

- **[CR 16.3 / MED — DEFERRED, "silently-ignored input"]** The `run` path reads `tDevice = tBody.%Get("device")` (and the TS tool forwards `body.device`), but `BackupManage` calls `Backup.General.StartTask(tTaskName, tJobBackup, 1)` WITHOUT the device argument — so a caller-supplied `device` override is silently dropped (the request still succeeds). This is the same class as the deferred `namespace`/`backupType`-informational pattern: the scriptable `StartTask` path runs a USER-DEFINED task BY NAME, and the backup destination is a property of that task definition, so a per-call device override is not part of the verified API path. AC-compliant (optional input). Suggested follow-up: either wire `device` into `StartTask`'s 4th positional `Device` arg (verify-first whether it overrides the task's configured destination), or drop `device` from the tool schema + body to avoid the false impression of control. Fold into the same ops-tool cleanup pass as the `namespace` deferral. `packages/iris-ops-mcp/src/tools/backup.ts`; `src/ExecuteMCPv2/REST/Monitor.cls` BackupManage.

- **[CR 16.3 / LOW — DEFERRED, theoretical, needs live confirmation]** `thaw` forwards a caller-supplied `password` to `Backup.General.ExternalThaw(tLogFile, tUsername, tPassword)`, and any non-OK `%Status` flows straight through `SanitizeError` to the client (Rule #9 passthrough). IF `ExternalThaw` ever embeds the submitted password in its auth-mismatch error text, that value would be echoed back. No evidence it does (the method body is kernel-generated; the doc only says the password is "checked against" the freeze password), so this is theoretical — and matches the narrow password-redaction concern that Epic 11's retro self-audit explicitly declined to codify as a rule (not a general pattern). Patching redaction blind risks corrupting legitimate diagnostic text (Rule #9 minimum-length-gate caution). Suggested follow-up: during a maintenance-window freeze/thaw round-trip smoke, capture the actual `ExternalThaw` mismatch error text; add field-specific password redaction ONLY if the password is observed in it. `src/ExecuteMCPv2/REST/Monitor.cls` BackupManage (thaw branch).

## Deferred from: code review of story 17.1 (2026-06-16)

Story 17.1 added `iris_default_settings_manage` (`list`/`get`/`set`/`delete`) on `@iris-mcp/interop` plus the `DefaultSettingsList`/`DefaultSettingsManage` ObjectScript handlers on `ExecuteMCPv2.REST.Interop` (+ GET/POST `/interop/defaultsettings` routes). Three-layer review (Blind Hunter, Edge Case Hunter, Acceptance Auditor): all four in-scope ACs (17.1.1–17.1.4) PASS; the flagged `delete` → `"deleted"` output naming is correct; the governance test genuinely drives `McpServerBase.handleToolCall` with the real schema + `mutates` (writes default-disabled, reads enabled, opt-in flips per-action). TWO patches applied inline (see story Review Findings): (1) `||` IdKey-delimiter injection guard in `DefaultSettingsManage`; (2) clarified `list` filter `"*"` semantics in the tool description. Redeploy + `gen:bootstrap` re-roll `56f492db456d` → `8c748712e247` (Rule #24, bootstrap.test.ts green); frozen governance baseline `1e62c5ad5bf7` git-clean, `--check` exit 0 (Rule #23/#25); strictly additive (`iris_production_item`/`ItemManage` byte-for-byte unchanged, interop 19→20).

The following items are DEFERRED — none is a current correctness/security bug:

- **[CR 17.1-1 / MED — DEFERRED, needs live-HTTP test]** The `||` IdKey-delimiter injection guard added to `DefaultSettingsManage` (Interop.cls ~1951) has no automated test. The TS unit tests use mocked HTTP, so a TS-layer test would only assert the slot value is forwarded in the body — it cannot exercise the server-side guard. Genuine coverage requires the lead's live-HTTP smoke (Rule #26): send a get/set/delete with a slot containing `||` and assert the server REJECTS it ("Key slot values may not contain the '||' delimiter") changing nothing. Suggested resolution: add that rejection assertion to the Story 17.1 / Epic 17 live smoke. `src/ExecuteMCPv2/REST/Interop.cls` DefaultSettingsManage.
- **[CR 17.1-2 / MED — DISMISSED-AS-INTENTIONAL, recorded]** An explicit empty-string key slot (`production:""` etc.) is coerced to the wildcard `"*"` row (`Set:tProduction="" tProduction="*"`), identical to omitting the slot (the class InitialExpression is `"*"`). A caller who accidentally sends `""` writes/deletes the broad `*` catch-all row rather than erroring. Low practical risk: the TS schema is `z.string()` and the documented contract is "omit for the `*` default," so this is a deliberate-caller path. Suggested follow-up (optional): if stricter input hygiene is ever wanted, reject explicit `""` slots distinctly from omitted ones. `src/ExecuteMCPv2/REST/Interop.cls` DefaultSettingsManage:1943-1950.
- **[CR 17.1-3 / LOW — DISMISSED, matches existing handlers]** `deployable` is coerced at the REST layer via `+tBody.%Get("deployable")`, so a DIRECT REST caller (bypassing the Zod schema) sending the STRING `"true"` would store `Deployable=0` (opposite of intent). MCP callers are safe — the TS schema enforces `z.boolean()`. This mirrors the established `+`-coercion in `ItemManage`/`AutoStart` in the same class. Direct-REST input hardening is a cross-handler concern, not this story. `src/ExecuteMCPv2/REST/Interop.cls` DefaultSettingsManage:2023.

## Deferred from: code review of story 17.2 (2026-06-16)

Story 17.2 extended the EXISTING `iris_production_item` tool (`@iris-mcp/interop`) + `ItemManage` (`ExecuteMCPv2.REST.Interop`) with `add`/`remove` actions (`mutates:{add,remove:"write"}`, default-disabled) and arbitrary host/adapter settings routed to `Ens.Config.Setting` via a new `ApplyArbitrarySetting` helper. Three-layer review (Blind Hunter, Edge Case Hunter, Acceptance Auditor against `17-0-api-probes.md` Area 2): **back-compat gate (Rule #19) PASS** — the 5 working property keys + the 4 existing actions' output shapes are byte-for-byte unchanged (the only existing-`set` delta is `adapterClassName` THROW→Setting, the AC-17.2.4-sanctioned additive error→success); full-object `toEqual` snapshots on request bodies AND `structuredContent` are non-vacuous. Bootstrap `8c748712e247`→`39dc932907cb` (bootstrap.test green); governance baseline frozen `1e62c5ad5bf7` git-clean, `--check` exit 0 (141 frozen/186 live/45 post-foundation); governance test genuinely drives `handleToolCall` (add/remove denied-by-default, enable/disable/get/set allowed, opt-in flip); interop suite 219. IRIS API verified against `irislib/Ens/Config/{Production,Item}.cls` (`LoadFromClass`/`SaveToClass`/`RemoveItem`/`FindItemByConfigName`/`FindSettingByName`); Rules #7/#9/#15/namespace all honored. No HIGH/MED blocking findings.

The following items are DEFERRED — all on the NEW, default-DISABLED, write-gated surface or pre-existing accepted IRIS limitations; none affects the back-compat gate or is a current correctness/security bug:

- **[CR 17.2-1 / LOW — DEFERRED]** `ApplyArbitrarySetting` returns a `%Status` that both call sites discard (`Do ..ApplyArbitrarySetting(...)`, Interop.cls:463,528); a failed setting application is swallowed yet the key is still pushed to `updatedSettings`. LOW impact — the helper only does in-memory `%New`/property-set/`Insert` (no save), so real persistence failures still surface at the checked `%Save()`/`SaveToClass`. Mirrors Story 5-3 "set save/update consistency" (closed won't-fix). Suggested resolution: capture + propagate the status before `%Push` (requires redeploy + bootstrap re-roll). `src/ExecuteMCPv2/REST/Interop.cls` ApplyArbitrarySetting call sites.
- **[CR 17.2-2 / MED — DEFERRED, lead-smoke-must-cover]** `@Host`/`@Adapter` suffix parser edge cases (Interop.cls:606-615): `$Find` matches the FIRST `@` (`A@B@Host` mis-routes to name `A@B@Host`/Adapter); leading `@` (`@Host`) yields an empty setting Name; lowercase/typo/unknown suffix (`@host`,`@Foo`) silently folds into the name with default Adapter target — all silently reported as successful `updatedSettings`. NEW code; no ObjectScript-layer test coverage (TS tests mock HTTP). Suggested resolution: scan the LAST `@`, validate a non-empty name, and reject/log an unknown suffix; add the rejection assertions to the Epic 17 live-HTTP smoke. `src/ExecuteMCPv2/REST/Interop.cls` ApplyArbitrarySetting.
- **[CR 17.2-3 / MED — DEFERRED, lead-smoke-must-cover]** `add` does not validate that host `className` exists/compiles or that the item Name is unique in the production (Interop.cls:489-533). A non-existent/non-host `className` writes a broken item (`SaveToClass` swallows `OnConfigChange <METHOD DOES NOT EXIST>` per `Production.cls:148-157`); the non-unique `(Production,Name)` index lets a duplicate-named item persist (add not idempotent). Out-of-scope for AC 17.2.3 (pins the verified `Items.Insert`/`SaveToClass` recipe). Suggested resolution: pre-check `FindItemByConfigName` for duplicate + `%Dictionary.CompiledClass.%ExistsId(className)` for the host class; add a duplicate-name + bogus-className assertion to the live smoke. `src/ExecuteMCPv2/REST/Interop.cls` add branch.
- **[CR 17.2-4 / MED — DEFERRED, pre-existing/accepted IRIS limitation]** set-vs-add/remove persistence-model split + `LoadFromClass` side effects (Interop.cls `ItemManage` add/remove branches; the `add`-then-`get` visibility note is now documented in the tool description — see the Story 18.0 RE-AFFIRMED-DEFER entry below): `set` persists via the extent (`%Save()`+`UpdateProduction()`) while `add`/`remove` persist via class-XData (`SaveToClass`); `LoadFromClass` `%DeleteId`s + reloads the extent from XData, so a just-`add`ed item is invisible to a subsequent `get`/`set` (extent readers) until the next add/remove `LoadFromClass` syncs it, and a `set`-then-`add`/`remove` sequence can revert extent-only changes. This is the SAME issue closed won't-fix in Story 5-3 ("ItemManage set save/update consistency — pre-existing IRIS pattern limitation, no incidents"). The `LoadFromClass` sync is the dev's live-verified, required fix for the add→remove round-trip (DISCREPANCY #1b); its destructive nature is inherent to the IRIS API. Suggested resolution: document the extent/XData split in the tool description; confirm `add` observability in the Epic 17 live smoke (round-trip add→remove already live-verified by dev). `src/ExecuteMCPv2/REST/Interop.cls` add/remove branches.
- **[CR 17.2-5 / LOW — RESOLVED in Story 18.0]** `LoadFromClass` return `%Status` was ignored (invoked with `Do`, no status check); a silent `LoadFromClass` failure proceeded to `%OpenId` on a stale/half-loaded extent. **Resolved** in `ItemManage` add and remove branches — both now capture and check the `%Status` and fail cleanly via `SanitizeError`. See the Story 18.0 RESOLVED entry below. `src/ExecuteMCPv2/REST/Interop.cls` add/remove branches.

## Deferred from: code review of story 17.3 (2026-06-16)

Story 17.3 added `iris_sql_analyze` (`explain`/`stats`/`indexUsage`/`running`) on `@iris-mcp/dev` — TypeScript/SQL-only, NO ObjectScript handler, NO bootstrap contribution (per `17-0-api-probes.md` Area 3). Three-layer review (Blind Hunter, Edge Case Hunter, Acceptance Auditor): all six ACs (17.3.1–17.3.6) PASS. `mutates:{explain,stats,indexUsage,running:"read"}` all-classified (required even for reads — `assertGovernanceClassification` throws on any non-baseline key lacking a class; the probe doc's "no mutates" note correctly superseded by the spec). Governance test genuinely drives `McpServerBase.handleToolCall` (real schema + `mutates`, spy handler) proving all 4 allowed-by-default + registration-no-throw. Frozen governance baseline `1e62c5ad5bf7` git-clean, `--check` exit 0 (141 frozen / 190 live / 49 post-foundation). NO bootstrap/ObjectScript change — `BOOTSTRAP_VERSION` stays `39dc932907cb`, `bootstrap.test.ts` green. Pattern fidelity to `sql.ts` confirmed (`atelierPath(…,"action/query")`, `result.content[]` parse, `IrisApiError`→`isError`, non-IrisApiError rethrow, structuredContent OBJECT never array). Dev tool count 24→25. TWO patches applied inline (see story Review Findings) — no items deferred. The `EXPLAIN <query>` direct interpolation is the intended pass-through (same trust model as `iris_sql_execute`, user owns their own query, read-only) and is NOT a finding.

No DEFERRED items for this story — both review findings were auto-resolved inline:

- **[CR 17.3-1 / MED — RESOLVED in code review, 2026-06-16]** The `stats`/`running` `filter` param doubled single-quotes (correct quote-breakout escape) but did NOT escape LIKE wildcards `%`/`_`, so a filter like `50%` or `a_b` silently broadened the match vs the advertised "substring filter" semantics (NOT an injection — quote escaping is sound). **Fix applied:** new `likeSubstringPredicate(filter)` helper escapes the LIKE escape char `\`, then `%`/`_`, then doubles the single-quote, and appends an explicit `ESCAPE '\'` clause — so the filter matches a LITERAL substring. Both call sites (`running` UserName, `stats` Statement) now use the helper. Two unit-test exact-SQL assertions updated for the `ESCAPE` clause; two new e2e tests added (`%`/`_` escaped; backslash escaped). `packages/iris-dev-mcp/src/tools/sqlAnalyze.ts`.
- **[CR 17.3-2 / LOW — RESOLVED in code review, 2026-06-16]** The `explain`/`indexUsage` required-`query` guard rejected `undefined`/`""` but a whitespace-only `query` (`"   "`) fell through to a server-side SQL error (`EXPLAIN    `) instead of the clean client-side "requires a 'query'" message. **Fix applied:** guard now uses `query.trim() === ""`. Two e2e tests added (whitespace-only `query` rejected before any HTTP for both `explain` and `indexUsage`). `packages/iris-dev-mcp/src/tools/sqlAnalyze.ts`.

Dismissed as noise / by-design (no action): `EXPLAIN <query>` interpolation (intended pass-through, read-only, user's own query — explicitly sanctioned); `parsePlanIndexes` bare-`map` precision trade-off (acceptable for machine-generated IRIS plan text; the char class stops cleanly at the `, looping` comma/space, no trailing-dot in real plan output); `String(...Plan ?? "")` non-string coercion (Plan is a live-verified string XML column); `maxRows` 0/negative (Zod `.int().min(1)` rejects pre-handler); `toTabular` heterogeneous keys (standard homogeneous-SQL-resultset assumption, mirrors `sql.ts`); optional `schema` filter param + `STATEMENT_DAILY_STATS`/`HOURLY_STATS` (spec marks both "e.g."/"optionally" — out of required scope).

---

## Triaged via Story 18.0 (2026-06-17)

Story 18.0 (Epic 18 retro-review gate — minimal cleanup epic, this one story) triaged the carried Epic 16–17 `deferred-work.md` items + Epic 17 retro Action Item #1 / AI#4 against Epic 18 scope. Strictly additive: the hardening rejects previously-silently-accepted BAD input on the NEW, opt-in, default-disabled `iris_production_item` add/arbitrary-settings surface and tightens read-only `namespace`-param descriptions — no existing enabled action's success output shape changed (the 17.2 back-compat snapshot tests stayed green). Governance baseline frozen `1e62c5ad5bf7` git-clean (141 keys; `gen:governance-baseline:check` exit 0 — the bare generator was NOT run, Rule #25). Interop.cls changed → bootstrap re-rolled `39dc932907cb` → `fd3f065bcd3c` (Rule #24; `bootstrap.test.ts` green; `governance-baseline.ts` unchanged).

### INCLUDE — resolved in Story 18.0

- **[CR 17.2-1 / LOW] `ApplyArbitrarySetting` `%Status` discarded at both call sites** — **RESOLVED.** Both call sites (`ItemManage` set + add branches, `src/ExecuteMCPv2/REST/Interop.cls`) now capture the helper's `%Status` and propagate it via `SanitizeError` BEFORE pushing the key to `updatedSettings`, so a failed setting application surfaces as an error instead of being reported as updated.
- **[CR 17.2-2 / MED] `@Host`/`@Adapter` suffix parser edges** — **RESOLVED.** `ApplyArbitrarySetting` now scans the **LAST** `@` (not `$Find`'s first), rejects an empty setting Name (leading `@` like `@Host`), and rejects an **unknown** suffix (anything other than `Host`/`Adapter`, e.g. `@Foo`/`@host`) with a clear error instead of silently folding it into the name with the default Adapter target. Lead live-HTTP smoke (Rule #26) to assert the rejections.
- **[CR 17.2-3 / MED] `add` does not validate className exists/compiles or item-Name uniqueness** — **RESOLVED.** The `add` branch now pre-checks `%Dictionary.CompiledClass.%ExistsId(className)` (rejects a non-existent/non-compiled host class) and `tProd.FindItemByConfigName(itemName)` for a duplicate item Name (rejects the non-idempotent duplicate) BEFORE `Items.Insert`/`SaveToClass`. Lead live-HTTP smoke to assert both rejections change nothing.
- **[CR 17.2-5 / LOW] `LoadFromClass` `%Status` ignored** — **RESOLVED.** Both the add and remove branches now capture the `LoadFromClass` `%Status` and fail cleanly (via `SanitizeError`) on a load failure instead of proceeding to `%OpenId`/`FindItemByConfigName` on a stale/half-loaded extent.
- **[CR 16.1-1 / CR 16.2-1 / CR 16.3-namespace] ops %SYS-scoped tools accept-but-ignore `namespace`** — **RESOLVED (descriptions only).** The `namespace` param descriptions in `process.ts` / `database.ts` / `backup.ts` now state explicitly that the value has NO EFFECT (the operation is %SYS-scoped). The param is **retained** (dropping an accepted optional param is a back-compat break); a TS unit test per tool asserts the description text AND that the param is still accepted/forwarded without error.

### DEFER (re-affirmed, doc-only include)

> **⚠ Partial supersede by Story 22.1 (2026-07-04):** AI#4 (`iris_backup_manage restore`) is now CLOSED-WITH-EVIDENCE (won't-fix, re-verified) — see the Story 22.1 closure table at the end of this file. (CR 17.2-4 is a pre-existing/accepted IRIS limitation, not one of the 14 carried items.)

- **[CR 17.2-4 / MED] set-vs-add/remove extent/XData persistence split; just-added item invisible to immediate get/set** — **RE-AFFIRMED DEFER (doc-only include).** Pre-existing/accepted IRIS limitation (mirrors Story 5-3 closed won't-fix). A tool-description NOTE was added to `iris_production_item` (`packages/iris-interop-mcp/src/tools/item.ts`) documenting that an `add`-ed item is not visible to an immediate `get`/`set` until the next add/remove `LoadFromClass` sync (Rule #27), with a TS test pinning the note. The underlying split is NOT re-architected.
- **[Epic 17 retro AI#4] `iris_backup_manage restore` scriptable path** — **RE-AFFIRMED DEFER.** IRIS restore is interactive (`^DBREST` / `CLUMENU^JRNRESTO`) with no scriptable `Backup.General` classmethod (Rule #16 live-probe, Story 16.3); no demand. `restore` stays absent from the action enum; the handler rejects an `action="restore"` with a clear message. Carried forward unchanged.

### RE-DEFER — carried forward unchanged (no Epic 18 include)

> **⚠ SUPERSEDED by Story 22.1 (2026-07-04):** every carried item in this section was driven to a TERMINAL disposition by the Deferred-Work Ledger Burn-Down. See the **Story 22.1 closure table at the end of this file** for each item's disposition + evidence. The "OPEN"/"DEFER" wording below is HISTORICAL; the ledger now stands at ZERO open carried items (only CR 22.0-D1/D2 + the new CR 22.1-1/22.1-2 remain, per AC 22.1.7).

- **[CR 16.0-1 / LOW]** Generator `--check` vs `governance.test.ts` lock-step divergence (shared-helper de-dup) — pure refactor, no current correctness impact (today's all-bare action surface agrees); touches frozen-baseline tooling. Target: a future generator/CI hardening pass.
- **[CR 16.0-2 / LOW]** Vanished-key `--check` CLI exit-1 path has no direct test — pairs with CR 16.0-1; add the unit test when the derivation is extracted into an importable helper.
- **[CR 16.1-2 / LOW]** `ProcessManage` TOCTOU window — now fails CLOSED / benign refusal; true atomicity unavailable in `SYS.Process`. Accepted standard pattern.
- **[CR 16.1-3 / LOW]** `ProcessGet` `MemoryUsed` mailbox-read latency — perf only; revisit if `get` latency is reported.
- **[CR 16.2-2 / LOW]** No numeric range validation on `percentFull`/`targetSize`/`initialSize` — clean-envelope path (SanitizeError + Catch) covers it; friendlier `.min()/.max()` pre-flight is a future input-hardening pass.
- **[CR 16.3-device / MED]** `run` reads `device` but `StartTask` called without it — backup destination is a property of the user-defined task; per-call device override not part of the verified API path. Future ops-tool cleanup (or drop `device` from schema).
- **[CR 16.3-thaw-password / LOW]** Theoretical `ExternalThaw` password echo — theoretical; needs live freeze/thaw round-trip to confirm. Add redaction only if observed.
- **[CR 17.1-1 / MED]** `||` IdKey-delimiter injection guard has no automated test — server-side guard requires live-HTTP; the Epic 18 / Story 18.0 lead smoke asserts the rejection (Rule #26). No code change.

### NEW — surfaced in Story 18.0 code review (deferred)

> **⚠ SUPERSEDED by Story 22.1 (2026-07-04):** CR 18.0-1 (remaining half) is RESOLVED and CR 18.0-2 is CLOSED-WITH-EVIDENCE by the Deferred-Work Ledger Burn-Down. See the **Story 22.1 closure table at the end of this file**. The "DEFERRED" wording below is HISTORICAL.

- **[CR 18.0-1 / MED — whitespace half RESOLVED 2026-06-17; abstract/`%`-package half still DEFERRED] `add` className guard hardening.** `ItemManage` add branch checks `'##class(%Dictionary.CompiledClass).%ExistsId(tClassName)` (the CR 17.2-3 / Rule #16 "exists AND compiles" contract). **RESOLVED (whitespace):** the add branch now `$ZStrip(tClassName, "<>W")`-trims the className before `ValidateRequired`/`%ExistsId`, so a leading/trailing-space className no longer yields a confusing "does not exist or is not compiled" rejection, an all-whitespace value collapses to the clean "required" error, and the stored item `ClassName` is clean. Verified live via direct REST (padded-valid → added with clean className; padded-bogus → rejection names the trimmed class; all-whitespace → required). Bootstrap re-rolled `ee8c41a12284` → `daeb5f0bd525`. **STILL DEFERRED (abstract/percent):** a *compiled-but-not-instantiable* host class (`[Abstract]`, or a `%Library.*` class) still passes the `%ExistsId` guard and fails later at production-start/`OnConfigChange` rather than at add time. **Suggested resolution (remaining):** add an "is a valid Ens host" check (e.g. `%IsA("Ens.Host")` / non-abstract) in a future input-hardening pass. `src/ExecuteMCPv2/REST/Interop.cls` add branch.
- **[CR 18.0-2 / LOW — Story 18.0 code review] `add` duplicate-name check relies on `FindItemByConfigName` collation.** The dup guard `Set tDup = tProd.FindItemByConfigName(tItemName)` matches per the config-name index collation; a case-variant name (`myservice` vs existing `MyService`) may not be detected by the guard while a later `SaveToClass`/index could still collide. **Deferral rationale:** not part of the codified CR 17.2-3; depends on the `(Production,Name)` index collation (Rule #2 — needs a live probe to confirm whether the variant is truly distinct). **Suggested resolution:** live-probe the index collation; if case-insensitive, the current guard is already correct (no action); if case-sensitive and a case-variant duplicate is undesirable, normalize before the lookup. Fold into the same future input-hardening pass as CR 18.0-1. `src/ExecuteMCPv2/REST/Interop.cls` add branch.

### Resolved in Epic 19 retrospective (2026-06-18) — both CR 19.0 LOW items addressed now

- **[CR 19.0-1 / LOW — RESOLVED 2026-06-18 (retro, user-requested)] Discovery now validates `profile` when `allProfiles:true`.** `computeServerDiscovery` (`packages/shared/src/server-discovery.ts`) now calls `resolveProfile(profiles, args.profile)` when an explicit non-empty `profile` is supplied alongside `allProfiles:true`, throwing `ProfileResolutionError` on an unknown name — the SAME structured error the single-profile branch raises — instead of silently ignoring the typo. The all-profiles map is still returned for a VALID `profile` (its single-policy output is superseded by the per-profile map; the `allProfiles` `.describe()` documents this precedence). Two regression tests added (`server-discovery.test.ts`: unknown-profile-under-allProfiles errors; valid-profile-under-allProfiles returns the full map). Verified live against the rebuilt dist. No deferral remaining.
- **[CR 19.0-2 / LOW — RESOLVED 2026-06-18 (retro, user-requested)] Invalid framework `server` arg no longer hard-fails discovery.** `handleToolCall` (`packages/shared/src/server-base.ts`) now catches the `server`→profile `ProfileResolutionError` for the connection-agnostic discovery tool ONLY and falls back to the reserved `default` profile (the tool never connects; its own `profile` arg selects the policy), so the "call discovery first to learn valid profile names" workflow stays usable even if the client guesses a bad `server`. NON-discovery tools still hard-fail on an unknown `server` (the uniform D2 gate is unchanged for them). Regression test added (`server-discovery.test.ts`: discovery ignores invalid `server` → roster returned; `iris_doc_get` with same bad `server` → structured error). Verified live against the rebuilt dist. No deferral remaining.

## Epic 20 retro-review gate (2026-07-02) — re-defer all; no colliding Story X.0

> **⚠ SUPERSEDED by Story 22.1 (2026-07-04):** the carried items re-deferred by this gate are ALL terminally disposed; see the **Story 22.1 closure table at the end of this file**. The "remain OPEN and carried" wording below is HISTORICAL.

Epic 20 (Production Recovery / Clean) is a focused single-story **feature** epic: Story 20.0 (`iris_production_control` `clean` action via `Ens.Director.CleanProduction()` + governance "write, default-enabled" foundation extension F2 + latent `recover()` arg-bug fix). Its substantive story already occupies the `20.0` slot, mirroring Epic 19.

The `/epic-cycle 20` retro-review gate read `epic-19-retro-2026-06-18.md` (Action Item #3) + this ledger's open items and triaged them against Epic 20 scope:

- **Decision: RE-DEFER all carried open items; create NO colliding Story X.0.** Every open item is the ObjectScript/ops hardening batch explicitly routed by the Epic 18 & 19 retros to a **future dedicated cleanup epic** — CR 16.0-1/-2 (generator/`--check` de-dup), CR 16.1-3 (`ProcessGet` mailbox latency), CR 16.2-2 (numeric range validation), CR 16.3-device (`StartTask` device arg), CR 16.3-thaw-password (theoretical echo), CR 17.1-1 (`||` IdKey guard live-test), CR 18.0-1 (abstract/`%`-class host guard, remaining half), CR 18.0-2 (dup-name index collation), and AI#4 (`iris_backup_manage restore` scriptable path — re-affirmed absent).
- **Rationale:** none of these touch Epic 20's surface (`ProductionControl`/`CleanProduction`, the `defaultEnabled` governance mechanism, or the `recover` arg fix). Epic 20 is NOT the cleanup epic they are routed to. Including them would scope-creep a tightly-scoped feature epic. Same handling as the Epic 19 gate.
- **Ledger status:** ~~unchanged — all items above remain OPEN and carried, awaiting the future cleanup epic's Story X.0 triage~~ **→ SUPERSEDED by Story 22.1 (2026-07-04): all items above were driven to TERMINAL dispositions; see the Story 22.1 closure table at the end of this file. Ledger now at ZERO open carried items.**

### DROP (dismissed, by-design)

- **[CR 16.0-3 / LOW]** `--check --force` precedence / unknown-flag handling — safe path always wins, refusal non-destructive; flag-conflict validation is gold-plating for a build-time generator. No action.
- **[CR 16.1-4 / LOW]** Self-PID guard protects `$JOB` not external client — `$JOB` is the only meaningfully self-protectable PID; matches AC intent. No action.
- **[CR 16.2-3 / LOW]** compact/truncate ByRef pre-init-0 vs failure indistinguishable — success path never renders on failure; invisible to caller. No action.
- **[CR 17.1-2 / 17.1-3 / LOW]** empty-slot→`*` coercion / `deployable` direct-REST coercion — documented deliberate-caller paths; MCP callers safe via Zod. No action.

## Epic 21 retrospective (2026-07-03) — ledger INCLUDED in next epic, not re-deferred

> **⚠ SUPERSEDED by Story 22.1 (2026-07-04):** the "**14 open items**" this note enumerates were ALL driven to TERMINAL dispositions by Epic 22 Story 22.1 (8 resolved / 6 closed-with-evidence / 1 operational-close for Action #2; 0 re-deferred). This note was the authoritative "open against this note" pointer for the next gate — it is now DISCHARGED. See the **Story 22.1 closure table at the end of this file**. Ledger is at ZERO open carried items.

The Epic 21 retrospective (`epic-21-retro-2026-07-03.md`) closed with a firm Project Lead directive: after three consecutive feature epics (19 → 20 → 21) re-deferring this ledger at each retro-review gate, **the next epic to be planned WILL include this ledger as in-scope cleanup work** — not merely triage-and-defer it again. The ledger stands at **14 open items** as of this retro: the 11 items re-deferred below (Epic 21 kickoff, 2026-07-02) plus CR 21.0-2 (correlator O(n²)), CR 21.1-1 (rule-A pairloop grouping asymmetry), and CR 21.1-2 (Src=Dst self-call depth-stack growth) — all added during Epic 21's code reviews, all LOW severity, none blocking. (CR 21.0-1 does NOT carry forward — it was resolved same-epic as improvement I3, Story 21.1.) Whichever epic is planned next should open its retro-review gate against this note rather than re-triaging from scratch.

## Epic 21 retro-review gate (2026-07-02) — re-defer all; no colliding Story X.0

> **⚠ SUPERSEDED by Story 22.1 (2026-07-04):** the 11 carried items + Epic 20 retro Action #2 listed below are ALL terminally disposed; see the **Story 22.1 closure table at the end of this file**. The "carried, operational" / "remain OPEN" wording below is HISTORICAL.

Epic 21 (Message Trace Sequence Diagram) is a focused two-story **feature** epic: Story 21.0 (diagram library core + endpoint + tool) already occupies the `21.0` slot and Story 21.1 (episode compression + dedup + docs) completes it — mirroring the Epic 19/20 single-feature-epic shape.

The `/epic-cycle 21` retro-review gate read `epic-20-retro-2026-07-02.md` (Action Item #3) + this ledger's open items and triaged them against Epic 21 scope:

- **Decision: RE-DEFER all 11 carried open items; create NO colliding Story X.0.** The items — CR 16.0-1/-2 (generator/`--check` de-dup + vanished-key CLI test), CR 16.1-3 (`ProcessGet` mailbox latency), CR 16.2-2 (numeric range validation), CR 16.3-device (`StartTask` device arg), CR 16.3-thaw-password (theoretical echo), CR 17.1-1 (`||` IdKey guard live-test), CR 18.0-1 (abstract/`%`-class host guard, remaining half), CR 18.0-2 (dup-name index collation), AI#4 (`iris_backup_manage restore` scriptable path — re-affirmed absent), and CR 20.0-1 (`recover` forwards ignored `force`) — are the ObjectScript/ops hardening batch explicitly routed by the Epic 18/19/20 retros to a **future dedicated cleanup epic**.
- **Rationale:** none touches Epic 21's surface (new `ExecuteMCPv2.Diagram.*` library, new `/interop/production/messages/diagram` endpoint, new `iris_message_diagram` tool). Epic 21 is NOT the cleanup epic they are routed to; including them would scope-creep a tightly-scoped feature epic. Same handling as the Epic 19/20 gates.
- **Epic 20 retro Action #2** (rebuild + reload running MCP servers so the `7aca352` F2/clean build takes effect) — **carried, operational**: not story work; noted that Epic 21's own smoke/reload activity may incidentally satisfy it.
- **Ledger status:** ~~unchanged — all items above remain OPEN and carried, awaiting the future cleanup epic's Story X.0 triage~~ **→ SUPERSEDED by Story 22.1 (2026-07-04): all items above were driven to TERMINAL dispositions; see the Story 22.1 closure table at the end of this file. Ledger now at ZERO open carried items.**

## Deferred from: code review of story 21.1 (2026-07-03)

> **⚠ SUPERSEDED by Story 22.1 (2026-07-04):** CR 21.1-1 is RESOLVED (stakeholder Option A — rule A extended to pairloops in `Compressor.cls`; symmetric + nest fixtures) and CR 21.1-2 is RESOLVED (unpaired Src=Dst self-call push-guard + boundary fixtures). See the **Story 22.1 closure table at the end of this file**. The "Deferral rationale" wording below is HISTORICAL.

- **[CR 21.1-1 / LOW]** Episode-tier rule-A unwind is gated on `Kind="arrow"` — a `pairloop` immediately following an abandoned sync frame does NOT unwind it (`Compressor.cls:EpisodeCore`, the `If tEv.Kind = "arrow" { While … }` guard before the request-shaped push). The same semantic traffic gets a different episode grouping depending on whether the pair tier collapsed it: an abandoned request + raw single pair unwinds and splits into two episodes, while abandoned request + `pairloop` glues into one composite episode. **Deferral rationale:** never renders untruthfully — Phase-2 pass-through preserves original event order, and merges happen only on identical composite signatures (in the analyzed repeated case `[reqLost, PL]×3` the composite grouping actually compresses BETTER and stays faithful); §6.4 item 4's core requirement ("must not swallow all later episodes") holds because the next request ARROW unwinds. Spec reads "a new request's source" over the raw-event depth stack "over sync calls" — pair-loops are synthetic units, so the choice is an interpretation, not a violation. **Suggested resolution:** in the future cleanup epic, either extend rule A to pairloops (unwind on `tEv.Req.Src`) with fixtures for both groupings, or codify the composite-episode interpretation in the proposal §6.3 text. Source: Blind Hunter (finding #2), Story 21.1 review.
- **[CR 21.1-2 / LOW]** Repeated unanswered sync SELF-calls (`Src = Dst`, no response rows) never trigger rule A (the frame's destination always equals the next request's source), so each occurrence pushes another frame — the depth stack grows linearly (bounded by maxRows ≤ 10000 local-array nodes, no crash) and every self-call lands in ONE episode, defeating episode compression for that pathological trace shape. **Deferral rationale:** degradation-not-corruption (full-length faithful diagram, unpaired warnings intact); requires a config item Inproc-calling itself repeatedly with no responses — not a shape any current fixture or live trace exhibits. **Suggested resolution:** in the future cleanup epic (alongside CR 21.0-2's correlator index maps), add a self-call guard (don't push when `Src=Dst` and unpaired, or cap stack growth) + a Src=Dst boundary fixture. Source: Blind Hunter (finding #5), Story 21.1 review.

## Deferred from: code review of story 21.0 (2026-07-02)

> **⚠ SUPERSEDED by Story 22.1 (2026-07-04):** CR 21.0-2 (correlator O(n²)) is CLOSED-WITH-EVIDENCE (live measurement: default maxRows=2000 → ~0.14 s; documented cap in `Correlator.cls`). See the **Story 22.1 closure table at the end of this file**. (CR 21.0-1 was already RESOLVED in Story 21.1.)

- ~~**[CR 21.0-1 / MED]** Queue fallback pairing fires on EMPTY `ReturnQueueName`~~ — **RESOLVED (Story 21.1, improvement I3, 2026-07-02)**: stakeholder decision amended proposal §6.2 with **[I3]** — the `ReturnQueueName` fallback fires only when BOTH values are non-empty and equal (`Correlator.cls` now guards `tReq.ReturnQueue '= ""`); a one-way async request + later CorrId-less reverse response-typed row stays UNPAIRED with both warnings emitted, and legitimate non-empty equal-queue fallback pairs still pair (fixture tests in `DiagramCorrelatorTest`). Deliberate deviation from reference behavior, recorded in §6.2 [I3]. *(Original finding: `"" = ""` trivially satisfied the equality, silently false-pairing one-way Queue requests. Source: Edge Case Hunter finding #1, Story 21.0 review.)*
- **[CR 21.0-2 / LOW]** Correlator pairing is O(n²) with no iteration bound — every Request forward-scans to end-of-list; an unmatched Queue request scans TWICE (CorrId primary + ReturnQueue fallback). At the advertised extremes (maxRows=10000 × 20 sessions, pathological all-unpaired trace — exactly the wedged-production shape users will diagram) a single blocking GET could burn ~10⁸ oref property accesses. Default maxRows=2000 keeps realistic traces fast. **Deferral rationale:** §6.2 prescribes the order-based forward-scan pairing; index structures (CorrId→position map, per-endpoint-pair response queues) preserve semantics but are an optimization outside review scope. **Suggested resolution:** add index maps in Story 21.1 or the future cleanup epic; alternatively cap fan-out cost (sessions × maxRows budget). Source: Blind Hunter (finding #4), Story 21.0 review.

## Deferred from: code review of story 20.0 (2026-07-02)

> **⚠ SUPERSEDED by Story 22.1 (2026-07-04):** CR 20.0-1 is RESOLVED (per-action documentation — `timeout`/`force` honored only by `stop`/`restart`, ignored for `start`/`update`/`recover`/`clean`; doc-only preserves the AC 20.0.7 back-compat gate byte-for-byte; +doc test). See the **Story 22.1 closure table at the end of this file**. The "Deferral rationale" wording below is HISTORICAL.

- **[CR 20.0-1 / LOW]** `recover` still accepts and forwards `force`, which the server now ignores entirely. Story 20.0 correctly fixed `RecoverProduction()` to a no-arg call (AC 20.0.6), and the tool description was narrowed to "Force stop on timeout." But `force` remains a top-level optional param on `iris_production_control` and the handler still does `if (force !== undefined) body.force = force`; `tForce` is still parsed server-side. A client sending `{action:"recover", force:true}` gets a silent no-op on the force intent (advertised-vs-actual drift). **Deferral rationale:** `force` is a pre-existing tool-wide parameter (not introduced by this story); removing/rejecting it per-action is a schema-shape change outside Story 20.0's additive scope and risks the AC 20.0.7 back-compat gate. Same class as the `force`/`timeout`-ignored-for-`clean` consistency note. **Suggested resolution:** in a future interop-cleanup story, either (a) reject `force`/`timeout` for actions that don't consume them with a clear validation error, or (b) document per-action which optional params are honored. Source: Blind Hunter (finding #2), Story 20.0 review.

## Epic 22 planned (2026-07-03) — ledger routed to Story 22.1 for TERMINAL disposition

Per the Epic 21 retro directive, Epic 22 (sprint-change-proposal-2026-07-03.md) includes the ledger as in-scope work: **Story 22.1** must drive all 14 open items (CR 16.0-1, CR 16.0-2, CR 16.1-3, CR 16.2-2, CR 16.3-device, CR 16.3-thaw-password, CR 17.1-1, CR 18.0-1 remaining half, CR 18.0-2, AI#4, CR 20.0-1, CR 21.0-2, CR 21.1-1, CR 21.1-2) plus Epic 20 retro Action #2 to a terminal disposition — resolved, closed-with-evidence, or closed-by-decision. **Re-deferral is not an allowed outcome.** The Epic 22 retro-review gate opens against this note.

## Deferred from: code review of story 22.0 (2026-07-03)

- **[CR 22.0-D1 / LOW]** `Generate.Count` aborts the ENTIRE scan when one document's `ScanDoc` errors — the enumerate→retrieve window is a TOCTOU: a document deleted (or renamed) between `StudioOpenDialog` enumeration and `GetTextAsArray` retrieval kills a long whole-namespace run with a hard error instead of skipping the vanished doc. **Deferral rationale:** skip-and-report is a design change to the facade's all-or-nothing contract (the hard per-document error is also what enforces the bucket-sum invariant discipline); no incident is possible on a quiescent namespace and the error message names the offending document. **Suggested resolution:** in a future hardening pass, collect per-document retrieval failures into a `skippedDocuments` array on the result (keeping classification errors hard), or document the all-or-nothing contract in the tool description. `src/ExecuteMCPv2/Loc/Generate.cls` scan loop. Source: Blind Hunter (LOW-5), Story 22.0 review.
- **[CR 22.0-D2 / LOW]** StudioOpenDialog multi-part spec order sensitivity: an exact document name listed BEFORE an overlapping wildcard part silently drops documents (live-probed: `ExecuteMCPv2.Loc.Generate.cls,ExecuteMCPv2.Loc.*.cls` → 2 of 3 docs; reversed order → all 3; identical-part dedup works). This is native IRIS `%RoutineMgr:StudioOpenDialog` behavior (spec re-processing at `irislib/%Library/RoutineMgr.cls:1146-1163`), not a port defect — the scanner passes the caller's spec through verbatim per ratified decision D2. **Deferral rationale:** neutralizing it (enumerating each comma part separately and unioning names) would change the ratified "spec semantics = StudioOpenDialog semantics" model and needs a stakeholder decision; CR 22.0-4 documented the quirk in the tool description, dev README, and tool_support.md as the current mitigation. **Suggested resolution:** if AI callers hit the quirk in practice, amend D2 to per-part-union enumeration in `Scanner.EnumerateDocs` (semantics-preserving for non-overlapping specs) + a regression test on the overlap shape. `src/ExecuteMCPv2/Loc/Scanner.cls:EnumerateDocs`. Source: reviewer live probe + Edge Case Hunter (LOW-D adjacent), Story 22.0 review.

---

## Story 22.1 (2026-07-04) — DEFERRED-WORK LEDGER BURN-DOWN — ALL 14 CARRIED ITEMS + EPIC 20 RETRO ACTION #2 TERMINALLY DISPOSED

Per the Epic 21 retro directive (line 566) and the Epic 22 routing note (line 595), Story 22.1 drove **every** open carried item to a TERMINAL disposition — **resolved**, **closed-with-evidence**, or **closed-by-decision**. **Re-deferral was NOT an allowed outcome (AC 22.1.1); zero items were re-deferred.** Rule #16 live probes (disposable `ExecuteMCPv2.Temp.Probe221`, deleted after) + `irislib/`/`irissys/` source (Rule #2) backed every verify-then-dispose call. BOOTSTRAP_VERSION `919124293f66` → `e931a96373f0` (Rule #24, idempotent); frozen governance baseline `1e62c5ad5bf7` git-clean, `gen:governance-baseline:check` exit 0 (Rules #23/#25); full monorepo suite green, lint + type-check clean.

| # | Item | Terminal disposition | Evidence |
|---|------|----------------------|----------|
| 1 | **CR 16.0-1** generator/`governance.test.ts` key-derivation lock-step | **RESOLVED** | Extracted `deriveKeysForTool` + `computeBaselineDrift` + `SERVER_PACKAGES` + `VANISHED_HINT` into `packages/shared/src/governance-baseline-derivation.ts` (reuses the gate's `unwrapActionOptions`); the generator AND `governance.test.ts` drift guard now import the SAME derivation. Frozen baseline byte-identical (`--check` exit 0, 141 keys, git-clean). |
| 2 | **CR 16.0-2** vanished-key `--check` exit-1 has no test | **RESOLVED** | New `packages/shared/src/__tests__/governance-baseline-derivation.test.ts` unit-tests `computeBaselineDrift` vanished-key detection with SYNTHETIC sets — no dist mutation / no frozen-file perturbation (the exact gap `governance-baseline-check.test.ts` documented it could not cover). |
| 3 | **CR 16.1-3** `ProcessGet` `MemoryUsed` mailbox latency | **CLOSED-WITH-EVIDENCE** | Live probe: MemoryUsed read via `%SYS.ProcessQuery` = **4.8 ms/call** (10 iters). Immaterial (far below any interactive threshold) → no fix. |
| 4 | **CR 16.2-2** no numeric range validation | **RESOLVED** | Zod `.min(1).max(100)` on `percentFull`, `.min(0)` on `targetSize` (0 = documented default), `.min(1)` on `initialSize` in `packages/iris-ops-mcp/src/tools/database.ts`; +bounds-rejection unit test. No `.refine()`. |
| 5 | **CR 16.3-device** `run` reads `device` but `StartTask` called without it | **RESOLVED** | Source (Rule #2): `Backup.General.StartTask`'s 4th `Device` arg is `[Internal]` + undocumented (doc covers only jobbackup/quietflag) → NOT wired (Rule #16). Retained the param + rewrote its description to state it has **NO EFFECT** (backup destination is the task definition's), mirroring the Story 18.0 `namespace` resolution (dropping an accepted param is a back-compat break); +desc unit test. |
| 6 | **CR 16.3-thaw-password** theoretical `ExternalThaw` echo | **CLOSED-WITH-EVIDENCE** | Source (Rule #2): `Backup.General.ExternalThaw` body is EMPTY (kernel-generated) — the ObjectScript layer constructs NO error text, so the submitted password cannot be echoed by an OS-layer error string; kernel `%Status` messages do not interpolate submitted auth values. No redaction needed. (A live freeze was deliberately avoided as instance-wide-disruptive; the empty-body evidence is conclusive.) |
| 7 | **CR 17.1-1** `||` IdKey-delimiter guard has no automated test | **CLOSED-WITH-EVIDENCE** | Guard already coded (`DefaultSettingsManage`). Live REST (bypasses governance to test the handler guard, Rule #26): `production:"ZZZSmoke221||evil"` → `ERROR #5001: Key slot values may not contain the '||' delimiter`, `result:{}` (no write). |
| 8 | **CR 18.0-1 remaining half** abstract/`%`-class host guard | **RESOLVED** | Added `$classmethod(cls,"%Extends","Ens.Host")` + `%Dictionary.CompiledClass.Abstract` guard in `ItemManage` add branch (API probe-verified: `Ens.BusinessService`→abstract, `%Stream.GlobalCharacter`→not-host, `EnsLib.File.PassthroughService`→accept). Live REST confirmed non-host + abstract rejected, concrete host passes the guard (fails later at production-not-found — proves order + no false-reject). |
| 9 | **CR 18.0-2** `add` dup-name relies on `FindItemByConfigName` collation | **CLOSED-WITH-EVIDENCE** | Source (Rule #2): `Ens.Config.Item` `Index Name On (Production As Exact, Name As Exact)` = case-SENSITIVE; `FindItemByConfigName`→`findItemByName` exact match. A case-variant is a genuinely DISTINCT item in IRIS, so the guard already matches IRIS semantics; normalizing to case-insensitive would WRONGLY reject valid distinct names → no action. |
| 10 | **AI#4** `iris_backup_manage restore` scriptable path | **CLOSED-WITH-EVIDENCE (won't-fix)** | Probe (Rule #16): `Backup.General` has NO `RestoreDatabase`/`Restore`/`RestoreTask`/`RestoreJournal`/`StartRestore` classmethod (all `%ExistsId`=0); source doc comments direct to "your external restore mechanism". Live REST confirmed the handler rejects `action="restore"` with a clear message, no crash. (Minor pre-existing Rule #33 note: the reject message names `^DBREST`/`CLUMENU^JRNRESTO` which `SanitizeError` strips to `( / )` — cosmetic, out of AI#4 scope; flagged for a future touch of `Monitor.cls`.) |
| 11 | **CR 20.0-1** `recover` forwards ignored `force`/`timeout` | **RESOLVED** | Per-action documentation (dev's recorded choice — doc-only, preserves the AC 20.0.7 back-compat gate byte-for-byte; a reject/no-forward would change the wire behavior for existing `{recover,force}` callers): `timeout`/`force` descriptions + the tool description now state they are honored ONLY by `stop`/`restart` and IGNORED for `start`/`update`/`recover`/`clean`; +doc unit test. |
| 12 | **CR 21.0-2** correlator O(n²) no iteration bound | **CLOSED-WITH-EVIDENCE (documented cap)** | Live probe: `Correlate` worst case (all-unpaired Queue, CorrId + ReturnQueue double-scan): default `maxRows=2000` → **0.136 s** (fast, acceptable); `10000` extreme → 4.6 s (functional, no crash — best-effort holds). Default keeps realistic traces fast; documented the cap + measurements + index-map upgrade path in `Correlator.cls` (no re-architecture). |
| 13 | **CR 21.1-1** episode rule-A pairloop asymmetry | **RESOLVED (closed-by-decision → code)** | Stakeholder Option A (extend rule A to pairloops). `Compressor.cls` `EpisodeCore`: an abandoned sync frame now ALSO unwinds when the next unit is a `pairloop` — on `tEv.Req.Src`. `[reqLost,singlePair]` and `[reqLost,pairloop]` now group symmetrically (both split into two episodes; no composite glue). New symmetric-grouping fixture in `DiagramEpisodeTest.cls`; no existing fixture assumed the old pairloop-glues-composite behavior (all 11 prior episode fixtures still green). |
| 14 | **CR 21.1-2** Src=Dst self-call depth-stack growth | **RESOLVED** | `Compressor.cls` `EpisodeCore`: an UNPAIRED sync self-call (Src=Dst, PairId=0) no longer pushes a frame (rule A could never unwind it, no response pops it) — so the stack stays bounded and repeated self-calls compress into one episodeloop instead of gluing into one episode. New Src=Dst boundary fixture in `DiagramEpisodeTest.cls`. |
| 15 | **Epic 20 retro Action #2** rebuild+reload MCP servers (`7aca352`) | **OPERATIONAL CLOSE (verified)** | The RUNNING iris-interop-mcp server's governance policy (`iris_server_profiles`) registers `iris_production_control:clean: true` (plus Epic 17 `production_item:add/remove` default-disabled and Epic 21 `iris_message_diagram`), confirming the post-`7aca352` F2/clean build is LIVE. No rebuild/reload needed for Action #2. |

**Disposition tally:** 8 resolved · 6 closed-with-evidence · 1 operational-close · **0 re-deferred**.

**Ledger status: ZERO open carried items.** The ONLY open items are Epic 22's own review deferrals — **CR 22.0-D1**, **CR 22.0-D2** (Story 22.0 review) and **CR 22.1-1**, **CR 22.1-2**, **CR 22.1-3** (Story 22.1 code review, 2026-07-04 — see the section at the end of this file) — all permitted to remain per AC 22.1.7 (Epic-22-own findings / pre-existing cosmetic, not carried items).

### OS test totals (Rule #35 — verified per-class, totals match method counts)

`DiagramEpisodeTest` 13/13 (incl. new `TestAbandonedPairloopGroupsSymmetricWithRawPair` + `TestUnpairedSelfCallDoesNotGrowStack`) · `DiagramEpisodeEdgeTest` 6/6 (incl. QA's `TestAbandonedThenContinuedPairloopFormsCompositeEpisode` + `TestPairedSelfCallStillPushesFrame` + `TestDeepSelfCallChainCollapsesAndIsolatesNextEpisode`) · `DiagramCompressorTest` 10/10 · `DiagramCorrelatorTest` 18/18 · `DiagramDedupTest` 3/3 · `DiagramGenerateTest` 11/11 — all touched Diagram OS classes green, no regression. CR 18.0-1's guard classification is pinned by the QA-added `InteropHostGuardTest` 5/5 (OS-level regression pin of the host/abstract/exists verdicts) AND the live REST smoke (the real handler POST path). *(Code-review 2026-07-04 re-verified all of these per-class on HSCUSTOM against the redeployed working tree: 13/13, 6/6, 10/10, 18/18, 3/3, 11/11, host-guard 5/5.)*

---

## Deferred from: code review of story 22.1 (2026-07-04)

Story 22.1's OWN code review (adversarial three-layer + live-IRIS verification) confirmed all 15 dispositions above are genuine and terminal (CR 18.0-1 re-verified live over HTTP against the real `ItemManage` handler — abstract/non-host rejected, concrete host passes; CR 16.0-1 `--check` exit 0 with the frozen 141-key baseline byte-identical; CR 20.0-1 docs confirmed truthful against the server `ProductionControl` handler; CR 21.1-1/-2 re-run per-class on HSCUSTOM). Two NEW LOW items were surfaced — both permitted to remain open per AC 22.1.7 (Epic-22-own findings, NOT carried items); neither is a live defect:

- **[CR 22.1-1 / LOW]** `EpisodeCore` rule-A pairloop unwind (the CR 21.1-1 fix) leaves `tReqSrc=""` when a `pairloop` render event has a non-object `.Req` (the `$IsObject(tEv.Req)` guard only gates the assignment, not the while-loop). Because the `While` runs for any `pairloop` and no frame destination equals `""`, an empty `tReqSrc` would over-unwind the ENTIRE abandoned-sync-frame stack (mis-grouping episodes — never a crash, never untruthful rendering since Phase-2 preserves event order). **Reachability: UNREACHABLE via the current pipeline** — the sole producer of `pairloop` events (`Compressor.cls:Compress`, line ~85 `Set tLoop.Req = tEv`) always sets `.Req` to a real request arrow with a non-empty `Src`, so `tReqSrc` is always non-empty for a real pairloop (confirmed independently by all three review layers + the pairloop-construction read). The `$IsObject` guard already prevents the only real risk (an `<INVALID OREF>` crash). **Deferral rationale:** latent defensive-only path in freshly-changed code; hardening requires an ObjectScript edit → bootstrap re-roll + redeploy immediately before the lead smoke, disproportionate for an unreachable path. **Suggested resolution (one line):** in a future `Compressor.cls` touch, add `(tReqSrc '= "")` to the rule-A `While` condition so an unknowable source conservatively does NOT unwind (matches the pre-CR-21.1-1 "pairloops never unwound" fallback) instead of over-popping. `src/ExecuteMCPv2/Diagram/Compressor.cls` EpisodeCore (~line 255). Source: Blind Hunter #1 / Edge Case Hunter #1 / Acceptance Auditor (INFO), Story 22.1 review.
- **[CR 22.1-2 / LOW]** CR 16.0-1's shared derivation introduces a dist/source split: `scripts/gen-governance-baseline.mjs` imports the helper from `packages/shared/dist/governance-baseline-derivation.js` (compiled DIST) while the in-suite drift guard (`governance.test.ts`) imports `../governance-baseline-derivation.js` (vitest resolves the TS SOURCE). If a developer edits the `.ts` derivation and runs `gen:governance-baseline` / `:check` WITHOUT first rebuilding `@iris-mcp/shared`, the generator would run STALE dist logic while the drift test runs the new source logic — reintroducing the exact lock-step divergence CR 16.0-1 set out to eliminate. **Reachability: LOW / maintenance-workflow** — no incident on today's all-bare-enum surface (both derivations coincide); a fully MISSING dist is a loud fail-fast (`ERR_MODULE_NOT_FOUND`, acceptable and pre-existing since the generator already imports server dists); the hazard only bites when the derivation logic is changed for a wrapped/edge action shape AND the dist is stale. The standard `pnpm test` flow rebuilds shared (turbo) before the lockstep test spawns the CLI, so CI is protected. **Suggested resolution:** in a future tooling pass, either add a `prebuild`/`turbo run build --filter @iris-mcp/shared` step to the `gen:governance-baseline*` npm scripts, or have `governance.test.ts` import the built dist (matching the generator), so both consumers read the same artifact. `scripts/gen-governance-baseline.mjs` + `packages/shared/src/__tests__/governance.test.ts`. Source: Edge Case Hunter #2, Story 22.1 review.

- **[CR 22.1-3 / LOW — pre-existing]** The `iris_backup_manage action="restore"` rejection message in `ExecuteMCPv2.REST.Monitor:BackupManage` names the IRIS restore entry points `^DBREST` / `CLUMENU^JRNRESTO`; because those are caret-globals, `SanitizeError` strips them (Rule #33), so the client sees the message with those references blanked to `( / )`. **Pre-existing** (in `Monitor.cls`, NOT introduced by Story 22.1 — surfaced while re-verifying AI#4's won't-fix rejection) and **cosmetic** — the rejection itself functions correctly (restore is refused, no crash, no write); only the two entry-point names are lost from the guidance text. **Assessed for in-scope fix (per the code-review directive) → DEFER:** the fix is a one-line reword (drop the carets: "DBREST" / "CLUMENU^JRNRESTO" → name them without the leading `^`, per Rule #33), but it touches `Monitor.cls` → bootstrap re-roll + redeploy for a cosmetic message in a won't-fix action — disproportionate to bundle into this review immediately before the lead smoke. **Suggested resolution:** fold the caret-strip reword into the next `Monitor.cls` touch (mirrors the Epic 20 `Ens.AppData` de-caret fix pattern). `src/ExecuteMCPv2/REST/Monitor.cls` BackupManage restore branch. Source: dev flag (Story 22.1) + code-review assessment.

**Dismissed (investigated, no action):** the Zod `.min/.max` on `database.ts` narrows only nonsensical inputs the server would reject anyway (intended CR 16.2-2 resolution — not a real back-compat break); `$classmethod(cls,"%Extends","Ens.Host")` cannot throw `<METHOD DOES NOT EXIST>` (live-confirmed `%Extends` is defined on `%Library.Base`, inherited by every compiled class — returns 0 cleanly for non-hosts) and always runs after the `%ExistsId` compiled-class gate; `deriveKeysForTool` is byte-behaviour-identical to the old inline generator logic (Edge Hunter verified + frozen hash `1e62c5ad5bf7` unchanged).

---

## Epic 23 planned (2026-07-08) — retro-review gate: all 5 Epic-22-own LOW items RE-DEFERRED

Epic 23 (`iris_health_check` — Composite Health Check, sprint-change-proposal-2026-07-07.md) is a focused OPS **feature** epic (Stories 23.0 TS threshold engine / 23.1 ObjectScript `/monitor/health` + bootstrap bump / 23.2 tool + docs; the "23.0" slot is a FEATURE story, not a cleanup slot). Its retro-review gate triaged the 5 Epic-22-own LOW review deferrals that stood at the close of Epic 22 (ledger at zero carried-open):

| Item | Location | Disposition | Rationale |
|---|---|---|---|
| CR 22.0-D1 | `Loc/Generate.cls` | RE-DEFER | Epic 23 does not touch Loc classes; skip-and-report is a facade-contract design change. |
| CR 22.0-D2 | `Loc/Scanner.cls` | RE-DEFER | Epic 23 does not touch Loc; per-part-union needs a stakeholder D2 decision. |
| CR 22.1-1 | `Diagram/Compressor.cls` | RE-DEFER | Epic 23 does not touch Diagram; UNREACHABLE defensive-only path. |
| CR 22.1-2 | `scripts/gen-governance-baseline.mjs` | RE-DEFER | Epic 23 does not touch the governance generator; CI (turbo build) already protects it. |
| CR 22.1-3 | `REST/Monitor.cls` (BackupManage) | RE-DEFER (**fold-in flagged**) | Cosmetic caret-strip in the restore-reject message; Story 23.1 WILL touch `Monitor.cls` (new `/monitor/health`) + re-roll bootstrap — flagged as an opportunistic fold-in for 23.1 if convenient, NOT required. |

**Decision:** all 5 RE-DEFERRED — first deferral, all LOW, below Rule #37's ≥3-re-deferral burn-down threshold; feature epic per the Epic 19/20/21 precedent → **no colliding Story X.0** (the 23.0 slot is a feature story). Ledger holds steady at these 5 Epic-22-own LOW items. **CR 22.1-3 flagged to Story 23.1** as a fold-in candidate.

---

## Deferred from: code review of story-23.0 (2026-07-07)

Story 23.0 is a research/spec-amendment story (pinned the `01-health-check.md` §3 probe table + §2 thresholds). Its own pin-correctness was verified CLEAN (every pinned API confirmed against handler source + live IRIS; `locks` formula verified byte-for-byte against `%Monitor.System.LockTable.GetSample`; `memory`-drop evidence confirmed via `SYS.Stats.Dashboard` having no memory field; Temp probe classes verified deleted). Nine spec clarifications were auto-fixed inline. The items below are **forward-looking implementation/design considerations surfaced by the three review layers** that belong to the *implementing* stories (23.1 ObjectScript endpoint / 23.2 TS verdict engine), not defects in 23.0's pins — deferred so 23.1/23.2 devs see them.

- **[CR 23.0-1 / MEDIUM — for 23.2]** `databases` space-health is INERT under the vendor-recommended common case. `dbFreePct=(maxSize-size)/maxSize*100` only fires when `maxSize>0`, but live-verified all 15 dev-instance DBs have `maxSize=0` (IRIS's own "recommended" unlimited setting) ⇒ every DB is `notApplicable` ⇒ the tool's headline "database space health" produces NO disk-exhaustion signal on a normally-configured instance. Unlike `journal` (which was engineered to read actual volume free space via `%Library.File.GetDirectorySpace`), `databases` offers zero volume-exhaustion protection. **Deferral rationale:** the pin (Config.Databases + SYS.Database Size/MaxSize) is correct and mirrors the existing handler; switching the metric is a design decision for the implementing story. **Suggested resolution:** 23.1/23.2 consider a per-DB volume-free-% via `%Library.File.GetDirectorySpace(<db directory>, .free, .total, 0)` (same call journal now uses) so unlimited-size DBs still get a disk-space signal, OR explicitly document that `databases` is unmounted/maxSize-only health in v1. Source: Blind Hunter #3.

- **[CR 23.0-2 / MEDIUM — for 23.1/23.2]** `license` numerator `CSPUsers()` may under-count license consumption. `licensePct=currentCSPUsers/userLimit*100` was cross-verified only vacuously (`0==0` on an idle instance) against `SYS.Stats.Dashboard.LicenseCurrentPct`. `CSPUsers()` counts CSP/web sessions; if non-CSP connections consume license units, the numerator under-counts and the license check silently fails to warn near exhaustion. **Deferral rationale:** cannot be distinguished on an idle instance; needs an under-load check 23.0 can't perform. **Suggested resolution:** 23.1/23.2 verify `CSPUsers()` vs `SYS.Stats.Dashboard.LicenseCurrent` under real load and prefer the IRIS-authoritative `LicenseCurrent`/`LicenseCurrentPct` (Management-Portal dashboard figure; also sidesteps the `userLimit=0` divide-by-zero). Spec §3 license row already carries this pointer. Source: Blind Hunter #4 (+ Edge Case Hunter #2 zero-denominator).

- **[CR 23.0-3 / MEDIUM — for 23.2]** No health-LEVEL (ok/warning) criteria for the applicable-but-unhealthy branch of `mirror`, `ecp`, `interop`, `system`. Only `journal`/`databases`/`license`/`locks` have thresholds and `alerts` has the state rule; for a mirror member whose backup is disconnected (`GetStatus` string), a configured-but-dead ECP, an interop production with a large queue backlog (no queue-depth threshold param exists), and `system` counters, the spec pins only the raw source / `notApplicable` gate, so those cases default to `ok` and hide real failures. **Deferral rationale:** verdict criteria are 23.2's job (thresholds live in TS by design) and partly a v1-scope product decision. **Suggested resolution:** 23.2 define ok/warning rules (e.g. an `interop` queue-depth threshold param; a `mirror` `GetStatus`-not-"Primary/Backup-normal" ⇒ warning) OR explicitly mark these areas informational (raw-only) in v1. Source: Edge Case Hunter #7, Blind Hunter #12.

- **[CR 23.0-4 / MEDIUM — for 23.2]** `databases` per-DB → single-area finding aggregation rule is unspecified. Raw is per-DB (`{size,maxSize,mounted}` × N; 15 on dev) but the §2 output example shows one finding per area with a scalar `value`/`threshold`. The spec never says how N per-DB results roll up into the `databases` area (worst-DB? one finding per DB? area `value` when DBs differ?). Never exercised because all 15 dev DBs are `notApplicable`. **Deferral rationale:** aggregation is verdict-engine (23.2) design. **Suggested resolution:** 23.2 pin an explicit rule — recommend worst-DB drives the area level with a per-DB breakdown in `raw`. Source: Edge Case Hunter #8.

- **[CR 23.0-5 / LOW — for 23.1]** `databases` `%OpenId` open-FAILURE is indistinguishable from `notApplicable`. The mirrored `Config.cls:DatabaseList` / `Monitor.cls:DatabaseCheck` wrap `SYS.Database.%OpenId(dir)` in Try/Catch and on failure leave `size/maxSize=0` (`mounted=0`); §3 maps `maxSize=0`→`notApplicable` and `mounted=0`→`notApplicable`, so a genuinely corrupt/locked DB whose open throws is silently reported `notApplicable` (healthy-ish) instead of `error`. **Deferral rationale:** 23.1 handler-robustness detail. **Suggested resolution:** 23.1 set an explicit error/open-failed flag in the per-DB raw so TS emits `level:"error"` for an open failure, distinct from the unlimited-size `notApplicable`. Source: Edge Case Hunter #10.

- **[CR 23.0-6 / LOW — for 23.2]** Tool-input validation gaps for the TS layer: (a) the dropped `memory` enum value — undefined whether `areas:["memory"]` is a Zod reject, silently ignored, or aliased to `system` (back-compat risk for a caller reading prior docs); (b) `areas:[]` empty-array semantics (check-nothing vs check-all) undefined ⇒ "All 0 areas healthy"; (c) threshold override has no range/ordering guard (`journalPctWarn=95, journalPctCrit=90` inverted; negative; >100) — AC 3 deliberately allows extreme values, but inverted/precedence behavior is unspecified. **Deferral rationale:** all three are 23.2 TS/Zod design. **Suggested resolution:** 23.2 decide + unit-test each (recommend: reject `memory` with a "renamed/dropped" message, treat `areas:[]` as all, document override precedence as per-threshold independent). Source: Edge Case Hunter #12/#13/#14.

---

## Deferred from: code review of story-23.1 (2026-07-07)

Story 23.1's code review (adversarial three-layer — Blind Hunter / Edge Case Hunter / Acceptance Auditor — plus live-IRIS probing on HSCUSTOM) closed at **0 HIGH, 0 MEDIUM (blocking), 0 patch**. The endpoint's two hardest properties were verified GENUINE (probed, not just read):
- **Namespace correctness** (highest-risk): confirmed live that `Ens.Director` exists in HSCUSTOM but NOT `%SYS`, and `SYS.Lock` exists in `%SYS` but NOT HSCUSTOM — so running the `interop` gate FIRST in the caller's namespace (before the `%SYS` switch) is load-bearing and correct. Live `/monitor/health` (HSCUSTOM-dispatched app) returns `interop.interopEnabled:true` (HSCUSTOM's state), not `%SYS`'s. `$NAMESPACE` restored on every path (success line 93, catch first-line 100, POST-parse early-Quit before the switch).
- **Per-area fault isolation + single render (Rule #7)**: live-verified — malformed POST body → one clean `ERROR #5001` envelope at HTTP 200; the two forced-failure `%UnitTest` methods drive REAL exceptions (`SYS.Lock` outside `%SYS`; `Ens.Director` absent in `%SYS`) through the live Try/Catch → `errors[locks]` / `interopEnabled:false` while other areas stay intact. 15/15 %UnitTest (`total=15` matches — no Rule #35 partial-snapshot); bootstrap `13b4b5f003ab` regen idempotent; `gen:governance-baseline:check` exit 0 (frozen `1e62c5ad5bf7` untouched, no new key); `bootstrap.test.ts` 42/42.

**Ledger reconciliation — fold-ins RESOLVED by Story 23.1 (closed, not carried):**
- **CR 22.1-3 → RESOLVED.** The `BackupManage` restore-reject de-caret reword was folded into Story 23.1 (Task 6) and **verified live**: POST `/monitor/backup/manage {"action":"restore"}` renders the full message with both entry-point names intact ("DBREST / CLUMENU in JRNRESTO"). Confirmed against `Utils.cls:SanitizeError` source — its backward-alnum-walk (lines 106-108) would strip a whole `CLUMENU^JRNRESTO` token (not just the leading `^`), so the caret-free reword is the correct fix. (Supersedes the CR 22.1-3 open entry above.)
- **CR 23.0-5 → RESOLVED.** Story 23.1 implemented the per-DB `openFailed` boolean in `HealthCheckDatabases`, distinguishing a genuine `%OpenId` throw (`openFailed:true`) from legitimate `maxSize=0`/`mounted=0` `notApplicable`. Live-verified all 15 DBs return `openFailed:false`.
- **CR 23.0-2 → 23.1-half DONE; 23.2 retains the decision.** `HealthCheckLicense` now emits the IRIS-authoritative `licenseCurrent`/`licenseCurrentPct` (`SYS.Stats.Dashboard`, real properties confirmed) alongside `currentCSPUsers`/`userLimit`. The "prefer the authoritative figure under load" choice remains Story 23.2's (verdict engine).
- **CR 23.0-1 / -3 / -4 / -6 → remain Story 23.2-scope** (TS verdict-engine / request-contract items; already tracked in the story-23.0 section above — NOT re-deferred here).

**New Story-23.1 review findings (all LOW, non-blocking — routed as noted):**

- **[CR 23.1-1 / LOW — for 23.2]** Request-parsing permissiveness in the `HealthCheck` dispatcher: a POST body with `areas` as a JSON string (`{"areas":"journal"}`), a bare JSON array (`["journal"]`), or an empty array (`{"areas":[]}`) all silently fall back to "all 9 areas"; a GET/POST list of only mis-cased or unknown names (`?areas=Journal`, `?areas=cpu`) silently returns an empty `{"areas":{},"errors":{}}` at HTTP 200. **Consistent with the documented design** (`HealthCheckParseAreas` doc: "Unknown names are silently ignored — the TS-layer zod enum (Story 23.2) is the authoritative validator; this endpoint stays permissive") and **benign** (superset or empty, never wrong data; verified live — no crash, valid JSON, HTTP 200 throughout). The sole real consumer is the 23.2 TS tool, which always sends well-formed comma-separated (GET) / `{"areas":[enum,...]}` (POST) input. **Deferral rationale:** the request contract + its authoritative validation belong to Story 23.2. **Suggested resolution:** 23.2 confirm the zod enum fully guards these shapes (recommended — treat the OS endpoint as an internal producer), OR tighten the OS parser to hard-reject/normalize a non-array `areas`. `src/ExecuteMCPv2/REST/Health.cls` HealthCheck ~59-72 / HealthCheckParseAreas ~127-135. Source: Edge Case Hunter #1/#2/#3/#4, Blind Hunter #3.
- **[CR 23.1-2 / LOW — for 23.2]** Unit-test hardening for the health endpoint. The 9 per-area success tests assert field PRESENCE (`%IsDefined`), not values, so a value/logic regression that still populates a defined field of plausible type (e.g. an inverted `ecp.configured`, a per-process sampler in `system`) could pass; the interop success/queue branch (`interopEnabled=1`, `queues`/`queueCount`) is asserted only for presence; and the `HealthCheck` dispatcher's `%request` parsing (POST/GET branch, single-render, %SYS ordering) is structurally not `%UnitTest`-able (documented) and is covered only by the live HTTP smoke. **Coverage is GENUINE, not illusory (Rule 3 satisfied):** the tests drive the REAL handler methods against live IRIS and WOULD fail on a crash / wrong-namespace / missing-field / isolation-leak / gate-leak regression (the two forced-failure tests are real, not mocks), and the dispatcher IS exercised by the live HTTP smoke (Rule #26). This is hardening, not a gap. **Deferral rationale:** Story 23.2 adds the TS verdict-engine unit tests + live smokes over this same endpoint and is the natural place to strengthen. **Suggested resolution:** in 23.2, assert deterministic values where portable (e.g. `interopEnabled=1` + `queueCount` present in the interop-enabled test namespace; a mounted DB's `openFailed=0`) and/or add an HTTP-level dispatcher smoke to CI. `src/ExecuteMCPv2/Tests/HealthCheckTest.cls`. Source: Blind Hunter #2, Edge Case Hunter #5/#6.
- **[CR 23.1-3 / LOW]** Result-set close hygiene in `Health.cls`. `HealthCheckInterop`'s `Ens.Queue:Enumerate` `%ResultSet` is never `.Close()`d, and `HealthCheckDatabases`/`HealthCheckSystem` close (or never close) their result sets only on the success path — so an exception mid-loop skips the close. Impact is minimal: IRIS `%ResultSet`/`%SQL.StatementResult` orefs release their cursors on scope-exit (method return), so there is no cross-call leak — but it is inconsistent with the sibling `Do tDBRS.Close()` in `HealthCheckDatabases` and matches the pre-existing laxity already tracked for `Config.cls:DatabaseList` / `SystemConfig.cls` (CR 11.3). **Suggested resolution:** add `Do:$IsObject(tQRS) tQRS.Close()` after the interop loop and move the databases/system closes to run on the error path too, in a future `Health.cls`/handler-hardening touch. `src/ExecuteMCPv2/REST/Health.cls:246,468`. Source: Blind Hunter #1, Edge Case Hunter #8.
- **[CR 23.1-4 / LOW]** `HealthCheck` discards the `%Status` returned by `HealthCheckParseAreas` (line 73), and every subsequent `If tWant("<area>")` assumes all 9 subscripts exist. Safe today — the parse body is all non-throwing string ops over a static CSV after `Kill pWant`, so it cannot fail post-Kill — but fragile if the CSV/population is ever refactored to bail early (a missing subscript would raise `<UNDEFINED>` into the outer Catch and error the whole request). **Suggested resolution:** either check the returned status or default-read the flags (`If +$Get(tWant("system"))`). `src/ExecuteMCPv2/REST/Health.cls:73`. Source: Blind Hunter #6, Edge Case Hunter #9.
- **[CR 23.1-5 / LOW]** GET repeated `areas` query params (`?areas=journal&areas=license`) read only the first value (`$Get(%request.Data("areas",1))`); the documented contract is comma-separated (`?areas=journal,license`, which works correctly), so a client using the repeated-param convention silently gets only the first area. Minor; no error, partial result. **Suggested resolution:** if leniency is wanted, loop `%request.Data("areas",n)` and join with commas before parsing. `src/ExecuteMCPv2/REST/Health.cls:70`. Source: Blind Hunter #3, Edge Case Hunter #7.

**Dismissed (investigated live, no action):**
- Blind Hunter "POST `areas` array with a non-string element throws `<INVALID OREF>`" — **empirically false**: verified live that `{"areas":[{"x":1}]}` and `{"areas":[123,"journal"]}` degrade gracefully (the element stringifies to a non-matching name and is ignored; `[123,"journal"]` → exactly `journal`), no throw, clean HTTP 200.
- Blind Hunter "`%Library.File.GetDirectorySpace` return treated as `%Status` may zero the volume values" — **verified non-issue**: live `journal.volumeTotalBytes=511067549696` / `volumeFreeBytes=349785391104` (non-zero, byte-identical to Story 23.0's probe), so `GetDirectorySpace` is status-returning and the `$$$ISERR` guard is correct.

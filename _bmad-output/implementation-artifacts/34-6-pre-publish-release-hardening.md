# Story 34.6: Pre-Publish Release Hardening

Status: done

<!-- Created 2026-08-15. Epic 34 re-opened a third time (Rule SC-5) on Project Lead escalation ahead of the first npm publish. -->

## Story

As a **maintainer about to publish this suite publicly**,
I want **the response payload bounded, the epic gate armed so it cannot silently skip at the packaging boundary, and the test-runner guard machine-detectable**,
so that **the first public release cannot ship an unbounded payload, a green build whose gate never ran, or an error state that AI-agent consumers read as success.**

## Why now

`@iris-mcp/*` has **never been published**. Everything sits at `0.0.2` with **9 unreleased changesets** spanning Epics 31–34, and the publish decision has been action item #1 in three consecutive retros. These five items are the subset whose blast radius changes materially the moment the package is public — an unbounded payload reaching real MCP clients, a release built with the gate skipped, and an error state that agent consumers silently misread.

All five are **Epic 34's own review findings**, so they close in Epic 34 rather than deferring to Epic 35.

## Acceptance Criteria

- **AC 34.6.1** (`34-2-R2` + `34-3-R2`, resolved together) — ONE documented output ceiling with an explicit elision marker, applied to **both** `/command` and `/classmethod`. `byRefValues` gains a node/byte budget with a `byRefTruncated` indicator. The ceiling and marker are stated in each tool's description and in the README. **Rule #38 discipline:** the cap truncates the **OUTPUT only** — the target already did the work — and must NOT be advertised as protection against the work itself or against a gateway timeout.
- **AC 34.6.2** (`34-4-R1`) — epic-gate leg (d) must be able to **FAIL** for the property it claims. Replace the namespace-invariant `%SYSTEM.Version.GetVersion()` oracle with a target returning `$NAMESPACE`, asserting `=== "USER"` **and** `!== <configured namespace>` (Rules #34/#40 — the namespace-sensitive area must genuinely differ). Apply to **both** the default-suite gate file and the `custom-rest.integration.test.ts` original.
- **AC 34.6.3** (`34-4-R3`) — the epic gate can no longer silently skip. The fixture probe re-throws anything that is not an `IrisApiError` carrying a not-found envelope (so a credential/network/licence fault fails **loudly** instead of collapsing into "fixtures not deployed"), **and** `IRIS_REQUIRE_LIVE=1` is set in whichever pipeline owns the release. **There is no CI in this repo** (`.github/workflows` absent — verified 2026-08-15), so the packaging/prepublish path is the only enforcement surface: the gate must fail **CLOSED** when packaging a release.
- **AC 34.6.4** (`34-5-R1`) — the zero-result guard is machine-detectable: it carries `structuredContent` with the same `{total, passed, failed, skipped, details, error}` object **and** `isError: true`. **This is a recorded Lead decision — do not re-litigate it, and do not pick only one half.** Applied to **ALL** guard paths including the package discovery-time guard, so no two guard paths diverge.
- **AC 34.6.5** — Rule #19: successful runs and under-ceiling responses are **byte-identical** to today at every level and on both endpoints. Proven mechanically. The ceiling is a deliberate, documented contract change on an unpublished package — state it in the changeset.
- **AC 34.6.6** — Rule #48 mutation evidence per fix (revert → red → restore byte-identically). Specifically: AC 34.6.2's leg (d) shown **RED** when `namespace` is dropped; AC 34.6.3's probe shown to **fail loudly** on a simulated credential fault rather than skipping.
- **AC 34.6.7** — Gates: `pnpm turbo run build test lint type-check` green; `gen:governance-baseline:check` (`:check` ONLY) exit 0 at frozen `1e62c5ad5bf7`/141/201/60; tool counts unmoved (#31); BOOTSTRAP_VERSION from→to recorded and Constraint C-2 re-verified if any bootstrapped class changes (#24; current `34233b5c9f63`); changeset added.

## Tasks / Subtasks

- [x] **Task 1 — Output ceiling on both endpoints** (AC: 34.6.1, 34.6.5)
  - [x] Pick and document ONE ceiling. The ledger suggests 64–256 KB; verify against what the CSP gateway and a real MCP client actually tolerate rather than picking a number by feel (Rule #36).
  - [x] Elision marker must be unambiguous and machine-detectable, not a bare "…".
  - [x] `byRefValues` node/byte budget + `byRefTruncated`.
  - [x] Update both tool descriptions and the README; state the Rule #38 caveat explicitly.
- [x] **Task 2 — Gate leg (d) can fail** (AC: 34.6.2, 34.6.6)
- [x] **Task 3 — Gate fails closed** (AC: 34.6.3, 34.6.6)
  - [x] Probe re-throws non-not-found errors.
  - [x] Arm `IRIS_REQUIRE_LIVE=1` on the release/prepublish path; confirm mechanically that the armed path actually runs it (a variable set in a script nothing invokes is the same defect one layer up).
- [x] **Task 4 — Machine-detectable guard, all paths** (AC: 34.6.4)
- [x] **Task 5 — Gates + changeset** (AC: 34.6.7)
- [x] **Task 6 — Review-continuation (cycle_iteration=2): close 3 HIGH findings** (Review Findings `34-6-CR-5`, `34-6-CR-6`, `34-6-CR-7`)
  - [x] `34-6-CR-5` — cap `returnValue` on `/classmethod` with the same 32768-char ceiling/marker; additive `returnValueTruncated` flag.
  - [x] `34-6-CR-6` — fix byRef byte-budget marker overshoot/data-destruction: never replace a value with a bigger marker; total never exceeds budget; marker matches docs.
  - [x] `34-6-CR-7` — document (not fix) non-ASCII request-body mis-decoding as a known limitation in the READMEs, referencing the ledger item.

## Dev Notes

### The two lead decisions are already made

**AC 34.6.4 — both `structuredContent` and `isError: true`.** The ledger left this as an either/or lead decision; it is settled as **both**. Rationale, so nobody re-opens it: the guard fires only when the caller named a target that produced nothing — a *failed request*, not a clean run — so `isError: true` is truthful; and carrying `structuredContent` keeps `structuredContent.total` reading `0` rather than regressing to `undefined`, which is what an agent consumer reads today. Picking one half leaves a consumer class blind.

**AC 34.6.1 — one ceiling, both endpoints, resolved together.** `34-2-R2` and `34-3-R2` are the same product decision seen from two endpoints; the ledger's own suggested resolution says resolve them together. Do not implement two different ceilings.

### Verified facts about this repo (2026-08-15) — do not re-derive

- **There is no CI.** `.github/workflows` does not exist. `IRIS_REQUIRE_LIVE` currently appears **only** inside `execute-classmethod-epic-gate.test.ts` and in planning/ledger docs — it is armed nowhere. That combination is exactly why AC 34.6.3 exists: today, a release built with IRIS unreachable passes with all six gate legs skipped and the suite green.
- **Nothing is published.** `@iris-mcp/shared` and `@iris-mcp/dev` are both `0.0.2`, `private: false`. 9 pending changesets. So a contract change here costs nothing externally — but it is the last moment that is true.
- Current ledger state: **0 HIGH · 10 MEDIUM · 43 LOW** open (95 items, 42 terminal), all at Rule #37 re-deferral count 1.

### Why `34-3-R2` is a genuine regression risk and not just a duplicate

Before Story 34.3, `Execute()` surfaced a capture `<MAXSTRING>` as a **bounded ERROR**. It now completes and ships the full capture through `%ToJSON` and the CSP gateway — so the unbounded ~3.6 MB success response is **newly reachable on `/command`**. For `/classmethod` this is not new (34.2 already returned success + `truncated` at the ceiling). The ledger's own note says the realistic client outcome at the ceiling is a **transport failure**, which means `truncated` is least visible exactly where it was designed to matter.

### Scope discipline

- Stories 34.0–34.5 stay `done` and are NOT re-touched beyond what these ACs require (Rule #52).
- **Out of scope**, staying in the ledger at Rule #37 count 1: `34-1-R5`, `34-1-R8`, `34-2-R4` (recursion bound), `34-2-R5` (null-device leak), `34-3-R3` (re-entrancy), and all 43 LOW items.
- The npm publish itself is a **separate Project Lead action**, not this story. Do not publish, do not run `changeset version`, do not bump versions.
- No new tool or action (Rule #31) — tool counts and package tool-array length tests must not move.
- Frozen governance baseline untouched (#23/#25) — `:check` only, never the bare generator.

### Constraints

- **Rule #38** — an output cap is not a scan cap. Never advertise it as timeout protection.
- **Rule #19** — under-ceiling behavior byte-identical; prove mechanically per endpoint.
- **Rule #36** — pick the ceiling from measured reality, not intuition.
- **Rule #54** — for each new branch (elision, budget exhaustion, probe re-throw), ask what real input reaches it; and any test fake must return a shape the real API can actually produce.
- **Rule #55** — never generate file content through a shell heredoc; use the file-editing tools.
- **Rule #51** — mechanical tallies only.
- If ObjectScript changes: argumented `Quit` ILLEGAL inside Try/Catch; triple `$$$`; no underscores in method/class-parameter names; never touch a `Storage` section; glob-prefixed `iris_doc_load` (Rule #17); Rule #7 I/O discipline on any render path.

### Testing

- Vitest under `packages/**` (default suite; `packages/iris-dev-mcp/vitest.config.ts` excludes `src/**/*.integration.test.ts`), plus `%UnitTest` under `ExecuteMCPv2.Tests.*` for any ObjectScript change.
- Baselines: `@iris-mcp/dev` 638 tests / 38 files, 0 skipped; `ExecuteMCPv2.Tests` 330/330; `iris-mcp-all` 112/17; turbo 29/29.
- **Rule #35** — compare any `iris_execute_tests` total against a mechanically-counted expectation; never trust a zero.
- Live IRIS reachable: 2026.1 Build 235U; `HSCUSTOM` primary, `USER` second.

### References

- [Source: `_bmad-output/implementation-artifacts/deferred-work.md`] — `34-2-R2`, `34-3-R2`, `34-4-R1`, `34-4-R3`, `34-5-R1` with each raising layer's suggested resolution
- [Source: `_bmad-output/planning-artifacts/epics.md#Story 34.6`] — AC text
- [Source: `packages/iris-dev-mcp/src/__tests__/execute-classmethod-epic-gate.test.ts`] — the gate file (legs and probe)
- [Source: `packages/iris-dev-mcp/src/tools/execute.ts`] — the guards and the response assembly
- [Source: `.claude/rules/project-rules.md`] — #7, #17, #19, #24, #31, #34, #35, #36, #38, #40, #48, #51, #52, #54, #55

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5), via bmad-dev-story workflow.

### Debug Log References

- **Rule #36 ceiling measurement (disposable Node probes, deleted after use):** raw HTTP against the live `/command` endpoint at sizes 8KB–3,000,000 characters — CSP Gateway/HTTP transport returned HTTP 200 with zero corruption/errors/timeouts at every size tested, up to 3,000,000 characters. Separately, a direct `iris_execute_command` call rendering exactly 40,000 characters displayed inline; 50,000 characters (50,031 total with JSON structural overhead) triggered this harness's own "exceeds maximum allowed tokens" redirect-to-file behavior — the concrete, live "real MCP client" data point behind the 32768 ceiling choice (comfortably under the observed ~50K degradation point, above the low end of the ledger's 64–256KB hypothesis but justified in writing at every ceiling-adjacent doc site as a deliberate deviation from that hypothesis, backed by the measurement).
- **Rule #16 probe (disposable class `ExecuteMCPv2.Temp.Story346OutputProbe`, created/compiled/tested/deleted from server AND disk, deletion verified via `iris_doc_list`):** confirmed that a caller omitting a trailing `Output` ObjectScript parameter is safe — the callee's `Set` on the unbound formal has no error and no effect — validating the design decision to add `InvokeWithArgs`' new `pByRefTruncated` as an optional trailing parameter without touching the 30+ pre-existing call sites in `ClassMethodArgsTest.cls`.
- **Live end-to-end verification (disposable Node probe against `/command` and `/classmethod`, deleted after use):** all six new behaviors confirmed against real IRIS (HSCUSTOM) — `/command` output truncated to exactly 32768 chars with the marker and `truncated:true`; `/classmethod` output likewise; a single huge `byRefValues` leaf (40000 chars) truncated to ≤32768 with `byRefTruncated:true` and `truncated:false` (independent flags); 1500 written subscripts capped to 999 surviving entries (1 node consumed by the root) with `subscriptsTruncated:true`; an under-ceiling call byte-identical with the new `byRefTruncated:false` field additively present; `TargetReturnNamespace` genuinely differing between `HSCUSTOM` and `USER` calls.
- **Mutation evidence (Rule #48), each independently — revert → red → restore byte-identical (`md5sum` confirmed) → green:**
  - AC 34.6.2: epic-gate leg (d) — omitted the `namespace` argument → RED (`expected 'HSCUSTOM' to be 'USER'`) → restored → 6/6 green.
  - AC 34.6.3: `fixtureIsDeployed`'s discrimination — reverted to the pre-fix catch-all → both new RE-THROWS tests RED (`promise resolved "false" instead of rejecting`) → restored → 9/9 green.
  - AC 34.6.3 packaging path — confirmed genuinely wired into the REAL `pnpm publish` lifecycle (not just my own script): `pnpm publish --dry-run --no-git-checks` with IRIS reachable ran `prepublishOnly` (visible pnpm banner + live gate output) and proceeded to a full tarball listing; the SAME command with `IRIS_PASSWORD` overridden to a wrong value exited 1 with `ELIFECYCLE Command failed with exit code 1` and produced ZERO tarball output (grep-confirmed absent) — the release aborts before packaging even begins, not just before upload. No `.tgz` artifact or registry write occurred in either run (dry-run only).
- **Suite counts, mechanically derived (Rule #35/#51):** `@iris-mcp/dev` 638→643 (+5: 3 new `fixtureIsDeployed` discrimination tests, 2 new `byRefTruncated` pass-through tests — no new `it()` blocks added elsewhere, only assertions added to existing ones for AC 34.6.4); `ExecuteMCPv2.Tests` 330→342 (+12: 6 `UtilsTest.TestApplyOutputCeiling*`, 5 `ClassMethodArgsTest` incl. the new fixture sanity test, 1 `CommandTest`); `iris-mcp-all` 112/17 unmoved (Rule #31 — no tool/action added); turbo 29/29.
- **Pre-existing, unrelated failure investigated and ruled out-of-scope:** `packages/iris-dev-mcp/src/__tests__/tools.integration.test.ts` (an `*.integration.test.ts` file this story never touched) fails one assertion (`Test.MCPIntegration.Temp` not found in a paginated `iris_doc_list` response) both with and without this story's changes — confirmed via `git stash` reverting all Story 34.6 source changes and re-running the file in isolation, which reproduced the identical failure on the unmodified baseline. Not part of the required gates (`pnpm turbo run test` never invokes `test:integration`); not fixed (Rule #52 scope discipline).

### Completion Notes List

- **AC 34.6.1 (ceiling + budget):** `ExecuteMCPv2.Utils` gained `OUTPUTCEILING` (32768, char-measured via `$Length`) and `BYREFNODECEILING` (1000) class parameters, plus `ApplyOutputCeiling(pOutput, ByRef pTruncated, pCeiling=-1)` — truncates to the ceiling with a structured `[IRIS-MCP-TRUNCATED ceiling=Nchars]` marker (content trimmed to leave room for the marker so total length never exceeds the ceiling), only ever SETS `pTruncated` (never clears an existing true, so callers OR it in for free by passing their flag ByRef). `BuildByRefNode` gained `pNodeBudget`/`pByteBudget`/`pByRefTruncated` ByRef parameters threaded across all 20 top-level positions from ONE shared pair of counters initialized in `InvokeWithArgs` — a node budget bounding tree width/depth and a byte budget (reusing `ApplyOutputCeiling` with an explicit remaining-budget ceiling, avoiding double-truncation) bounding total leaf-value content; a cut-short container is marked `subscriptsTruncated: true`. `InvokeWithArgs` gained an 8th, OPTIONAL, additive trailing `Output pByRefTruncated` parameter (live-probe-verified safe to omit at existing call sites). `Command.cls`'s `Execute()` and `ClassMethod()` both apply `ApplyOutputCeiling` to the captured `output` after capture completes (never during — the existing `<MAXSTRING>` capture-overflow mechanism from Story 34.3 is untouched and orthogonal); `ClassMethod()` additionally threads `byRefTruncated` into both the success envelope and both error envelopes (the `$$$ISERR(tSC)` branch and the outer `Catch`), for shape symmetry with `truncated`. TS-side, `execute.ts`'s two tool descriptions document the ceiling, marker format, budget, and the Rule #38 caveat verbatim; the tools themselves needed no logic change (pure pass-through of whatever fields the server sends) — proven by a new pass-through test using an over-ceiling mocked envelope. README updated at both tool entries plus both JSON output examples (now showing `byRefTruncated`).
- **AC 34.6.2:** `ClassMethodArgsFixture` gained `TargetReturnNamespace() { Quit $NAMESPACE }`. Epic-gate leg (d) and the `custom-rest.integration.test.ts` original both now call it with `namespace:"USER"` and assert the returned value both `=== "USER"` and `!== ctx.resolveNamespace()` (the configured default, `HSCUSTOM`).
- **AC 34.6.3:** `fixtureIsDeployed` in the epic-gate file now re-throws any caught error that is not `error instanceof IrisApiError && error.statusCode === 404` (matching the pre-existing `iris_doc_get`/`integration-setup.ts` convention for the identical check) instead of collapsing every failure into `false`. `packages/iris-dev-mcp/package.json` gained a `prepublishOnly` script pointing at a new `scripts/prepublish-gate.mjs`, which sets `IRIS_REQUIRE_LIVE=1` in the child environment and runs the epic-gate file via `pnpm exec vitest run` — spawned through `cmd.exe /d /s /c` on Windows (mirroring the established Story 32.2 fix for the identical `spawn()`-cannot-execute-a-`.cmd`-shim class of failure) rather than an inline `VAR=1 command` package.json script (which is POSIX-only shell syntax and silently fails on Windows, this repo's own dev/publish platform, without adding an otherwise-unneeded `cross-env` dependency).
- **AC 34.6.4:** New `zeroResultGuardResponse(error: string): ToolResult` helper in `execute.ts`, called from BOTH the package discovery-time guard and the shared post-run guard (covering all three levels) — returns `{content: [...], structuredContent: {total:0,passed:0,failed:0,skipped:0,details:[],error}, isError: true}`. All pre-existing guard-shape tests in `execute.test.ts` updated to assert the new envelope (message TEXT itself unchanged — Rule #19 applies to the wording, not to the deliberate AC 34.6.4 shape change, which is a recorded Lead decision, not a back-compat-constrained surface).
- **AC 34.6.5:** Proven mechanically at every level — live HTTP spot-checks (under-ceiling `%SYSTEM.Encryption.Base64Encode` call byte-identical plus additively-present `byRefTruncated:false`), `%UnitTest` (`TestApplyOutputCeilingUnderCeilingUnchanged`/`ExactlyAtCeilingUnchanged`), and TS unit tests (`execute.test.ts`'s existing back-compat suite untouched in behavior, new pass-through/absent-field tests added).
- **AC 34.6.6:** See Debug Log References — three independent mutation cycles (leg (d), probe discrimination, packaging fail-closed) all revert→red→restore-byte-identical→green.
- **AC 34.6.7:** `pnpm turbo run build test lint type-check` 29/29; `gen:governance-baseline:check` (`:check` ONLY) exit 0, frozen `1e62c5ad5bf7`/141/201/60 unchanged; no tool/action added (`iris-mcp-all` 112/17 unmoved); `BOOTSTRAP_VERSION` `34233b5c9f63` → `ff5029239b07` (29 classes, same order/count — `Utils.cls`/`Command.cls` content changed, no roster change) regenerated via `pnpm run gen:bootstrap` and confirmed idempotent (re-run produces the identical hash); Constraint C-2 re-verified after the `Command.cls` recompile (`iris_doc_list(filter="ExecuteMCPv2.REST.Command", generated=true)` → exactly one `.1.int`); changeset `execute-response-ceiling-and-gate-hardening.md` added covering both `@iris-mcp/shared` and `@iris-mcp/dev`; `CHANGELOG.md` extended (three new subsections under the existing pre-release Epic 34 entry, since nothing has published yet); `deferred-work.md` gained the Story 34.6 terminal-disposition table closing all five escalated items as RESOLVED.
- Deferred-work ledger updated: all five items escalated to this story (`34-2-R2`, `34-3-R2`, `34-4-R1`, `34-4-R3`, `34-5-R1`) closed TERMINAL as RESOLVED. Ledger state after this story: 0 HIGH / 5 MEDIUM / 43 LOW open (95 items, 47 terminal).
- No probe classes or scripts left behind: `ExecuteMCPv2.Temp.Story346OutputProbe` deleted from server (confirmed via `iris_doc_list`) and disk; all disposable Node HTTP probe scripts deleted from the scratchpad.

### File List

- `src/ExecuteMCPv2/Utils.cls`
- `src/ExecuteMCPv2/REST/Command.cls`
- `src/ExecuteMCPv2/Tests/ClassMethodArgsFixture.cls`
- `src/ExecuteMCPv2/Tests/ClassMethodArgsTest.cls`
- `src/ExecuteMCPv2/Tests/CommandTest.cls`
- `src/ExecuteMCPv2/Tests/UtilsTest.cls`
- `packages/iris-dev-mcp/src/tools/execute.ts`
- `packages/iris-dev-mcp/src/__tests__/execute.test.ts`
- `packages/iris-dev-mcp/src/__tests__/execute-classmethod-epic-gate.test.ts`
- `packages/iris-dev-mcp/src/__tests__/custom-rest.integration.test.ts`
- `packages/iris-dev-mcp/package.json`
- `packages/iris-dev-mcp/scripts/prepublish-gate.mjs` (new)
- `packages/iris-dev-mcp/README.md`
- `packages/shared/src/bootstrap-classes.ts` (regenerated — `BOOTSTRAP_VERSION` `34233b5c9f63` → `ff5029239b07` → `e95957368a12` in the review-continuation pass)
- `CHANGELOG.md`
- `.changeset/execute-response-ceiling-and-gate-hardening.md` (new)
- `_bmad-output/implementation-artifacts/deferred-work.md` (terminal disposition for `34-2-R2`, `34-3-R2`, `34-4-R1`, `34-4-R3`, `34-5-R1`; plus the QA-pass and code-review finding sections)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (status tracking)
- `_bmad-output/implementation-artifacts/cycle-log-epic-34.md`

Added by the QA pass (were missing from this list — code-review finding `34-6-CR-20`):

- `scripts/verify-iris-reachable.mjs` (new)
- `packages/shared/package.json`, `packages/iris-admin-mcp/package.json`, `packages/iris-data-mcp/package.json`, `packages/iris-interop-mcp/package.json`, `packages/iris-ops-mcp/package.json`, `packages/iris-mcp-all/package.json` (`prepublishOnly` wiring)
- `packages/iris-dev-mcp/src/__tests__/tools.integration.test.ts`

Added by the code review:

- `packages/client-config/package.json` (`prepublishOnly` wiring — finding `34-6-CR-1`)

Added/touched by the review-continuation pass (2026-08-15, cycle_iteration=2 — closing `34-6-CR-5`, `34-6-CR-6`, `34-6-CR-7`; all other files above were already in this list and were touched again this pass):

- `README.md` (root — new "Non-ASCII Request-Body Content Is Mis-Decoded" subsection under Known Limitations, `34-6-CR-7`)

Touched by the re-review pass (2026-08-15, cycle_iteration=2 — patching `34-6-CR2-1` … `34-6-CR2-5`):

- `src/ExecuteMCPv2/Utils.cls` (byRefValues byte budget now charges subscript keys and OREF placeholders)
- `src/ExecuteMCPv2/Tests/ClassMethodArgsTest.cls` (2 new `%UnitTest` methods; one tautological assertion strengthened)
- `packages/iris-dev-mcp/src/tools/execute.ts` (byRef elision contract corrected in the tool description)
- `packages/iris-dev-mcp/README.md` (same correction)
- `scripts/verify-iris-reachable.mjs` (stale "deliberately NOT wired into client-config" banner corrected)
- `.changeset/execute-response-ceiling-and-gate-hardening.md` (same correction; `@iris-mcp/client-config` added to frontmatter; two new paragraphs)
- `CHANGELOG.md` (new "Fixed — the `byRefValues` character budget did not bound `byRefValues`" entry)
- `packages/shared/src/bootstrap-classes.ts` (regenerated — `BOOTSTRAP_VERSION` `e95957368a12` → `f2d14153e102`)

## Change Log

- 2026-08-15: Story implemented — response-payload ceiling (32768 chars, `ExecuteMCPv2.Utils.ApplyOutputCeiling`) applied to `output` on both `/command` and `/classmethod`, plus a combined 1000-node/32768-char `byRefValues` budget surfaced as a new `byRefTruncated` field; epic-gate leg (d) replaced its namespace-invariant oracle with a genuinely namespace-sensitive one; the epic gate's fixture probe now re-throws non-404 errors and `IRIS_REQUIRE_LIVE=1` is armed on `@iris-mcp/dev`'s `prepublishOnly` hook (mechanically confirmed via `pnpm publish --dry-run` to abort packaging before a tarball is produced when IRIS is unreachable); `iris_execute_tests`' zero-result guard now carries both `isError:true` and `structuredContent` on every guard path. All five items escalated to this story from the deferred-work ledger (`34-2-R2`, `34-3-R2`, `34-4-R1`, `34-4-R3`, `34-5-R1`) closed TERMINAL as RESOLVED. Gates: `pnpm turbo run build test lint type-check` 29/29; `@iris-mcp/dev` 638→643 tests; `ExecuteMCPv2.Tests` 330→342; `gen:governance-baseline:check` unchanged (frozen `1e62c5ad5bf7`/141/201/60); `BOOTSTRAP_VERSION` `34233b5c9f63`→`ff5029239b07`; Constraint C-2 re-verified; no tool/action count moved. Changes left UNCOMMITTED for the lead's per-story smoke gate + commit.
- 2026-08-15 (review-continuation pass, cycle_iteration=2): closed the 3 HIGH findings the code review returned this story to `in-progress` over. `34-6-CR-5` — `returnValue` on `/classmethod` was completely unbounded; now capped at the same 32768-char ceiling/marker as `output`, with a new additive `returnValueTruncated` flag (present, defaulted false, on error envelopes too). `34-6-CR-6` — the `byRefValues` shared byte budget could destroy small real values by replacing them with elision markers LARGER than the value itself, and could overshoot its own documented budget; fixed in two parts, both necessary: `ApplyOutputCeiling` now falls back to a marker-less hard cut whenever the marker itself would not fit the remaining ceiling, and a new `AddByRefPosition` helper gives all 20 top-level `byRefValues` positions the same budget pre-check the recursive subscript walk already had, omitting a position entirely once the budget is already exhausted rather than materializing a misleading placeholder. `34-6-CR-7` — non-ASCII request-body mis-decoding documented (not code-fixed, per the review's own explicit call) as a new "Known Limitations" section in the suite root README and a matching note in `@iris-mcp/dev`'s README. All three pinned at the REAL deployed `/classmethod` endpoint (3 new legs — (e-2), (e-3), (e-4) — in `execute-classmethod-epic-gate.test.ts`, the same file `prepublish-gate.mjs` runs) and mutation-verified independently (revert → red → restore byte-identical, `md5sum`-confirmed → green) — including discovering, via mutation testing, that CR-6's two fix halves needed two SEPARATE endpoint-level scenarios (leg (e-3) for the top-level-omission half, leg (e-4), added mid-pass, for the marker-vs-ceiling half) since neither scenario alone exercised both halves. Gates: `pnpm turbo run build test lint type-check` 29/29; `ExecuteMCPv2.Tests` 343→346; `@iris-mcp/dev` 644→649; `gen:governance-baseline:check` unchanged (141/201/60); `BOOTSTRAP_VERSION` `ff5029239b07`→`e95957368a12`, idempotent; Constraint C-2 re-verified; `iris-mcp-all` 112/17 and tool/action counts unmoved. Ledger (`deferred-work.md`): `34-6-CR-5`/`34-6-CR-6` RESOLVED, `34-6-CR-7` CLOSED-BY-DECISION — ledger state 0 HIGH / 11 MEDIUM / 50 LOW = 61 open across 117 distinct items (56 terminal). Status set to `review`. Changes left UNCOMMITTED for the lead's per-story smoke gate + commit.

### QA Results (2026-08-15, bmad-qa-generate-e2e-tests)

**Priority 1 — the 32768-char ceiling.**

- **Basis challenged and upheld.** The ceiling is grounded in a genuinely live measurement (this session's own MCP client diverting inline output to a file at 50,031 characters), not intuition, and the deviation from the ledger's 64–256 KB hypothesis is stated explicitly, in writing, at every ceiling-adjacent doc site (class banner, both tool descriptions, README ×2, CHANGELOG) — never silently overridden. Verdict: **defensible as a conservative default**, correctly and repeatedly caveated as fitted to one observed client rather than a universal constant.
- **Configurability: none exists** (confirmed by reading `Utils.cls`/`Command.cls`/`execute.ts` — no env var, no per-call parameter; `OUTPUTCEILING` is a hard-coded class `Parameter`). This is a real, if minor, usability trade-off (a legitimate large capture — e.g., full `%UnitTest.Manager.RunTest` narration — is now hard-capped with no escape hatch, where previously it was unbounded). Judged **in-scope and acceptable for this story**: AC 34.6.1's own text calls for "ONE documented output ceiling," and Dev Notes explicitly reject two different ceilings — extending to a *configurable* ceiling would be additional scope beyond what was asked. Flagged here as a residual design question for a future story, not fixed.
- **Rule #38 verified compliant** across all four doc surfaces (class banner, both `execute.ts` tool descriptions, `README.md` ×2, `CHANGELOG.md`) — every site states explicitly that the ceiling is not Web Gateway timeout or execution-cost protection, consistent with the dev's own transport measurement (clean to 3,000,000 characters).
- **UTF-8/surrogate-pair boundary safety — tested live, specifically, as instructed.** Live investigation (`iris_execute_command`, `iris_global_set`, a disposable `ExecuteMCPv2.Temp.Story346UnicodeBoundaryProbe` — compiled, exercised, deleted from server and disk, deletion verified) found: (a) IRIS's `$Char`/$Length` semantics are genuinely UTF-16-code-unit-based (not full-code-point-based — `$Char(128512)` for the full U+1F600 codepoint is invalid/out-of-range on this instance; a real astral character requires a 2-unit surrogate pair, `$Char(55357)_$Char(56832)`); (b) a truncation cut landing EXACTLY between the high and low surrogate half — the worst case for "epic's original bug class" corruption — does **not** break the response: `%ToJSON` does not throw on the resulting lone/unpaired surrogate, and `%FromJSON` re-parses the truncated envelope cleanly (verified across a ceiling sweep guaranteeing both a mid-pair and a clean-boundary cut). Permanently pinned as `UtilsTest.TestApplyOutputCeilingMidSurrogatePairStaysValidJSON` (new) — the pre-existing `TestCaptureMetacharsAndUnicode` Rule #58 coverage embeds Unicode content well below any ceiling, so it could never have caught a truncation-boundary defect.
- **Separate, pre-existing finding (NOT a Story 34.6 defect, NOT fixed — out of scope, recorded for visibility only):** non-ASCII/astral content submitted through the JSON request body of these custom REST endpoints (`/command`'s `command` field, `/classmethod`'s `args`, `iris_global_set`'s `value` — confirmed on two independent endpoints sharing `ReadRequestBody`, which 14 REST handler classes use) arrives mis-decoded — e.g. 😀 becomes 4 separate garbled BMP characters matching its raw UTF-8 byte values (240/159/152/128), the classic UTF-8-read-as-Latin-1 pattern. Content that *originates* server-side (compiled class-source literals, like the existing Rule #58 fixtures) round-trips correctly on the way OUT (verified: `TargetWriteMetachars`, called with no Unicode input from the caller, returns "héllo-世界-😀" correctly). This is orthogonal to the ceiling logic (confirmed it does not corrupt truncation at any cut point either way) and predates this story (`ReadRequestBody`/`%FromJSON` are untouched by Story 34.6's File List) — **not fixed** here per Rule #52 scope discipline; a proper fix requires researching IRIS's CSP request-body charset negotiation, which is its own investigation. Not ledgered as a numbered item (out of this pass's mandate to resolve), but stated here in enough detail to be found and scoped later.

**Priority 2 — the prepublish gate.**

- Confirmed independently (not just re-trusting the dev's report) that `prepublishOnly` fires in the REAL `pnpm publish --dry-run` lifecycle and fails closed on a simulated credential fault (fresh `pnpm publish --dry-run --no-git-checks` run with a deliberately wrong `IRIS_PASSWORD`: gate output visible, exit 1, zero tarball).
- **Confirmed the suspected gap, live, with concrete evidence — this was a real, HIGH-severity hole.** `.changeset/config.json` groups every `@iris-mcp/*` package as `"fixed"` (all bump/publish together); `pnpm --filter @iris-mcp/shared publish --dry-run --no-git-checks` packaged a complete tarball with **zero** gate output (no `prepublishOnly` at all on that package). Worse, `pnpm -r --filter @iris-mcp/dev --filter @iris-mcp/shared publish --dry-run --no-git-checks` with a bad password showed `@iris-mcp/shared` — a dependency of every other package — publishes to completion in dependency-topological order **before** `@iris-mcp/dev`'s gate ever runs and fails. In a real release, `@iris-mcp/shared` (and, standalone, `@iris-mcp/admin`/`@iris-mcp/data`/`@iris-mcp/interop`/`@iris-mcp/ops`/`@iris-mcp/all`) would already be on the registry by the time that failure is observed.
- **Fixed.** New `scripts/verify-iris-reachable.mjs` (minimal, unconditional IRIS-reachability check — reuses `@iris-mcp/shared`'s built `loadConfig`/`IrisHttpClient`/`ping`, mirrors the existing dist-import pattern in `scripts/lib/tool-catalog.mjs`) wired as `prepublishOnly` on `@iris-mcp/shared`, `@iris-mcp/admin`, `@iris-mcp/data`, `@iris-mcp/interop`, `@iris-mcp/ops`, `@iris-mcp/all`. Deliberately **not** wired into `@iris-mcp/client-config` — no IRIS dependency at all (local client-config-file manager); gating it on IRIS reachability would be a mismatched requirement.
- **Mutation-verified (Rule #48):** the identical bad-credential recursive dry-run that previously produced a complete `@iris-mcp/shared` tarball now produces **zero** tarballs (grep-confirmed) — `@iris-mcp/shared`'s own new gate catches it before `@iris-mcp/dev` is even reached. A good-credential recursive run still publishes both packages cleanly (2 tarballs, both gates ran and passed). Spot-checked independently on `@iris-mcp/ops`; `@iris-mcp/client-config`'s package.json confirmed untouched (`git diff --stat` empty).
- Note for the record: on the failure path, the top-level `node scripts/verify-iris-reachable.mjs` process (unlike `@iris-mcp/dev`'s own gate, which isolates its live check inside a spawned vitest subprocess) hits Node's known Windows `UV_HANDLE_CLOSING` teardown assertion when exiting with an open keepalive socket — already documented in this repo's Rule #22 as benign teardown noise. The reported exit code is a large crash-style number instead of a clean `1`, but it is still reliably non-zero, so `pnpm`/`npm` still correctly treat it as a failure (`ELIFECYCLE Command failed`) and packaging still aborts. Not fixed further — eliminating it would mean restructuring the script into a subprocess-isolated shape for cosmetic benefit only.
- Rule #54 discrimination check verified live: `fixtureIsDeployed`'s `IrisApiError.statusCode === 404` reuse of the `doc.ts`/`iris_doc_get` pattern was cross-checked against a genuinely nonexistent class (`iris_doc_get` metadataOnly on a fabricated class name returns a clean `exists:false`, confirming the real Atelier `/doc/` endpoint's 404 shape matches what the discrimination logic expects); `http-client.ts`'s auto-401-retry-then-throw path confirmed to genuinely produce `IrisApiError` with `statusCode===401` for a credential fault (read, not just assumed).

**Priority 3.**

- **AC 34.6.5 / Rule #19 — verified independently, per endpoint, live** (the dev's own spot-check evidence was `/classmethod`-specific): `iris_execute_command` with `Write "hello world under ceiling"` → `{"output":"hello world under ceiling","truncated":false}` (byte-identical); `iris_execute_classmethod` on `%SYSTEM.Encryption.Base64Encode("hello")` → `{"returnValue":"aGVsbG8=",...,"truncated":false,"byRefTruncated":false}` (correct value, additive fields present). Both endpoints now independently confirmed byte-identical under ceiling.
- **AC 34.6.4 — verified.** Unit-test coverage in `execute.test.ts` is comprehensive and exercises the REAL handler (only the HTTP client is mocked): package discovery-time guard, package run-time-zero guard, class-level guard, and method-level guard each assert `isError:true` and `structuredContent.total === 0` (via `expect(result.structuredContent).toEqual(parsed)` with `parsed.total` asserted `0`). Both guard call sites route through the SAME `zeroResultGuardResponse()` helper (static-code confirmation the paths cannot diverge).
- **AC 34.6.2 — mutation-verified independently** (not re-trusting the dev's report): omitted the `namespace` argument from leg (d)'s call in `execute-classmethod-epic-gate.test.ts` → RED (`expected 'HSCUSTOM' to be 'USER'`) → reverted, md5sum-confirmed byte-identical (`d37788337d8896e76a1cf8a081a44e27` before and after) → re-ran, 9/9 green.
- **Rule #24/C-2:** `BOOTSTRAP_VERSION ff5029239b07` confirmed idempotent (`pnpm run gen:bootstrap` re-run produces byte-identical output — `git diff --stat` unchanged before/after). `iris_doc_list(filter="ExecuteMCPv2.REST.Command", generated=true)` → exactly one `.1.int` (Constraint C-2 holds).
- **Rule #25:** `gen:governance-baseline:check` (`:check` only) exit 0 — 141 frozen / 201 live / 60 post-foundation, matching the frozen baseline exactly.
- **Rule #31:** `iris-mcp-all` 112 tests / 17 files, unmoved.
- **Rule #35 — totals compared mechanically, never trusted.** `ExecuteMCPv2.Tests` package run: reported `total:343` vs. mechanically grepped `343` `Method Test*(` declarations across all 29 `.cls` files in `src/ExecuteMCPv2/Tests/` — exact match, 343/343 passed (342 pre-QA + 1 new). `@iris-mcp/dev` vitest: 643/643 (unmoved by this QA pass — no TS test files added/removed, only assertions and one pre-existing file fixed). `pnpm turbo run build test lint type-check`: 29/29.
- **The pre-existing `tools.integration.test.ts` failure — was NOT properly closed by the dev (prose-only, never fixed, never ledgered — exactly the "normalizing known test failures" anti-pattern flagged for this QA pass).** Independently re-confirmed via `git stash` (all uncommitted changes, dev's and QA's, stashed together back to the last commit) that the identical failure (`"iris_doc_list with category=CLS includes the test class"`, line 167) reproduces on the pristine baseline; stash popped, all changes restored. **Fixed**: the test called `iris_doc_list` with `{category:"CLS"}` and no `filter` — an unscoped scan whose first (paginated) page no longer includes the created test class as system-class counts grew, exactly the footgun `iris_doc_list`'s own tool description warns about. Added `filter: "Test.MCPIntegration"`, matching the tool's documented usage guidance. Verified: 19/19 (was 18/19). Recorded in `deferred-work.md` as `34-6-QA-2` (LOW, RESOLVED) so it is never mistaken for an outstanding gap.

**Test coverage added:**
- `src/ExecuteMCPv2/Tests/UtilsTest.cls` — `TestApplyOutputCeilingMidSurrogatePairStaysValidJSON` (1 new `%UnitTest` method; runs in the required `ExecuteMCPv2.Tests` gate).
- `packages/iris-dev-mcp/src/__tests__/tools.integration.test.ts` — existing test fixed (scoped with `filter`); not part of any required default-suite gate (`*.integration.test.ts` is excluded by `vitest.config.ts`), so this does not change any reported default-suite count.

**Product/process fixes applied (beyond the dev's own scope):**
- `scripts/verify-iris-reachable.mjs` (new) + `prepublishOnly` added to `packages/shared/package.json`, `packages/iris-admin-mcp/package.json`, `packages/iris-data-mcp/package.json`, `packages/iris-interop-mcp/package.json`, `packages/iris-ops-mcp/package.json`, `packages/iris-mcp-all/package.json` — closes the prepublish-gate coverage gap (Priority 2).
- `.changeset/execute-response-ceiling-and-gate-hardening.md` — package list extended to the five newly-gated packages; new paragraph documenting the gate-coverage fix.
- `_bmad-output/implementation-artifacts/deferred-work.md` — new "QA-pass findings" section (`34-6-QA-1` prepublish-gate coverage, RESOLVED; `34-6-QA-2` `tools.integration.test.ts`, RESOLVED).

**Final gates (after the QA pass, superseded by the code-review gates below):** `pnpm turbo run build test lint type-check` 29/29 successful (fresh run, post-fix); `ExecuteMCPv2.Tests` package 343/343 (mechanically cross-checked); `gen:governance-baseline:check` exit 0, 141/201/60 unchanged; `BOOTSTRAP_VERSION` unchanged and idempotent; Constraint C-2 holds; tool counts unmoved. No probe classes or scripts left behind (`ExecuteMCPv2.Temp.Story346UnicodeBoundaryProbe` deleted from server and disk, deletion verified via `iris_doc_list`). All changes left UNCOMMITTED, as instructed.

### Review Findings (2026-08-15, bmad-code-review — 3 adversarial layers, reviewed as a RELEASE GATE)

All three layers (Blind Hunter · Edge Case Hunter · Acceptance Auditor) DELIVERED an explicit findings payload within the Rule #57 20-minute hard bound (~10.5 / ~14.8 / ~12.3 min from launch), against a frozen diff snapshot. **`failed_layers` is empty — this close is NORMAL, not degraded.** 20 findings after dedup/triage: **4 patched, 16 deferred, 0 dismissed, 0 decision-needed.** Full detail, severity, rationale and suggested resolution for every item is in `deferred-work.md` under "Code-review findings — Story 34.6" (`34-6-CR-1` … `34-6-CR-20`).

Every finding was independently re-verified LIVE by the reviewing lead against real IRIS (2026.1 Build 235U, `HSCUSTOM`/`USER`) — dry-run publishes only, no registry write; all live mutations restored byte-identically (`md5sum`-confirmed) and recompiled.

**Patched during review (applied and verified):**

- [x] [Review][Patch] **`@iris-mcp/client-config` was publishable, ungated, and FIRST in publish order** — the same partial-release hole `34-6-QA-1` closed, reproduced one package over [`packages/client-config/package.json`] — `34-6-CR-1` (HIGH). Live: a bad-credential `pnpm -r publish --dry-run` produced a complete 278.3 kB tarball with zero gate output before `@iris-mcp/shared`'s gate fired. Fixed by wiring `verify-iris-reachable.mjs`; mutation-verified **0 tarballs** now (was 1), good-credential run still publishes all 8, exit 0.
- [x] [Review][Patch] **The response-payload ceiling had ZERO regression coverage at its integration point** [`packages/iris-dev-mcp/src/__tests__/execute-classmethod-epic-gate.test.ts`] — `34-6-CR-2` (HIGH). Proven by deleting both `ApplyOutputCeiling` calls from `Command.cls` and recompiling: the live `/command` endpoint returned **50,000 untruncated characters** while `CommandTest` (18/18), the full dev suite (**643/643**) and `custom-rest.integration` (12/12) all stayed GREEN. Fixed by adding **leg (e)**, which drives BOTH deployed endpoints and asserts over-ceiling and under-ceiling byte-identity; it is in the default suite AND the file `prepublish-gate.mjs` runs, so the pin is armed on the packaging path. Mutation-verified RED (`expected 50000 to be 32768`).
- [x] [Review][Patch] **Epic-gate leg (d) had two false-RED modes that would BLOCK the release** [`execute-classmethod-epic-gate.test.ts`, `custom-rest.integration.test.ts`] — `34-6-CR-3` (MEDIUM). (a) Hard-coded `"USER"` made the two assertions mutually unsatisfiable under `IRIS_NAMESPACE=USER` — this package's own README-documented default (live: `expected 'USER' not to be 'USER'`). (b) The leg called a fixture in a namespace the `beforeAll` probe never checks, resolving here only via a DB mapping. Fixed in BOTH files: the second namespace is DERIVED and the target is the always-present `%SYSTEM.SYS.NameSpace`. AC 34.6.2's mutation property is preserved and **strengthened** — dropping `namespace` is now RED under both configurations.
- [x] [Review][Patch] **Two TS fixtures encoded shapes the real server cannot produce (Rule #54)** [`packages/iris-dev-mcp/src/__tests__/execute.test.ts:529,535`] — `34-6-CR-4` (LOW). `32728 + 41`-char marker = 32769 (one over an unreachable ceiling), and a `ceiling=20` marker alongside 20 retained chars when the real code emits the marker alone. Corrected against live-pinned arithmetic (`marker = 36 + digits(ceiling)`), with a self-checking length assertion.

**Resolved in review-continuation pass (2026-08-15, cycle_iteration=2) — the 3 HIGHs the review returned this story to `in-progress` over:**

- [x] [Review][Resolved] **`returnValue` is completely unbounded on `/classmethod`** — `34-6-CR-5` (**HIGH**). Fixed: `ExecuteMCPv2.REST.Command`'s `ClassMethod()` now applies `ApplyOutputCeiling` to `returnValue` — the primary payload field — with the same documented ceiling/marker as `output`, surfaced via a new additive `returnValueTruncated` boolean (present, defaulted false, on both error envelopes too). Pinned at the REAL deployed `/classmethod` endpoint via new leg (e-2) in `execute-classmethod-epic-gate.test.ts`. Mutation-verified RED when the `ApplyOutputCeiling` call on `tReturnStr` is removed; restored byte-identically, recompiled, green. Full evidence: `deferred-work.md` → "Terminal disposition — Story 34.6 review-continuation items".
- [x] [Review][Resolved] **byRef byte-budget exhaustion DESTROYS subsequent small values and replaces them with LARGER markers** — `34-6-CR-6` (**HIGH**). Fixed in two parts, both necessary: `ApplyOutputCeiling` now falls back to a marker-less hard cut whenever the elision marker itself would not fit in the remaining ceiling (a result is now provably never longer than the ceiling it was given, nor longer than the value it replaces — pinned by a new sweeping `%UnitTest`, `TestApplyOutputCeilingNeverExceedsCeilingOrOriginalLength`, ceilings 0..60); and a new `AddByRefPosition` helper wraps all 20 top-level `byRefValues` positions with the same budget pre-check the recursive subscript walk already had, OMITTING a position entirely once the shared budget is already exhausted (never a misleading empty-string placeholder). Reproduced the exact review scenario (`Target20`: one huge value then `"42"`/`"hello"`/`"x"`) at both `%UnitTest` and the REAL deployed `/classmethod` endpoint (new leg (e-3)); positions 1-3 are now omitted, never destroyed, and total `byRefValues` content never exceeds 32768. Mutation-verified RED independently for each half of the fix; restored byte-identically, recompiled, green.
- [x] [Review][Documented, not code-fixed] **Non-ASCII request bodies silently mis-decoded across all 14 REST handlers** (46 call sites, both counts mechanical) — `34-6-CR-7` (**HIGH**). Per the review's own explicit call, NOT code-fixed here (pre-existing, cross-cutting, out of Rule #52 scope) — instead documented prominently: a new "Known Limitations" section in the suite root `README.md` (mechanism, affected tools, at-rest data-corruption consequence for `iris_global_set`, workaround) plus a matching cross-referenced note in `packages/iris-dev-mcp/README.md`. Both reference this ledger item by ID so it can never again be found ONLY in story prose. Closed-by-decision in `deferred-work.md`.

**Deferred (open — full entries in `deferred-work.md`):**

- [x] [Review][Defer] `ApplyOutputCeiling` returns MORE than its ceiling for every ceiling 0..37, contradicting its own banner; tests deliberately step around the broken band — `34-6-CR-8` (MEDIUM).
- [x] [Review][Defer] The advertised marker literal `ceiling=32768chars` never appears for truncated byRef leaves; only the `[IRIS-MCP-TRUNCATED` prefix works, documented nowhere — `34-6-CR-9` (MEDIUM).
- [x] [Review][Defer] A cut through a surrogate pair emits **invalid UTF-8 on the wire** and inflates the client-visible length to 32,770; QA's pinning test is a closed `%ToJSON`→`%FromJSON` loop inside IRIS, blind to the wire (Rule #36 oracle gap) — `34-6-CR-10` (MEDIUM).
- [x] [Review][Defer] No prepublish gate verifies the package was actually BUILT — `prepublishOnly` runs before `prepack`, no package has `prepare`/`prepack`, all declare `files:["dist"]` — `34-6-CR-11` (MEDIUM).
- [x] [Review][Defer] Per-package `prepublishOnly` hooks structurally cannot gate a multi-package release: publish order is `client-config → shared → admin → data → **dev** → interop → ops → all`, so `@iris-mcp/dev`'s deep gate runs FIFTH and a genuine regression still leaves four packages published — `34-6-CR-12` (MEDIUM).
- [x] [Review][Defer] `verify-iris-reachable.mjs` silently falls back to hard-coded `localhost`/`_SYSTEM`/`SYS`, so it can rubber-stamp on a maintainer box regardless of release target — `34-6-CR-13` (MEDIUM).
- [x] [Review][Defer] Rule #56 enumeration gap: `extensions/iris-mcp-launcher` is npm-publishable, unscoped, and ungated — 9 publishable manifests exist, not 8 — `34-6-CR-14` (LOW).
- [x] [Review][Defer] The TS layer discards `byRefTruncated` on the error path, so the server's new error-envelope field is unreachable to clients — `34-6-CR-15` (LOW).
- [x] [Review][Defer] `verify-iris-reachable.mjs` aborts (0xC0000409) rather than exiting 1 on the credential-fault path; `abort()` can trigger Windows Error Reporting / an `AeDebug` modal that would hang the publish — `34-6-CR-16` (LOW).
- [x] [Review][Defer] The "budget shared across every marked position" property has no test at all, and the node-budget assertion (`<= 1000`) is too loose to detect a broken budget — `34-6-CR-17` (LOW).
- [x] [Review][Defer] `pCeiling`'s sentinel accepts the whole negative range rather than the documented `-1` — `34-6-CR-18` (LOW).
- [x] [Review][Defer] Changeset marks feat-shaped changes as `patch` across 7 packages; published manifests carry a `prepublishOnly` path that does not exist in the tarball — `34-6-CR-19` (LOW).
- [x] [Review][Defer] Documentation/bookkeeping accuracy: `truncated:true` on an error envelope with no `output` field; three sibling early-returns lacking `structuredContent`; the three `fixtureIsDeployed` unit tests skipping without live IRIS; ledger tally; the "29 `.cls` files" figure (actual **30**, though the 343 total is correct); File List omissions — `34-6-CR-20` (LOW).

**Corrections applied to this story's own records during review:** the File List below now includes the QA-added and review-added files it omitted; the ledger's post-story tally is superseded by a mechanically recounted figure in `deferred-work.md` (**117 distinct / 53 terminal / 64 open — 3 HIGH · 11 MEDIUM · 50 LOW**).

**Gates re-run by the reviewer after all patches (own evidence, not re-trusted):** `pnpm turbo run build test lint type-check` **29/29 exit 0**; `@iris-mcp/dev` **644/644** (643 + the new leg (e)); epic gate **10/10** with `IRIS_REQUIRE_LIVE=1`, and 10/10 under `IRIS_NAMESPACE=USER` (previously RED); `ExecuteMCPv2.Tests` **343/343** vs a mechanically grepped 343 (Rule #35); `gen:governance-baseline:check` (`:check` only) **exit 0, 141/201/60**; `BOOTSTRAP_VERSION ff5029239b07` **idempotent** (md5 identical before/after `gen:bootstrap`); Constraint C-2 holds (exactly one `ExecuteMCPv2.REST.Command.1.int`); tool counts unmoved (no tool/action added). `src/ExecuteMCPv2/REST/Command.cls` restored byte-identically after two mutation cycles (`feb4a1c99480b404ab3ad58b2e8df2f6`) and recompiled; live endpoint re-verified correct (32768 + marker + `truncated:true`; under-ceiling `{"output":"2","truncated":false}`). No probe classes created; all disposable probe scripts confined to the scratchpad.

### Review Findings (2026-08-15, bmad-code-review RE-REVIEW — cycle_iteration=2, RELEASE GATE)

All three adversarial layers (Blind Hunter · Edge Case Hunter · Acceptance Auditor) DELIVERED an explicit findings payload against a frozen diff snapshot, inside the Rule #57 20-minute hard bound (a real background timer was armed at launch; all three returned before it fired). **`failed_layers` is empty — this close is NORMAL, not degraded.** 15 findings after dedup/triage: **5 patched, 10 deferred, 0 dismissed, 0 decision-needed.** Full detail in `deferred-work.md` under "Code-review findings — Story 34.6, re-review pass" (`34-6-CR2-1` … `34-6-CR2-15`).

**The three HIGH closures are CONFIRMED, each with the reviewer's own mutation evidence** (Rule #48 — not the dev's report). Every mutation was uploaded AND recompiled via `iris_doc_load` rather than `iris_doc_compile` alone, which would have recompiled the still-mutated server copy and produced a silently-failed "restore":

- `34-6-CR-5` — removing the `ApplyOutputCeiling` call on `tReturnStr` turned gate leg (e-2) RED at the REAL deployed `/classmethod` endpoint (`expected 50000 to be 32768`); restored byte-identically (`cb9cc4904448d5600ef725e32937a4f1`), recompiled, green.
- `34-6-CR-6` part 1 — reverting the marker-vs-ceiling guard turned `UtilsTest.TestApplyOutputCeilingNeverExceedsCeilingOrOriginalLength` RED with 38 assertions across ceilings 0–37, and leg (e-4) RED (`expected 38 to be 20`), while leg (e-3) stayed green.
- `34-6-CR-6` part 2 — reverting `AddByRefPosition`'s pre-check turned `TestByRefValueBudgetExhaustionOmitsRatherThanDestroys` RED (all 3 omission assertions) and leg (e-3) RED (`expected true to be false`), while leg (e-4) stayed green. Restored (`21c4d88beb0bf2f6b4ca54183d715080`), recompiled, green.
- The dev's claim that the two legs test genuinely independent properties is therefore independently confirmed, not merely asserted.

**Patched during this review:**

- [x] [Review][Patch] **The `byRefValues` 32768-character budget did not bound `byRefValues`** — `34-6-CR2-1` (**HIGH**). The budget charged only leaf VALUES; subscript KEYS and `<Object:...>` placeholders were free. Live: 400 positions keyed by 900-character subscripts emitted **363,908 characters** with `byRefTruncated: false` and 32,368 of the budget still unspent; a realistic file-path-keyed array emitted 92,067. Found independently by all three layers AND by the reviewer's own probes. Fixed by charging every emitted piece of content (same case now emits 32,797 with `byRefTruncated: true`), pinned by two new `%UnitTest` methods, mutation-verified RED reproducing the exact 363,908 figure.
- [x] [Review][Patch] **The published byRef elision contract was a false dichotomy** — `34-6-CR2-2` (MEDIUM). The tool description and README promised "marker, or omitted entirely"; the code has a third outcome — an UNMARKED prefix cut at a small positive budget — which the story's own leg (e-4) pins as correct. Both surfaces now describe all three outcomes and warn that absence of a marker does not prove a byRef value is complete.
- [x] [Review][Patch] **Two shipping artifacts still asserted the `client-config` exclusion that `34-6-CR-1` reversed** — `34-6-CR2-3` (MEDIUM). `verify-iris-reachable.mjs`'s banner and the changeset's final paragraph both said "Deliberately NOT wired into `@iris-mcp/client-config`" while the manifest carries the gate. Corrected in both, with the real rationale; `@iris-mcp/client-config` added to the changeset frontmatter.
- [x] [Review][Patch] **Ledger row `34-6-CR-8` was stale-open** — `34-6-CR2-4` (MEDIUM). Its defect was fully fixed by the CR-6 part-1 rework (verified live at ceilings 0–60) yet it was still counted as an open MEDIUM release risk. Superseded; tally corrected.
- [x] [Review][Patch] **A "total across ALL surviving positions" assertion measured exactly one position** — `34-6-CR2-5` (MEDIUM). It was a strictly weaker duplicate of the assertion two lines above and could not fail. Now sums the whole serialized structure.

**Deferred (open — full entries in `deferred-work.md`):** `34-6-CR2-6` (per-field ceilings sum to ~98 KB, ~131 KB after escaping, vs. the 50,031-character threshold the ceiling was calibrated from — MEDIUM); `34-6-CR2-7` (the fixture probe checks class existence but not this story's five new fixture methods, so a stale fixture hard-blocks the publish with `<METHOD DOES NOT EXIST>` — MEDIUM); `34-6-CR2-8` … `34-6-CR2-15` (LOW: `returnValueTruncated` dropped on the TS error path; `pTruncated` left undefined when nothing truncates; `BuildByRefNode`'s three new REQUIRED params; leg (e-4) hard-pinned to 32768; tautological `toEqual(parsed)` assertions; two stale test banners; the `integration-setup.ts` reuse claim; the QA "README x2" overcount).

**Prior-fix non-regression, re-verified:** gate 13/13 green under BOTH the default namespace and `IRIS_NAMESPACE=USER`; all 8 workspace manifests including `@iris-mcp/client-config` carry a `prepublishOnly` gate; Constraint C-2 holds (exactly one `ExecuteMCPv2.REST.Command.1.int`).

**Gates re-run by the reviewer after all patches (own evidence):** `pnpm turbo run build test lint type-check` **29/29**; `@iris-mcp/dev` **649/649** across 38 files (unmoved — no TS tests added); `ExecuteMCPv2.Tests` **348/348** vs a mechanically grepped `grep -h "^Method Test" src/ExecuteMCPv2/Tests/*.cls | wc -l` = **348** (Rule #35, was 346 + 2 new); `iris-mcp-all` **112/17** unmoved (Rule #31); `gen:governance-baseline:check` (`:check` only) **exit 0, frozen 141 / live 201 / post-foundation 60**; `BOOTSTRAP_VERSION` `e95957368a12` → **`f2d14153e102`**, idempotent (identical md5 `b2975bf5f97aed2263876873e5bbf284` on re-run), 29 classes, no roster change.

### Post-Review Status

**Updated 2026-08-15 (review-continuation pass, cycle_iteration=2).** The 3 HIGH findings this story was returned to `in-progress` over are now closed: `34-6-CR-5` and `34-6-CR-6` **RESOLVED** with code fixes (both pinned at the real deployed endpoint AND mutation-verified — see below); `34-6-CR-7` **CLOSED-BY-DECISION**, documented rather than code-fixed per the review's own explicit call. The 6 MEDIUM + 7 LOW findings (`34-6-CR-8` through `34-6-CR-20`) were explicitly OUT of this pass's scope (Rule #52) and remain open at Rule #37 re-deferral count 0, untouched. Full evidence below and in `deferred-work.md`'s "Terminal disposition — Story 34.6 review-continuation items" section.

### Review-Continuation Pass (2026-08-15, cycle_iteration=2)

**`34-6-CR-5` fix.** `ExecuteMCPv2.REST.Command`'s `ClassMethod()` now applies `ApplyOutputCeiling` to `tReturnStr` (the primary payload field) with the SAME documented ceiling/marker as `output`, surfaced via a new additive `returnValueTruncated` boolean — its own flag (not folded into the unrelated `truncated`, which describes only `output`), matching the story's own established one-flag-per-response-field pattern (`truncated`/`byRefTruncated`). Present, defaulted `false`, on both error envelopes too, for the same shape symmetry `byRefTruncated` already established. TS layer needed no logic change on the success path (pure `structuredContent: result` pass-through); two new TS unit tests pin the pass-through and Rule #19 back-compat (absent, not `false`, on a legacy envelope) explicitly.

**`34-6-CR-6` fix — two necessary parts.** (1) `ExecuteMCPv2.Utils.ApplyOutputCeiling` now falls back to a plain, marker-less hard cut of the first `pCeiling` characters whenever the elision marker itself (`36 + digits(pCeiling)` chars, never smaller than ~37) would not fit within the remaining ceiling — previously it emitted the marker anyway, which is both LARGER than the ceiling it must respect AND, when the ceiling is smaller than the original value, LARGER than the small value it replaces (the literal CR-6 defect: `"42"`/`"hello"`/`"x"` destroyed by ~37-char markers). The invariant now holds for every ceiling: the result never exceeds `pCeiling`, and never exceeds the original value's own length. (2) A new `AddByRefPosition` helper wraps all 20 top-level `byRefValues` positions in `InvokeWithArgs` with the SAME budget pre-check the recursive subscript walk already had (previously only the recursive loop had it) — a position is OMITTED from `byRefValues` entirely once the shared node/byte budget is already exhausted, rather than materialized as a misleading empty-string placeholder. Both parts are independently necessary: reproducing the code review's exact scenario (`Target20`, one value large enough to exhaust the budget to precisely zero, followed by small values) is fully protected by part (2) alone (the omission guard short-circuits before ever reaching the marker arithmetic); a value that leaves a small POSITIVE remaining budget (smaller than the marker) is protected only by part (1). Both scenarios are pinned independently, at both the `%UnitTest`/unit level and the REAL deployed `/classmethod` endpoint (see below).

**`34-6-CR-7` — documented, not code-fixed**, per the review's own explicit instruction (pre-existing, cross-cutting across 14 REST handler classes / 46 call sites spanning all five servers — genuinely out of Rule #52 scope). A new "Known Limitations" section was added to the suite root `README.md` (full mechanism, affected tools, the at-rest data-corruption consequence for `iris_global_set`, the workaround) with a matching, cross-referenced note in `packages/iris-dev-mcp/README.md` (the package owning the three concretely-named affected tools: `iris_execute_command`, `iris_execute_classmethod`, `iris_global_set`). Both reference `deferred-work.md`'s ledger item `34-6-CR-7` by ID.

**Endpoint-level pins (Rule #54 — the review's own headline lesson for this story).** Three new legs added to `packages/iris-dev-mcp/src/__tests__/execute-classmethod-epic-gate.test.ts` (the SAME default-suite, `IRIS_REQUIRE_LIVE=1`-armed gate file `scripts/prepublish-gate.mjs` runs — never a re-implementation of the handler's own sequence):
- **leg (e-2)** — drives `/classmethod` with a new fixture (`ClassMethodArgsFixture.TargetReturnOverCeiling`, returns 50,000 characters directly) to prove `returnValue` capping is actually WIRED at the endpoint, plus a Rule #19 under-ceiling byte-identity check.
- **leg (e-3)** — reproduces the code review's exact `Target20` scenario (one 40,000-char value, then `"42"`/`"hello"`/`"x"`) at the real endpoint: position 0 truncates to exactly 32768, positions 1-3 are OMITTED (not destroyed), total content never exceeds the budget.
- **leg (e-4)** — a SEPARATE scenario (discovered necessary while building leg (e-3)'s mutation evidence — see below) where one value leaves a small POSITIVE remaining budget (20 chars) rather than exactly zero, proving the marker-vs-ceiling hard-cut fix independently at the endpoint.

**Mutation evidence (Rule #48), each independently — revert → red → restore byte-identical (`md5sum`-confirmed) → recompile → green:**
- `34-6-CR-5`: removed the `ApplyOutputCeiling` call on `tReturnStr` in `Command.cls` → leg (e-2) RED (`expected 50000 to be 32768`) → restored (`cb9cc4904448d5600ef725e32937a4f1`), recompiled → green.
- `34-6-CR-6` part 1: reverted `ApplyOutputCeiling`'s marker-vs-ceiling guard in `Utils.cls` (restored the unconditional-marker shape) → RED independently at BOTH the unit level (`UtilsTest.TestApplyOutputCeilingNeverExceedsCeilingOrOriginalLength`, 38 failing assertions — ceilings 0 through 37 inclusive, mechanically counted — reproducing the exact overshoot) AND the endpoint level (leg (e-4): `expected 38 to be 20`) — **leg (e-3) alone stayed GREEN under this exact mutation**, which is why leg (e-4) exists: it isolates the marker-vs-ceiling half from the top-level-omission half. Restored (`21c4d88beb0bf2f6b4ca54183d715080`), recompiled → both green.
- `34-6-CR-6` part 2: reverted `AddByRefPosition` to call `BuildByRefNode` unconditionally (no budget pre-check) → RED at both the unit level (`ClassMethodArgsTest.TestByRefValueBudgetExhaustionOmitsRatherThanDestroys`, all 3 omission assertions failed) AND the endpoint level (leg (e-3): `expected true to be false` — position "1" reappeared in `byRefValues`) — leg (e-4) stayed green under this mutation (its scenario never reaches a fully-zero budget), confirming the two legs test genuinely independent properties. Restored (`21c4d88beb0bf2f6b4ca54183d715080`), recompiled → both green.

**Suite counts, mechanically derived (Rule #35/#51):** `ExecuteMCPv2.Tests` 343→346 (+3: 2 new `UtilsTest.TestApplyOutputCeiling*` methods, 1 new `ClassMethodArgsTest` method; one existing `UtilsTest` method's assertions corrected in place for the new behavior, not counted as new) — reported `total:346` matches a mechanical `grep -c "^Method Test"` count of 346 across the Tests package. `@iris-mcp/dev` 644→649 (+5: leg (e-2)/(e-3)/(e-4) in the epic-gate file, 2 new `returnValueTruncated` pass-through/back-compat tests in `execute.test.ts`; the existing over-ceiling pass-through test was extended in place, not counted as new). `iris-mcp-all` 112/17 unmoved (Rule #31 — no tool/action added). `pnpm turbo run build test lint type-check`: **29/29**.

**Gates (post-fix, this pass's own evidence):** `pnpm turbo run build test lint type-check` 29/29; `ExecuteMCPv2.Tests` 346/346; `@iris-mcp/dev` 649/649; `gen:governance-baseline:check` (`:check` only) exit 0, frozen `141`/live `201`/post-foundation `60` unchanged; `BOOTSTRAP_VERSION` `ff5029239b07` → `e95957368a12` (29 classes, same order — `Utils.cls`/`Command.cls` content changed, no roster change), regenerated via `pnpm run gen:bootstrap` and confirmed idempotent (byte-identical re-run, md5 `d9f0a1e80c78cdb6b460af6ed6656a44`); Constraint C-2 re-verified after the `Command.cls` recompile (`iris_doc_list(filter="ExecuteMCPv2.REST.Command", generated=true)` → exactly one `ExecuteMCPv2.REST.Command.1.int`). No probe classes created (all new ObjectScript is permanent fixture/test methods on already-existing test classes); the two disposable mutation-testing snapshot files (`Utils.cls.fixed`, `Command.cls.fixed`) used to restore byte-identically were confined to the scratchpad and deleted after use.

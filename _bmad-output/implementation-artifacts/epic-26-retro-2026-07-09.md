# Epic 26 Retrospective — Interop Message Resend / Replay (`iris_message_resend`)

**Date:** 2026-07-09
**Branch:** `epic26` → merged to `feature/feature-wave-1`
**Facilitator:** Bob (Scrum Master) · **Project Lead:** Josh

---

## Epic Summary

| Metric | Result |
|---|---|
| Stories | 5/5 done (26.0 probe · 26.1 ObjectScript+guards · 26.2 TS tool · 26.3 docs+prompt · 26.4 Rule #37 burn-down) |
| HIGH shipped | **0** (1 HIGH found + fixed inside the 26.4 review — CR 26.4-R1) |
| Rework loops | 0 |
| Tool count | interop 21 → 22 (`iris_message_resend`) |
| Prompts | 9 → 10 (`resend-failed-messages`, Epic-25-gated, now shipped) |
| BOOTSTRAP_VERSION | `13b4b5f003ab` → `1f3afba4ac52` (26.1) → `f16c695aa9ef` (26.4 dev) → `e5c18edd00c0` (26.4 review) |
| Frozen governance baseline | `1e62c5ad5bf7` UNTOUCHED throughout (`:check` exit 0 every checkpoint; 3 new resend keys post-foundation, live 194→197) |
| Deferred-work ledger | **ZERO carried-open** after the 26.4 burn-down (16 items: 11 resolved / 4 closed-by-decision / 1 closed-with-evidence / 0 re-deferred) |

**Capability shipped:** `iris_message_resend` — the highest-frequency interop write in real production support, with the strictest write discipline in the suite: 6 pre-mutation guards (numeric IDs, item+from required, ≤7-day window, ≤500 hard cap refuse-not-truncate, dryRun+confirm double-gate, production-running precheck), all verified live NO-WRITE; resends only via the Story-26.0-pinned `Ens.MessageHeader.ResendDuplicatedMessage`.

---

## What Went Well

- **Probe-first (Rule #16) paid off repeatedly.** Story 26.0's live probe pinned the resend API from `irislib` source (rejecting the spec's "resubmit variants" candidate) AND caught two wrong spec claims before any dependent code shipped (→ Rule #47).
- **Adversarial code review earned its keep on the burn-down.** The 26.4 Opus review live-verified over HTTP and caught a *defective* "resolved" fix (CR 23.1-5) that the green default suite was blind to (→ Rule #48). QA independently mutation-verified the Compressor guard.
- **Rule #37 burn-down converged to zero.** After 3 consecutive re-deferrals (Epics 23/24/25), the dedicated Story 26.4 drove all 16 carried items terminal with 0 re-deferred — the ledger is clean for the first time since Epic 25's close.
- **Live smokes were substantive, not ceremonial.** Real resend `82651→82658` verified via `iris_production_messages` (SessionId preserved); every destructive path refused with before/after no-write proof; Rule #34 second-namespace (SADEMO) confirmed the guards are namespace-generic; scratch cleaned to zero.
- **Invariants held under a broad, multi-package epic** (ObjectScript + 5 TS packages + generators + docs): frozen baseline untouched, Rule #39 dual-roster correct for the new class, additive-only.

## What Could Have Gone Better

- **Two spec "reuse the existing X" claims were wrong** (nonexistent `horologToIso` helper; filterless `MessageTrace` query) — both internal-code claims, both caught by source-read but only because the dev probed rather than trusted. → **Rule #47.**
- **A burn-down "resolved" fix was itself defective** and passed a green suite — caught only by the review's live-HTTP verification. → **Rule #48.**
- **The running MCP servers still carry the pre-26 build** — `iris_message_resend` + the new prompt are not yet live at the MCP layer; the governance-disabled-write LIVE smoke was covered by 26.2 unit tests (real `handleToolCall`), not a live MCP call. → operational action item.

---

## Rules Codified (Rule #1 meta-rule)

Both nominated candidates confirmed by the Project Lead as passing the general-pattern bar:

- **Rule #47 — Verify a spec's "reuse the existing X" INTERNAL-code claim against source before building.** Generalizes Rule #16/#42 from IRIS-system APIs to internal helpers/queries/modules. Evidence: Story 26.0 (`horologToIso`) + Story 26.1 (`MessageTrace` filters).
- **Rule #48 — A burn-down "resolved" fix must be live/mutation-verified, not trusted on a green suite.** Extends Rule #37 with a verification-rigor requirement for the "resolved" outcome. Evidence: Story 26.4 CR 23.1-5 defective-fix caught live.

(Project-rules.md footer updated 46 → 48; Epic-26 audit note appended.)

---

## Action Items

| # | Item | Owner | Category |
|---|---|---|---|
| 1 | **Rebuild + reload the running MCP servers** so `iris_message_resend` + the `resend-failed-messages` prompt go live at the MCP layer (mirrors Epic 20 retro Action #2). Enables the governance-disabled-write LIVE smoke that 26.2 covered only via unit tests. | Project Lead (operational) | operational |
| 2 | Rules #47 + #48 codified in `project-rules.md` — DONE this retro. | Lead | process |

## Deferred-Work Ledger Status

**ZERO carried-open.** The only open items are Epic-26's OWN review findings (CR 26.1-1..6, CR 26.2-1, CR 26.3-*, CR 26.4-*), all LOW, permitted per AC 26.4.3 — they carry into the Epic 27 retro-review gate as the new (small, non-growing) batch.

## Next Epic Readiness — Epic 27: Environment Diff & Promotion (`iris_env_diff` / `iris_env_promote`)

- **Dependency:** builds on the profile-client plumbing + the strict-write-discipline pattern Epic 26 exercised (`promote:execute` will be default-disabled behind allowlist + confirm + plan-hash + target-profile-governance gates).
- **PREREQ — SETTLED at this retro:** the "≥2 reachable profiles" requirement is resolved — two profiles on the single dev instance with different default namespaces is the accepted path (per the 2026-07-07 sprint-change decision). **No kickoff blocker.**
- No significant discovery from Epic 26 invalidates the Epic 27 plan.

---

## Readiness Assessment

| Dimension | Status |
|---|---|
| Testing & quality | ✅ 0 HIGH shipped; full suite green (13/13 turbo); OS %UnitTest count-checked; live smokes each story |
| Deployment (branch) | ✅ epic26 merged to feature/feature-wave-1 (merge gate) |
| MCP-layer live | ⚠️ pending server reload (Action #1) — code is correct + unit/live-endpoint verified |
| Technical health | ✅ frozen baseline untouched; ledger at zero carried-open; bootstrap dual-roster correct |
| Blockers into Epic 27 | none (profile prereq settled) |

**Verdict:** Epic 26 is complete and clean. One operational follow-up (MCP reload); Epic 27 is unblocked.
